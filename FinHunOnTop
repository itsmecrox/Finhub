local k = {};
local l = 0;
local function p()
	l = l + 1;
	return "__bl_continue_" .. (l .. "__");
end;
if not table.clear then
	table.clear = function(k)
			for l in pairs(k) do
				rawset(k, l, nil);
			end;
		end;
end;
if not task.defer then
	task.defer = function(k, ...)
			return task.spawn(k, ...);
		end;
end;
if not utf8 then
	utf8 = { char = function(...)
				local k = "";
				for l, p in ipairs({ ... }) do
					k = k .. string.char(p);
				end;
				return k;
			end };
end;
local function a(k, ...)
	local l, p = pcall(k, ...);
	if not l then
		warn("[Finhub] Error: " .. tostring(p));
	end;
	return l, p;
end;
local function y()
	local k = game:GetService("RunService");
	local l = (gethui and gethui()) or game:GetService("CoreGui");
	local p = Instance.new("ScreenGui");
	p.Name = "FinhubLoadingNotification";
	p.ResetOnSpawn = false;
	p.IgnoreGuiInset = true;
	p.DisplayOrder = 67676769769;
	p.ZIndexBehavior = Enum.ZIndexBehavior.Global;
	p.Parent = l;
	local a = Instance.new("TextLabel");
	a.Name = "LoadingLabel";
	a.AnchorPoint = Vector2.new(.5, .5);
	a.Position = UDim2.fromScale(.5, .5);
	a.Size = UDim2.fromOffset(280, 48);
	a.BackgroundTransparency = 1;
	a.Font = Enum.Font.GothamMedium;
	a.TextSize = 22;
	a.TextColor3 = Color3.fromRGB(255, 105, 180);
	a.Text = "Finhub loading.";
	a.Parent = p;
	local y = 0;
	local v = 1;
	local j = k.RenderStepped:Connect(function(k)
			y = y + k;
			if y < .35 then
				return;
			end;
			y = 0;
			v = v % 3 + 1;
			a.Text = "Finhub loading" .. string.rep(".", v);
		end);
	return function()
		if j then
			j:Disconnect();
			j = nil;
		end;
		if p then
			p:Destroy();
		end;
	end;
end;
(getgenv()).FinhubModuleCache = (getgenv()).FinhubModuleCache or {};
local function v(k, l)
	local p = (getgenv()).FinhubModuleCache;
	local a = typeof(k) == "Finhub" and k:GetFullName() or tostring(k);
	if p[a] ~= nil then
		return p[a];
	end;
	local y = l and (os.clock() + l) or math.huge;
	local v;
	while os.clock() < y do
		if typeof(k) == "Instance" then
			if not k.Parent then
				task.wait(.1);
				continue;
			end;
		end;
		local l, y = pcall(require, k);
		if l then
			p[a] = y;
			return y;
		end;
		v = y;
		task.wait(.1);
	end;
	error("[Finhub] module load failed: " .. (a .. (" (" .. (tostring(v) .. ")"))));
end;
(getgenv()).FinhubRequire = v;
local function j()
	local k = game:GetService("ReplicatedStorage");
	local l = game:GetService("Players");
	local p = l.LocalPlayer or l.PlayerAdded:Wait();
	local a = p:WaitForChild("PlayerScripts", 8);
	local y = k:WaitForChild("Modules", 8);
	pcall(function()
		v(y:WaitForChild("EnumLibrary", 4));
	end);
	pcall(function()
		v(y:WaitForChild("Utility", 4));
	end);
	local j = a:WaitForChild("Controllers", 8);
	pcall(function()
		v(j:WaitForChild("FighterController", 4));
	end);
	pcall(function()
		v(j:WaitForChild("CameraController", 4));
	end);
end;
local function s()
	local k = game:GetService("CoreGui");
	local l = k:FindFirstChild("RobloxGui");
	if l then
		local k = l:FindFirstChild("ErrorPrompt");
		if k and k:IsA("GuiObject") then
			k.Visible = false;
		end;
	end;
	local p = k:FindFirstChild("RobloxPromptGui");
	if p and p:IsA("GuiObject") then
		p.Visible = false;
	end;
end;
local function G()
	(getgenv()).FinhubACBypass = (getgenv()).FinhubACBypass or {};
	local k = (getgenv()).FinhubACBypass;
	if k._initialized then
		return;
	end;
	k._initialized = true;
	k._active = false;
	k._bypassed = false;
	local function l(k, ...)
		if type(k) ~= "function" then
			return nil;
		end;
		local l, p = pcall(k, ...);
		if l then
			return p;
		end;
		return nil;
	end;
	local p = type(hookfunction) == "function";
	local a = type(hookmetamethod) == "function";
	local v = type(cloneref) == "function";
	local G = type(checkcaller) == "function";
	local g = type(getrenv) == "function";
	local T = type(setthreadidentity) == "function";
	local c = type(getthreadidentity) == "function";
	local e = type(isexecutorclosure) == "function";
	local b = type(newcclosure) == "function";
	local f = type(iscclosure) == "function";
	if not ((p and (a and G))) then
		warn("[Finhub AC] Executor missing required hook functions. Bypass may not work fully.");
	end;
	local function N(k)
		local p, a = pcall(game.GetService, game, k);
		if p and a then
			if v then
				return l(cloneref, a) or a;
			end;
			return a;
		end;
		return nil;
	end;
	local function K()
		if k._active then
			return;
		end;
		k._active = true;
		local p = N("Players");
		local a = N("ReplicatedFirst");
		local y = N("ReplicatedStorage");
		local v = N("ScriptContext");
		if not ((p and (a and (y and v)))) then
			warn("[Finhub AC] Failed to get required services");
			return;
		end;
		local j = p.LocalPlayer;
		if not j then
			warn("[Finhub AC] LocalPlayer not found");
			return;
		end;
		local s = { Enabled = true, Disabled = false };
		local e = { "Kick", "kick" };
		local K = {
				[69] = 2,
				[138] = 3,
				[207] = 4,
				[276] = 5,
				[345] = 6,
				[414] = 7,
			};
		local n = {
				1,
				2,
				3,
				4,
				5,
			};
		local d = nil;
		local Y = false;
		local M = false;
		local i = "";
		local R = .6;
		local B = .25;
		local W = .5;
		local L = 0;
		local P = 128;
		local x = {};
		if not setstackhidden then
			local k;
			if g and ((getrenv()).debug and (getrenv()).debug.traceback) then
				k = l(hookfunction, (getrenv()).debug.traceback, function(...)
						if G and checkcaller() then
							return k(...);
						end;
						local l, p = pcall(k, ...);
						if not l then
							return k(...);
						end;
						local a, y = ...;
						local v = k(...);
						local j = {};
						if typeof(a) == "string" or typeof(a) == "number" then
							table.insert(j, tostring(a));
						end;
						if typeof(y) ~= "number" then
							y = 1;
						else
							y = math.floor(tonumber(y));
						end;
						local s = 1;
						local g = {};
						for k in v:gmatch("[^\n]+") do
							table.insert(g, k);
						end;
						for k = math.max(2, y + 1), #g, 1 do
							local l = g[k];
							if l and l:find("%.") then
								local k = l:find("%.");
								local p = l:find(":");
								if not p or k < p then
									table.insert(j, l);
								end;
							end;
						end;
						return table.concat(j, "\n") .. "\n";
					end);
			end;
			setstackhidden = function(k, l)
					if type(l) ~= "boolean" then
						error("hidden must be boolean");
					end;
					local p, a = pcall(function()
							if typeof(k) == "number" then
								return debug.info(k + 2, "f");
							end;
							return k;
						end);
					if p and a then
						x[a] = not l;
					end;
				end;
		end;
		local t = setmetatable({}, { __mode = "k" });
		local function S(k)
			if type(k) == "function" then
				t[k] = true;
			end;
			return k;
		end;
		local function V(k)
			return t[k] == true;
		end;
		local X = function(k, ...)
				local p = { ... };
				local a, y, v, j;
				if k == hookmetamethod then
					y = p[1];
					v = p[2];
					j = p[3];
				else
					a = p[1];
					j = p[2];
				end;
				local s;
				if b then
					if k == hookfunction and (f and iscclosure(a)) then
						j = newcclosure(j);
					end;
					if not ((f and iscclosure(j))) then
						j = newcclosure(j);
					end;
				end;
				l(setstackhidden, j, true);
				local G, g = pcall(function()
						S(j);
						if k == hookmetamethod then
							s = k(y, v, j);
						else
							s = k(a, j);
						end;
					end);
				if not G then
					warn("[Finhub AC] Bypass hook failed");
					return nil;
				end;
				return s;
			end;
		local Z = function(k, ...)
				if G and checkcaller() then
					return k(...);
				end;
				local l = c and getthreadidentity() or 2;
				if l ~= 2 and T then
					setthreadidentity(2);
				end;
				local p = { k(...) };
				if l ~= 2 and T then
					setthreadidentity(l);
				end;
				return table.unpack(p);
			end;
		local E = v.Error:Connect(S(function(k, l, p)
				k = tostring(k);
				l = tostring(l);
				if l:find("PlayerScripts.Controllers.MiscellaneousController") and k:find("attempt to index number with number") then
					warn("[Finhub AC] AC detection pattern blocked");
				end;
			end));
		local J = a:WaitForChild("LocalScript3", 10);
		local U = y:WaitForChild("Remotes", 10);
		if U then
			U = U:WaitForChild("RemoteEvent", 10);
		end;
		if not J or not U then
			warn("[Finhub AC] AC components not found - game may have updated");
			return;
		end;
		local z;
		z = X(hookmetamethod, J, "__index", function(l, p)
				local a = not k._bypassed and (G and checkcaller());
				if l == J and (not a and s[p] ~= nil) then
					return s[p];
				end;
				if G and checkcaller() then
					return z(l, p);
				end;
				return Z(z, l, p);
			end);
		local w;
		w = X(hookmetamethod, J, "__newindex", function(l, p, a)
				local y = not k._bypassed and (G and checkcaller());
				if l == J and (not y and s[p] ~= nil) then
					s[p] = a;
					if p == "Enabled" then
						s.Disabled = not a;
					elseif p == "Disabled" then
						s.Enabled = not a;
					end;
					return;
				end;
				if G and checkcaller() then
					return w(l, p, a);
				end;
				return Z(w, l, p, a);
			end);
		i = "";
		d = tick();
		local q;
		q = X(hookfunction, U.FireServer, function(k, ...)
				local l = tick();
				local p = { ... };
				if not Y then
					Y = true;
					local l = p[1];
					if type(l) == "table" and (#l >= 1 and ((type(l[1]) == "string" or type(l[1]) == "number"))) then
						i = tostring(l[1]);
					end;
					d = tick();
					L = 1;
					M = true;
					return Z(q, k, ...);
				end;
				local a = l - ((d or l));
				if a > 0 then
					if L == 0 then
						R = a;
					else
						R = W * a + ((1 - W)) * R;
					end;
					L = L + 1;
					if R < B then
						R = B;
					end;
				end;
				local y = Z(q, k, ...);
				d = tick();
				return y;
			end);
		local function u()
			local k = math.random(1, 5);
			local l = {};
			local p = { 7 };
			l[7] = true;
			while #p < k do
				local k = math.random(1, 6);
				if not l[k] then
					l[k] = true;
					table.insert(p, k);
				end;
			end;
			table.sort(p);
			local a = {};
			for k = 1, 7, 1 do
				if l[k] then
					a[k] = {};
				else
					a[k] = n;
				end;
			end;
			return a, p;
		end;
		local function H(k, l, p)
			local a = k[1];
			local y = #a;
			local v = p[math.random(1, #p)];
			local j;
			local s = a[y];
			if type(s) == "table" and type(v) == "number" then
				j = s[v];
			else
				for k = y, 1, -1 do
					if type(a[k]) ~= "table" then
						continue;
					end;
					local l = a[k];
					if type(v) == "number" and l[v] ~= nil then
						j = l[v];
						break;
					else
						j = l;
						break;
					end;
				end;
				if j == nil then
					j = {};
				end;
			end;
			local G = {};
			for l, p in ipairs(l) do
				local a = K[p];
				if a and not G[a] then
					k[a] = j;
					G[a] = true;
				end;
			end;
			return k;
		end;
		local function F(k, l)
			local p, a = u();
			local y = math.random(1, 8);
			local v = { i, buffer.tostring(k) };
			local j = math.random(0, 2);
			for k = 1, j, 1 do
				v[#v + 1] = "";
			end;
			while #v < (y - 1) do
				v[#v + 1] = math.random(5, 100000);
			end;
			v[#v + 1] = p;
			local s = {
					v,
					{},
					nil,
					nil,
					nil,
					nil,
					nil,
				};
			return H(s, l, a);
		end;
		task.spawn(function()
			local l = getfenv();
			if l then
				l.script = J;
			end;
			while not M do
				task.wait();
			end;
			J.Enabled = false;
			U.OnClientEvent:Connect(function(...)
				d = tick();
				local k = Instance.new("RemoteEvent");
				k.Parent = nil;
				k:FireServer();
				local l = { ... };
				local p = l[1];
				local a = l[2];
				local y = l[3];
				if typeof(p) ~= "buffer" or type(a) ~= "number" or type(y) ~= "table" then
					warn("[Finhub AC] Invalid challenge format");
					return;
				end;
				local v = F(p, y);
				task.defer(function()
					local l = tick() - ((d or 0));
					local p = R - l;
					if p > 0 then
						task.wait(p);
					end;
					U:FireServer(table.unpack(v, 1, 5));
					d = tick();
					k:Destroy();
				end);
			end);
			k._bypassed = true;
			E:Disconnect();
		end);
		for k, l in ipairs(e) do
			local p = j[l];
			if type(p) == "function" then
				local k;
				k = X(hookfunction, p, function(l, ...)
						if l == j and (G and not checkcaller()) then
							warn("[Finhub AC] Kick blocked: " .. tostring((...)));
							return nil;
						end;
						return k(l, ...);
					end);
			end;
		end;
		for k, l in ipairs(getconnections(v.Error)) do
			if not l.Function then
				continue;
			end;
			if V(l.Function) then
				continue;
			end;
			X(hookfunction, l.Function, function(...)
				return nil;
			end);
		end;
		if v.Error and v.Error.Connect then
			X(hookfunction, v.Error.Connect, function(...)
				return nil;
			end);
		end;
		task.spawn(function()
			local l = tick() + 10;
			while not k._bypassed and tick() < l do
				task.wait(.5);
			end;
			if k._bypassed then
				k._active = true;
				if (getgenv()).FinhubNotify then
					(getgenv()).FinhubNotify("AC Bypass active", 3);
				end;
			else
				warn("[Finhub AC] Bypass initialization timed out");
			end;
		end);
	end;
	local n, d = pcall(K);
	if not n then
		warn("[Finhub AC] Bypass init error: " .. tostring(d));
	end;
	(getgenv()).FinhubACBypassStatus = function()
			return k._bypassed;
		end;
	(getgenv()).FinhubACBypassReload = function()
			k._initialized = false;
			k._bypassed = false;
			k._active = false;
			pcall(K);
		end;
	local Y = game:GetService("RunService");
	local M = y();
	local i = true;
	local R = Y.RenderStepped:Connect(function()
			if i then
				s();
			end;
		end);
	task.wait(.8);
	pcall(j);
	i = false;
	if R then
		R:Disconnect();
		R = nil;
	end;
	if M then
		M();
	end;
end;
local g = game:GetService("RunService");
G();
local T = game:GetService("UserInputService");
pcall(function()
	if setfpscap then
		setfpscap(0);
	end;
end);
local function c(k, l, p)
	local a = 1;
	while true do
		local y, v = k:find(l, a, true);
		if not y then
			break;
		end;
		k = k:sub(1, y - 1) .. (p .. k:sub(v + 1));
		a = y + #p;
	end;
	return k;
end;
(getgenv()).FinhubSliderFillState = (getgenv()).FinhubSliderFillState or {};
(getgenv()).FinhubSliderFollowRate = (getgenv()).FinhubSliderFollowRate or 11;
(getgenv()).FinhubSetSliderFill = function(k, l, p, a, y)
		if not k or not k.Parent then
			return;
		end;
		l = math.floor(tonumber(l) or 0);
		local v = (getgenv()).FinhubSliderFillState;
		if y then
			k.Size = UDim2.new(0, l, 1, 0);
			if p and a then
				p.Visible = not ((l == a.MaxSize or l == 0));
			end;
			v[k] = nil;
			return;
		end;
		local j = v[k];
		if j then
			j.target = l;
			j.hideBorder = p;
			j.sliderObj = a;
		else
			v[k] = {
					target = l,
					pos = k.Size.X.Offset,
					hideBorder = p,
					sliderObj = a,
				};
		end;
	end;
if not (getgenv()).FinhubSliderFillBound then
	(getgenv()).FinhubSliderFillBound = true;
	g.RenderStepped:Connect(function(k)
		local l = (getgenv()).FinhubSliderFillState;
		if not l then
			return;
		end;
		k = math.clamp(tonumber(k) or 0, .001, .066666666666667);
		local p = (getgenv()).FinhubSliderFollowRate or 11;
		local a = 1 - math.exp(-p * k);
		for k, p in pairs(l) do
			if not k.Parent then
				l[k] = nil;
			else
				p.pos = p.pos + ((p.target - p.pos)) * a;
				if math.abs(p.target - p.pos) < .35 then
					p.pos = p.target;
				end;
				local l = math.floor(p.pos + .5);
				k.Size = UDim2.new(0, l, 1, 0);
				if p.hideBorder and p.sliderObj then
					p.hideBorder.Visible = not ((l == p.sliderObj.MaxSize or l == 0));
				end;
			end;
		end;
	end);
end;
local e = "https://raw.githubusercontent.com/makarmatvij7-svg/LunoriaaLib/main/";
local b = (loadstring(game:HttpGet(e .. "Library.lua")))();
local N = (loadstring(game:HttpGet(e .. "addons/ThemeManager.lua")))();
local K = (loadstring(game:HttpGet(e .. "addons/SaveManager.lua")))();
local n = Color3.fromRGB(255, 105, 180);
local d = {
		Color3.fromRGB(255, 105, 180),
		Color3.fromRGB(255, 105, 180),
		Color3.fromRGB(255, 105, 180),
		Color3.fromRGB(255, 105, 180),
	};
local function Y(k, l)
	return (math.abs(k.R - l.R) + math.abs(k.G - l.G)) + math.abs(k.B - l.B) < .04;
end;
local function M(k)
	for l, p in ipairs(d) do
		if Y(k, p) then
			return true;
		end;
	end;
	return false;
end;
local function R()
	if not b then
		return;
	end;
	b.AccentColor = n;
	if b.GetDarkerColor then
		b.AccentColorDark = b:GetDarkerColor(n);
	else
		local k, l, p = Color3.toHSV(n);
		b.AccentColorDark = Color3.fromHSV(k, l, p / 1.5);
	end;
	if b.UpdateColorsUsingRegistry then
		pcall(function()
			b:UpdateColorsUsingRegistry();
		end);
	end;
	pcall(function()
		if b.ScreenGui then
			for k, l in ipairs(b.ScreenGui:GetDescendants()) do
				if l:IsA("GuiObject") and M(l.BackgroundColor3) then
					l.BackgroundColor3 = n;
				end;
				if l:IsA("TextLabel") and M(l.TextColor3) then
					l.TextColor3 = n;
				end;
			end;
		end;
	end);
end;
R();
(getgenv()).FinhubApplyAccentTheme = R;
local B = 2147483646;
local W = 2147483647;
local L = 80;
local P = 10;
local function x()
	pcall(function()
		if b and b.ScreenGui then
			b.ScreenGui.DisplayOrder = B;
			b.ScreenGui.IgnoreGuiInset = true;
			b.ScreenGui.ZIndexBehavior = Enum.ZIndexBehavior.Global;
		end;
	end);
	pcall(function()
		local k = (getgenv()).FinhubCosmeticUIState;
		if k and k.gui then
			k.gui.DisplayOrder = W;
			k.gui.IgnoreGuiInset = true;
			k.gui.ZIndexBehavior = Enum.ZIndexBehavior.Global;
		end;
	end);
	pcall(function()
		local k = (getgenv()).FinhubKeybindList;
		if k and k.gui then
			k.gui.DisplayOrder = L;
			k.gui.IgnoreGuiInset = true;
			k.gui.ZIndexBehavior = Enum.ZIndexBehavior.Global;
		end;
	end);
end;
(getgenv()).FinhubApplyUiLayering = x;
local t = b:CreateWindow({
		Title = "Finhub",
		Center = true,
		AutoShow = true,
		TabPadding = 6,
		MenuFadeTime = 0,
		RedRightText = " Rivals",
		Size = Vector2.new(700, 600),
	});
x();
(getgenv()).FinhubMenuCursor = (getgenv()).FinhubMenuCursor or {};
local function S()
	local k = (getgenv()).FinhubMenuCursor;
	if k.cursor then
		pcall(function()
			k.cursor:Remove();
		end);
	end;
	if k.outline then
		pcall(function()
			k.outline:Remove();
		end);
	end;
	k.cursor = Drawing.new("Triangle");
	k.cursor.Filled = true;
	k.cursor.Thickness = 1;
	k.cursor.Visible = false;
	k.outline = Drawing.new("Triangle");
	k.outline.Filled = false;
	k.outline.Thickness = 1;
	k.outline.Color = Color3.new(0, 0, 0);
	k.outline.Visible = false;
	k.initialized = true;
	k.wasOpen = false;
	k.savedMouseIcon = true;
end;
local function V()
	if not b or b.Unloaded then
		return false;
	end;
	if t and (t.Holder and t.Holder.Parent) then
		return t.Holder.Visible;
	end;
	return false;
end;
local function X()
	return V();
end;
S();
local Z = 240;
local E = "FinhubMenuCursorDraw";
local J = 0;
local function U()
	local k = (getgenv()).FinhubMenuCursor;
	if not k or not k.cursor or not k.outline then
		return;
	end;
	local l = V();
	if not l then
		if k.cursor.Visible then
			k.cursor.Visible = false;
			k.outline.Visible = false;
		end;
		if k.wasOpen then
			T.MouseIconEnabled = k.savedMouseIcon;
		end;
		k.wasOpen = false;
		return;
	end;
	if not k.wasOpen then
		k.savedMouseIcon = T.MouseIconEnabled;
	end;
	T.MouseIconEnabled = false;
	local p = T:GetMouseLocation();
	local a = (b and b.AccentColor) or Color3.fromRGB(255, 105, 180);
	k.cursor.Visible = true;
	k.outline.Visible = true;
	k.cursor.Color = a;
	k.cursor.PointA = Vector2.new(p.X, p.Y);
	k.cursor.PointB = Vector2.new(p.X + 16, p.Y + 6);
	k.cursor.PointC = Vector2.new(p.X + 6, p.Y + 16);
	k.outline.PointA = k.cursor.PointA;
	k.outline.PointB = k.cursor.PointB;
	k.outline.PointC = k.cursor.PointC;
	k.wasOpen = true;
end;
pcall(function()
	g:UnbindFromRenderStep(E);
end);
pcall(function()
	g:BindToRenderStep(E, Enum.RenderPriority.Last.Value + 10, function(k)
		J = J + ((k or 0));
		local l = 1 / Z;
		if J < l then
			return;
		end;
		J = J % l;
		U();
	end);
end);
(getgenv()).FinhubIsMenuOpen = V;
(getgenv()).FinhubLanguage = (getgenv()).FinhubLanguage or "english";
local z = { korean = {
			["Assault Rifle"] = "\235\143\140\234\178\169 \236\134\140\236\180\157",
			Handgun = "\234\182\140\236\180\157",
			Shotgun = "\236\130\176\237\131\132\236\180\157",
			Sniper = "\236\160\128\234\178\169\236\180\157",
			Bow = "\237\153\156",
			["Burst Rifle"] = "\235\178\132\236\138\164\237\138\184 \236\134\140\236\180\157",
			Crossbow = "\236\132\157\234\182\129",
			Gunblade = "\234\177\180\235\184\148\235\160\136\236\157\180\235\147\156",
			RPG = "RPG",
			["Energy Rifle"] = "\236\151\144\235\132\136\236\167\128 \236\134\140\236\180\157",
			Flamethrower = "\237\153\148\236\151\188\235\176\169\236\130\172\234\184\176",
			["Grenade Launcher"] = "\236\156\160\237\131\132 \235\176\156\236\130\172\234\184\176",
			Minigun = "\235\175\184\235\139\136\234\177\180",
			["Paintball Gun"] = "\237\142\152\236\157\184\237\138\184\235\179\188 \234\177\180",
			Distortion = "\235\148\148\236\138\164\237\134\160\236\133\152",
			Permafrost = "\236\152\129\234\181\172 \235\143\153\237\134\160",
			Daggers = "\235\139\168\234\178\128",
			["Flare Gun"] = "\237\148\140\235\160\136\236\150\180 \234\177\180",
			Revolver = "\235\166\172\235\179\188\235\178\132",
			Shorty = "\236\135\188\237\139\176",
			Spray = "\236\138\164\237\148\132\235\160\136\236\157\180",
			Uzi = "UZI",
			["Energy Pistols"] = "\236\151\144\235\132\136\236\167\128 \234\182\140\236\180\157",
			Exogun = "\236\151\145\236\134\140\234\177\180",
			Slingshot = "\236\131\136\236\180\157",
			Warper = "\236\155\140\237\141\188",
			Fists = "\236\163\188\235\168\185",
			["Battle Axe"] = "\236\160\132\237\136\172 \235\143\132\235\129\188",
			Chainsaw = "\236\160\132\234\184\176\237\134\177",
			Katana = "\236\185\180\237\131\128\235\130\152",
			Knife = "\235\130\152\236\157\180\237\148\132",
			["Riot Shield"] = "\235\176\169\237\140\168",
			Scythe = "\235\130\171",
			Maul = "\235\167\157\236\185\152",
			Trowel = "\235\170\168\236\162\133\236\130\189",
			Grenade = "\236\136\152\235\165\152\237\131\132",
			Flashbang = "\236\132\172\234\180\145\237\131\132",
			["Freeze Ray"] = "\235\131\137\235\143\153 \234\180\145\236\132\160",
			["Jump Pad"] = "\236\160\144\237\148\132 \237\140\168\235\147\156",
			Molotov = "\237\153\148\236\151\188\235\179\145",
			Satchel = "\234\176\128\235\176\169",
			["Smoke Grenade"] = "\236\151\176\235\167\137\237\131\132",
			["War Horn"] = "\235\130\152\237\140\148",
			Medkit = "\236\157\152\235\163\140 \237\130\164\237\138\184",
			["Substapce Tripmine"] = "\236\167\128\235\162\176",
			Warpstone = "\236\155\140\237\148\132\236\132\157",
			Hook = "\234\176\136\234\179\160\235\166\172",
			Spear = "\236\176\189",
			Grappler = "\234\183\184\235\158\152\237\148\140\235\159\172",
			Maul = "\235\167\157\236\185\152",
		}, spanish = {
			["Assault Rifle"] = "rifle de asalto",
			Handgun = "pistola",
			Shotgun = "escopeta",
			Sniper = "francotirador",
			Bow = "arco",
			["Burst Rifle"] = "rifle r\195\161faga",
			Crossbow = "ballesta",
			Gunblade = "espada-pistola",
			RPG = "RPG",
			["Energy Rifle"] = "rifle de energ\195\173a",
			Flamethrower = "lanzallamas",
			["Grenade Launcher"] = "lanzagranadas",
			Minigun = "minigun",
			["Paintball Gun"] = "pistola de paintball",
			Distortion = "distorsi\195\179n",
			Permafrost = "permafrost",
			Daggers = "dagas",
			["Flare Gun"] = "pistola de bengalas",
			Revolver = "rev\195\179lver",
			Shorty = "shorty",
			Spray = "spray",
			Uzi = "uzi",
			["Energy Pistols"] = "pistolas de energ\195\173a",
			Exogun = "exopistola",
			Slingshot = "honda",
			Warper = "teletransportador",
			Fists = "pu\195\177os",
			["Battle Axe"] = "hacha de batalla",
			Chainsaw = "motosierra",
			Katana = "katana",
			Knife = "cuchillo",
			["Riot Shield"] = "escudo",
			Scythe = "guada\195\177a",
			Maul = "mazo",
			Trowel = "paleta",
			Grenade = "granada",
			Flashbang = "granada cegadora",
			["Freeze Ray"] = "rayo congelante",
			["Jump Pad"] = "plataforma de salto",
			Molotov = "c\195\179ctel molotov",
			Satchel = "mochila",
			["Smoke Grenade"] = "granada de humo",
			["War Horn"] = "cuerno de guerra",
			Medkit = "botiqu\195\173n",
			["Substapce Tripmine"] = "mina",
			Warpstone = "piedra de teletransporte",
			Hook = "gancho",
			Spear = "lanza",
			Grappler = "gancho",
			Maul = "mazo",
		} };
local w = { english = {
			studs = "%.0f studs",
			studs_short = "%d studs",
			select_weapon = "select a weapon first",
			apply = "apply",
			apply_all = "apply to all",
			language = "language",
			cosmetic_title = "cosmetic changer",
			weapons = "weapons",
			cosmetics = "cosmetics",
			filter_weapons = "filter weapons...",
			filter_cosmetics = "filter cosmetics...",
			skin = "skin",
			wrap = "wrap",
			rclick_hint = "r-click weapon: remove skin",
			unlock_all = "unlock all",
			inverted = "inverted",
			favorited = "favorited",
		}, korean = {
			studs = "%.0f \236\138\164\237\132\176\235\147\156",
			studs_short = "%d \236\138\164\237\132\176\235\147\156",
			select_weapon = "\235\172\180\234\184\176\235\165\188 \235\168\188\236\160\128 \236\132\160\237\131\157\237\149\152\236\132\184\236\154\148",
			apply = "\236\160\129\236\154\169",
			apply_all = "\236\160\132\236\178\180 \236\160\129\236\154\169",
			language = "\236\150\184\236\150\180",
			cosmetic_title = "\236\189\148\236\138\164\235\169\148\237\139\177 \235\179\128\234\178\189",
			weapons = "\235\172\180\234\184\176",
			cosmetics = "\236\189\148\236\138\164\235\169\148\237\139\177",
			filter_weapons = "\235\172\180\234\184\176 \234\178\128\236\131\137...",
			filter_cosmetics = "\236\189\148\236\138\164\235\169\148\237\139\177 \234\178\128\236\131\137...",
			skin = "\236\138\164\237\130\168",
			wrap = "\235\158\169",
			rclick_hint = "\236\154\176\237\129\180\235\166\173: \236\138\164\237\130\168 \236\160\156\234\177\176",
			unlock_all = "\236\160\132\236\178\180 \237\149\180\236\160\156",
			inverted = "\235\176\152\236\160\132",
			favorited = "\236\166\144\234\178\168\236\176\190\234\184\176",
		}, spanish = {
			studs = "%.0f studs",
			studs_short = "%d studs",
			select_weapon = "selecciona un arma primero",
			apply = "aplicar",
			apply_all = "aplicar a todos",
			language = "idioma",
			cosmetic_title = "cambiador de cosm\195\169ticos",
			weapons = "armas",
			cosmetics = "cosm\195\169ticos",
			filter_weapons = "filtrar armas...",
			filter_cosmetics = "filtrar cosm\195\169ticos...",
			skin = "skin",
			wrap = "wrap",
			rclick_hint = "clic derecho: quitar skin",
			unlock_all = "desbloquear todo",
			inverted = "invertido",
			favorited = "favorito",
		} };
(getgenv()).FinhubTranslateWeapon = function(k)
		if not k or type(k) ~= "string" then
			return k;
		end;
		local l = (getgenv()).FinhubLanguage or "english";
		if l == "english" then
			return k;
		end;
		local p = z[l];
		return (p and p[k]) or k;
	end;
(getgenv()).FinhubTranslateLabel = function(k)
		if not k or type(k) ~= "string" then
			return k;
		end;
		local l = (getgenv()).FinhubLanguage or "english";
		if l == "english" then
			return k:lower();
		end;
		local p = (getgenv()).FinhubTranslateWeapon(k);
		if p ~= k then
			return p:lower();
		end;
		return k:lower();
	end;
(getgenv()).FinhubGetUiFont = function()
		if (getgenv()).FinhubLanguage == "korean" and (getgenv()).FinhubUIFont then
			return (getgenv()).FinhubUIFont;
		end;
		if b and b.Font then
			return b.Font;
		end;
		return (getgenv()).FinhubUIFont or Enum.Font.Roboto;
	end;
(getgenv()).FinhubL = function(k, ...)
		local l = (getgenv()).FinhubLanguage or "english";
		local p = w[l] or w.english;
		local a = p[k] or w.english[k] or k;
		if select("#", ...) > 0 then
			return string.format(a, ...);
		end;
		return a;
	end;
local q = game:GetService("HttpService");
local u = { english = {
			file = "Finhub_ui_en.otf",
			fontFile = "Finhub_ui_en.font",
			name = "FinhubUI_En",
			url = "https://github.com/nyrus573l/esp-fonts/raw/refs/heads/main/fortnite.otf",
		}, korean = {
			file = "Finhub_ui_ko.otf",
			fontFile = "Finhub_ui_ko.font",
			name = "FinhubUI_Ko",
			url = "https://github.com/googlefonts/noto-cjk/raw/main/Sans/OTF/Korean/NotoSansCJKkr-Regular.otf",
		}, spanish = {
			file = "Finhub_ui_es.otf",
			fontFile = "Finhub_ui_es.font",
			name = "FinhubUI_Es",
			url = "https://github.com/nyrus573l/esp-fonts/raw/refs/heads/main/fortnite.otf",
		} };
local function H(k)
	k = k or (getgenv()).FinhubLanguage or "english";
	local l = u[k] or u.english;
	local p, a = pcall(function()
			if isfile and (writefile and getcustomasset) then
				if not isfile(l.file) then
					writefile(l.file, game:HttpGet(l.url));
				end;
				if isfile(l.fontFile) then
					pcall(function()
						delfile(l.fontFile);
					end);
				end;
				local k = { name = l.name, faces = { {
								name = "Regular",
								weight = 400,
								style = "normal",
								assetId = getcustomasset(l.file),
							} } };
				writefile(l.fontFile, q:JSONEncode(k));
				return Font.new(getcustomasset(l.fontFile));
			end;
			return nil;
		end);
	if p and a then
		(getgenv()).FinhubUIFont = a;
		return a;
	end;
	(getgenv()).FinhubUIFont = Font.new("rbxasset://fonts/families/GothamSSm.json", Enum.FontWeight.Medium);
	return (getgenv()).FinhubUIFont;
end;
(getgenv()).FinhubReloadLanguageFont = function(k)
		k = k or (getgenv()).FinhubLanguage or "english";
		if k == "english" and (b and b.Font) then
			(getgenv()).FinhubUIFont = b.Font;
			return b.Font;
		end;
		return H(k);
	end;
task.defer(function()
	pcall(function()
		(getgenv()).FinhubReloadLanguageFont((getgenv()).FinhubLanguage);
	end);
end);
(getgenv()).FinhubDrawingTextPool = (getgenv()).FinhubDrawingTextPool or {};
(getgenv()).FinhubTrackDrawingText = function(k)
		table.insert((getgenv()).FinhubDrawingTextPool, k);
		pcall(function()
			local l = (getgenv()).FinhubUIFont;
			if l and k.FontFace ~= nil then
				k.FontFace = l;
			end;
		end);
		return k;
	end;
(getgenv()).FinhubApplyDrawingUIFont = function()
		local k = (getgenv()).FinhubUIFont;
		if not k then
			return;
		end;
		for l, p in ipairs((getgenv()).FinhubDrawingTextPool) do
			pcall(function()
				if p.FontFace ~= nil then
					p.FontFace = k;
				end;
			end);
		end;
	end;
(getgenv()).FinhubApplyUiFont = function(k, l)
		if not k then
			return;
		end;
		if l then
			k.TextSize = l;
		end;
		local p = (getgenv()).FinhubGetUiFont();
		pcall(function()
			if typeof(p) == "Font" then
				k.FontFace = p;
			else
				k.Font = p;
			end;
		end);
	end;
(getgenv()).FinhubApplyUIFont = function()
		local k = (getgenv()).FinhubGetUiFont();
		if not k then
			return;
		end;
		(getgenv()).FinhubUIFont = k;
		if _G.ESPObjects then
			for l, p in pairs(_G.ESPObjects) do
				if p and p.text then
					for l, p in pairs(p.text) do
						if typeof(p) == "Finhub" and p:IsA("TextLabel") then
							p.FontFace = k;
						end;
					end;
				end;
			end;
		end;
		if (getgenv()).FinhubApplyDrawingUIFont then
			pcall((getgenv()).FinhubApplyDrawingUIFont);
		end;
		if (getgenv()).FinhubApplyKbListTheme then
			pcall((getgenv()).FinhubApplyKbListTheme);
		end;
	end;
(getgenv()).FinhubHudFonts = {
		{ n = "gotham", draw = 2, face = function()
				return Font.new("rbxasset://fonts/families/GothamSSm.json", Enum.FontWeight.Medium);
			end },
		{ n = "code", draw = 3, face = function()
				return Font.fromEnum(Enum.Font.Code);
			end },
		{ n = "roboto", draw = 1, face = function()
				return Font.fromEnum(Enum.Font.Roboto);
			end },
		{ n = "builder", draw = 0, face = function()
				return Font.fromEnum(Enum.Font.BuilderSans);
			end },
		{ n = "arial", draw = 1, face = function()
				return Font.fromEnum(Enum.Font.Arial);
			end },
		{ n = "legacy", draw = 0, face = function()
				return Font.fromEnum(Enum.Font.Legacy);
			end },
		{ n = "source", draw = 2, face = function()
				return Font.fromEnum(Enum.Font.SourceSans);
			end },
		{ n = "custom", draw = 2, face = function()
				return (getgenv()).FinhubUIFont or Font.fromEnum(Enum.Font.Gotham);
			end },
	};
(getgenv()).FinhubHudFontNames = {};
for k, l in ipairs((getgenv()).FinhubHudFonts) do
	(getgenv()).FinhubHudFontNames[k] = l.n;
end;
(getgenv()).FinhubHudFontIdx = 1;
(getgenv()).FinhubHudDrawFont = 2;
(getgenv()).FinhubHudFontIndexByName = function(k)
		for l, p in ipairs((getgenv()).FinhubHudFonts) do
			if p.n == k then
				return l;
			end;
		end;
		return 1;
	end;
(getgenv()).FinhubApplyHudFont = function(k)
		k = math.clamp(tonumber(k) or (getgenv()).FinhubHudFontIdx or 1, 1, #(getgenv()).FinhubHudFonts);
		local l = (getgenv()).FinhubHudFonts[k];
		(getgenv()).FinhubHudFontIdx = k;
		(getgenv()).FinhubHudDrawFont = l.draw or 2;
		local p;
		pcall(function()
			p = l.face();
		end);
		if p then
			(getgenv()).FinhubEspUiFont = p;
		end;
		if _G.ESPObjects then
			for k, l in pairs(_G.ESPObjects) do
				if l and l.text then
					for k, l in pairs(l.text) do
						if typeof(l) == "Finhub" and (l:IsA("TextLabel") and p) then
							l.FontFace = p;
						end;
					end;
				end;
			end;
		end;
		for k, l in ipairs((getgenv()).FinhubDrawingTextPool or {}) do
			pcall(function()
				if l.Font ~= nil then
					l.Font = (getgenv()).FinhubHudDrawFont;
				end;
				if p and l.FontFace ~= nil then
					l.FontFace = p;
				end;
			end);
		end;
	end;
local F = {
		Combat = t:AddTab("combat"),
		Character = t:AddTab("character"),
		Visuals = t:AddTab("visuals"),
		World = t:AddTab("world"),
		Misc = t:AddTab("misc"),
		["UI Settings"] = t:AddTab("settings"),
	};
local function Q()
	if not t or not t.Holder then
		return;
	end;
	local k = t.Holder:FindFirstChildOfClass("Frame");
	if not k then
		return;
	end;
	local l, p, a;
	for k, y in ipairs(k:GetChildren()) do
		if y:IsA("TextLabel") and y.TextXAlignment == Enum.TextXAlignment.Left then
			a = y;
		elseif y:IsA("Frame") then
			local k = y:FindFirstChildOfClass("UIListLayout");
			if k and k.FillDirection == Enum.FillDirection.Horizontal then
				l = y;
				p = k;
			end;
		end;
	end;
	if not l then
		return;
	end;
	local y = a and a.Position.X.Offset or 12;
	local v = 0;
	if a then
		pcall(function()
			v = a.TextBounds.X;
		end);
		if v <= 0 then
			v = 52;
		end;
	end;
	l.Position = UDim2.new(0, (y + v) + 10, 0, 4);
	l.Size = UDim2.new(1, -(((y + v) + 18)), 0, 22);
	l.AnchorPoint = Vector2.new(0, 0);
	if p then
		p.HorizontalAlignment = Enum.HorizontalAlignment.Left;
		p.Padding = UDim.new(0, t.TabPadding or 6);
	end;
end;
task.defer(Q);
local A = game:GetService("ReplicatedStorage");
local I = game:GetService("Players");
local m = game:GetService("Workspace");
local o = workspace.CurrentCamera;
local r = game:GetService("UserInputService");
local C = v(A.Modules.Utility);
local D = v(A.Modules.EnumLibrary);
local h = I.LocalPlayer;
_G.AspectRatioSettings = _G.AspectRatioSettings or { Enabled = false, X = 13, Y = 10 };
local function O()
	local k = _G.AspectRatioSettings;
	if k and k.Enabled then
		return k.X / k.Y;
	end;
	return 1;
end;
local function kJ(k, l)
	local p = O();
	if p == 1 then
		return k;
	end;
	l = l or workspace.CurrentCamera;
	if not l then
		return k;
	end;
	local a = l.ViewportSize.Y * .5;
	return Vector3.new(k.X, a + ((k.Y - a)) * p, k.Z);
end;
local function lJ(k, l)
	l = l or workspace.CurrentCamera;
	if not l or not k then
		return nil, false;
	end;
	local p, a = l:WorldToViewportPoint(k);
	if not a or p.Z <= 0 then
		return p, false;
	end;
	return kJ(p, l), true;
end;
local function pJ(k, l, p)
	p = p or workspace.CurrentCamera;
	if not p then
		return Vector2.zero;
	end;
	local a = p.ViewportSize;
	local y = O();
	local v = a.Y * .5;
	local j = v + ((a.Y * l - v)) * y;
	return Vector2.new(a.X * k, j);
end;
local function aJ(k)
	return pJ(.5, .5, k);
end;
local function yJ(k)
	k = k or workspace.CurrentCamera;
	if not k then
		return nil;
	end;
	local l = k.CFrame;
	if O() == 1 then
		return l;
	end;
	local p = l.Position;
	local a = l.LookVector;
	local y = l.RightVector;
	local v = (y:Cross(a)).Unit;
	return CFrame.fromMatrix(p, y, v, -a);
end;
(getgenv()).FinhubGetAspectStretch = O;
(getgenv()).FinhubWorldToScreen = lJ;
(getgenv()).FinhubScreenAnchor = pJ;
(getgenv()).FinhubScreenCenter = aJ;
(getgenv()).FinhubGetUnstretchedCameraCFrame = yJ;
_G.Features = _G.Features or {};
_G.Features.CrossbowSoundId = "rbxassetid://165946246";
(getgenv()).FinhubCombatLastShotAt = (getgenv()).FinhubCombatLastShotAt or 0;
local function vJ()
	(getgenv()).FinhubCombatLastShotAt = tick();
end;
(getgenv()).FinhubMarkCombatShot = vJ;
(getgenv()).FinhubWorldToScreenEsp = function(k, l)
		return lJ(k, l);
	end;
local jJ;
local sJ = setmetatable({}, { __mode = "k" });
local GJ;
local gJ;
local TJ;
local function cJ(k)
	if not k then
		return nil, nil, nil;
	end;
	local l = k:FindFirstAncestorOfClass("Model");
	if not l then
		return nil, nil, nil;
	end;
	local p = I:GetPlayerFromCharacter(l);
	local a = l:FindFirstChildOfClass("Humanoid");
	return p, a, k.Name;
end;
local function eJ(k)
 
end;
local function bJ()
	local k = h.Character;
	return k and k:FindFirstChild("HumanoidRootPart");
end;
local function fJ(k)
	if not k then
		return;
	end;
	pcall(function()
		k.CFrame = CFrame.new(math.random(-10000, 10000), -99999999999, math.random(-10000, 10000));
	end);
end;
do
	local k = F.Combat:AddLeftTabbox();
	local l = k:AddTab("silent");
	local p = k:AddTab("customization");
	local a = F.Combat:AddRightTabbox();
	local y = a:AddTab("aimbot");
	local v = a:AddTab("customization");
	local j, s, G, T;
	do
		T = {
				"Flamethrower",
				"Fists",
				"Battle Axe",
				"Chainsaw",
				"Katana",
				"Knife",
				"Riot Shield",
				"Scythe",
				"Maul",
				"Trowel",
				"Grenade",
				"Flashbang",
				"Jump Pad",
				"Molotov",
				"Satchel",
				"Smoke Grenade",
				"War Horn",
				"Medkit",
				"Subspace Tripmine",
				"Warpstone",
			};
		j = {
				Line = "rbxassetid://2511451159",
				Beam = "rbxassetid://12781852245",
				Lightning = "rbxassetid://446111271",
				Heartrate = "rbxassetid://5830549480",
				Chain = "rbxassetid://9632168658",
				Glitch = "rbxassetid://8089467613",
				Swirl = "rbxassetid://5638168605",
				Neon = "rbxassetid://6361963422",
				Plasma = "rbxassetid://8993645509",
				Laser = "rbxassetid://14549123968",
			};
		s = {
				["Rust HS"] = "rbxassetid://5043539486",
				Neverlose = "rbxassetid://97643101798871",
				["Minecraft Bow"] = "rbxassetid://3442683707",
				["Minecraft Hit"] = "rbxassetid://8766809464",
				CSGO = "rbxassetid://5764885315",
				Bubble = "rbxassetid://6534947588",
				Lazer = "rbxassetid://130791043",
				Pick = "rbxassetid://1347140027",
				Pop = "rbxassetid://198598793",
				Rust = "rbxassetid://1255040462",
				Sans = "rbxassetid://3188795283",
				Fart = "rbxassetid://130833677",
				Big = "rbxassetid://5332005053",
				Vine = "rbxassetid://5332680810",
				UwU = "rbxassetid://8679659744",
				Bruh = "rbxassetid://4578740568",
				Skeet = "rbxassetid://5633695679",
				Fatality = "rbxassetid://6534947869",
				Bonk = "rbxassetid://5766898159",
				Minecraft = "rbxassetid://5869422451",
				Gamesense = "rbxassetid://4817809188",
				RIFK7 = "rbxassetid://9102080552",
				Bamboo = "rbxassetid://3769434519",
				Crowbar = "rbxassetid://546410481",
				Weeb = "rbxassetid://6442965016",
				Beep = "rbxassetid://8177256015",
				Bambi = "rbxassetid://8437203821",
				Stone = "rbxassetid://3581383408",
				["Old Fatality"] = "rbxassetid://6607142036",
				Click = "rbxassetid://8053704437",
				Ding = "rbxassetid://7149516994",
				Snow = "rbxassetid://6455527632",
				Laser = "rbxassetid://7837461331",
				Mario = "rbxassetid://2815207981",
				Steve = "rbxassetid://4965083997",
				["Call of Duty"] = "rbxassetid://5952120301",
				Bat = "rbxassetid://3333907347",
				["TF2 Critical"] = "rbxassetid://296102734",
				Saber = "rbxassetid://8415678813",
				Baimware = "rbxassetid://3124331820",
				Osu = "rbxassetid://7149255551",
				TF2 = "rbxassetid://2868331684",
				Slime = "rbxassetid://6916371803",
				["Among Us"] = "rbxassetid://5700183626",
				One = "rbxassetid://7380502345",
				["Soft Bell"] = "rbxassetid://9114487369",
				["Minecraft Bow Hit"] = "rbxassetid://1053296915",
			};
		G = {
				"Head",
				"HumanoidRootPart",
				"Torso",
				"UpperTorso",
				"LowerTorso",
				"Left Arm",
				"LeftHand",
				"LeftLowerArm",
				"LeftUpperArm",
				"Right Arm",
				"RightHand",
				"RightLowerArm",
				"RightUpperArm",
				"Left Leg",
				"LeftFoot",
				"LeftLowerLeg",
				"LeftUpperLeg",
				"Right Leg",
				"RightFoot",
				"RightLowerLeg",
				"RightUpperLeg",
				"Neck",
				"Back",
				"Front",
				"Closest",
				"Random",
			};
	end;
	local c = false;
	local e = {};
	local function b()
		local k = I.LocalPlayer;
		if not k:FindFirstChild("PlayerScripts") then
			k.PlayerScriptsAdded:Wait();
		end;
		task.spawn(function()
			local l, p = nil, 0;
			while p < 10 do
				pcall(function()
					local p = k.PlayerScripts.Modules.Items:FindFirstChild("Katana", true);
					if p then
						l = require(p);
					end;
				end);
				if not l then
					for k, p in pairs(k.PlayerScripts:GetDescendants()) do
						if p.Name == "Katana" and p:IsA("ModuleScript") then
							local k, a = pcall(require, p);
							if k then
								l = a;
								break;
							end;
						end;
					end;
				end;
				if l and (type(l) == "table" and l.StartAiming) then
					break;
				end;
				p += 1;
				task.wait(1);
			end;
			if l and (type(l) == "table" and l.StartAiming) then
				local k = l.StartAiming;
				l.StartAiming = function(l, p)
						local a = l.ClientFighter;
						local y = a and a.Player;
						if y then
							e[y] = true;
							local k = l.Info.DeflectDuration or .6;
							task.delay(k, function()
								e[y] = nil;
							end);
						end;
						return k(l, p);
					end;
			end;
		end);
	end;
	b();
	local function f(k)
		return e[k] == true;
	end;
	local function N(k)
		if not k then
			return false;
		end;
		for l, p in ipairs(T) do
			if k == p then
				return true;
			end;
		end;
		return false;
	end;
	local function K()
		local k = m:FindFirstChild("ViewModels");
		if not k then
			return nil;
		end;
		local l = k:FindFirstChild("FirstPerson");
		if not l then
			return nil;
		end;
		for k, l in ipairs(l:GetChildren()) do
			local p = {};
			for k in l.Name:gmatch("[^-]+") do
				table.insert(p, k:match("^%s*(.-)%s*$"));
			end;
			if #p >= 2 then
				return p[2];
			end;
		end;
		return nil;
	end;
	local n = Instance.new("ScreenGui");
	n.Name = "FOVScreenGui";
	n.DisplayOrder = P;
	n.ResetOnSpawn = false;
	n.IgnoreGuiInset = true;
	n.ZIndexBehavior = Enum.ZIndexBehavior.Sibling;
	n.Parent = game.CoreGui;
	local function d(k, l)
		local p = Instance.new("Frame");
		p.Name = k;
		p.BackgroundTransparency = 1;
		p.BorderSizePixel = 0;
		p.Visible = false;
		p.Parent = n;
		local a = Instance.new("Frame");
		a.Size = UDim2.new(1, 0, 1, 0);
		a.BackgroundColor3 = Color3.new(1, 1, 1);
		a.BackgroundTransparency = l.FilledTransparency;
		a.BorderSizePixel = 0;
		a.Visible = false;
		a.ZIndex = 1;
		a.Parent = p;
		local y = Instance.new("UICorner");
		y.CornerRadius = UDim.new(1, 0);
		y.Parent = a;
		local v = Instance.new("UIGradient");
		v.Color = ColorSequence.new({ ColorSequenceKeypoint.new(0, l.FilledColor1), ColorSequenceKeypoint.new(1, l.FilledColor2) });
		v.Rotation = l.FilledRotation;
		v.Parent = a;
		local j = Instance.new("Frame");
		j.Size = UDim2.new(1, 0, 1, 0);
		j.BackgroundTransparency = 1;
		j.BorderSizePixel = 0;
		j.ZIndex = 2;
		j.Parent = p;
		local s = Instance.new("UICorner");
		s.CornerRadius = UDim.new(1, 0);
		s.Parent = j;
		local G = Instance.new("UIStroke");
		G.Color = Color3.new(1, 1, 1);
		G.Thickness = l.OutlineThickness;
		G.Transparency = l.OutlineTransparency;
		G.ApplyStrokeMode = Enum.ApplyStrokeMode.Border;
		G.Parent = j;
		local g = Instance.new("UIGradient");
		g.Color = ColorSequence.new({ ColorSequenceKeypoint.new(0, l.OutlineColor1), ColorSequenceKeypoint.new(1, l.OutlineColor2) });
		g.Rotation = l.OutlineRotation;
		g.Parent = G;
		return {
			container = p,
			fill = a,
			fillgrad = v,
			stroke = G,
			strokegrad = g,
		};
	end;
	local Y = {
			OutlineColor1 = Color3.fromRGB(255, 255, 255),
			OutlineColor2 = Color3.fromRGB(255, 255, 255),
			OutlineRotation = 0,
			OutlineThickness = 1.5,
			OutlineTransparency = 0,
			FilledEnabled = false,
			FilledColor1 = Color3.fromRGB(255, 255, 255),
			FilledColor2 = Color3.fromRGB(0, 0, 0),
			FilledRotation = 0,
			FilledTransparency = .7,
			FilledAnimated = false,
			FilledSpeed = 1,
			SpinOn = false,
			SpinSpd = 1,
		};
	local M = d("SilentFOV", Y);
	local i = M.container;
	local R = M.fill;
	local B = M.fillgrad;
	local W = M.stroke;
	local L = M.strokegrad;
	local function x()
		L.Color = ColorSequence.new({ ColorSequenceKeypoint.new(0, Y.OutlineColor1), ColorSequenceKeypoint.new(1, Y.OutlineColor2) });
	end;
	local function t()
		B.Color = ColorSequence.new({ ColorSequenceKeypoint.new(0, Y.FilledColor1), ColorSequenceKeypoint.new(1, Y.FilledColor2) });
	end;
	local S = {
			OutlineColor1 = Color3.fromRGB(255, 255, 255),
			OutlineColor2 = Color3.fromRGB(255, 255, 255),
			OutlineRotation = 0,
			OutlineThickness = 1.5,
			OutlineTransparency = 0,
			FilledEnabled = false,
			FilledColor1 = Color3.fromRGB(255, 255, 255),
			FilledColor2 = Color3.fromRGB(0, 0, 0),
			FilledRotation = 0,
			FilledTransparency = .7,
			FilledAnimated = false,
			FilledSpeed = 1,
			SpinOn = false,
			SpinSpd = 1,
		};
	local V = d("AimbotFOV", S);
	local X = V.container;
	local Z = V.fill;
	local E = V.fillgrad;
	local J = V.stroke;
	local U = V.strokegrad;
	local function z()
		U.Color = ColorSequence.new({ ColorSequenceKeypoint.new(0, S.OutlineColor1), ColorSequenceKeypoint.new(1, S.OutlineColor2) });
	end;
	local function w()
		E.Color = ColorSequence.new({ ColorSequenceKeypoint.new(0, S.FilledColor1), ColorSequenceKeypoint.new(1, S.FilledColor2) });
	end;
	local q = {
			enabled = false,
			color = Color3.fromRGB(255, 255, 255),
			style = "Line",
			glow = 0,
			size = 1,
			duration = 3,
			fadeTime = .5,
		};
	local u = false;
	local H = {
			enabled = false,
			style = "Rust HS",
			volume = .5,
			pitch = 1.0,
		};
	local Q = setmetatable({}, { __mode = "k" });
	local O = {
			enabled = false,
			hitPart = "Head",
			fovRadius = 100,
			autoShoot = false,
			followMuzzle = false,
			hitChance = 100,
		};
	local kJ = {
			enabled = false,
			masterEnabled = false,
			keyMode = "toggle",
			showFov = false,
			targetPart = "Head",
			fovRadius = 500,
			smoothness = 2,
			aimCurve = "Linear",
			followMuzzle = false,
			lockedTarget = nil,
			smoothCF = nil,
		};
	local pJ = {
			enabled = false,
			connection = nil,
			target = nil,
			origCFrame = nil,
		};
	local gJ = {};
	local TJ = 0;
	local bJ = 0;
	local fJ = .1;
	local NJ = nil;
	local KJ = setmetatable({}, { __mode = "k" });
	local nJ = 0;
	I.PlayerRemoving:Connect(function(k)
		KJ[k] = nil;
	end);
	local function dJ()
		local k = m:FindFirstChild("ViewModels");
		if not k then
			return nil;
		end;
		local l = k:FindFirstChild("FirstPerson");
		if not l then
			return nil;
		end;
		local p = h.Name;
		for k, l in pairs(l:GetChildren()) do
			if l:IsA("Model") and l.Name:find("^" .. p) then
				local k = l:FindFirstChild("ItemVisual");
				if k then
					local l = k:FindFirstChild("Body");
					if l then
						local k = l:FindFirstChild("BodyPrimary");
						if k then
							local l = k:FindFirstChild("_muzzle");
							if l and l:IsA("Attachment") then
								return l.WorldPosition;
							end;
						end;
					end;
				end;
			end;
		end;
		return nil;
	end;
	local function YJ(k)
		if not k then
			return nil;
		end;
		local l, p = lJ(k, o);
		if not p then
			return nil;
		end;
		return Vector2.new(l.X, l.Y);
	end;
	local function MJ()
		if O.followMuzzle then
			local k = YJ(dJ());
			if k then
				return k;
			end;
		end;
		return aJ(o);
	end;
	local function iJ()
		if kJ.followMuzzle then
			local k = YJ(dJ());
			if k then
				return k;
			end;
		end;
		return aJ(o);
	end;
	local function RJ(k, l, p)
		if u then
			return nil;
		end;
		if p then
			local l = dJ();
			if l then
				k = l;
			end;
		end;
		local a = q;
		if p and a.style == "Line" then
			local p = l - k;
			local y = p.Magnitude;
			local v = {};
			local j = 5;
			for l = 1, math.ceil(y / j), 1 do
				local s = k + p.Unit * ((((l - 1)) * j));
				local G = k + p.Unit * math.min(l * j, y);
				local g = Drawing.new("Line");
				g.Thickness = 2 * a.size;
				g.Color = a.color;
				g.Transparency = 1;
				g.Visible = false;
				table.insert(v, { Line = g, StartPos = s, EndPos = G });
			end;
			local s = {
					Segments = v,
					Lifetime = a.duration,
					CreatedTime = tick(),
					IsHitTracer = true,
					Is2D = true,
					FadeStartTime = (tick() + a.duration) - a.fadeTime,
					StartPos = k,
					EndPos = l,
				};
			table.insert(gJ, s);
			return s;
		end;
		local y = Instance.new("Attachment");
		y.Parent = workspace.Terrain;
		local v = Instance.new("Attachment");
		v.Parent = workspace.Terrain;
		local s = Instance.new("Beam");
		s.Attachment0 = y;
		s.Attachment1 = v;
		s.Color = ColorSequence.new(a.color);
		local G = a.style == "Laser" and .02 or (a.style == "Line" and .05 or .15);
		s.Width0 = G * a.size;
		s.Width1 = G * a.size;
		s.Transparency = NumberSequence.new({ NumberSequenceKeypoint.new(0, 0), NumberSequenceKeypoint.new(.8, .1), NumberSequenceKeypoint.new(1, .5) });
		s.FaceCamera = false;
		s.LightEmission = a.glow;
		s.LightInfluence = 1 - a.glow;
		local g = nil;
		if a.glow > .3 then
			g = Instance.new("PointLight");
			g.Brightness = a.glow * 2;
			g.Range = 10 * a.size;
			g.Color = a.color;
			g.Parent = v;
		end;
		if a.style == "Line" then
			s.Texture = "";
			s.TextureLength = 1;
			s.TextureSpeed = 0;
		elseif j[a.style] then
			s.Texture = j[a.style];
			s.TextureLength = 4;
			s.TextureSpeed = 1;
		else
			s.Texture = "";
		end;
		s.Parent = workspace.Terrain;
		y.WorldPosition = k;
		v.WorldPosition = l;
		local T = {
				Line = s,
				Attachment0 = y,
				Attachment1 = v,
				Light = g,
				Lifetime = a.duration,
				CreatedTime = tick(),
				IsHitTracer = p,
				Is2D = false,
				FadeStartTime = (tick() + a.duration) - a.fadeTime,
			};
		table.insert(gJ, T);
		return T;
	end;
	local function BJ()
		for k, l in ipairs(gJ) do
			if l and (not l.Is2D and l.Line) then
				local k = tick() - l.CreatedTime;
				local p = q;
				if k >= l.FadeStartTime then
					local a = math.clamp(((k - l.FadeStartTime)) / p.fadeTime, 0, 1);
					l.Line.Transparency = NumberSequence.new({ NumberSequenceKeypoint.new(0, a), NumberSequenceKeypoint.new(.8, a + .1), NumberSequenceKeypoint.new(1, 1) });
					if l.Line.LightEmission > 0 then
						l.Line.LightEmission = p.glow * ((1 - a));
					end;
					if l.Light then
						l.Light.Brightness = l.Light.Brightness * ((1 - a));
					end;
				end;
			end;
		end;
	end;
	local function WJ()
		local k = h.Character;
		if not k then
			local k = workspace.CurrentCamera;
			return k and (k.CFrame.Position + k.CFrame.LookVector * 4) or Vector3.zero;
		end;
		local l = m:FindFirstChild("ViewModels");
		if l then
			local k = l:FindFirstChild("FirstPerson");
			if k then
				for k, l in ipairs(k:GetChildren()) do
					if not l:IsA("Model") then
						continue;
					end;
					local p = l:FindFirstChild("Muzzle") or l:FindFirstChild("MuzzleFlash") or l:FindFirstChild("Barrel") or l:FindFirstChild("GunTip") or l:FindFirstChild("Flash") or l:FindFirstChild("Fire") or l:FindFirstChild("Tip");
					if p then
						if p:IsA("Attachment") then
							return p.WorldPosition;
						end;
						if p:IsA("BasePart") then
							return p.Position;
						end;
					end;
					for k, l in ipairs(l:GetChildren()) do
						if l:IsA("BasePart") then
							local k = l.Name:lower();
							if k:find("tip") or k:find("barrel") or k:find("muzzle") or k:find("flash") or k:find("fire") or k:find("gun") then
								return l.Position;
							end;
						end;
					end;
					local a = l.PrimaryPart or l:FindFirstChildWhichIsA("BasePart");
					if a then
						return a.Position;
					end;
				end;
			end;
		end;
		local p = workspace.CurrentCamera;
		if p then
			return p.CFrame.Position + p.CFrame.LookVector * 4;
		end;
		local a = k:FindFirstChild("HumanoidRootPart");
		return a and a.Position or Vector3.zero;
	end;
	local LJ = nil;
	local PJ = nil;
	local xJ = nil;
	local tJ = nil;
	_G.Features = _G.Features or {};
	_G.Features.DisableGunSounds = _G.Features.DisableGunSounds or false;
	local function SJ()
		return _G.Features.DisableGunSounds == true;
	end;
	local function VJ()
		local function k(k)
			if not k then
				return;
			end;
			for k, l in ipairs(k:GetDescendants()) do
				if l:IsA("Sound") and (isRivalsGunSound(l) and not l:GetAttribute("FinhubShootSound")) then
					pcall(function()
						if l.Volume <= 0 then
							l.Volume = 1;
						end;
					end);
				elseif l:IsA("SoundGroup") then
					pcall(function()
						if l.Volume <= 0 then
							l.Volume = 1;
						end;
					end);
				end;
			end;
		end;
		k(workspace:FindFirstChild("ViewModels"));
		local l = workspace.CurrentCamera;
		if l then
			k(l);
		end;
		local p = h.Character;
		if p then
			k(p);
		end;
	end;
	local function XJ()
		return q.enabled;
	end;
	local function ZJ()
		local k = h:FindFirstChild("PlayerScripts");
		if not k then
			return nil;
		end;
		local l = k:FindFirstChild("Controllers");
		if not l then
			return nil;
		end;
		local p = l:FindFirstChild("FighterController");
		if not p or not p:IsA("ModuleScript") then
			return nil;
		end;
		local a, y = pcall(require, p);
		if a then
			return y;
		end;
		return nil;
	end;
	local function EJ()
		local k = ZJ();
		if k and (k.LocalFighter and k.LocalFighter.EquippedItem) then
			local l = k.LocalFighter.EquippedItem;
			local p = l:Get("CurrentAmmo");
			if p == nil then
				p = l:Get("Ammo");
			end;
			if p == nil then
				p = l:Get("Bullets");
			end;
			local a = l:Get("MaxAmmo") or l:Get("MaxBullets") or 0;
			if p ~= nil then
				return p, a;
			end;
		end;
		local l = h:FindFirstChild("PlayerGui");
		if l then
			for k, l in ipairs(l:GetDescendants()) do
				if l:IsA("Frame") and l.Visible then
					local k = l:FindFirstChild("Reserve");
					if k and (l:FindFirstChild("Icon") and l:FindFirstChild("ItemName")) then
						local l = k:FindFirstChild("Ammo");
						if l then
							local k = tonumber(l.Text:match("%d+")) or 0;
							return k, k;
						end;
					end;
				end;
			end;
		end;
		return nil, nil;
	end;
	local function JJ(k)
		if not k then
			return false;
		end;
		return k == "Slingshot" or k == "CoreProjectile" or k == "OuterProjectile" or k:find("Projectile", 1, true) ~= nil;
	end;
	local function UJ(k)
		if not k then
			return false;
		end;
		local l = k:IsA("BasePart") and k or k:FindFirstChildWhichIsA("BasePart");
		if not l then
			if k:IsA("Model") then
				l = k.PrimaryPart or k:FindFirstChildWhichIsA("BasePart");
			end;
		end;
		if not l then
			return false;
		end;
		if not JJ(k.Name) and not JJ(l.Name) then
			local p = k:FindFirstChild("CoreProjectile", true) or k:FindFirstChild("OuterProjectile", true) or k:FindFirstChild("Slingshot", true);
			if not p then
				return false;
			end;
			l = p:IsA("BasePart") and p or p:FindFirstChildWhichIsA("BasePart") or l;
		end;
		local p = WJ();
		if ((l.Position - p)).Magnitude <= 45 then
			return true;
		end;
		local a = workspace.CurrentCamera;
		if a and ((l.Position - a.CFrame.Position)).Magnitude <= 45 then
			return true;
		end;
		local y = h.Character and h.Character:FindFirstChild("HumanoidRootPart");
		if y and ((l.Position - y.Position)).Magnitude <= 45 then
			return true;
		end;
		return false;
	end;
	local function zJ(k)
		if typeof(k) ~= "Finhub" or not k:IsA("Sound") then
			return false;
		end;
		if k:GetAttribute("FinhubShootSound") then
			return false;
		end;
		local l = k.Parent;
		if not l then
			return false;
		end;
		local p = workspace:FindFirstChild("ViewModels");
		if p and k:IsDescendantOf(p) then
			return true;
		end;
		local a = workspace.CurrentCamera;
		if a and k:IsDescendantOf(a) then
			return true;
		end;
		local y = h.Character;
		if y and k:IsDescendantOf(y) then
			local l = k.Name:lower();
			if l:find("gun") or l:find("fire") or l:find("shoot") or l:find("weapon") or l:find("muzzle") or l:find("shell") or l:find("reload") or l:find("bolt") or l:find("chamber") or l:find("rifle") or l:find("pistol") or l:find("shotgun") or l:find("bullet") then
				return true;
			end;
		end;
		return false;
	end;
	local function wJ(k)
		if not zJ(k) then
			return;
		end;
		pcall(function()
			k:Stop();
		end);
		k.Volume = 0;
	end;
	local function qJ(k)
		if typeof(k) ~= "Finhub" or not k:IsA("Sound") or k:GetAttribute("FinhubShootSound") then
			return;
		end;
		if not k:GetAttribute("FinhubGunMuteBound") then
			k:SetAttribute("FinhubGunMuteBound", true);
			(k:GetPropertyChangedSignal("Volume")):Connect(function()
				if SJ() and zJ(k) then
					pcall(function()
						k.Volume = 0;
					end);
				end;
			end);
			(k:GetPropertyChangedSignal("Playing")):Connect(function()
				if not k.Playing or not zJ(k) then
					return;
				end;
				task.defer(function()
					if XJ() then
						handleLocalShot(true);
					else
						registerLocalShot(true);
					end;
				end);
				if SJ() then
					wJ(k);
				end;
			end);
		end;
		if SJ() then
			wJ(k);
		end;
	end;
	local function uJ()
		if not SJ() then
			return;
		end;
		local k = workspace:FindFirstChild("ViewModels");
		if k then
			for k, l in ipairs(k:GetDescendants()) do
				if l:IsA("Sound") or l:IsA("SoundGroup") then
					if l:IsA("Sound") then
						qJ(l);
					else
						l.Volume = 0;
					end;
				end;
			end;
		end;
		local l = workspace.CurrentCamera;
		if l then
			for k, l in ipairs(l:GetDescendants()) do
				if l:IsA("Sound") then
					qJ(l);
				end;
			end;
		end;
	end;
	(getgenv()).FinhubRefreshGunSoundMute = uJ;
	local function HJ(k)
		local l = K();
		if not l or N(l) then
			return;
		end;
		local p = tick();
		if not k and p - TJ < fJ then
			return;
		end;
		TJ = p;
		vJ();
	end;
	local function FJ(k)
		HJ(k);
		local l = WJ();
		local p;
		local a;
		if l then
			local k = o.CFrame;
			p = k.Position + k.LookVector * 1000;
			local y = RaycastParams.new();
			y.FilterDescendantsInstances = { h.Character };
			y.FilterType = Enum.RaycastFilterType.Blacklist;
			local v = workspace:Raycast(l, ((p - l)).Unit * 1000, y);
			if v then
				p = v.Position;
				a = v.Instance;
			end;
		end;
		if a then
			eJ(a);
		end;
		if not q.enabled or not l then
			return;
		end;
		RJ(l, p, a ~= nil);
	end;
	(getgenv()).FinhubHandleLocalShot = FJ;
	local function QJ()
		if PJ then
			return;
		end;
		local k = 0;
		PJ = g.Heartbeat:Connect(function()
				local l = tick();
				if l < k then
					return;
				end;
				k = l + .25;
				pcall(function()
					if SJ() then
						uJ();
					end;
					local k = EJ();
					if k == nil then
						LJ = nil;
						return;
					end;
					if LJ ~= nil and k < LJ then
						local l = math.floor(LJ - k);
						for k = 1, l, 1 do
							if XJ() then
								FJ(true);
							else
								HJ(true);
							end;
						end;
					end;
					LJ = k;
				end);
			end);
	end;
	local function AJ(k)
		if not k then
			return;
		end;
		local l = false;
		local p = cJ;
		local a = eJ;
		local function y(k)
			if l or not k or not k.Parent then
				return;
			end;
			if type(a) == "function" then
				a(k);
			end;
			if type(p) ~= "function" then
				return;
			end;
			local y, v = p(k);
			if not y or not v or y == player then
				l = true;
				return;
			end;
			l = true;
		end;
		local function v(k)
			if not k or not k:IsA("BasePart") then
				return;
			end;
			k.Touched:Connect(y);
		end;
		v(k);
		for k, l in ipairs(k:GetDescendants()) do
			v(l);
		end;
	end;
	local function IJ()
		if xJ then
			return;
		end;
		xJ = workspace.DescendantAdded:Connect(function(k)
				if not UJ(k) then
					return;
				end;
				task.defer(function()
					if XJ() then
						FJ(true);
					else
						HJ(true);
					end;
					AJ(k);
				end);
			end);
	end;
	local function mJ(k)
		if not k then
			return;
		end;
		for k, l in ipairs(k:GetDescendants()) do
			if l:IsA("Sound") then
				qJ(l);
			end;
		end;
	end;
	local oJ = false;
	local function rJ()
		if oJ then
			return;
		end;
		oJ = true;
		QJ();
		IJ();
		local k = workspace:FindFirstChild("ViewModels");
		if not k then
			k = workspace:WaitForChild("ViewModels", 10);
		end;
		if k and not tJ then
			mJ(k);
			tJ = k.DescendantAdded:Connect(function(k)
					if k:IsA("Sound") then
						qJ(k);
					elseif k:IsA("SoundGroup") and SJ() then
						pcall(function()
							k.Volume = 0;
						end);
					end;
				end);
		end;
		local l = workspace.CurrentCamera;
		if l then
			mJ(l);
		end;
		r.InputBegan:Connect(function(k, l)
			if l then
				return;
			end;
			if k.UserInputType ~= Enum.UserInputType.MouseButton1 then
				return;
			end;
			if r:GetFocusedTextBox() then
				return;
			end;
			if XJ() then
				FJ(false);
			else
				HJ(false);
			end;
		end);
		uJ();
	end;
	task.defer(rJ);
	local function CJ()
		local k = s[H.style];
		if not k then
			return;
		end;
		local l = Instance.new("Sound");
		l.SoundId = k;
		l.Volume = H.volume;
		l.Pitch = H.pitch;
		local p = workspace.CurrentCamera;
		if p then
			local k = Instance.new("Attachment");
			k.Parent = p;
			l.Parent = k;
		else
			l.Parent = workspace;
		end;
		l:Play();
		(game:GetService("Debris")):AddItem(l, 5);
		if l.Parent then
			(game:GetService("Debris")):AddItem(l.Parent, 5);
		end;
	end;
	local function DJ()
		if not NJ then
			return;
		end;
		local k = NJ;
		if not k or not k.Parent then
			return;
		end;
		local l = k:FindFirstChildOfClass("Humanoid");
		if not l then
			return;
		end;
		local p = I:GetPlayerFromCharacter(k);
		if not p then
			return;
		end;
		local a = KJ[p] or l.Health;
		if l.Health < a then
			local l = h.Character;
			if l and l:FindFirstChild("HumanoidRootPart") then
				local p = k:FindFirstChild("HumanoidRootPart");
				if p then
					local a = ((l.HumanoidRootPart.Position - p.Position)).Magnitude;
					if a <= 1500 then
						local y = ((p.Position - l.HumanoidRootPart.Position)).Unit;
						local v = RaycastParams.new();
						v.FilterDescendantsInstances = { l, k };
						v.FilterType = Enum.RaycastFilterType.Exclude;
						local j = workspace:Raycast(l.HumanoidRootPart.Position, y * a, v);
						local s = true;
						if j then
							local l = j.Instance.Parent;
							if l ~= k and l.Parent ~= k then
								s = false;
							end;
						end;
						if s and tick() - nJ > .05 then
							nJ = tick();
							if H.enabled then
								task.spawn(CJ);
							end;
						end;
					end;
				end;
			end;
		end;
		KJ[p] = l.Health;
	end;
	g.RenderStepped:Connect(function()
		local k = i.Visible;
		local l = X.Visible;
		local p = #gJ > 0;
		if not k and (not l and not p) then
			return;
		end;
		if k then
			local k = MJ();
			local l = O.fovRadius;
			i.Size = UDim2.fromOffset(l * 2, l * 2);
			i.Position = UDim2.fromOffset(k.X - l, k.Y - l);
			if Y.FilledAnimated then
				B.Rotation = math.sin(tick() * Y.FilledSpeed) * 180 + Y.FilledRotation;
			elseif Y.SpinOn then
				B.Rotation = Y.FilledRotation + (((tick() * Y.SpinSpd) * 90)) % 360;
			end;
			if Y.SpinOn then
				L.Rotation = Y.OutlineRotation + (((tick() * Y.SpinSpd) * 90)) % 360;
			end;
		end;
		if l then
			local k = iJ();
			local l = kJ.fovRadius;
			X.Size = UDim2.fromOffset(l * 2, l * 2);
			X.Position = UDim2.fromOffset(k.X - l, k.Y - l);
			if S.FilledAnimated then
				E.Rotation = math.sin(tick() * S.FilledSpeed) * 180 + S.FilledRotation;
			elseif S.SpinOn then
				E.Rotation = S.FilledRotation + (((tick() * S.SpinSpd) * 90)) % 360;
			end;
			if S.SpinOn then
				U.Rotation = S.OutlineRotation + (((tick() * S.SpinSpd) * 90)) % 360;
			end;
		end;
		if p then
			BJ();
			local k = tick();
			local l = o;
			local p = h.Character;
			local a = p and p:FindFirstChild("HumanoidRootPart");
			local y = a and a.Position;
			for p, a in ipairs(gJ) do
				if a and (a.Is2D and a.Segments) then
					local p = k - a.CreatedTime;
					for k, v in ipairs(a.Segments) do
						local j = false;
						local s, G;
						local g, T = lJ(v.StartPos, l);
						local c, e = lJ(v.EndPos, l);
						if T and e then
							if not y or (((v.StartPos - y)).Magnitude >= 2 and ((v.EndPos - y)).Magnitude >= 2) then
								if math.abs(g.X) <= 20000 and (math.abs(g.Y) <= 20000 and (math.abs(c.X) <= 20000 and math.abs(c.Y) <= 20000)) then
									s = Vector2.new(g.X, g.Y);
									G = Vector2.new(c.X, c.Y);
									local k = ((G - s)).Magnitude;
									if k > .1 and k < 3000 then
										j = true;
									end;
								end;
							end;
						end;
						if j then
							v.Line.From = s;
							v.Line.To = G;
							v.Line.Visible = true;
							local k = q.fadeTime;
							if p >= a.Lifetime - k then
								v.Line.Transparency = 1 - math.clamp(((p - ((a.Lifetime - k)))) / k, 0, 1);
							else
								v.Line.Transparency = 1;
							end;
						else
							v.Line.Visible = false;
						end;
					end;
				end;
			end;
			for l = #gJ, 1, -1 do
				local p = gJ[l];
				if p and k - p.CreatedTime >= p.Lifetime then
					if p.Is2D and p.Segments then
						for k, l in ipairs(p.Segments) do
							if l.Line then
								l.Line:Remove();
							end;
						end;
					elseif p.Line then
						p.Line:Destroy();
					end;
					if p.Attachment0 then
						p.Attachment0:Destroy();
					end;
					if p.Attachment1 then
						p.Attachment1:Destroy();
					end;
					if p.Light then
						p.Light:Destroy();
					end;
					table.remove(gJ, l);
				end;
			end;
		end;
		DJ();
	end);
	local function hJ(k, l)
		local p = function(l)
				return k:FindFirstChild(l);
			end;
		if l == "Head" then
			return p("Head");
		elseif l == "HumanoidRootPart" then
			return p("HumanoidRootPart");
		elseif l == "Torso" then
			return p("Torso") or p("UpperTorso");
		elseif l == "UpperTorso" then
			return p("UpperTorso");
		elseif l == "LowerTorso" then
			return p("LowerTorso");
		elseif l == "Left Arm" then
			return p("Left Arm") or p("LeftUpperArm");
		elseif l == "LeftHand" then
			return p("LeftHand") or p("Left Arm");
		elseif l == "LeftLowerArm" then
			return p("LeftLowerArm");
		elseif l == "LeftUpperArm" then
			return p("LeftUpperArm");
		elseif l == "Right Arm" then
			return p("Right Arm") or p("RightUpperArm");
		elseif l == "RightHand" then
			return p("RightHand") or p("Right Arm");
		elseif l == "RightLowerArm" then
			return p("RightLowerArm");
		elseif l == "RightUpperArm" then
			return p("RightUpperArm");
		elseif l == "Left Leg" then
			return p("Left Leg") or p("LeftUpperLeg");
		elseif l == "LeftFoot" then
			return p("LeftFoot") or p("Left Leg");
		elseif l == "LeftLowerLeg" then
			return p("LeftLowerLeg");
		elseif l == "LeftUpperLeg" then
			return p("LeftUpperLeg");
		elseif l == "Right Leg" then
			return p("Right Leg") or p("RightUpperLeg");
		elseif l == "RightFoot" then
			return p("RightFoot") or p("Right Leg");
		elseif l == "RightLowerLeg" then
			return p("RightLowerLeg");
		elseif l == "RightUpperLeg" then
			return p("RightUpperLeg");
		elseif l == "Neck" then
			return p("Neck");
		elseif l == "Back" then
			return p("Back") or p("HumanoidRootPart");
		elseif l == "Front" then
			return p("Front") or p("HumanoidRootPart");
		elseif l == "Closest" then
			local l = o.CFrame.Position;
			local a = o.CFrame.LookVector;
			local y, v = nil, math.huge;
			for k, p in pairs(k:GetChildren()) do
				if p:IsA("BasePart") then
					local k = 1 - a:Dot(((p.Position - l)).Unit);
					if k < v then
						v = k;
						y = p;
					end;
				end;
			end;
			return y or p("HumanoidRootPart");
		elseif l == "Random" then
			local l = {};
			for k, p in pairs(k:GetChildren()) do
				if p:IsA("BasePart") then
					table.insert(l, p);
				end;
			end;
			if #l > 0 then
				return l[math.random(1, #l)];
			end;
		end;
		return k:FindFirstChild("HumanoidRootPart");
	end;
	local function OJ()
		if O.hitChance >= 100 then
			return true;
		end;
		if O.hitChance <= 0 then
			return false;
		end;
		return math.random(1, 100) <= O.hitChance;
	end;
	local function kn(k, l)
		local p, a = nil, math.huge;
		local y = o;
		for v, j in ipairs(I:GetPlayers()) do
			if j ~= h then
				local v = j.Character;
				if v then
					local j = v:FindFirstChildOfClass("Humanoid");
					if j and (j.Health > 0 and not v:FindFirstChildOfClass("ForceField")) then
						local j = v:FindFirstChild("HumanoidRootPart");
						if j then
							local s, G = lJ(j.Position, y);
							if G then
								local y = s.X - l.X;
								local j = s.Y - l.Y;
								local G = math.sqrt(y * y + j * j);
								if G <= k and G < a then
									p = v;
									a = G;
								end;
							end;
						end;
					end;
				end;
			end;
		end;
		return p;
	end;
	local function ln(k)
		local l = kn(k, MJ());
		NJ = l;
		return l;
	end;
	local pn = nil;
	local an = 0;
	local yn = .05;
	local function vn()
		if not O.enabled then
			return;
		end;
		local k = K();
		if k and N(k) then
			return;
		end;
		local l = tick();
		if l - an < yn then
			return;
		end;
		if not OJ() then
			return;
		end;
		local p = ln(O.fovRadius);
		if not p then
			return;
		end;
		local a = I:GetPlayerFromCharacter(p);
		if c and (a and f(a)) then
			return;
		end;
		local y = hJ(p, O.hitPart);
		if not y then
			return;
		end;
		local v = h.Character;
		local j = v and v:FindFirstChild("HumanoidRootPart");
		if not j then
			return;
		end;
		local s = pn and pn.EquippedItem;
		if not s then
			return;
		end;
		local G = s:Get("ObjectID");
		if not G then
			return;
		end;
		an = l;
		local g = j.Position;
		local T = y.Position;
		local e = { [utf8.char(1)] = {
					[utf8.char(0)] = C:EncodeCFrame(CFrame.new(g, T)),
					[utf8.char(1)] = C:EncodeCFrame(CFrame.new(g, T)),
					[utf8.char(2)] = y,
					[utf8.char(3)] = C:EncodeCFrame(CFrame.new(.43, .25, .42)),
				} };
		pcall(function()
			A.Remotes.Replication.Fighter.UseItem:FireServer(G, D:ToEnum("StartShooting"), e, nil);
		end);
	end;
	g.Heartbeat:Connect(function()
		if O.enabled then
			if O.autoShoot or r:IsMouseButtonPressed(Enum.UserInputType.MouseButton1) then
				vn();
			end;
		end;
	end);
	repeat
		task.wait(.1);
	until game:IsLoaded();
	repeat
		task.wait(.1);
	until h.Parent;
	repeat
		task.wait(.1);
	until h:FindFirstChild("PlayerScripts");
	task.wait(.8);
	h:WaitForChild("PlayerScripts", 10);
	local jn;
	pcall(function()
		local k = h.PlayerScripts:WaitForChild("Controllers", 10);
		local l = k:FindFirstChild("CameraController");
		if l and l:IsA("ModuleScript") then
			jn = require(l);
		end;
		local p = k:FindFirstChild("FighterController");
		if p and p:IsA("ModuleScript") then
			local k = require(p);
			pn = k.LocalFighter;
		end;
	end);
	local function sn()
		kJ.lockedTarget = nil;
		kJ.smoothCF = nil;
	end;
	local function Gn()
		if kJ.followMuzzle then
			return iJ();
		end;
		local k = r:GetMouseLocation();
		return Vector2.new(k.X, k.Y);
	end;
	local function gn()
		local k, l = nil, kJ.fovRadius;
		local p = Gn();
		if not p then
			return nil;
		end;
		local a = workspace.CurrentCamera or o;
		for y, v in ipairs(I:GetPlayers()) do
			if v ~= h and v.Character then
				local y = v.Character:FindFirstChild(kJ.targetPart);
				if y and y:IsDescendantOf(workspace) then
					local v, j = lJ(y.Position, a);
					if j then
						local a = v.X - p.X;
						local j = v.Y - p.Y;
						local s = math.sqrt(a * a + j * j);
						if s < l then
							l = s;
							k = y;
						end;
					end;
				end;
			end;
		end;
		return k;
	end;
	local function Tn(k)
		local l = math.clamp(tonumber(kJ.smoothness) or 2, .1, 10);
		local p = kJ.aimCurve or "Linear";
		local a = 6 / l;
		if p == "Instant" then
			return 1;
		elseif p == "Expo" then
			return 1 - math.exp(-((4 / l)) * k);
		elseif p == "EaseIn" then
			local l = math.clamp(a * k, 0, 1);
			return l * l;
		elseif p == "EaseOut" then
			local l = math.clamp(a * k, 0, 1);
			return 1 - ((1 - l)) * ((1 - l));
		elseif p == "EaseInOut" then
			local l = math.clamp(a * k, 0, 1);
			if l < .5 then
				return (2 * l) * l;
			end;
			return 1 - (((-2 * l + 2)) ^ 2) / 2;
		elseif p == "Cubic" then
			local l = math.clamp(a * k, 0, 1);
			return (l * l) * l;
		end;
		return math.clamp(a * k, 0, 1);
	end;
	local cn = "FinhubAimbotUpdate";
	local en;
	local bn = false;
	local function fn(k)
		k = k or (.0041666666666667);
		if not kJ.enabled then
			sn();
			return;
		end;
		local l = workspace.CurrentCamera;
		if not l then
			return;
		end;
		o = l;
		if not kJ.lockedTarget then
			kJ.lockedTarget = gn();
			kJ.smoothCF = yJ(l);
			if not kJ.lockedTarget then
				return;
			end;
		end;
		if not kJ.lockedTarget.Parent or not kJ.lockedTarget:IsDescendantOf(workspace) then
			sn();
			return;
		end;
		local p = h.Character;
		if not p then
			return;
		end;
		local a = p:FindFirstChild("Head");
		if not a then
			sn();
			return;
		end;
		if not jn then
			return;
		end;
		if not kJ.smoothCF then
			kJ.smoothCF = yJ(l);
		end;
		local y = CFrame.lookAt(l.CFrame.Position, kJ.lockedTarget.Position);
		local v = Tn(k);
		kJ.smoothCF = kJ.smoothCF:Lerp(y, v);
		if jn and jn.MimicRotation then
			pcall(function()
				jn:MimicRotation(kJ.smoothCF);
			end);
		end;
	end;
	local function Nn()
		if en then
			en:Disconnect();
			en = nil;
		end;
		if bn then
			pcall(function()
				g:UnbindFromRenderStep(cn);
			end);
			bn = false;
		end;
		X.Visible = kJ.showFov;
		if not kJ.enabled then
			sn();
			return;
		end;
		local k = pcall(function()
				g:UnbindFromRenderStep(cn);
				g:BindToRenderStep(cn, Enum.RenderPriority.Camera.Value + 1, fn);
			end);
		if k then
			bn = true;
		else
			en = g.RenderStepped:Connect(fn);
		end;
	end;
	local function Kn()
		local k, l = nil, math.huge;
		local p = aJ(o);
		for a, y in I:GetPlayers() do
			if y ~= h and y.Character then
				local a = y.Character:FindFirstChild("HumanoidRootPart");
				if a then
					local v, j = lJ(a.Position, o);
					if j then
						local a = ((p - Vector2.new(v.X, v.Y))).Magnitude;
						if a < l then
							k = y.Character;
							l = a;
						end;
					end;
				end;
			end;
		end;
		return k;
	end;
	local function nn()
		if pJ.connection then
			pJ.connection:Disconnect();
		end;
		pJ.connection = g.Heartbeat:Connect(function()
				local k = h.Character;
				if not k or not k:FindFirstChild("HumanoidRootPart") then
					return;
				end;
				if not pJ.target then
					return;
				end;
				local l = pJ.target:FindFirstChild("HumanoidRootPart");
				if not l then
					return;
				end;
				local p = l.Position + (-l.CFrame.LookVector * 5);
				k.HumanoidRootPart.CFrame = CFrame.new(p, l.Position);
			end);
	end;
	local function dn()
		if pJ.connection then
			pJ.connection:Disconnect();
			pJ.connection = nil;
		end;
	end;
	local Yn = nil;
	local function Mn()
		if Yn then
			return;
		end;
		Yn = g.Heartbeat:Connect(function()
				if not pJ.target then
					if Yn then
						Yn:Disconnect();
						Yn = nil;
					end;
					return;
				end;
				local k = pJ.target:FindFirstChild("Humanoid");
				if not k or k.Health <= 0 then
					local k = h.Character;
					if k and (k:FindFirstChild("HumanoidRootPart") and pJ.origCFrame) then
						k.HumanoidRootPart.CFrame = pJ.origCFrame;
					end;
					pJ.target = nil;
					dn();
					if Yn then
						Yn:Disconnect();
						Yn = nil;
					end;
				end;
			end);
	end;
	h.CharacterRemoving:Connect(function()
		dn();
		pJ.target = nil;
		pJ.origCFrame = nil;
		if Yn then
			Yn:Disconnect();
			Yn = nil;
		end;
	end);
	I.PlayerRemoving:Connect(function(k)
		if pJ.target and k.Character == pJ.target then
			local k = h.Character;
			if k and (k:FindFirstChild("HumanoidRootPart") and pJ.origCFrame) then
				k.HumanoidRootPart.CFrame = pJ.origCFrame;
			end;
			pJ.target = nil;
			dn();
			if Yn then
				Yn:Disconnect();
				Yn = nil;
			end;
		end;
	end);
	task.spawn(function()
		while true do
			task.wait(5);
			pcall(function()
				local k = workspace.CurrentCamera;
				if k then
					for k, l in ipairs(k:GetChildren()) do
						if l:IsA("BillboardGui") and l.Name == "FortniteDamageNumber" then
							if not l.Adornee or not l.Adornee.Parent then
								l:Destroy();
							end;
						end;
					end;
					if game:GetService("Workspace") and (game:GetService("Workspace")).Terrain then
						local k = (game:GetService("Workspace")).Terrain;
						for k, l in ipairs(k:GetChildren()) do
							if l:IsA("Attachment") then
								local k = l:FindFirstChildWhichIsA("Beam");
								if not k then
									local k = true;
									for l, p in ipairs(l.Parent:GetChildren()) do
										if p:IsA("Beam") then
											k = false;
											break;
										end;
									end;
									if k then
										pcall(function()
											l:Destroy();
										end);
									end;
								end;
							end;
						end;
					end;
				end;
			end);
		end;
	end);
	(l:AddToggle("SilentAim", { Text = "enable", Default = false, Callback = function(k)
			O.enabled = k;
			if not k then
				NJ = nil;
			end;
		end })):AddKeyPicker("SilentAimKey", {
		Text = "Silent Aim",
		Default = "None",
		Mode = "Toggle",
		NoUI = true,
		SyncToggleState = false,
		Callback = function(k)
			O.enabled = k;
			if not k then
				NJ = nil;
			end;
		end,
	});
	(l:AddToggle("BackshootToggle", { Text = "backshoot", Default = false, Callback = function(k)
			pJ.enabled = k;
			if k and pJ.target then
				nn();
				Mn();
			else
				dn();
				if pJ.target and pJ.origCFrame then
					local k = h.Character;
					if k and k:FindFirstChild("HumanoidRootPart") then
						k.HumanoidRootPart.CFrame = pJ.origCFrame;
					end;
				end;
				if Yn then
					Yn:Disconnect();
					Yn = nil;
				end;
			end;
		end })):AddKeyPicker("SelectTargetKey", {
		Text = "Backshoot",
		Default = "None",
		Mode = "Toggle",
		NoUI = true,
		SyncToggleState = false,
		Callback = function(k)
			if not k then
				return;
			end;
			local l = h.Character;
			if not l or not l:FindFirstChild("HumanoidRootPart") then
				return;
			end;
			if pJ.target then
				if pJ.origCFrame then
					l.HumanoidRootPart.CFrame = pJ.origCFrame;
				end;
				pJ.target = nil;
				dn();
				if Yn then
					Yn:Disconnect();
					Yn = nil;
				end;
			else
				pJ.target = Kn();
				if pJ.target then
					pJ.origCFrame = l.HumanoidRootPart.CFrame;
					if pJ.enabled then
						nn();
					end;
					Mn();
				end;
			end;
		end,
	});
	l:AddToggle("AntiKatana", { Text = "anti katana", Default = false, Callback = function(k)
			c = k;
		end });
	l:AddSlider("HitChance", {
		Text = "hit chance",
		Default = 100,
		Min = 0,
		Max = 100,
		Rounding = 0,
		Compact = true,
		Callback = function(k)
			O.hitChance = k;
		end,
	});
	l:AddDropdown("HitPartDropdown", {
		Text = "hit part",
		Default = "Head",
		Values = G,
		Callback = function(k)
			O.hitPart = k;
		end,
	});
	((p:AddToggle("ShowFOV", { Text = "show fov", Default = false, Callback = function(k)
			i.Visible = k;
		end })):AddColorPicker("FOVOutlineColor1", { Default = Color3.fromRGB(255, 255, 255), Title = "outline color 1", Callback = function(k)
			Y.OutlineColor1 = k;
			x();
		end })):AddColorPicker("FOVOutlineColor2", { Default = Color3.fromRGB(255, 255, 255), Title = "outline color 2", Callback = function(k)
			Y.OutlineColor2 = k;
			x();
		end });
	((p:AddToggle("SilentFOVFilled", { Text = "fov fill", Default = false, Callback = function(k)
			Y.FilledEnabled = k;
			R.Visible = k;
		end })):AddColorPicker("SilentFOVFillColor1", { Default = Color3.fromRGB(255, 255, 255), Title = "fill color 1", Callback = function(k)
			Y.FilledColor1 = k;
			t();
		end })):AddColorPicker("SilentFOVFillColor2", { Default = Color3.fromRGB(0, 0, 0), Title = "fill color 2", Callback = function(k)
			Y.FilledColor2 = k;
			t();
		end });
	p:AddToggle("SilentFOVFillAnimated", { Text = "animated fill", Default = false, Callback = function(k)
			Y.FilledAnimated = k;
			if not k then
				B.Rotation = Y.FilledRotation;
			end;
		end });
	p:AddSlider("FOVRadius", {
		Text = "fov radius",
		Default = 100,
		Min = 10,
		Max = 750,
		Rounding = 1,
		Compact = true,
		Callback = function(k)
			O.fovRadius = k;
		end,
	});
	p:AddSlider("SilentFOVOutlineThickness", {
		Text = "outline thickness",
		Default = 1.5,
		Min = .5,
		Max = 5,
		Rounding = 1,
		Compact = true,
		Callback = function(k)
			Y.OutlineThickness = k;
			W.Thickness = k;
		end,
	});
	p:AddSlider("SilentFOVOutlineTransparency", {
		Text = "outline transparency",
		Default = 0,
		Min = 0,
		Max = 1,
		Rounding = 2,
		Compact = true,
		Callback = function(k)
			Y.OutlineTransparency = k;
			W.Transparency = k;
		end,
	});
	p:AddSlider("SilentFOVOutlineRotation", {
		Text = "outline rotation",
		Default = 0,
		Min = 0,
		Max = 360,
		Rounding = 0,
		Compact = true,
		Callback = function(k)
			Y.OutlineRotation = k;
			L.Rotation = k;
		end,
	});
	p:AddSlider("SilentFOVFillTransparency", {
		Text = "fill transparency",
		Default = .7,
		Min = 0,
		Max = 1,
		Rounding = 2,
		Compact = true,
		Callback = function(k)
			Y.FilledTransparency = k;
			R.BackgroundTransparency = k;
		end,
	});
	p:AddSlider("SilentFOVFillRotation", {
		Text = "fill rotation",
		Default = 0,
		Min = 0,
		Max = 360,
		Rounding = 0,
		Compact = true,
		Callback = function(k)
			Y.FilledRotation = k;
			if not Y.FilledAnimated then
				B.Rotation = k;
			end;
		end,
	});
	p:AddSlider("SilentFOVFillSpeed", {
		Text = "fill speed",
		Default = 1,
		Min = .1,
		Max = 10,
		Rounding = 1,
		Compact = true,
		Callback = function(k)
			Y.FilledSpeed = k;
		end,
	});
	p:AddToggle("SilentFOVSpin", { Text = "fov spin", Default = false, Callback = function(k)
			Y.SpinOn = k;
			if not k then
				L.Rotation = Y.OutlineRotation;
				if not Y.FilledAnimated then
					B.Rotation = Y.FilledRotation;
				end;
			end;
		end });
	p:AddSlider("SilentFOVSpinSpd", {
		Text = "spin speed",
		Default = 1,
		Min = .1,
		Max = 10,
		Rounding = 1,
		Compact = true,
		Callback = function(k)
			Y.SpinSpd = k;
		end,
	});
	p:AddToggle("SilentFOVFollowMuzzle", { Text = "follow muzzle", Default = false, Callback = function(k)
			O.followMuzzle = k;
		end });
	(y:AddToggle("AimbotToggle", { Text = "enable", Default = false, Callback = function(k)
			kJ.masterEnabled = k;
			if not k then
				kJ.enabled = false;
				sn();
				Nn();
			end;
		end })):AddKeyPicker("AimbotKey", {
		Text = "Aimbot",
		Default = "None",
		Mode = "Toggle",
		NoUI = true,
		SyncToggleState = false,
		Modes = { "Toggle", "Hold" },
		Callback = function(k)
			local l = Options and Options.AimbotKey;
			if not l or l.Mode ~= "Toggle" or not kJ.masterEnabled then
				return;
			end;
			kJ.enabled = k;
			if not k then
				sn();
			end;
			Nn();
		end,
	});
	g.RenderStepped:Connect(function()
		local k = Options and Options.AimbotKey;
		if not k then
			return;
		end;
		local l = k.Mode and string.lower(k.Mode) or "toggle";
		if l ~= kJ.keyMode then
			kJ.keyMode = l;
			kJ.enabled = false;
			sn();
			Nn();
		end;
		if l ~= "hold" or not kJ.masterEnabled then
			return;
		end;
		local p = k:GetState();
		if p == kJ.enabled then
			return;
		end;
		kJ.enabled = p;
		if not p then
			sn();
		end;
		Nn();
	end);
	y:AddSlider("AimbotSmoothness", {
		Text = "smoothness",
		Default = 2,
		Min = .1,
		Max = 10,
		Rounding = 2,
		Compact = true,
		Callback = function(k)
			kJ.smoothness = math.clamp(k, .1, 10);
		end,
	});
	y:AddDropdown("AimbotCurve", {
		Text = "aim curve",
		Default = "Linear",
		Values = {
			"Linear",
			"Expo",
			"EaseIn",
			"EaseOut",
			"EaseInOut",
			"Cubic",
			"Instant",
		},
		Callback = function(k)
			kJ.aimCurve = k;
		end,
	});
	y:AddDropdown("AimbotHitPart", {
		Text = "hit part",
		Default = "Head",
		Values = {
			"Head",
			"HumanoidRootPart",
			"Torso",
			"UpperTorso",
			"LowerTorso",
		},
		Callback = function(k)
			kJ.targetPart = k;
		end,
	});
	((v:AddToggle("ShowAimbotFOV", { Text = "show fov", Default = false, Callback = function(k)
			kJ.showFov = k;
			X.Visible = k;
		end })):AddColorPicker("AimbotFOVOutlineColor1", { Default = Color3.fromRGB(255, 255, 255), Title = "outline color 1", Callback = function(k)
			S.OutlineColor1 = k;
			z();
		end })):AddColorPicker("AimbotFOVOutlineColor2", { Default = Color3.fromRGB(255, 255, 255), Title = "outline color 2", Callback = function(k)
			S.OutlineColor2 = k;
			z();
		end });
	((v:AddToggle("AimbotFOVFilled", { Text = "fov fill", Default = false, Callback = function(k)
			S.FilledEnabled = k;
			Z.Visible = k;
		end })):AddColorPicker("AimbotFOVFillColor1", { Default = Color3.fromRGB(255, 255, 255), Title = "fill color 1", Callback = function(k)
			S.FilledColor1 = k;
			w();
		end })):AddColorPicker("AimbotFOVFillColor2", { Default = Color3.fromRGB(0, 0, 0), Title = "fill color 2", Callback = function(k)
			S.FilledColor2 = k;
			w();
		end });
	v:AddToggle("AimbotFOVFillAnimated", { Text = "animated fill", Default = false, Callback = function(k)
			S.FilledAnimated = k;
			if not k then
				E.Rotation = S.FilledRotation;
			end;
		end });
	v:AddSlider("AimbotFOV", {
		Text = "fov radius",
		Default = 500,
		Min = 10,
		Max = 1000,
		Rounding = 0,
		Compact = true,
		Callback = function(k)
			kJ.fovRadius = k;
		end,
	});
	v:AddSlider("AimbotFOVOutlineThickness", {
		Text = "outline thickness",
		Default = 1.5,
		Min = .5,
		Max = 5,
		Rounding = 1,
		Compact = true,
		Callback = function(k)
			S.OutlineThickness = k;
			J.Thickness = k;
		end,
	});
	v:AddSlider("AimbotFOVOutlineTransparency", {
		Text = "outline transparency",
		Default = 0,
		Min = 0,
		Max = 1,
		Rounding = 2,
		Compact = true,
		Callback = function(k)
			S.OutlineTransparency = k;
			J.Transparency = k;
		end,
	});
	v:AddSlider("AimbotFOVOutlineRotation", {
		Text = "outline rotation",
		Default = 0,
		Min = 0,
		Max = 360,
		Rounding = 0,
		Compact = true,
		Callback = function(k)
			S.OutlineRotation = k;
			U.Rotation = k;
		end,
	});
	v:AddSlider("AimbotFOVFillTransparency", {
		Text = "fill transparency",
		Default = .7,
		Min = 0,
		Max = 1,
		Rounding = 2,
		Compact = true,
		Callback = function(k)
			S.FilledTransparency = k;
			Z.BackgroundTransparency = k;
		end,
	});
	v:AddSlider("AimbotFOVFillRotation", {
		Text = "fill rotation",
		Default = 0,
		Min = 0,
		Max = 360,
		Rounding = 0,
		Compact = true,
		Callback = function(k)
			S.FilledRotation = k;
			if not S.FilledAnimated then
				E.Rotation = k;
			end;
		end,
	});
	v:AddSlider("AimbotFOVFillSpeed", {
		Text = "fill speed",
		Default = 1,
		Min = .1,
		Max = 10,
		Rounding = 1,
		Compact = true,
		Callback = function(k)
			S.FilledSpeed = k;
		end,
	});
	v:AddToggle("AimbotFOVSpin", { Text = "fov spin", Default = false, Callback = function(k)
			S.SpinOn = k;
			if not k then
				U.Rotation = S.OutlineRotation;
				if not S.FilledAnimated then
					E.Rotation = S.FilledRotation;
				end;
			end;
		end });
	v:AddSlider("AimbotFOVSpinSpd", {
		Text = "spin speed",
		Default = 1,
		Min = .1,
		Max = 10,
		Rounding = 1,
		Compact = true,
		Callback = function(k)
			S.SpinSpd = k;
		end,
	});
	v:AddToggle("AimbotFOVFollowMuzzle", { Text = "follow muzzle", Default = false, Callback = function(k)
			kJ.followMuzzle = k;
		end });
	Nn();
	rJ();
	do
		local k = F.Combat:AddLeftGroupbox("gun");
		local l = game:GetService("Players");
		local p = l.LocalPlayer;
		local a = game:GetService("ReplicatedStorage");
		local y = game:GetService("RunService");
		local v = game:GetService("Workspace");
		(getgenv()).CombatMods = {
				RapidFire = false,
				NoSpread = false,
				NoRecoil = false,
				MaxAccuracy = false,
				RapidAttack = false,
				GunModule = nil,
				MeleeModule = nil,
				OriginalGunStartShooting = nil,
				OriginalMeleeStartShooting = nil,
				OriginalGetSpread = nil,
				OriginalRecoil = nil,
			};
		task.spawn(function()
			local k, l = pcall(function()
					return require(p.PlayerScripts.Modules.ItemTypes.Gun);
				end);
			if k and l then
				(getgenv()).CombatMods.GunModule = l;
				if l.StartShooting then
					(getgenv()).CombatMods.OriginalGunStartShooting = l.StartShooting;
					l.StartShooting = function(k, l, p)
							local a;
							if (getgenv()).CombatMods.RapidFire then
								a = k.Info.ShootCooldown;
								k.Info.ShootCooldown = 0;
							end;
							local y = { (getgenv()).CombatMods.OriginalGunStartShooting(k, l, p) };
							if (getgenv()).CombatMods.RapidFire then
								k.Info.ShootCooldown = a;
							end;
							return unpack(y);
						end;
				end;
				if l._Recoil then
					(getgenv()).CombatMods.OriginalRecoil = l._Recoil;
					l._Recoil = function(k, l)
							if (getgenv()).CombatMods.NoRecoil then
								return;
							end;
							return (getgenv()).CombatMods.OriginalRecoil(k, l);
						end;
				end;
			end;
		end);
		task.spawn(function()
			local k, l = pcall(function()
					return require(a.Modules.GameplayUtility);
				end);
			if k and (l and l.GetSpread) then
				(getgenv()).CombatMods.GameplayUtility = l;
				(getgenv()).CombatMods.OriginalGetSpread = l.GetSpread;
				l.GetSpread = function(k, l, p, a, y, v, j)
						if (getgenv()).CombatMods.NoSpread or (getgenv()).CombatMods.MaxAccuracy then
							return CFrame.new();
						end;
						return (getgenv()).CombatMods.OriginalGetSpread(k, l, p, a, y, v, j);
					end;
			end;
		end);
		task.spawn(function()
			local k, l = pcall(function()
					return require(p.PlayerScripts.Modules.ItemTypes.Melee);
				end);
			if k and l then
				(getgenv()).CombatMods.MeleeModule = l;
				if l.StartShooting then
					(getgenv()).CombatMods.OriginalMeleeStartShooting = l.StartShooting;
					l.StartShooting = function(k, l, p)
							local a;
							if (getgenv()).CombatMods.RapidAttack then
								a = k.Info.AttackCooldown;
								k.Info.AttackCooldown = 0;
							end;
							local y = { (getgenv()).CombatMods.OriginalMeleeStartShooting(k, l, p) };
							if (getgenv()).CombatMods.RapidAttack and a then
								k.Info.AttackCooldown = a;
							end;
							return unpack(y);
						end;
				end;
			end;
		end);
		local j = nil;
		local function s()
			local k = v:FindFirstChild("ViewModels");
			if k then
				local l = k:FindFirstChild("FirstPerson");
				if l then
					for k, l in pairs(l:GetChildren()) do
						if l:IsA("Model") then
							local k = l:FindFirstChild("ItemVisual");
							if k then
								local l = k:FindFirstChild("Body");
								if l then
									local k = l:FindFirstChild("BodyPrimary");
									if k then
										local l = k:FindFirstChild("_muzzle");
										if l then
											local k = l:FindFirstChild("SpotLight");
											if k then
												k:Destroy();
											end;
											for k, l in pairs(l:GetChildren()) do
												if l:IsA("ParticleEmitter") and l.Name == "ParticleEmiter" then
													l:Destroy();
												end;
											end;
										end;
									end;
								end;
							end;
						end;
					end;
				end;
			end;
		end;
		k:AddToggle("NoCooldown", { Text = "rapid fire", Default = false, Callback = function(k)
				(getgenv()).CombatMods.RapidFire = k;
			end });
		k:AddToggle("NoSpread", { Text = "no spread", Default = false, Callback = function(k)
				(getgenv()).CombatMods.NoSpread = k;
			end });
		k:AddToggle("NoRecoil", { Text = "no recoil", Default = false, Callback = function(k)
				(getgenv()).CombatMods.NoRecoil = k;
			end });
		k:AddToggle("MaxAccuracy", { Text = "max accuracy", Default = false, Callback = function(k)
				(getgenv()).CombatMods.MaxAccuracy = k;
			end });
		k:AddToggle("RapidAttack", { Text = "rapid attack", Default = false, Callback = function(k)
				(getgenv()).CombatMods.RapidAttack = k;
			end });
		k:AddToggle("NoMuzzleFlash", { Text = "no muzzle flash", Default = false, Callback = function(k)
				if k then
					s();
					if j then
						j:Disconnect();
					end;
					j = y.RenderStepped:Connect(s);
				else
					if j then
						j:Disconnect();
						j = nil;
					end;
				end;
			end });
	end;
	jJ = F.World:AddRightTabbox();
	GJ = function(k)
			local l = sJ[k];
			if l ~= nil then
				return l;
			end;
			if not k:IsA("BillboardGui") then
				return;
			end;
			if k.Name == "FortniteDamageNumber" then
				return;
			end;
			local p = k:FindFirstChildWhichIsA("TextLabel", true);
			if not p then
				return;
			end;
			local a = tonumber(p.Text);
			if not ((a and a > 0)) then
				return;
			end;
			local y = k.Adornee or (k.Parent and (k.Parent:IsA("BasePart") and k.Parent));
			if not y then
				return;
			end;
			local v = { lbl = p, dmg = a, adornee = y };
			sJ[k] = v;
			return v;
		end;
	((function()
		local k = game:GetService("HttpService");
		local l = game:GetService("TweenService");
		local p = {};
		p.main = (getgenv()).FinhubUIFont or Font.new("rbxasset://fonts/families/GothamSSm.json", Enum.FontWeight.Black);
		local a = jJ:AddTab("hit effects");
		local y = false;
		local v = Color3.fromRGB(255, 105, 180);
		local j = { Particles = true };
		local s = 7;
		local G = 2.8;
		local g = 1;
		local T = a:AddToggle("HitEffects", { Text = "enable", Default = false, Callback = function(k)
					y = k;
				end });
		T:AddColorPicker("HitEffectColorPicker", { Default = Color3.fromRGB(255, 105, 180), Title = "Hit Effect Color", Callback = function(k)
				v = k;
			end });
		local c = a:AddDependencyBox();
		c:AddSlider("HitEffectFloatSpeed", {
			Text = "float speed",
			Default = 7,
			Min = 1,
			Max = 25,
			Rounding = 1,
			Callback = function(k)
				s = k;
			end,
		});
		c:AddSlider("HitEffectAliveTime", {
			Text = "alive time",
			Default = 2.8,
			Min = .5,
			Max = 8,
			Rounding = 1,
			Suffix = "s",
			Callback = function(k)
				G = k;
				g = k / 2.8;
			end,
		});
		c:SetupDependencies({ { T, true } });
		a:AddDropdown("HitEffectStyleDropdown", {
			Text = "style",
			Default = { "Particles" },
			Values = {
				"Particles",
				"Fortnite",
				"Shockwave",
				"Lightning",
				"Blood",
				"Fire",
				"Ice",
				"Hearts",
				"Stars",
				"Confetti",
				"Ripple",
				"Sparks",
				"Neon",
				"Void",
				"Plasma",
				"Glitch",
				"Cosmic Shit",
			},
			Multi = true,
			Callback = function(k)
				j = k;
			end,
		});
		local function e(k)
			if k:IsA("BasePart") then
				return k;
			elseif k:IsA("Attachment") then
				return k.Parent;
			elseif k:IsA("Model") and k.PrimaryPart then
				return k.PrimaryPart;
			end;
		end;
		local function b(k, l)
			l = l or .35;
			return Color3.new(math.clamp(k.R + l, 0, 1), math.clamp(k.G + l * .5, 0, 1), math.clamp(k.B + l * .25, 0, 1));
		end;
		local function f(k, l)
			l = l or .45;
			return Color3.new(math.clamp(k.R - l, 0, 1), math.clamp(k.G - l, 0, 1), math.clamp(k.B - l * .5, 0, 1));
		end;
		local function N()
			return g;
		end;
		local function K(k)
			return k * N();
		end;
		local function n(k)
			local l = j;
			if type(l) ~= "table" then
				k(l);
				return;
			end;
			for l, p in pairs(l) do
				local a;
				if type(l) == "number" and type(p) == "string" then
					a = p;
				elseif p == true then
					a = l;
				end;
				if a then
					k(a);
				end;
			end;
		end;
		local function d(k, p, a)
			a = a or {};
			local y = Instance.new("Part");
			y.Name = a.Name or "HitRing";
			y.Shape = Enum.PartType.Cylinder;
			y.Anchored = true;
			y.CanCollide = false;
			y.CanQuery = false;
			y.CanTouch = false;
			y.Material = a.Material or Enum.Material.Neon;
			y.Color = p;
			y.Transparency = a.StartTransparency or .35;
			local v = a.StartSize or .35;
			y.Size = Vector3.new(.05, v, v);
			y.CFrame = k.CFrame * CFrame.Angles(0, 0, math.rad(90));
			y.Parent = workspace.CurrentCamera;
			local j = a.EndSize or 5;
			local s = K(a.Duration or .55);
			(l:Create(y, TweenInfo.new(s, Enum.EasingStyle.Quad, Enum.EasingDirection.Out), { Size = Vector3.new(.05, j, j), Transparency = 1 })):Play();
			task.delay(s + .1, function()
				if y and y.Parent then
					y:Destroy();
				end;
			end);
			return y;
		end;
		local function Y(k, p, a, y, v)
			v = K(v or .35);
			local j = Instance.new("PointLight");
			j.Name = "HitFlash";
			j.Color = p;
			j.Brightness = a or 6;
			j.Range = y or 14;
			j.Parent = k;
			(l:Create(j, TweenInfo.new(v, Enum.EasingStyle.Quad, Enum.EasingDirection.Out), { Brightness = 0, Range = 0 })):Play();
			task.delay(v + .05, function()
				if j and j.Parent then
					j:Destroy();
				end;
			end);
		end;
		local function M(k, l)
			if getActiveParticles() >= MAX_ACTIVE_PARTICLES then
				return nil;
			end;
			local p = Instance.new("ParticleEmitter");
			p.Name = l.Name or "HitEffect";
			p.Texture = l.Texture or "rbxassetid://6603835352";
			if typeof(l.Color) == "ColorSequence" then
				p.Color = l.Color;
			else
				p.Color = ColorSequence.new(l.Color or v);
			end;
			p.LightEmission = l.LightEmission or 1;
			p.Brightness = l.Brightness or 8;
			p.LightInfluence = l.LightInfluence or 0;
			p.Orientation = l.Orientation or Enum.ParticleOrientation.FacingCamera;
			p.LockedToPart = l.LockedToPart or false;
			p.Size = l.Size or NumberSequence.new(.12);
			p.Transparency = l.Transparency or NumberSequence.new({ NumberSequenceKeypoint.new(0, 0), NumberSequenceKeypoint.new(1, 1) });
			p.Speed = l.Speed or NumberRange.new(12, 18);
			p.SpreadAngle = l.SpreadAngle or Vector2.new(120, 120);
			p.EmissionDirection = l.EmissionDirection or Enum.NormalId.Top;
			local a = l.Lifetime or NumberRange.new(.6, 1.2);
			local y = N();
			p.Lifetime = NumberRange.new(a.Min * y, a.Max * y);
			p.Drag = l.Drag or 2;
			p.Acceleration = l.Acceleration or Vector3.new(0, 4, 0);
			p.RotSpeed = l.RotSpeed or NumberRange.new(0, 0);
			p.Rate = 0;
			p.Enabled = true;
			p.Parent = k;
			p:Emit(l.EmitCount or 48);
			task.delay(K(l.Cleanup or 4), function()
				if p and p.Parent then
					p:Destroy();
				end;
			end);
		end;
		local function i(k, l)
			l = l or v;
			local p = e(k);
			if not p or not p:IsA("BasePart") then
				return;
			end;
			local a = Instance.new("ParticleEmitter");
			a.Name = "TinyGlowingDots";
			a.Texture = "rbxassetid://6603835352";
			a.Color = ColorSequence.new(l);
			a.LightEmission = 1;
			a.Brightness = 13;
			a.LightInfluence = 0;
			a.Orientation = Enum.ParticleOrientation.FacingCamera;
			a.LockedToPart = false;
			a.Size = NumberSequence.new({ NumberSequenceKeypoint.new(0, .09), NumberSequenceKeypoint.new(.5, .135), NumberSequenceKeypoint.new(1, .068) });
			a.Transparency = NumberSequence.new({ NumberSequenceKeypoint.new(0, 0), NumberSequenceKeypoint.new(.25, 0), NumberSequenceKeypoint.new(1, 1) });
			a.Speed = NumberRange.new(22, 29);
			a.SpreadAngle = Vector2.new(130, 130);
			a.EmissionDirection = Enum.NormalId.Top;
			local y = K(4);
			a.Lifetime = NumberRange.new(y, y + .1);
			a.Drag = 3.2;
			a.Acceleration = Vector3.new(0, 5, 0);
			a.Rate = 0;
			a.Enabled = true;
			a.Parent = p;
			a:Emit(64);
			task.delay(K(5), function()
				if a and a.Parent then
					a:Destroy();
				end;
			end);
		end;
		local R = {};
		local B = 5.5;
		local W = 45;
		local L = 120;
		local P = false;
		local x, t = -10, 10;
		local S, V = 1, 8;
		local X, Z = -4, 4;
		local function E(k, l)
			return k + math.random() * ((l - k));
		end;
		local function J(k)
			local l = R[k] or {};
			local p = nil;
			local a = -1;
			for k = 1, 60, 1 do
				local y = Vector3.new(E(x, t), E(S, V), E(X, Z));
				local v = math.huge;
				for k, l in ipairs(l) do
					local p = ((y - l)).Magnitude;
					if p < v then
						v = p;
					end;
				end;
				if v >= B then
					return y;
				end;
				if v > a then
					a = v;
					p = y;
				end;
			end;
			return p;
		end;
		local function U()
			local k = 0;
			for l, p in ipairs(workspace.CurrentCamera:GetChildren()) do
				if p:IsA("BillboardGui") and p.Name == "FortniteDamageNumber" then
					k = k + 1;
				end;
			end;
			return k;
		end;
		local function z()
			local k = 0;
			local function l(p)
				for p, a in ipairs(p:GetChildren()) do
					if a:IsA("ParticleEmitter") then
						k = k + 1;
					end;
					l(a);
				end;
			end;
			l(workspace.CurrentCamera);
			return k;
		end;
		local function w(k, a, y)
			a = a or v;
			local j = U();
			if j >= W then
				return;
			end;
			local g;
			if k:IsA("BasePart") then
				g = k;
			elseif k:IsA("Model") and k.PrimaryPart then
				g = k.PrimaryPart;
			end;
			if not g then
				return;
			end;
			local T = g;
			if not R[T] then
				R[T] = {};
			end;
			local c = J(T);
			local e = s or 7;
			local b = G or 2.8;
			local f = c + Vector3.new(0, e, 0);
			local N = c;
			table.insert(R[T], N);
			local n = Instance.new("BillboardGui");
			n.Name = "FortniteDamageNumber";
			n.Size = UDim2.new(0, 70, 0, 40);
			n.StudsOffset = c;
			n.AlwaysOnTop = true;
			n.LightInfluence = 0;
			n.Adornee = g;
			n.Parent = workspace.CurrentCamera;
			local d = Instance.new("TextLabel", n);
			d.Size = UDim2.new(1, 0, 1, 0);
			d.BackgroundTransparency = 1;
			d.Text = y;
			d.TextColor3 = a;
			d.TextTransparency = 1;
			d.TextStrokeColor3 = Color3.new(0, 0, 0);
			d.TextStrokeTransparency = 1;
			d.TextScaled = true;
			d.FontFace = p.main;
			d.ZIndex = 1;
			local Y = K(.5);
			(l:Create(d, TweenInfo.new(.12, Enum.EasingStyle.Quad, Enum.EasingDirection.Out), { TextTransparency = 0, TextStrokeTransparency = .55 })):Play();
			(l:Create(n, TweenInfo.new(b, Enum.EasingStyle.Quad, Enum.EasingDirection.Out), { StudsOffset = f })):Play();
			task.delay(b, function()
				if not n or not n.Parent then
					return;
				end;
				(l:Create(d, TweenInfo.new(Y, Enum.EasingStyle.Quad, Enum.EasingDirection.In), { TextTransparency = 1, TextStrokeTransparency = 1 })):Play();
				task.delay(Y + .02, function()
					if n and n.Parent then
						n:Destroy();
					end;
					if R[T] then
						for k, l in ipairs(R[T]) do
							if l == N then
								table.remove(R[T], k);
								break;
							end;
						end;
						if #R[T] == 0 then
							R[T] = nil;
						end;
					end;
				end);
			end);
		end;
		local function q(k, l)
			local p = e(k);
			if not p then
				return;
			end;
			Y(p, l, 8, 18, .4);
			d(p, l, {
				Name = "ShockwaveRing",
				StartSize = .5,
				EndSize = 7,
				Duration = .5,
			});
			d(p, b(l, .2), {
				Name = "ShockwaveRing2",
				StartSize = .25,
				EndSize = 5,
				Duration = .35,
				StartTransparency = .55,
			});
			M(p, {
				Name = "ShockwaveBurst",
				Texture = "rbxassetid://243660364",
				Color = l,
				EmitCount = 28,
				Speed = NumberRange.new(14, 24),
				SpreadAngle = Vector2.new(180, 180),
				EmissionDirection = Enum.NormalId.Front,
				Lifetime = NumberRange.new(.25, .45),
				Drag = 5,
				Size = NumberSequence.new({ NumberSequenceKeypoint.new(0, .35), NumberSequenceKeypoint.new(1, .05) }),
				Cleanup = 1.5,
			});
		end;
		local function u(k, l)
			local p = e(k);
			if not p then
				return;
			end;
			local a = Color3.new(.85, .92, 1);
			Y(p, a, 14, 22, .2);
			M(p, {
				Name = "LightningCore",
				Texture = "rbxassetid://446111271",
				Color = a,
				Brightness = 16,
				EmitCount = 22,
				Speed = NumberRange.new(32, 48),
				SpreadAngle = Vector2.new(18, 18),
				Lifetime = NumberRange.new(.08, .22),
				Drag = .2,
				Acceleration = Vector3.new(0, -20, 0),
				Cleanup = 1,
			});
			M(p, {
				Name = "LightningArcs",
				Texture = "rbxassetid://12781852245",
				Color = ColorSequence.new({ ColorSequenceKeypoint.new(0, a), ColorSequenceKeypoint.new(.5, l), ColorSequenceKeypoint.new(1, a) }),
				Brightness = 12,
				EmitCount = 40,
				Speed = NumberRange.new(20, 38),
				SpreadAngle = Vector2.new(120, 120),
				Lifetime = NumberRange.new(.12, .35),
				Drag = 1,
				Acceleration = Vector3.new(0, -14, 0),
				Cleanup = 1.5,
			});
		end;
		local function H(k, l)
			local p = e(k);
			if not p then
				return;
			end;
			local a = Color3.new(math.clamp(l.R * .9 + .35, 0, 1), math.clamp(l.G * .15, 0, .35), math.clamp(l.B * .15, 0, .35));
			M(p, {
				Name = "BloodSplatter",
				Texture = "rbxassetid://243660364",
				Color = a,
				EmitCount = 70,
				Speed = NumberRange.new(12, 28),
				SpreadAngle = Vector2.new(175, 175),
				Lifetime = NumberRange.new(.7, 1.3),
				Drag = 5,
				Acceleration = Vector3.new(0, -22, 0),
				Size = NumberSequence.new({ NumberSequenceKeypoint.new(0, .22), NumberSequenceKeypoint.new(.6, .14), NumberSequenceKeypoint.new(1, .04) }),
			});
			M(p, {
				Name = "BloodMist",
				Texture = "rbxassetid://6603835352",
				Color = f(a, .15),
				EmitCount = 20,
				Speed = NumberRange.new(4, 10),
				SpreadAngle = Vector2.new(180, 180),
				Lifetime = NumberRange.new(.4, .8),
				Transparency = NumberSequence.new({ NumberSequenceKeypoint.new(0, .35), NumberSequenceKeypoint.new(1, 1) }),
				Size = NumberSequence.new(.5),
				Cleanup = 2,
			});
		end;
		local function F(k, l)
			local p = e(k);
			if not p then
				return;
			end;
			local a = Color3.new(math.clamp(l.R + .25, 0, 1), math.clamp(l.G * .6 + .2, 0, 1), math.clamp(l.B * .2, 0, .4));
			Y(p, a, 10, 16, .45);
			M(p, {
				Name = "FireFlames",
				Texture = "rbxassetid://241650108",
				Color = ColorSequence.new({ ColorSequenceKeypoint.new(0, Color3.fromRGB(255, 105, 180)), ColorSequenceKeypoint.new(.45, a), ColorSequenceKeypoint.new(1, Color3.fromRGB(139, 0, 80)) }),
				EmitCount = 55,
				Speed = NumberRange.new(8, 18),
				SpreadAngle = Vector2.new(70, 70),
				Lifetime = NumberRange.new(.45, .95),
				Acceleration = Vector3.new(0, 14, 0),
				LightEmission = 1,
				Brightness = 14,
				Size = NumberSequence.new({ NumberSequenceKeypoint.new(0, .5), NumberSequenceKeypoint.new(1, .05) }),
			});
			M(p, {
				Name = "FireEmbers",
				Texture = "rbxassetid://12781852245",
				Color = a,
				EmitCount = 25,
				Speed = NumberRange.new(14, 26),
				SpreadAngle = Vector2.new(180, 180),
				Lifetime = NumberRange.new(.6, 1.1),
				Drag = 2,
				Acceleration = Vector3.new(0, 6, 0),
				Cleanup = 2.5,
			});
		end;
		local function Q(k, l)
			local p = e(k);
			if not p then
				return;
			end;
			local a = Color3.new(math.clamp(l.R * .35 + .55, 0, 1), math.clamp(l.G * .5 + .55, 0, 1), math.clamp(l.B * .4 + .75, 0, 1));
			Y(p, a, 6, 12, .35);
			M(p, {
				Name = "IceShards",
				Texture = "rbxassetid://243660364",
				Color = a,
				EmitCount = 48,
				Speed = NumberRange.new(10, 20),
				SpreadAngle = Vector2.new(110, 110),
				Lifetime = NumberRange.new(.9, 1.5),
				Drag = 1.5,
				RotSpeed = NumberRange.new(-180, 180),
				Acceleration = Vector3.new(0, -6, 0),
				Size = NumberSequence.new({ NumberSequenceKeypoint.new(0, .28), NumberSequenceKeypoint.new(1, .06) }),
			});
			M(p, {
				Name = "IceFrost",
				Texture = "rbxassetid://6603835352",
				Color = Color3.new(1, 1, 1),
				EmitCount = 24,
				Speed = NumberRange.new(2, 6),
				SpreadAngle = Vector2.new(180, 180),
				Lifetime = NumberRange.new(1.2, 1.8),
				Transparency = NumberSequence.new({ NumberSequenceKeypoint.new(0, .5), NumberSequenceKeypoint.new(1, 1) }),
				Size = NumberSequence.new(.4),
				Cleanup = 2.5,
			});
		end;
		local function A(k, l)
			local p = e(k);
			if not p then
				return;
			end;
			local a = Color3.new(math.clamp(l.R + .15, 0, 1), math.clamp(l.G * .35 + .2, 0, 1), math.clamp(l.B * .4 + .35, 0, 1));
			M(p, {
				Name = "HeartsHit",
				Texture = "rbxassetid://120777973",
				Color = a,
				EmitCount = 24,
				Speed = NumberRange.new(5, 12),
				SpreadAngle = Vector2.new(55, 55),
				Lifetime = NumberRange.new(1.1, 1.9),
				Acceleration = Vector3.new(0, 8, 0),
				Drag = 1,
				Size = NumberSequence.new({ NumberSequenceKeypoint.new(0, .45), NumberSequenceKeypoint.new(1, .2) }),
				LightEmission = .6,
			});
		end;
		local function I(k, l)
			local p = e(k);
			if not p then
				return;
			end;
			Y(p, l, 7, 14, .3);
			M(p, {
				Name = "StarsBurst",
				Texture = "rbxassetid://1084982556",
				Color = l,
				EmitCount = 40,
				Speed = NumberRange.new(16, 28),
				SpreadAngle = Vector2.new(180, 180),
				Lifetime = NumberRange.new(.6, 1.05),
				RotSpeed = NumberRange.new(-240, 240),
				Brightness = 12,
				Size = NumberSequence.new({ NumberSequenceKeypoint.new(0, .4), NumberSequenceKeypoint.new(1, .1) }),
			});
		end;
		local function m(k, l)
			local p = e(k);
			if not p then
				return;
			end;
			local a = b(l, .3);
			local y = Color3.new(l.G, l.B, l.R);
			M(p, {
				Name = "ConfettiHit",
				Texture = "rbxassetid://243660364",
				Color = ColorSequence.new({
					ColorSequenceKeypoint.new(0, l),
					ColorSequenceKeypoint.new(.33, a),
					ColorSequenceKeypoint.new(.66, y),
					ColorSequenceKeypoint.new(1, b(a, .2)),
				}),
				EmitCount = 80,
				Speed = NumberRange.new(14, 30),
				SpreadAngle = Vector2.new(180, 180),
				Lifetime = NumberRange.new(1.4, 2.4),
				Drag = 2.5,
				Acceleration = Vector3.new(0, -10, 0),
				RotSpeed = NumberRange.new(-420, 420),
				Size = NumberSequence.new({ NumberSequenceKeypoint.new(0, .22), NumberSequenceKeypoint.new(1, .08) }),
			});
		end;
		local function o(k, l)
			local p = e(k);
			if not p then
				return;
			end;
			d(p, l, {
				Name = "RippleHit1",
				StartSize = .3,
				EndSize = 4.5,
				Duration = .45,
				StartTransparency = .3,
			});
			task.delay(.12, function()
				if p and p.Parent then
					d(p, b(l, .15), {
						Name = "RippleHit2",
						StartSize = .2,
						EndSize = 6.5,
						Duration = .65,
						StartTransparency = .5,
					});
				end;
			end);
			M(p, {
				Name = "RippleDroplets",
				Texture = "rbxassetid://6603835352",
				Color = l,
				EmitCount = 16,
				Speed = NumberRange.new(6, 12),
				SpreadAngle = Vector2.new(180, 180),
				Lifetime = NumberRange.new(.5, .9),
				Transparency = NumberSequence.new({ NumberSequenceKeypoint.new(0, .2), NumberSequenceKeypoint.new(1, 1) }),
				Size = NumberSequence.new(.12),
				Cleanup = 2,
			});
		end;
		local function r(k, l)
			local p = e(k);
			if not p then
				return;
			end;
			Y(p, l, 9, 12, .25);
			M(p, {
				Name = "SparksHit",
				Texture = "rbxassetid://12781852245",
				Color = l,
				EmitCount = 65,
				Speed = NumberRange.new(22, 42),
				SpreadAngle = Vector2.new(180, 180),
				Lifetime = NumberRange.new(.18, .5),
				Drag = 4,
				Brightness = 14,
				LightEmission = 1,
				Acceleration = Vector3.new(0, -16, 0),
			});
			M(p, {
				Name = "SparksGlow",
				Texture = "rbxassetid://6603835352",
				Color = b(l, .4),
				EmitCount = 12,
				Speed = NumberRange.new(4, 8),
				SpreadAngle = Vector2.new(180, 180),
				Lifetime = NumberRange.new(.35, .6),
				Size = NumberSequence.new(.25),
				Cleanup = 1.5,
			});
		end;
		local function C(k, l)
			local p = e(k);
			if not p then
				return;
			end;
			Y(p, l, 16, 20, .5);
			d(p, l, {
				Name = "NeonPulseRing",
				StartSize = .15,
				EndSize = 4,
				Duration = .35,
				StartTransparency = .15,
			});
			M(p, {
				Name = "NeonPulseCore",
				Texture = "rbxassetid://6603835352",
				Color = l,
				Brightness = 16,
				LightEmission = 1,
				EmitCount = 50,
				Speed = NumberRange.new(4, 10),
				SpreadAngle = Vector2.new(180, 180),
				Lifetime = NumberRange.new(.5, .9),
				Size = NumberSequence.new({ NumberSequenceKeypoint.new(0, .6), NumberSequenceKeypoint.new(.5, .35), NumberSequenceKeypoint.new(1, 0) }),
				Transparency = NumberSequence.new({ NumberSequenceKeypoint.new(0, 0), NumberSequenceKeypoint.new(1, 1) }),
			});
		end;
		local function D(k, l)
			local p = e(k);
			if not p then
				return;
			end;
			local a = f(l, .55);
			local y = b(l, .1);
			M(p, {
				Name = "VoidRiftCore",
				Texture = "rbxassetid://243660364",
				Color = a,
				EmitCount = 35,
				Speed = NumberRange.new(2, 8),
				SpreadAngle = Vector2.new(180, 180),
				EmissionDirection = Enum.NormalId.Bottom,
				Lifetime = NumberRange.new(.8, 1.4),
				Drag = 3,
				Acceleration = Vector3.new(0, -14, 0),
				Size = NumberSequence.new({ NumberSequenceKeypoint.new(0, .5), NumberSequenceKeypoint.new(1, .1) }),
				LightEmission = .2,
				Brightness = 2,
			});
			M(p, {
				Name = "VoidRiftEdge",
				Texture = "rbxassetid://446111271",
				Color = y,
				EmitCount = 45,
				Speed = NumberRange.new(10, 22),
				SpreadAngle = Vector2.new(180, 180),
				Lifetime = NumberRange.new(.4, .85),
				RotSpeed = NumberRange.new(-300, 300),
				Brightness = 10,
				LightEmission = 1,
				Cleanup = 2,
			});
		end;
		local function h(k, l)
			local p = e(k);
			if not p then
				return;
			end;
			local a = Color3.new(math.clamp(1 - l.R, 0, 1), math.clamp(l.G, 0, 1), math.clamp(l.B + .3, 0, 1));
			Y(p, l, 12, 18, .35);
			M(p, {
				Name = "PlasmaCore",
				Texture = "rbxassetid://446111271",
				Color = ColorSequence.new({ ColorSequenceKeypoint.new(0, l), ColorSequenceKeypoint.new(.5, a), ColorSequenceKeypoint.new(1, l) }),
				Brightness = 15,
				EmitCount = 55,
				Speed = NumberRange.new(6, 16),
				SpreadAngle = Vector2.new(180, 180),
				Lifetime = NumberRange.new(.35, .7),
				Size = NumberSequence.new({ NumberSequenceKeypoint.new(0, .55), NumberSequenceKeypoint.new(1, .08) }),
				LightEmission = 1,
			});
			d(p, a, {
				Name = "PlasmaRing",
				StartSize = .4,
				EndSize = 5.5,
				Duration = .4,
				StartTransparency = .25,
			});
		end;
		local function O(k, l)
			local p = e(k);
			if not p then
				return;
			end;
			for k = 1, 6, 1 do
				M(p, {
					Name = "GlitchShard" .. k,
					Texture = "rbxassetid://243660364",
					Color = (k % 2 == 0) and l or b(l, .5),
					EmitCount = 8,
					Speed = NumberRange.new(18, 32),
					SpreadAngle = Vector2.new(40, 40),
					EmissionDirection = Enum.NormalId.Top,
					Lifetime = NumberRange.new(.1, .25),
					Drag = 0,
					RotSpeed = NumberRange.new(-500, 500),
					Size = NumberSequence.new({ NumberSequenceKeypoint.new(0, .35), NumberSequenceKeypoint.new(1, .1) }),
					Transparency = NumberSequence.new({ NumberSequenceKeypoint.new(0, math.random() * .4), NumberSequenceKeypoint.new(1, 1) }),
					Cleanup = .8,
				});
			end;
			Y(p, b(l, .6), 8, 10, .15);
		end;
		local function kJ(k, l)
			local p = e(k);
			if not p then
				return;
			end;
			Y(p, b(l, .25), 5, 16, .6);
			M(p, {
				Name = "CosmicDust",
				Texture = "rbxassetid://1084982556",
				Color = ColorSequence.new({ ColorSequenceKeypoint.new(0, Color3.new(1, 1, 1)), ColorSequenceKeypoint.new(.4, l), ColorSequenceKeypoint.new(1, f(l, .2)) }),
				EmitCount = 60,
				Speed = NumberRange.new(3, 14),
				SpreadAngle = Vector2.new(180, 180),
				Lifetime = NumberRange.new(1.2, 2),
				Drag = .5,
				Acceleration = Vector3.new(0, 2, 0),
				RotSpeed = NumberRange.new(-80, 80),
				Brightness = 9,
				Size = NumberSequence.new({ NumberSequenceKeypoint.new(0, .08), NumberSequenceKeypoint.new(.5, .18), NumberSequenceKeypoint.new(1, .04) }),
				Transparency = NumberSequence.new({ NumberSequenceKeypoint.new(0, .1), NumberSequenceKeypoint.new(1, 1) }),
			});
			d(p, b(l, .1), {
				Name = "CosmicRing",
				StartSize = .2,
				EndSize = 3.5,
				Duration = .8,
				StartTransparency = .65,
				Material = Enum.Material.Glass,
			});
		end;
		local lJ = {
				Particles = i,
				Fortnite = w,
				Shockwave = q,
				Lightning = u,
				Blood = H,
				Fire = F,
				Ice = Q,
				Hearts = A,
				Stars = I,
				Confetti = m,
				Ripple = o,
				Sparks = r,
				Neon = C,
				Void = D,
				Plasma = h,
				Glitch = O,
				["Cosmic Shit"] = kJ,
			};
		workspace.DescendantAdded:Connect(function(k)
			if not y then
				return;
			end;
			if not k:IsA("BillboardGui") then
				return;
			end;
			local l = GJ(k);
			if not l then
				return;
			end;
			task.defer(function()
				if not k or not k.Parent then
					return;
				end;
				k.Enabled = false;
				n(function(k)
					local p = lJ[k];
					if p then
						if k == "Fortnite" then
							p(l.adornee, v, l.lbl.Text);
						else
							p(l.adornee, v);
						end;
					end;
				end);
			end);
		end);
	end))();
end;
do
	local k = jJ:AddTab("hit sounds");
	local l = false;
	local p = "Rust HS";
	local a = .5;
	local y = 1.0;
	local v = "Hitsounds";
	local j = v .. "/sounds";
	local s = {
			"Among Us",
			"Bonk",
			"Bruh",
			"Fart",
			"Minecraft",
			"Neverlose",
			"Osu",
			"Stars",
			"Rust HS",
			"Vine",
		};
	local G = {
			["Among Us"] = "amongus.mp3",
			Bonk = "bonk.mp3",
			Bruh = "bruh.mp3",
			Fart = "fart.mp3",
			Minecraft = "minecraft.mp3",
			Neverlose = "neverlose.mp3",
			Osu = "osu.mp3",
			Stars = "Stars.mp3",
			["Rust HS"] = "rust hs.mp3",
			Vine = "vine.mp3",
		};
	local function g()
		if not isfolder(v) then
			makefolder(v);
		end;
		if not isfolder(j) then
			makefolder(j);
		end;
		local k = {
				["amongus.mp3"] = "https://www.myinstants.com/media/sounds/roblox-death-sound_ytkBL7X.mp3",
				["minecraft.mp3"] = "https://www.myinstants.com/media/sounds/steve-old-hurt-sound_XKZxUk4.mp3",
				["bruh.mp3"] = "https://www.myinstants.com/media/sounds/discord-notification.mp3",
				["fart.mp3"] = "https://www.myinstants.com/media/sounds/fart-moan3.mp3",
				["neverlose.mp3"] = "https://www.myinstants.com/media/sounds/neverlose-s.mp3",
				["rust hs.mp3"] = "https://www.myinstants.com/media/sounds/eaolwpzhgsba.mp3",
				["osu.mp3"] = "https://www.myinstants.com/media/sounds/osu-hit-sound.mp3",
				["Stars.mp3"] = "https://www.myinstants.com/media/sounds/starshitsound.mp3",
				["bonk.mp3"] = "https://www.myinstants.com/media/sounds/bonk.mp3",
				["vine.mp3"] = "https://www.myinstants.com/media/sounds/vine-boom.mp3",
			};
		for k, l in pairs(k) do
			local p = j .. ("/" .. k);
			if not isfile(p) then
				writefile(p, game:HttpGet(l, true));
				task.wait(.8);
			end;
		end;
	end;
	g();
	local function T()
		if not l then
			return;
		end;
		local k = G[p] or G["Rust HS"];
		local v = j .. ("/" .. k);
		if not isfile(v) then
			v = j .. "/rust hs.mp3";
		end;
		local s = Instance.new("Sound");
		s.SoundId = getsynasset and getsynasset(v) or getcustomasset(v);
		s.Volume = a;
		s.PlaybackSpeed = y;
		s.Parent = workspace;
		s:Play();
		task.delay(5, function()
			if s and s.Parent then
				s:Destroy();
			end;
		end);
	end;
	k:AddToggle("HitSounds", { Text = "enable", Default = false, Callback = function(k)
			l = k;
		end });
	local c;
	k:AddInput("SoundSearch", {
		Text = "search sounds",
		Placeholder = "",
		ClearTextOnFocus = false,
		Callback = function(k)
			if k == "" then
				c:SetValues(s);
				c:SetValue("Rust HS");
				p = "Rust HS";
				return;
			end;
			local l = {};
			k = k:lower();
			for p, a in ipairs(s) do
				if (a:lower()):find(k, 1, true) then
					table.insert(l, a);
				end;
			end;
			if #l > 0 then
				c:SetValues(l);
				c:SetValue(l[1]);
				p = l[1];
			end;
		end,
	});
	c = k:AddDropdown("SoundStyleDropdown", {
			Text = "sound",
			Default = "Rust HS",
			Values = s,
			Callback = function(k)
				p = k;
			end,
		});
	k:AddSlider("SoundVolume", {
		Text = "volume",
		Default = 50,
		Min = 1,
		Max = 100,
		Rounding = 0,
		Compact = true,
		Callback = function(k)
			a = k / 100;
		end,
	});
	k:AddSlider("SoundPitch", {
		Text = "pitch",
		Default = 100,
		Min = 50,
		Max = 200,
		Rounding = 0,
		Compact = true,
		Callback = function(k)
			y = k / 100;
		end,
	});
	k:AddToggle("DisableGunSounds", { Text = "disable gun sounds", Default = _G.Features.DisableGunSounds, Callback = function(k)
			_G.Features.DisableGunSounds = k;
			if k then
				if (getgenv()).FinhubRefreshGunSoundMute then
					(getgenv()).FinhubRefreshGunSoundMute();
				end;
			else
				restoreGunSoundVolumes();
			end;
		end });
	workspace.DescendantAdded:Connect(function(k)
		if not l then
			return;
		end;
		if not k:IsA("BillboardGui") then
			return;
		end;
		local p = GJ(k);
		if not p then
			return;
		end;
		task.defer(function()
			if not k or not k.Parent then
				return;
			end;
			T();
		end);
	end);
end;
do
	local k = F.World:AddRightTabbox();
	local l = k:AddTab("view model");
	local p = k:AddTab("viewport");
	local a = false;
	local y = false;
	local v = false;
	local j = false;
	local s = false;
	local G = Color3.fromRGB(255, 255, 255);
	local T = Color3.fromRGB(255, 255, 255);
	local c = Color3.fromRGB(255, 255, 255);
	local e = Color3.fromRGB(255, 255, 255);
	local b = Color3.fromRGB(255, 255, 255);
	local f = Color3.fromRGB(255, 255, 255);
	local N = Enum.Material.Neon;
	local K = Enum.Material.ForceField;
	local n = Color3.fromRGB(255, 255, 255);
	local d = Color3.fromRGB(255, 255, 255);
	local Y = Color3.fromRGB(255, 255, 255);
	local M = 1;
	local i = 0;
	local R = Color3.fromRGB(255, 255, 255);
	local B = Color3.fromRGB(255, 255, 255);
	local W = Color3.fromRGB(255, 255, 255);
	local L = 1;
	local P = 0;
	local x = 0;
	local t = 0;
	local S = false;
	local X = false;
	local Z = Color3.fromRGB(255, 255, 255);
	local E = Color3.fromRGB(255, 105, 180);
	local J = Color3.fromRGB(255, 255, 255);
	local U = Color3.fromRGB(255, 105, 180);
	local z = Enum.Material.Neon;
	local w = Enum.Material.Neon;
	local q = 0;
	local u = 0;
	local H = 0;
	local Q = 0;
	local A = 0;
	local I = 0;
	local o = .12;
	local r = {};
	local C = {};
	local D = {};
	local h = {};
	local O = {};
	local kJ = {};
	local lJ = Instance.new("Folder");
	lJ.Name = "\000ChamOutlines";
	lJ.Parent = game.CoreGui;
	local pJ = {
			ChamOutlineShell = true,
			ChamOutlineGlow = true,
			ChamOutlineBloom = true,
			WeaponWireframeCore = true,
			WeaponWireframeGlow = true,
			WeaponWireframeBloom = true,
			GunChamNeonGlow = true,
		};
	local aJ = false;
	local yJ = 0;
	local vJ = 0;
	local jJ = 0;
	local function sJ(k)
		if not C[k] then
			C[k] = {
					Material = k.Material,
					Color = k.Color,
					Transparency = k.Transparency,
					Reflectance = k.Reflectance,
					textures = {},
					surfaceAppearances = {},
				};
			if k:IsA("MeshPart") then
				C[k].TextureID = k.TextureID;
			end;
			for l, p in ipairs(k:GetChildren()) do
				if p:IsA("Texture") or p:IsA("Decal") then
					C[k].textures[p] = p.Transparency;
				elseif p:IsA("SurfaceAppearance") then
					table.insert(C[k].surfaceAppearances, p:Clone());
				end;
			end;
		end;
	end;
	local function GJ(k)
		local l = C[k];
		if not l then
			return;
		end;
		pcall(function()
			k.Material = l.Material;
			k.Color = l.Color;
			k.Transparency = l.Transparency;
			k.Reflectance = l.Reflectance or 0;
			if k:IsA("MeshPart") and l.TextureID ~= nil then
				k.TextureID = l.TextureID;
			end;
			for k, l in pairs(l.textures) do
				if k and k.Parent then
					k.Transparency = l;
				end;
			end;
			for l, p in ipairs(l.surfaceAppearances or {}) do
				if p and not k:FindFirstChild(p.Name) then
					local l = p:Clone();
					l.Parent = k;
				end;
			end;
		end);
	end;
	local function gJ(k, l, p, a, y)
		sJ(k);
		pcall(function()
			k.Material = p;
			k.Color = l;
			k.Transparency = math.clamp(a or 0, 0, 1);
			k.Reflectance = 0;
			k.LocalTransparencyModifier = 0;
			if k:IsA("MeshPart") then
				k.TextureID = "";
			end;
			local function v(p)
				if p:IsA("SurfaceAppearance") or p:IsA("WrapLayer") then
					p:Destroy();
				elseif p:IsA("Texture") or p:IsA("Decal") then
					if C[k] and not C[k].textures[p] then
						C[k].textures[p] = p.Transparency;
					end;
					if y then
						p:Destroy();
					else
						p.Transparency = 1;
					end;
				elseif p:IsA("SpecialMesh") or p:IsA("Mesh") then
					p.TextureId = "";
					if p:IsA("SpecialMesh") then
						p.VertexColor = l;
					end;
				end;
			end;
			for k, l in ipairs(k:GetChildren()) do
				v(l);
			end;
			if y then
				for k, l in ipairs(k:GetDescendants()) do
					v(l);
				end;
			end;
		end);
	end;
	local function TJ(k, l, p, a)
		gJ(k, l, p, a, true);
		pcall(function()
			k.Material = p;
			k.Color = l;
			k.Transparency = math.clamp(a or 0, 0, 1);
			k.Reflectance = 0;
			k.LocalTransparencyModifier = 0;
			if k:IsA("MeshPart") then
				k.TextureID = "";
			end;
			for k, p in ipairs(k:GetDescendants()) do
				if p:IsA("SurfaceAppearance") or p:IsA("WrapLayer") then
					p:Destroy();
				elseif p:IsA("Texture") or p:IsA("Decal") then
					p:Destroy();
				elseif p:IsA("SpecialMesh") or p:IsA("Mesh") then
					p.TextureId = "";
					if p:IsA("SpecialMesh") then
						p.VertexColor = l;
					end;
				end;
			end;
		end);
	end;
	local function cJ(k, l, p)
		return Color3.new(k.R + ((l.R - k.R)) * p, k.G + ((l.G - k.G)) * p, k.B + ((l.B - k.B)) * p);
	end;
	local function eJ(k, l, p, a, y)
		local v = ((a + y)) % 1;
		if v < .33 then
			return cJ(k, l, v / .33);
		elseif v < .66 then
			return cJ(l, p, ((v - .33)) / .33);
		else
			return cJ(p, k, ((v - .66)) / .34);
		end;
	end;
	local function bJ(k)
		local l, p = math.huge, -math.huge;
		for k, a in ipairs(k:GetDescendants()) do
			if a:IsA("BasePart") then
				l = math.min(l, a.Position.Y);
				p = math.max(p, a.Position.Y);
			end;
		end;
		if l == math.huge then
			return 0, 1;
		end;
		return l, p;
	end;
	local function fJ(k, l)
		local p, a = math.huge, -math.huge;
		for k, y in ipairs(k:GetDescendants()) do
			if y:IsA("BasePart") and l(y) then
				p = math.min(p, y.Position.Y);
				a = math.max(a, y.Position.Y);
			end;
		end;
		if p == math.huge then
			return 0, 1;
		end;
		return p, a;
	end;
	local function NJ(k, l, p, a, y)
		local v, j = y[1], y[2];
		local s = math.max(j - v, .001);
		local G = math.clamp(((k.Position.Y - v)) / s, 0, 1);
		return cJ(a, p, G);
	end;
	local function KJ(k, l, p)
		local a = p * 2;
		if a > 1 then
			a = 2 - a;
		end;
		return cJ(l, k, a);
	end;
	local function nJ(k)
		if pJ[k.Name] then
			return false;
		end;
		if k.Transparency >= .99 then
			return false;
		end;
		local l = k.Name:lower();
		if l:find("hitbox") or l:find("collision") or l:find("trigger") or l:find("proxy") then
			return false;
		end;
		return true;
	end;
	local function dJ(k)
		return string.format("%d_%d_%d", math.floor(k.R * 255), math.floor(k.G * 255), math.floor(k.B * 255));
	end;
	local function YJ(k)
		local l = k:FindFirstChild("ChamOutlineShell");
		if l then
			pcall(function()
				l:Destroy();
			end);
		end;
		local p = k:FindFirstChild("ChamOutline");
		if p and p:IsA("Highlight") then
			pcall(function()
				p:Destroy();
			end);
		end;
	end;
	local function MJ(k)
		for l, p in ipairs({ O, kJ }) do
			local a = p[k];
			if a then
				pcall(function()
					a:Destroy();
				end);
				p[k] = nil;
			end;
		end;
	end;
	local function iJ()
		local k = {};
		for l in pairs(O) do
			k[l] = true;
		end;
		for l in pairs(kJ) do
			k[l] = true;
		end;
		for k in pairs(k) do
			MJ(k);
		end;
	end;
	local function RJ(k)
		YJ(k);
		MJ(k);
		local l = D[k];
		if l then
			pcall(function()
				l:Destroy();
			end);
			D[k] = nil;
		end;
		h[k] = nil;
	end;
	local function BJ()
		iJ();
		local k = {};
		for l in pairs(D) do
			k[l] = true;
		end;
		for k in pairs(k) do
			RJ(k);
		end;
	end;
	local function WJ(k, l, p, a, y, v, j)
		local s = p[l];
		if not s or not s.Parent then
			s = k:Clone();
			s.Name = a;
			for k, l in ipairs(s:GetDescendants()) do
				if l:IsA("Script") or l:IsA("LocalScript") or l:IsA("Highlight") then
					l:Destroy();
				end;
			end;
			for k, l in ipairs(s:GetChildren()) do
				if l:IsA("Texture") or l:IsA("Decal") or l:IsA("SurfaceAppearance") then
					l:Destroy();
				end;
			end;
			s.Anchored = false;
			s.CanCollide = false;
			s.CanQuery = false;
			s.CanTouch = false;
			s.Massless = true;
			s.CastShadow = false;
			local y = Instance.new("WeldConstraint");
			y.Part0 = k;
			y.Part1 = s;
			y.Parent = s;
			s.Parent = k;
			p[l] = s;
		end;
		s.Material = Enum.Material.Neon;
		s.Transparency = v;
		if s:IsA("BasePart") then
			s.Size = k.Size * y + j;
		end;
	end;
	local function LJ(k, l, p)
		if not k or not k.Parent then
			MJ(k);
			return;
		end;
		local a = math.clamp(((tonumber(p) or 5)) / 10, 0, 1);
		local y = 1.02 + a * .07;
		local v = 1.06 + a * .2;
		local j = math.clamp(.55 - a * .4, .05, .95);
		local s = math.clamp(.88 - a * .55, .08, .95);
		local G = Vector3.new(.008, .008, .008) + Vector3.new(.02, .02, .02) * a;
		local g = Vector3.new(.02, .02, .02) + Vector3.new(.06, .06, .06) * a;
		WJ(k, k, O, "ChamOutlineGlow", y, j, G);
		WJ(k, k, kJ, "ChamOutlineBloom", v, s, g);
		local T = O[k];
		local c = kJ[k];
		if T then
			T.Color = l;
		end;
		if c then
			c.Color = l;
		end;
	end;
	local function PJ(k, l)
		if not k or not k.Parent then
			return;
		end;
		local p = dJ(l);
		local a = D[k];
		if a and (a.Parent and (a.Adornee == k and h[k] == p)) then
			return;
		end;
		YJ(k);
		if not a or not a.Parent then
			a = Instance.new("Highlight");
			a.Name = "ChamOutline";
			a.DepthMode = Enum.HighlightDepthMode.AlwaysOnTop;
			a.Parent = lJ;
			D[k] = a;
		end;
		a.OutlineColor = l;
		a.OutlineTransparency = 0;
		a.FillColor = l;
		a.FillTransparency = 1;
		a.Adornee = k;
		h[k] = p;
	end;
	local function xJ(k)
		for l in pairs(D) do
			if not k[l] then
				RJ(l);
			end;
		end;
	end;
	local function tJ()
		return a or y or j or s or v or S or X;
	end;
	local function SJ(k)
		local l = r[k];
		if l then
			pcall(function()
				l:Destroy();
			end);
			r[k] = nil;
		end;
		local p = k and k:FindFirstChild("FinhubVmPartHighlight");
		if p and p:IsA("Highlight") then
			pcall(function()
				p:Destroy();
			end);
		end;
	end;
	local function VJ()
		for k in pairs(r) do
			SJ(k);
		end;
		r = {};
	end;
	local function XJ(k, l, p, a, y)
		if not k or not k.Parent then
			SJ(k);
			return;
		end;
		SJ(k);
		local v = Instance.new("Highlight");
		v.Name = "FinhubVmPartHighlight";
		v.DepthMode = Enum.HighlightDepthMode.AlwaysOnTop;
		v.FillColor = l;
		v.OutlineColor = p;
		v.FillTransparency = math.clamp(a or 0, 0, 1);
		v.OutlineTransparency = math.clamp(y or 0, 0, 1);
		v.Adornee = k;
		v.Parent = k;
		r[k] = v;
	end;
	local function ZJ(k)
		for l in pairs(r) do
			if not k[l] then
				SJ(l);
			end;
		end;
	end;
	local function EJ(k)
		local l = {};
		for k in k.Name:gmatch("[^-]+") do
			table.insert(l, (k:match("^%s*(.-)%s*$")):lower());
		end;
		if #l >= 2 then
			local k = l[2];
			if k == "arms" or k == "arm" then
				return true;
			end;
			if k:find("arm", 1, true) and (not k:find("charm", 1, true) and not k:find("armor", 1, true)) then
				return true;
			end;
			if k:find("glove", 1, true) then
				return true;
			end;
			return false;
		end;
		local p = k.Name:lower();
		return p:find("arms", 1, true) ~= nil or p:find("glove", 1, true) ~= nil;
	end;
	local JJ = {
			hitbox = true,
			collision = true,
			trigger = true,
			proxy = true,
			handguard = true,
			handle = true,
			foregrip = true,
			charm = true,
			armor = true,
		};
	local function UJ(k)
		local l = k:lower();
		for k in pairs(JJ) do
			if l:find(k, 1, true) then
				return false;
			end;
		end;
		if l == "leftarm" or l == "rightarm" or l == "larm" or l == "rarm" then
			return true;
		end;
		if l:find("leftarm", 1, true) or l:find("rightarm", 1, true) then
			return true;
		end;
		if l:find("left arm", 1, true) or l:find("right arm", 1, true) then
			return true;
		end;
		if l:find("forearm", 1, true) or l:find("upperarm", 1, true) or l:find("lowerarm", 1, true) then
			return true;
		end;
		if l:find("glove", 1, true) or l:find("sleeve", 1, true) then
			return true;
		end;
		if l:find("finger", 1, true) or l:find("wrist", 1, true) or l:find("palm", 1, true) or l:find("thumb", 1, true) then
			return true;
		end;
		if l:find("hand", 1, true) then
			if l:find("handguard", 1, true) or l:find("handle", 1, true) then
				return false;
			end;
			return true;
		end;
		return false;
	end;
	local function zJ(k, l)
		local p = k.Parent;
		while p and p ~= l do
			local k = p.Name:lower();
			if k == "arms" or k == "leftarm" or k == "rightarm" or k == "left arm" or k == "right arm" or k == "hands" or k == "gloves" then
				return true;
			end;
			if k:find("glove", 1, true) or k:find("sleeve", 1, true) then
				return true;
			end;
			if k:find("arm", 1, true) and (not k:find("armor", 1, true) and (not k:find("handguard", 1, true) and (not k:find("foregrip", 1, true) and not k:find("charm", 1, true)))) then
				return true;
			end;
			p = p.Parent;
		end;
		return false;
	end;
	local function wJ(k, l)
		if not k or not k:IsA("BasePart") then
			return false;
		end;
		if pJ[k.Name] or k.Transparency >= .99 then
			return false;
		end;
		if EJ(l) then
			local l = k.Name:lower();
			if pJ[k.Name] then
				return false;
			end;
			if l:find("hitbox") or l:find("collision") or l:find("trigger") or l:find("proxy") then
				return false;
			end;
			return true;
		end;
		if zJ(k, l) then
			return true;
		end;
		return UJ(k.Name);
	end;
	local function qJ(k, l)
		if not k or not k:IsA("BasePart") then
			return false;
		end;
		if pJ[k.Name] or k.Transparency >= .99 then
			return false;
		end;
		if EJ(l) then
			return false;
		end;
		if wJ(k, l) then
			return false;
		end;
		return nJ(k);
	end;
	local uJ;
	local HJ;
	local FJ = "FinhubViewModelChams";
	local QJ = 60;
	local AJ = 0;
	local IJ = nil;
	local mJ = true;
	local oJ = false;
	local rJ = false;
	local function CJ()
		mJ = true;
	end;
	local function DJ()
		BJ();
		iJ();
		VJ();
		for k, l in pairs(C) do
			if k and k.Parent then
				GJ(k);
			end;
		end;
		C = {};
		IJ = nil;
		mJ = true;
	end;
	local function hJ(k)
		if not k or rJ then
			return;
		end;
		rJ = true;
		k.ChildAdded:Connect(function()
			task.defer(CJ);
		end);
		k.ChildRemoved:Connect(function()
			task.defer(CJ);
		end);
	end;
	local function OJ(k)
		IJ = {};
		for k, l in ipairs(k:GetChildren()) do
			if l:IsA("Model") then
				for k, p in ipairs(l:GetDescendants()) do
					if p:IsA("BasePart") then
						local k = wJ(p, l);
						local a = qJ(p, l);
						if k or a then
							table.insert(IJ, {
								part = p,
								model = l,
								isArm = k,
								isGun = a,
							});
						end;
					end;
				end;
			end;
		end;
		mJ = false;
	end;
	local function kn()
		pcall(function()
			g:UnbindFromRenderStep(FJ);
		end);
		if HJ then
			HJ:Disconnect();
			HJ = nil;
		end;
		oJ = false;
		rJ = false;
		DJ();
	end;
	local function ln()
		if HJ then
			HJ:Disconnect();
			HJ = nil;
		end;
		pcall(function()
			g:UnbindFromRenderStep(FJ);
		end);
		local k = pcall(function()
				g:BindToRenderStep(FJ, Enum.RenderPriority.Last.Value, uJ);
			end);
		if not k then
			HJ = g.RenderStepped:Connect(uJ);
		end;
	end;
	local function pn()
		CJ();
		if tJ() then
			ln();
		else
			kn();
		end;
	end;
	(getgenv()).FinhubUpdViewModelChams = pn;
	uJ = function(k)
			if V and V() then
				return;
			end;
			k = k or (.0041666666666667);
			if S then
				A = ((A + k * o)) % 1;
			end;
			if X then
				I = ((I + k * o)) % 1;
			end;
			local l = a or y;
			if l or j or s or S or X then
				AJ = AJ + k;
				if AJ >= (1 / QJ) then
					AJ = AJ - (1 / QJ);
					if a or j or S then
						i = ((i + ((1 / QJ)) * M)) % 1;
					end;
					if y or s or X then
						P = ((P + ((1 / QJ)) * L)) % 1;
					end;
				end;
			end;
			if not tJ() then
				if IJ or next(C) then
					DJ();
				end;
				return;
			end;
			local p = m:FindFirstChild("ViewModels");
			if not p then
				if not oJ then
					DJ();
					oJ = true;
				end;
				return;
			end;
			local g = p:FindFirstChild("FirstPerson");
			if not g then
				if not oJ then
					DJ();
					oJ = true;
				end;
				rJ = false;
				return;
			end;
			oJ = false;
			hJ(g);
			if mJ or not IJ then
				OJ(g);
			end;
			local z = {};
			local F = {};
			local r = {};
			for k, l in ipairs(IJ) do
				local p = l.part;
				if not p or not p.Parent then
					CJ();
					continue;
				end;
				local g = l.isArm;
				local M = l.isGun and not l.isArm;
				local L = l.model;
				F[p] = true;
				sJ(p);
				if v and g then
					p.Transparency = 1;
					for k, l in ipairs(p:GetChildren()) do
						if l:IsA("Texture") or l:IsA("Decal") or l:IsA("SurfaceAppearance") then
							l.Transparency = 1;
						end;
					end;
					RJ(p);
					SJ(p);
				else
					local k = S and M;
					local l = X and g;
					local v = not k and (not l and (((g and y) or (M and a))));
					local L = (g and (s and not l)) or (M and (j and not k));
					local V = eJ(g and R or n, g and B or d, g and W or Y, 0, g and P or i);
					local F = eJ(g and e or G, g and b or T, g and f or c, 0, g and P or i);
					if k then
						GJ(p);
						local k = KJ(Z, E, A);
						local l = cJ(k, Color3.new(0, 0, 0), .35);
						XJ(p, k, l, q, u);
						r[p] = true;
					elseif l then
						local k = KJ(J, U, I);
						local l = cJ(k, Color3.new(0, 0, 0), .35);
						TJ(p, k, w, H);
						XJ(p, k, l, H, Q);
						r[p] = true;
					elseif v then
						local k = g and K or N;
						local l = g and x or t;
						if g then
							TJ(p, V, k, l);
						else
							gJ(p, V, k, l, false);
						end;
						SJ(p);
					elseif not L then
						GJ(p);
						SJ(p);
					else
						SJ(p);
					end;
					if L then
						PJ(p, F);
						z[p] = true;
					else
						RJ(p);
					end;
				end;
			end;
			xJ(z);
			ZJ(r);
			for k, l in pairs(C) do
				if not F[k] then
					GJ(k);
					C[k] = nil;
				end;
			end;
		end;
	local an;
	local function yn()
		if an then
			an:Disconnect();
		end;
		an = (game:GetService("RunService")).RenderStepped:Connect(function()
				if V and V() then
					return;
				end;
				if not aJ then
					return;
				end;
				local k = m:FindFirstChild("ViewModels");
				if not k then
					return;
				end;
				local l = k:FindFirstChild("FirstPerson");
				if not l then
					return;
				end;
				local p = workspace.CurrentCamera;
				local a = p.CFrame * CFrame.new(yJ, vJ, jJ);
				for k, l in ipairs(l:GetChildren()) do
					if l:IsA("Model") then
						local k = l.PrimaryPart;
						if k then
							l:SetPrimaryPartCFrame(a);
						end;
					end;
				end;
			end);
	end;
	local function vn()
		if an then
			an:Disconnect();
			an = nil;
		end;
	end;
	p:AddToggle("EnableViewport", { Text = "enable", Default = false, Callback = function(k)
			aJ = k;
			if k then
				yn();
			else
				vn();
			end;
		end });
	p:AddSlider("OffsetX", {
		Text = "x",
		Default = 0,
		Min = -10,
		Max = 10,
		Rounding = 2,
		Compact = true,
		Callback = function(k)
			yJ = k;
		end,
	});
	p:AddSlider("OffsetY", {
		Text = "y",
		Default = 0,
		Min = -10,
		Max = 10,
		Rounding = 2,
		Compact = true,
		Callback = function(k)
			vJ = k;
		end,
	});
	p:AddSlider("OffsetZ", {
		Text = "z",
		Default = 0,
		Min = -10,
		Max = 10,
		Rounding = 2,
		Compact = true,
		Callback = function(k)
			jJ = k;
		end,
	});
	local jn = false;
	local sn = {};
	local Gn = {};
	local gn = nil;
	local Tn = {
			Idle = { "idle" },
			Reloading = { "reload" },
			Sprinting = { "sprint" },
			Crouching = { "crouch" },
			Shooting = { "shoot", "fire", "shot" },
			Aiming = { "aim", "ads", "scope" },
		};
	local function cn(k)
		local l = { k.Name:lower() };
		local p = k.Animation;
		if p then
			table.insert(l, p.Name:lower());
			table.insert(l, (tostring(p.AnimationId)):lower());
		end;
		return table.concat(l, " ");
	end;
	local function en(k)
		if not jn or type(sn) ~= "table" then
			return false;
		end;
		local l = cn(k);
		for k, p in pairs(Tn) do
			if sn[k] then
				for k, p in ipairs(p) do
					if l:find(p, 1, true) then
						return true;
					end;
				end;
			end;
		end;
		return false;
	end;
	local function bn(k)
		if en(k) then
			pcall(function()
				k:Stop(0);
			end);
		end;
	end;
	local function fn(k)
		if typeof(k) ~= "Finhub" or not k:IsA("Animator") then
			return;
		end;
		if k:GetAttribute("FinhubNoAnimBound") then
			return;
		end;
		k:SetAttribute("FinhubNoAnimBound", true);
		local l = k.Parent;
		if l and l:IsA("Humanoid") then
			table.insert(Gn, l.AnimationPlayed:Connect(function(k)
				if jn then
					task.defer(function()
						bn(k);
					end);
				end;
			end));
		end;
	end;
	local function Nn(k)
		if not k then
			return;
		end;
		for k, l in ipairs(k:GetDescendants()) do
			if l:IsA("Animator") then
				fn(l);
				if jn then
					for k, l in ipairs(l:GetPlayingAnimationTracks()) do
						bn(l);
					end;
				end;
			end;
		end;
	end;
	local function Kn()
		local k = m:FindFirstChild("ViewModels");
		if not k then
			return;
		end;
		local l = k:FindFirstChild("FirstPerson");
		if l then
			Nn(l);
			if not k:GetAttribute("FinhubNoAnimVmHooked") then
				k:SetAttribute("FinhubNoAnimVmHooked", true);
				table.insert(Gn, l.DescendantAdded:Connect(function(k)
					if k:IsA("Animator") then
						fn(k);
					end;
				end));
			end;
		end;
	end;
	local function nn()
		if gn then
			gn:Disconnect();
			gn = nil;
		end;
		for k, l in ipairs(Gn) do
			pcall(function()
				l:Disconnect();
			end);
		end;
		table.clear(Gn);
		local k = m:FindFirstChild("ViewModels");
		if k then
			k:SetAttribute("FinhubNoAnimVmHooked", nil);
			for k, l in ipairs(k:GetDescendants()) do
				if l:IsA("Animator") then
					l:SetAttribute("FinhubNoAnimBound", nil);
				end;
			end;
		end;
	end;
	local function dn()
		nn();
		Kn();
		gn = g.RenderStepped:Connect(function()
				if not jn then
					return;
				end;
				if V and V() then
					return;
				end;
				Kn();
				local k = m:FindFirstChild("ViewModels");
				local l = k and k:FindFirstChild("FirstPerson");
				if not l then
					return;
				end;
				for k, l in ipairs(l:GetDescendants()) do
					if l:IsA("Animator") then
						for k, l in ipairs(l:GetPlayingAnimationTracks()) do
							bn(l);
						end;
					end;
				end;
			end);
	end;
	local function Yn()
		if jn then
			dn();
		else
			nn();
		end;
	end;
	local Mn = p:AddToggle("NoAnimations", { Text = "no animations", Default = false, Callback = function(k)
				jn = k;
				Yn();
			end });
	local Rn = p:AddDependencyBox();
	Rn:AddDropdown("NoAnimationsList", {
		Text = "disabled animations",
		Values = {
			"Idle",
			"Reloading",
			"Sprinting",
			"Crouching",
			"Shooting",
			"Aiming",
		},
		Default = {},
		Multi = true,
		Callback = function(k)
			sn = k or {};
		end,
	});
	Rn:SetupDependencies({ { Mn, true } });
	local Bn = {
			"ForceField",
			"Neon",
			"SmoothPlastic",
			"Glass",
			"Ice",
			"Plastic",
			"Wood",
			"Marble",
			"Granite",
			"Brick",
			"Cobblestone",
			"Concrete",
			"Slate",
			"Foil",
		};
	local Wn = (((l:AddToggle("GunChams", { Text = "gun chams", Default = false, Callback = function(k)
				a = k;
				pn();
			end })):AddColorPicker("GunColor1", { Default = Color3.fromRGB(255, 255, 255), Title = "gun color 1", Callback = function(k)
				n = k;
			end })):AddColorPicker("GunColor2", { Default = Color3.fromRGB(255, 255, 255), Title = "gun color 2", Callback = function(k)
				d = k;
			end })):AddColorPicker("GunColor3", { Default = Color3.fromRGB(255, 255, 255), Title = "gun color 3", Callback = function(k)
				Y = k;
			end });
	local Ln = l:AddDependencyBox();
	Ln:AddDropdown("GunMaterial", {
		Text = "gun material",
		Default = "Neon",
		Values = Bn,
		Callback = function(k)
			N = Enum.Material[k];
		end,
	});
	Ln:AddSlider("GunChamSpeed", {
		Text = "gradient speed",
		Default = M,
		Min = .1,
		Max = 10,
		Rounding = 1,
		Compact = true,
		Callback = function(k)
			M = k;
		end,
	});
	Ln:AddSlider("GunChamTransparency", {
		Text = "gun transparency",
		Default = t,
		Min = 0,
		Max = 1,
		Rounding = 2,
		Compact = true,
		Callback = function(k)
			t = k;
		end,
	});
	Ln:SetupDependencies({ { Wn, true } });
	local Pn = (((l:AddToggle("ArmChams", { Text = "arm chams", Default = false, Callback = function(k)
				y = k;
				pn();
			end })):AddColorPicker("ArmColor1", { Default = Color3.fromRGB(255, 255, 255), Title = "arm color 1", Callback = function(k)
				R = k;
			end })):AddColorPicker("ArmColor2", { Default = Color3.fromRGB(255, 255, 255), Title = "arm color 2", Callback = function(k)
				B = k;
			end })):AddColorPicker("ArmColor3", { Default = Color3.fromRGB(255, 255, 255), Title = "arm color 3", Callback = function(k)
				W = k;
			end });
	local xn = l:AddDependencyBox();
	xn:AddDropdown("ArmMaterial", {
		Text = "arm material",
		Default = "ForceField",
		Values = Bn,
		Callback = function(k)
			K = Enum.Material[k];
		end,
	});
	xn:AddSlider("ArmChamSpeed", {
		Text = "gradient speed",
		Default = L,
		Min = .1,
		Max = 10,
		Rounding = 1,
		Compact = true,
		Callback = function(k)
			L = k;
		end,
	});
	xn:AddSlider("ArmChamTransparency", {
		Text = "arm transparency",
		Default = x,
		Min = 0,
		Max = 1,
		Rounding = 2,
		Compact = true,
		Callback = function(k)
			x = k;
		end,
	});
	xn:SetupDependencies({ { Pn, true } });
	(((l:AddToggle("GunOutline", { Text = "gun outline", Default = false, Callback = function(k)
			j = k;
			pn();
		end })):AddColorPicker("GunOutlineColor1", { Title = "gun outline 1", Default = Color3.fromRGB(255, 255, 255), Callback = function(k)
			G = k;
		end })):AddColorPicker("GunOutlineColor2", { Title = "gun outline 2", Default = Color3.fromRGB(255, 255, 255), Callback = function(k)
			T = k;
		end })):AddColorPicker("GunOutlineColor3", { Title = "gun outline 3", Default = Color3.fromRGB(255, 255, 255), Callback = function(k)
			c = k;
		end });
	(((l:AddToggle("ArmOutline", { Text = "arm outline", Default = false, Callback = function(k)
			s = k;
			pn();
		end })):AddColorPicker("ArmOutlineColor1", { Title = "arm outline 1", Default = Color3.fromRGB(255, 255, 255), Callback = function(k)
			e = k;
		end })):AddColorPicker("ArmOutlineColor2", { Title = "arm outline 2", Default = Color3.fromRGB(255, 255, 255), Callback = function(k)
			b = k;
		end })):AddColorPicker("ArmOutlineColor3", { Title = "arm outline 3", Default = Color3.fromRGB(255, 255, 255), Callback = function(k)
			f = k;
		end });
	local tn = ((l:AddToggle("GunHighlight", { Text = "gun highlight", Default = false, Callback = function(k)
				S = k;
				pn();
			end })):AddColorPicker("GunHighlightTop", { Default = Color3.fromRGB(255, 255, 255), Title = "gun top", Callback = function(k)
				Z = k;
			end })):AddColorPicker("GunHighlightBottom", { Default = Color3.fromRGB(255, 105, 180), Title = "gun bottom", Callback = function(k)
				E = k;
			end });
	local Sn = l:AddDependencyBox();
	Sn:AddDropdown("GunHighlightMaterial", {
		Text = "gun highlight material",
		Default = "Neon",
		Values = Bn,
		Callback = function(k)
			z = Enum.Material[k];
		end,
	});
	Sn:AddSlider("GunHighlightFillTransparency", {
		Text = "transparency",
		Default = q,
		Min = 0,
		Max = 1,
		Rounding = 2,
		Compact = true,
		Callback = function(k)
			q = k;
		end,
	});
	Sn:AddSlider("GunHighlightOutlineTransparency", {
		Text = "outline transparency",
		Default = u,
		Min = 0,
		Max = 1,
		Rounding = 2,
		Compact = true,
		Callback = function(k)
			u = k;
		end,
	});
	Sn:SetupDependencies({ { tn, true } });
	local Vn = ((l:AddToggle("ArmHighlight", { Text = "arm highlight", Default = false, Callback = function(k)
				X = k;
				pn();
			end })):AddColorPicker("ArmHighlightTop", { Default = Color3.fromRGB(255, 255, 255), Title = "arm top", Callback = function(k)
				J = k;
			end })):AddColorPicker("ArmHighlightBottom", { Default = Color3.fromRGB(255, 105, 180), Title = "arm bottom", Callback = function(k)
				U = k;
			end });
	local Xn = l:AddDependencyBox();
	Xn:AddDropdown("ArmHighlightMaterial", {
		Text = "arm highlight material",
		Default = "Neon",
		Values = Bn,
		Callback = function(k)
			w = Enum.Material[k];
		end,
	});
	Xn:AddSlider("ArmHighlightFillTransparency", {
		Text = "arm highlight fill transparency",
		Default = H,
		Min = 0,
		Max = 1,
		Rounding = 2,
		Compact = true,
		Callback = function(k)
			H = k;
		end,
	});
	Xn:AddSlider("ArmHighlightOutlineTransparency", {
		Text = "arm highlight outline transparency",
		Default = Q,
		Min = 0,
		Max = 1,
		Rounding = 2,
		Compact = true,
		Callback = function(k)
			Q = k;
		end,
	});
	Xn:SetupDependencies({ { Vn, true } });
	l:AddToggle("DisableArms", { Text = "disable arms", Default = false, Callback = function(k)
			v = k;
			pn();
		end });
end;
do
	local k = F.World:AddLeftGroupbox("bullet tracers");
	local l = true;
	local p = Color3.fromRGB(255, 255, 255);
	local a = 3;
	local y = 1;
	local v = .5;
	local j = 0;
	local s = "Line";
	local G = {};
	local T = nil;
	local c = false;
	local e = {};
	local b = .1;
	local f = 1.0;
	local N = {
			Line = "",
			Beam = "rbxassetid://12781852245",
			Lightning = "rbxassetid://446111271",
			Heartrate = "rbxassetid://5830549480",
			Chain = "rbxassetid://9632168658",
			Glitch = "rbxassetid://8089467613",
			Swirl = "rbxassetid://5638168605",
			Neon = "rbxassetid://6361963422",
			Plasma = "rbxassetid://8993645509",
			Laser = "rbxassetid://14549123968",
		};
	local function K()
		local k = workspace:FindFirstChild("ViewModels");
		if not k then
			return nil;
		end;
		local l = k:FindFirstChild("FirstPerson");
		if not l then
			return nil;
		end;
		local p = h.Name;
		for k, l in ipairs(l:GetChildren()) do
			if l:IsA("Model") and l.Name:find("^" .. p) then
				local k = l:FindFirstChild("ItemVisual");
				if k then
					local l = k:FindFirstChild("Body");
					if l then
						local k = l:FindFirstChild("BodyPrimary");
						if k then
							local l = k:FindFirstChild("_muzzle");
							if l and l:IsA("Attachment") then
								return l.WorldPosition;
							end;
						end;
					end;
				end;
			end;
		end;
		if type(findShotMuzzlePosition) == "function" then
			return findShotMuzzlePosition();
		end;
		return nil;
	end;
	local function n(k, l)
		if j <= 0 then
			return k.EndPos;
		end;
		local p = math.max(j * .25, .001);
		local a = math.clamp(l / p, 0, 1);
		return k.StartPos:Lerp(k.EndPos, a);
	end;
	local function d(k, l)
		local j = Drawing.new("Line");
		j.Thickness = 4 * y;
		j.Color = Color3.new(0, 0, 0);
		j.Transparency = 1;
		j.Visible = false;
		local s = Drawing.new("Line");
		s.Thickness = 2 * y;
		s.Color = p;
		s.Transparency = 1;
		s.Visible = false;
		table.insert(G, {
			IsLine = true,
			Outline = j,
			Line = s,
			StartPos = k,
			EndPos = l,
			Lifetime = a,
			FadeTime = v,
			CreatedTime = tick(),
		});
	end;
	local function Y(k, l)
		local j = Instance.new("Attachment");
		j.Parent = workspace.Terrain;
		local g = Instance.new("Attachment");
		g.Parent = workspace.Terrain;
		local T = Instance.new("Beam");
		T.Attachment0 = j;
		T.Attachment1 = g;
		T.Color = ColorSequence.new(p);
		local c = s == "Laser" and .02 or .15;
		T.Width0 = c * y;
		T.Width1 = c * y;
		T.Transparency = NumberSequence.new(0);
		T.FaceCamera = true;
		T.LightEmission = .8;
		T.LightInfluence = .2;
		local e = N[s];
		if e and e ~= "" then
			T.Texture = e;
			T.TextureLength = 4;
			T.TextureSpeed = 1;
		else
			T.Texture = "";
			T.TextureLength = 1;
			T.TextureSpeed = 0;
		end;
		T.Parent = workspace.Terrain;
		j.WorldPosition = k;
		g.WorldPosition = l;
		table.insert(G, {
			IsLine = false,
			Beam = T,
			Attachment0 = j,
			Attachment1 = g,
			StartPos = k,
			EndPos = l,
			Lifetime = a,
			FadeTime = v,
			CreatedTime = tick(),
		});
	end;
	local function M(k, l)
		if DISABLE_TRACERS or not k or not l then
			return;
		end;
		local p = math.floor(k.X * 5 + .5);
		local a = math.floor(k.Y * 5 + .5);
		local y = math.floor(k.Z * 5 + .5);
		local v = math.floor(l.X * 5 + .5);
		local j = math.floor(l.Y * 5 + .5);
		local G = math.floor(l.Z * 5 + .5);
		local g = ((((p * 1000000000.0 + a * 1000000.0) + y * 1000.0) + v) + j * .001) + G * 1e-06;
		local T = tick();
		local c = e[g];
		if c and (T - c) < b then
			return;
		end;
		e[g] = T;
		if s == "Line" then
			d(k, l);
		else
			Y(k, l);
		end;
	end;
	local function i(k)
		if k.IsLine then
			if k.Outline then
				k.Outline:Remove();
			end;
			if k.Line then
				k.Line:Remove();
			end;
		else
			if k.Beam then
				k.Beam:Destroy();
			end;
			if k.Attachment0 then
				k.Attachment0:Destroy();
			end;
			if k.Attachment1 then
				k.Attachment1:Destroy();
			end;
		end;
	end;
	local R = 0;
	local B = 120;
	local W = workspace.CurrentCamera;
	local function L()
		if #G == 0 then
			return;
		end;
		R = R + 1;
		local k = tick();
		local l = W or workspace.CurrentCamera;
		W = l;
		local p = h.Character;
		local a = p and (p:FindFirstChild("HumanoidRootPart") and p.HumanoidRootPart.Position);
		if R % B == 0 then
			for l, p in next, e do
				if (k - p) > f then
					e[l] = nil;
				end;
			end;
		end;
		local y = #G;
		while y >= 1 do
			local p = G[y];
			local v = k - p.CreatedTime;
			if v >= p.Lifetime then
				i(p);
				G[y] = G[#G];
				G[#G] = nil;
			else
				local k;
				local y = p.Lifetime - p.FadeTime;
				if v >= y then
					k = 1 - math.clamp(((v - y)) / p.FadeTime, 0, 1);
				else
					k = 1;
				end;
				if p.IsLine then
					local y = false;
					local j = n(p, v);
					local s = l and l.CFrame.Position;
					local G = s and ((p.StartPos - s)).Magnitude <= 12;
					if G or not a or ((p.StartPos - a)).Magnitude > 2 then
						local a, v = lJ(p.StartPos, l);
						local s, G = lJ(j, l);
						if v and G then
							local l, v = a.X, a.Y;
							local j, G = s.X, s.Y;
							if l == l and (v == v and (j == j and (G == G and (math.abs(l) < 20000 and (math.abs(v) < 20000 and (math.abs(j) < 20000 and math.abs(G) < 20000)))))) then
								local a = j - l;
								local s = G - v;
								local g = math.sqrt(a * a + s * s);
								if g > .1 and g < 3000 then
									local a = Vector2.new(l, v);
									local s = Vector2.new(j, G);
									p.Outline.From = a;
									p.Outline.To = s;
									p.Outline.Transparency = k;
									p.Outline.Visible = true;
									p.Line.From = a;
									p.Line.To = s;
									p.Line.Transparency = k;
									p.Line.Visible = true;
									y = true;
								end;
							end;
						end;
					end;
					if not y then
						p.Outline.Visible = false;
						p.Line.Visible = false;
					end;
				else
					local l = n(p, v);
					if p.Attachment0 then
						p.Attachment0.WorldPosition = p.StartPos;
					end;
					if p.Attachment1 then
						p.Attachment1.WorldPosition = l;
					end;
					p.Beam.Transparency = NumberSequence.new(1 - k);
				end;
			end;
			y = y - 1;
		end;
	end;
	local function P()
		if c then
			return;
		end;
		pcall(function()
			local k = require(h.PlayerScripts.Modules.TracerEffect);
			local p = k.Play;
			k.Play = function(k, a, y, v)
					if l and (a and (a.IsLocal and a.RaycastResults)) then
						local k = K();
						if k then
							for l, p in ipairs(a.RaycastResults) do
								if p.Position then
									M(k, p.Position);
								end;
							end;
						end;
					end;
					return p(k, a, y, v);
				end;
			c = true;
		end);
	end;
	local function x()
		if DISABLE_TRACERS then
			l = false;
			return;
		end;
		P();
		if not T then
			T = g.RenderStepped:Connect(L);
		end;
	end;
	local function t()
		if T then
			T:Disconnect();
			T = nil;
		end;
		for k, l in ipairs(G) do
			i(l);
		end;
		G = {};
	end;
	(k:AddToggle("TracerEnabled", { Text = "enable", Default = true, Callback = function(k)
			l = k;
			if k then
				x();
			else
				t();
			end;
		end })):AddColorPicker("TracerColor", { Default = Color3.fromRGB(255, 255, 255), Title = "tracer color", Callback = function(k)
			p = k;
			for l, p in ipairs(G) do
				if p.IsLine then
					if p.Line then
						p.Line.Color = k;
					end;
				elseif p.Beam then
					p.Beam.Color = ColorSequence.new(k);
				end;
			end;
		end });
	k:AddDropdown("TracerStyle", {
		Text = "style",
		Default = "Line",
		Values = {
			"Line",
			"Beam",
			"Lightning",
			"Heartrate",
			"Chain",
			"Glitch",
			"Swirl",
			"Neon",
			"Plasma",
			"Laser",
		},
		Callback = function(k)
			s = k;
		end,
	});
	k:AddSlider("TracerDuration", {
		Text = "duration",
		Default = 3,
		Min = .1,
		Max = 10,
		Rounding = 1,
		Compact = true,
		Callback = function(k)
			a = k;
			for l, p in ipairs(G) do
				p.Lifetime = k;
			end;
		end,
	});
	k:AddSlider("TracerSize", {
		Text = "size",
		Default = 1,
		Min = .5,
		Max = 5,
		Rounding = 1,
		Compact = true,
		Callback = function(k)
			y = k;
			for l, p in ipairs(G) do
				if p.IsLine then
					if p.Outline then
						p.Outline.Thickness = 4 * k;
					end;
					if p.Line then
						p.Line.Thickness = 2 * k;
					end;
				elseif p.Beam then
					local l = s == "Laser" and .02 or .15;
					p.Beam.Width0 = l * k;
					p.Beam.Width1 = l * k;
				end;
			end;
		end,
	});
	k:AddSlider("TracerFade", {
		Text = "fade time",
		Default = .5,
		Min = 0,
		Max = 2,
		Rounding = 1,
		Compact = true,
		Callback = function(k)
			v = k;
			for l, p in ipairs(G) do
				p.FadeTime = k;
			end;
		end,
	});
	k:AddSlider("TracerLerpSpeed", {
		Text = "lerp speed",
		Default = 0,
		Min = 0,
		Max = 5,
		Rounding = 1,
		Compact = true,
		Callback = function(k)
			j = k;
		end,
	});
	task.defer(function()
		x();
		for k = 1, 30, 1 do
			if c then
				break;
			end;
			P();
			task.wait(.5);
		end;
	end);
end;
local function NJ(k)
	if not k then
		return "None";
	end;
	local l = workspace:FindFirstChild("ViewModels");
	if not l then
		return "None";
	end;
	local p = k.Name;
	local function a(k)
		local l = {};
		for k in k:gmatch("[^-]+") do
			table.insert(l, k:match("^%s*(.-)%s*$"));
		end;
		if #l >= 2 and l[1] == p then
			return l[2];
		end;
		return nil;
	end;
	for k, l in ipairs(l:GetChildren()) do
		local p = a(l.Name);
		if p then
			return p;
		end;
	end;
	local y = l:FindFirstChild("FirstPerson");
	if y then
		for k, l in ipairs(y:GetChildren()) do
			local p = a(l.Name);
			if p then
				return p;
			end;
		end;
	end;
	return "None";
end;
local KJ = {};
do
	local k = cloneref(game:GetService("ReplicatedStorage"));
	local l = cloneref(game:GetService("Players"));
	local p = cloneref(game:GetService("RunService"));
	local a = cloneref(game:GetService("UserInputService"));
	local y = cloneref(game:GetService("Workspace"));
	local j = l.LocalPlayer;
	local s = y.CurrentCamera;
	local G = {
			enums = v(k.Modules.EnumLibrary),
			fighter = v(j.PlayerScripts.Controllers.FighterController),
			camcontrol = v(j.PlayerScripts.Controllers.CameraController),
			utility = v(k.Modules.Utility),
		};
	local g = {
			"MatchId",
			"DuelId",
			"RoundId",
			"GameId",
			"MatchUUID",
			"ArenaId",
			"FinhubId",
		};
	local T = {
			"InDuel",
			"InMatch",
			"InRound",
			"InGame",
			"InFight",
			"IsInMatch",
			"IsInDuel",
			"MatchActive",
			"Fighting",
		};
	local c = {
			"InSpawn",
			"InLobby",
			"IsSpectating",
			"InSafeZone",
			"InIntermission",
			"IsRespawning",
		};
	local function e(k)
		if not k then
			return false;
		end;
		local l = y:FindFirstChild("Live");
		if not l then
			return false;
		end;
		if l:FindFirstChild(k.Name) then
			return true;
		end;
		if l:FindFirstChild(tostring(k.UserId)) then
			return true;
		end;
		for l, p in ipairs(l:GetChildren()) do
			if p.Name == k.Name or p.Name == tostring(k.UserId) then
				return true;
			end;
		end;
		local p = k.Character;
		if p and ((p.Parent == l or p:IsDescendantOf(l))) then
			return true;
		end;
		local a = G.fighter;
		if a and type(a.GetFighter) == "function" then
			local p, y = pcall(a.GetFighter, a, k);
			if p and (y and (y.Entity and y.Entity.Parent)) then
				if y.Entity.Parent == l or y.Entity:IsDescendantOf(l) then
					return true;
				end;
			end;
		end;
		return false;
	end;
	local function b(k)
		if not k then
			return nil;
		end;
		for l, p in ipairs(g) do
			local a = k:GetAttribute(p);
			if a ~= nil and (a ~= "" and (a ~= 0 and a ~= false)) then
				return tostring(a);
			end;
		end;
		local l = k.Character;
		if l then
			for k, p in ipairs(g) do
				local a = l:GetAttribute(p);
				if a ~= nil and (a ~= "" and (a ~= 0 and a ~= false)) then
					return tostring(a);
				end;
			end;
		end;
		return nil;
	end;
	local function f()
		local k = j;
		if not k then
			return false;
		end;
		for l, p in ipairs(c) do
			local a = k:GetAttribute(p);
			if a == true or a == 1 or a == "true" then
				return false;
			end;
		end;
		local p = k.Character;
		if not p then
			return false;
		end;
		for k, l in ipairs(c) do
			local a = p:GetAttribute(l);
			if a == true or a == 1 or a == "true" then
				return false;
			end;
		end;
		local a = p:FindFirstChildOfClass("Humanoid");
		local y = p:FindFirstChild("HumanoidRootPart");
		if not a or a.Health <= 0 or not y then
			return false;
		end;
		for l, a in ipairs(T) do
			local y = k:GetAttribute(a);
			if y == true or y == 1 or y == "true" then
				return true;
			end;
			local v = p:GetAttribute(a);
			if v == true or v == 1 or v == "true" then
				return true;
			end;
		end;
		if e(k) then
			return true;
		end;
		local v = G.fighter;
		if v and (v.LocalFighter and (v.LocalFighter.Entity and v.LocalFighter.Entity.Parent)) then
			return true;
		end;
		local s = b(k);
		if s then
			for l, p in l:GetPlayers() do
				if p ~= k and b(p) == s then
					return true;
				end;
			end;
		end;
		if v then
			for l, p in ipairs({ v.Fighters, v.AllFighters }) do
				if p then
					for l, p in pairs(p) do
						if p and (p.Player and (p.Player ~= k and ((p.IsEnemy or p.Enemy)))) then
							local k = p.Player.Character;
							local l = k and k:FindFirstChildOfClass("Humanoid");
							if l and l.Health > 0 then
								return true;
							end;
						end;
					end;
				end;
			end;
		end;
		return false;
	end;
	local N = { t = 0, v = true };
	local function K()
		local k = os.clock();
		if k - N.t < .15 then
			return N.v;
		end;
		N.t = k;
		N.v = not f();
		return N.v;
	end;
	(getgenv()).FinhubIsInActiveMatch = f;
	(getgenv()).FinhubIsInLobby = K;
	local n = {
			target = {
				enabled = false,
				character = nil,
				auto = false,
				autoshoot = true,
				shootAttempts = 1,
				hitpart = "Head",
				lastchar = nil,
				lastplayer = nil,
				manualkey = false,
				immune = false,
				attackPosition = "default",
				attackCustomEnabled = false,
				weaponPick = "primary",
				forceWeapon = true,
				rageMasterOn = false,
				meleeFeetDrop = 3.5,
				underOffset = 4,
				customHeight = 2,
				customFront = 0,
				customSide = 0,
				customVertical = 0,
				customRadius = 0,
			},
			prediction = {
				enabled = false,
				multiplier = 1.2,
				velocity = Vector3.new(0, 0, 0),
				acceleration = Vector3.new(0, 0, 0),
				lastposition = nil,
				lasttime = 0,
				velbuffer = {},
				posbuffer = {},
				maxvelsamples = 15,
				maxpossamples = 5,
			},
			orbit = {
				active = false,
				angle = 0,
				serverpos = nil,
				connection = nil,
				speed = 9000,
				height = 1,
				radius = 0,
				originalpos = nil,
			},
			state = {
				reloading = false,
				outofammo = false,
				csyncactive = false,
				reloadStartedAt = 0,
			},
			voidhide = {
				enabled = true,
				originalPosition = nil,
				active = false,
				connection = nil,
			},
			voidspam = {
				enabled = false,
				shoot_min = 1,
				shoot_max = 1,
				hide_min = 1,
				hide_max = 1,
				phase = nil,
				lastswitch = 0,
				currentduration = 0,
			},
			visualizer = { enabled = true, tracer = {
					color = Color3.fromRGB(255, 105, 180),
					thickness = 1,
					transparency = 1,
					start_point = "cursor",
					outline = true,
					outline_color = Color3.fromRGB(0, 0, 0),
					outline_thickness = 1,
				}, indicator = { display_options = { "name", "position", "hit reg" }, color = Color3.fromRGB(255, 255, 255), accent_color = Color3.fromRGB(255, 105, 180) } },
			hitNotifications = {
				enabled = true,
				color = Color3.fromRGB(255, 182, 193),
				textSize = 14,
				maxVisible = 8,
				duration = 3,
				stackGap = 6,
				position = "Top Left",
				offsetX = 12,
				offsetY = 12,
				inAnimation = "fade bounce",
				outAnimation = "fade",
				animInDuration = .52,
				animOutDuration = .38,
			},
			ragestatus = {
				enabled = false,
				mode = "static",
				color = Color3.fromRGB(255, 182, 193),
				staticOffsetX = 0,
				staticOffsetY = 40,
				showAmmo = true,
				hideOnReload = true,
				textSize = 15,
				lineGap = 15,
				fontName = "gotham",
			},
		};
	local d = {
			lastAttackTick = 0,
			hitPartByPlayer = {},
			lastHitAtByPlayer = {},
			targetHudAt = 0,
			rageStatusAt = 0,
			restoreVisualAt = 0,
			killStartAt = nil,
			rageStatusCacheAt = 0,
			cachedRageStatusLine = "",
			cachedRageStatusDetail = "",
			lastRageStatusText = "",
			lastRageDetailText = "",
			lastRageAmmoText = "",
			lastRageColor = nil,
		};
	local Y = {
			active = false,
			hrp = nil,
			mainConnection = nil,
			restoreConnection = nil,
			currentVoidPos = nil,
			lastTeleportTime = 0,
		};
	local M = G.fighter.LocalFighter;
	local i;
	local R = Drawing.new("Circle");
	R.Thickness = 1.5;
	R.NumSides = 36;
	R.Filled = false;
	R.Transparency = 1;
	R.Visible = false;
	R.Radius = 12;
	R.Color = Color3.fromRGB(255, 20, 147);
	local B = Drawing.new("Circle");
	B.Thickness = 4;
	B.NumSides = 36;
	B.Filled = false;
	B.Transparency = 1;
	B.Visible = false;
	B.Radius = 12;
	B.Color = Color3.fromRGB(0, 0, 0);
	local W = Drawing.new("Line");
	W.Visible = false;
	W.Thickness = 2;
	W.Transparency = 1;
	W.Color = Color3.fromRGB(255, 105, 180);
	local L = Drawing.new("Line");
	L.Visible = false;
	L.Thickness = 4;
	L.Transparency = 1;
	L.Color = Color3.fromRGB(0, 0, 0);
	local P = {};
	local function x()
		local k = y:FindFirstChild("ViewModels");
		if not k then
			return nil;
		end;
		local l = k:FindFirstChild("FirstPerson");
		if not l then
			return nil;
		end;
		for k, l in ipairs(l:GetChildren()) do
			local p = {};
			for k in l.Name:gmatch("[^-]+") do
				table.insert(p, k:match("^%s*(.-)%s*$"));
			end;
			if #p >= 2 then
				return p[2];
			end;
		end;
		return nil;
	end;
	local function t()
		local k = y:FindFirstChild("ViewModels");
		if not k then
			return nil;
		end;
		local l = k:FindFirstChild("FirstPerson");
		if not l then
			return nil;
		end;
		for k, l in pairs(l:GetChildren()) do
			if l.Name:find(j.Name) then
				local k = l:FindFirstChild("ItemVisual");
				if k then
					local l = k:FindFirstChild("Body");
					if l then
						local k = l:FindFirstChild("BodyPrimary");
						if k then
							local l = k:FindFirstChild("_muzzle");
							if l then
								return l.WorldPosition;
							end;
						end;
					end;
				end;
			end;
		end;
		return nil;
	end;
	local S = { primary = 1, secondary = 2, melee = 3 };
	local V = {
			["Battle Axe"] = true,
			Chainsaw = true,
			Daggers = true,
			Fists = true,
			Gunblade = true,
			Katana = true,
			Knife = true,
			Scythe = true,
			Trowel = true,
		};
	local Z = { ["Energy Rifle"] = true, ["Energy Pistols"] = true };
	local function E()
		local k = x();
		if not k then
			return false;
		end;
		return Z[k] == true;
	end;
	local function J()
		return S[n.target.weaponPick or "primary"] or 1;
	end;
	local function U()
		return ((n.target.weaponPick or "primary")) == "melee";
	end;
	local function z()
		if E() then
			return nil, nil, false;
		end;
		local k, l = pcall(function()
				return v(j.PlayerScripts.Controllers.FighterController);
			end);
		if k and (l and (l.LocalFighter and l.LocalFighter.EquippedItem)) then
			local k = l.LocalFighter.EquippedItem;
			local p, a, y = "", 0, 0;
			local v = false;
			local j = pcall(function()
					p = tostring(k:Get("Name") or k.Name or "");
					a = k:Get("CurrentAmmo") or k:Get("Ammo") or 0;
					y = k:Get("MaxAmmo") or k:Get("MaxBullets") or a;
					if type(p) == "string" and (p:lower()):find("melee", 1, true) then
						v = true;
					end;
					if V[p] then
						v = true;
					end;
				end);
			if not j then
				return 0, 0, U();
			end;
			if U() then
				v = true;
			end;
			return tonumber(a) or 0, tonumber(y) or 0, v;
		end;
		return 0, 0, U();
	end;
	local function w(k)
		if type(k) ~= "string" or k == "" then
			return false;
		end;
		if (k:lower()):find("melee", 1, true) then
			return true;
		end;
		return V[k] == true;
	end;
	local function q(k)
		if not k then
			return false;
		end;
		local l = k:Get("Name") or k.Name;
		if w(l) then
			return true;
		end;
		local p = k:Get("ItemType") or k:Get("Type") or k:Get("Category");
		return type(p) == "string" and (p:lower()):find("melee", 1, true) ~= nil;
	end;
	local function u(k)
		return k and (k:lower()):find("slingshot", 1, true) ~= nil;
	end;
	local function H()
		local k = G.fighter and G.fighter.LocalFighter;
		if not k then
			return nil;
		end;
		return k.Items;
	end;
	local function Q(k)
		local l = H();
		if not l then
			return nil;
		end;
		if l[k] then
			return l[k];
		end;
		if l[tostring(k)] then
			return l[tostring(k)];
		end;
		for l, p in pairs(l) do
			if p and typeof(p) == "table" then
				local a = tonumber(p:Get("Slot") or p:Get("Index") or p:Get("ItemSlot") or l);
				if a == k then
					return p;
				end;
				local y = p:Get("ItemType") or p:Get("Type");
				if k == 3 and ((y == "Melee" or (type(y) == "string" and (y:lower()):find("melee", 1, true)))) then
					return p;
				end;
				if k == 1 and ((y == "Primary" or y == 1 or y == "1")) then
					return p;
				end;
				if k == 2 and ((y == "Secondary" or y == 2 or y == "2")) then
					return p;
				end;
			end;
		end;
		local p = {};
		for k, l in pairs(l) do
			if l then
				p[#p + 1] = l;
			end;
		end;
		table.sort(p, function(k, l)
			local p = tonumber(k:Get("Slot") or k:Get("Index")) or 99;
			local a = tonumber(l:Get("Slot") or l:Get("Index")) or 99;
			return p < a;
		end);
		return p[k];
	end;
	local function A(k)
		if not k then
			return nil;
		end;
		local l = H();
		local p = k:Get("ObjectID");
		if l and p then
			for k = 1, 3, 1 do
				local a = l[k] or l[tostring(k)];
				if a and a:Get("ObjectID") == p then
					return k;
				end;
			end;
			for k, l in pairs(l) do
				if l and l:Get("ObjectID") == p then
					local l = tonumber(k);
					if l and (l >= 1 and l <= 3) then
						return l;
					end;
				end;
			end;
		end;
		if q(k) then
			return 3;
		end;
		return nil;
	end;
	local I = 0;
	local function m(k)
		pcall(function()
			local l = game:GetService("VirtualInputManager");
			local p = (({ Enum.KeyCode.One, Enum.KeyCode.Two, Enum.KeyCode.Three }))[k];
			if not p then
				return;
			end;
			l:SendKeyEvent(true, p, false, game);
			task.wait(.02);
			l:SendKeyEvent(false, p, false, game);
		end);
		if keypress then
			pcall(keypress, (({ 49, 50, 51 }))[k]);
			task.wait(.02);
			if keyrelease then
				pcall(keyrelease, (({ 49, 50, 51 }))[k]);
			end;
		end;
	end;
	local function o()
		if Toggles and Toggles.TargetOn then
			return Toggles.TargetOn.Value == true;
		end;
		return n.target.rageMasterOn == true;
	end;
	local function r()
		if not o() then
			return;
		end;
		local l = J();
		local p = G.fighter and G.fighter.LocalFighter;
		if not p then
			return;
		end;
		local a = p.EquippedItem;
		if a and A(a) == l then
			return;
		end;
		local y = tick();
		if y - I < .06 then
			return;
		end;
		I = y;
		local v = Q(l);
		local j = v and v:Get("ObjectID");
		if j then
			local l = k.Remotes.Replication.Fighter.UseItem;
			for k, p in ipairs({
				"Equip",
				"Switch",
				"Select",
				"EquipItem",
				"ChangeItem",
			}) do
				pcall(function()
					local k = G.enums:ToEnum(p);
					if k then
						l:FireServer(j, k, nil, nil);
					end;
				end);
			end;
		end;
		pcall(function()
			if p.EquipItem then
				p:EquipItem(l);
			end;
			if p.Equip then
				p:Equip(l);
			end;
			if p.SwitchToSlot then
				p:SwitchToSlot(l);
			end;
		end);
		pcall(function()
			local k = G.fighter;
			if k.EquipItem then
				k:EquipItem(l);
			end;
			if k.SwitchItem then
				k:SwitchItem(l);
			end;
		end);
		m(l);
	end;
	local function C()
		if E() then
			return false;
		end;
		local k = G.fighter and G.fighter.LocalFighter;
		if not k or not k.EquippedItem then
			return false;
		end;
		local l = false;
		pcall(function()
			local p = k.EquippedItem;
			for k, a in ipairs({ "Reloading", "IsReloading", "IsReload" }) do
				local y = p:Get(a);
				if y == true or y == 1 or y == "true" then
					l = true;
					return;
				end;
			end;
		end);
		if l then
			return true;
		end;
		local p = n.state.reloadStartedAt or 0;
		return p > 0 and (tick() - p) < 1.75;
	end;
	local function D()
		if E() then
			n.state.outofammo = false;
			n.state.reloading = false;
			n.state.reloadStartedAt = 0;
			return;
		end;
		local k, l, p = z();
		local a = n.state.outofammo;
		if p then
			n.state.outofammo = false;
		else
			n.state.outofammo = ((tonumber(k) or 0)) <= 0;
		end;
		local y = C();
		if y and not n.state.reloading then
			n.state.reloadStartedAt = tick();
		elseif not y then
			n.state.reloadStartedAt = 0;
		end;
		n.state.reloading = y;
		if n.state.outofammo and not a then
			if u(x()) then
				n.voidspam.phase = "hide";
			end;
		elseif not n.state.outofammo and a then
			if n.voidspam.enabled then
				n.voidspam.phase = "shoot";
				n.voidspam.lastswitch = tick();
			else
				n.voidspam.phase = nil;
			end;
		end;
	end;
	local function h(k)
		if not ((n.state.reloading and n.ragestatus.hideOnReload)) then
			return false;
		end;
		local l = bJ();
		if l and not k then
			fJ(l);
			return true;
		end;
		local p = math.random(-10000, 10000);
		local a = math.random(-10000, 10000);
		n.orbit.serverpos = Vector3.new(p, -99999999999, a);
		if k and (M and (M.Entity and M.Entity.RootPart)) then
			pcall(function()
				M.Entity.RootPart.CFrame = CFrame.new(n.orbit.serverpos);
			end);
		end;
		return true;
	end;
	local function O(k)
		if not k then
			return false;
		end;
		return j:GetAttribute("TeamID") == k:GetAttribute("TeamID");
	end;
	local function kJ(k)
		if not k or not k.Parent then
			return false;
		end;
		local p = k:FindFirstChildOfClass("Humanoid");
		if not p or p.Health <= 0 then
			return false;
		end;
		if not k:FindFirstChild("HumanoidRootPart") then
			return false;
		end;
		local a = l:GetPlayerFromCharacter(k);
		if not a or O(a) then
			return false;
		end;
		return true;
	end;
	local function pJ()
		local k = a:GetMouseLocation();
		local p = nil;
		local y = math.huge;
		for l, a in l:GetPlayers() do
			if a ~= j and a.Character then
				local l = a.Character;
				if kJ(l) then
					local a = l:FindFirstChild("HumanoidRootPart");
					if a then
						local v = (getgenv()).FinhubWorldToScreen or lJ;
						local j, G = v(a.Position, s);
						if G and j then
							local a = ((Vector2.new(j.X, j.Y) - k)).Magnitude;
							if a < y then
								p = l;
								y = a;
							end;
						end;
					end;
				end;
			end;
		end;
		return p;
	end;
	local function aJ(k, l)
		if not k then
			return nil;
		end;
		if l == "Closest" then
			local l = j.Character and j.Character:FindFirstChild("HumanoidRootPart");
			if not l then
				return k:FindFirstChild("Head");
			end;
			local p, a = nil, math.huge;
			for k, y in pairs(k:GetChildren()) do
				if y:IsA("BasePart") then
					local k = ((y.Position - l.Position)).Magnitude;
					if k < a then
						a = k;
						p = y;
					end;
				end;
			end;
			return p or k:FindFirstChild("Head");
		elseif l == "Random" then
			local l = { "Head", "HumanoidRootPart", "UpperTorso" };
			return k:FindFirstChild(l[math.random(#l)]) or k:FindFirstChild("Head");
		else
			return k:FindFirstChild(l) or k:FindFirstChild("Head");
		end;
	end;
	local function yJ()
		if not n.target.character or not n.prediction.enabled then
			n.prediction.velocity = Vector3.new(0, 0, 0);
			n.prediction.lastposition = nil;
			return;
		end;
		local k = n.target.character:FindFirstChild("HumanoidRootPart");
		if not k then
			return;
		end;
		local l = tick();
		local p = l - n.prediction.lasttime;
		if p > 0 and p < .1 then
			local a = k.Position;
			if n.prediction.lastposition then
				local k = ((a - n.prediction.lastposition)) / p;
				n.prediction.velocity = n.prediction.velocity:Lerp(k, .6);
			end;
			n.prediction.lastposition = a;
			n.prediction.lasttime = l;
		end;
	end;
	local function vJ(k, l)
		if not n.prediction.enabled or not k then
			return k and k.Position or Vector3.new();
		end;
		local p = k.Position;
		local a = ((p - l)).Magnitude;
		local y = 0;
		pcall(function()
			y = (game:GetService("Stats")).Network.ServerStatsItem["Data Ping"]:GetValue() / 1000;
		end);
		local v = a / 3000;
		local j = ((v + y)) * n.prediction.multiplier;
		return p + (n.prediction.velocity * j);
	end;
	local function jJ()
		local k = j.Character;
		if not k then
			return false;
		end;
		local l = k:FindFirstChildOfClass("Humanoid");
		if not l or l.Health <= 0 then
			return false;
		end;
		if E() then
			return true;
		end;
		local p, a, y = z();
		if p == nil then
			return true;
		end;
		return p > 0 or y;
	end;
	local function sJ(k)
		return math.clamp(tonumber(k) or 1, .1, 2);
	end;
	local function GJ(k, l)
		local p = sJ(k);
		local a = sJ(l);
		if a < p then
			p, a = a, p;
		end;
		if p == a then
			return p;
		end;
		return p + math.random() * ((a - p));
	end;
	local gJ, TJ = 2147483646, 1147483646;
	local cJ = { cf = nil, lv = nil, av = nil };
	local function eJ()
		cJ.cf, cJ.lv, cJ.av = nil, nil, nil;
	end;
	local function NJ()
		local k = j.Character;
		return k and k:FindFirstChild("HumanoidRootPart");
	end;
	local function nJ(k, l, p, a)
		local y = math.random(k, l);
		while y >= p and y <= a do
			y = math.random(k, l);
		end;
		return y;
	end;
	local function dJ(k)
		if not k then
			return;
		end;
		if K() then
			return;
		end;
		cJ.cf = k.CFrame;
		cJ.lv = k.AssemblyLinearVelocity;
		cJ.av = k.AssemblyAngularVelocity;
		local l, p = gJ, TJ;
		pcall(function()
			local a = Vector3.new(nJ(-l, l, -p, p), nJ(-l, l, -p, p), nJ(-l, l, -p, p));
			local y = Vector3.new(nJ(-l, l, -p, p), nJ(-l, l, -p, p), nJ(-l, l, -p, p));
			k.CFrame = CFrame.new(a) * CFrame.Angles(math.pi, math.pi, math.pi);
			k.AssemblyLinearVelocity = y;
			k.AssemblyAngularVelocity = y;
		end);
	end;
	p:BindToRenderStep("vfr_csync", Enum.RenderPriority.First.Value, function()
		if not cJ.cf then
			return;
		end;
		local k = NJ();
		if not k then
			return;
		end;
		pcall(function()
			k.CFrame = cJ.cf;
			k.AssemblyLinearVelocity = cJ.lv;
			k.AssemblyAngularVelocity = cJ.av;
		end);
	end);
	local function YJ()
		if U() and not n.target.attackCustomEnabled then
			local k = tonumber(n.target.meleeFeetDrop) or 3.5;
			return {
				height = 0,
				front = 0,
				side = 0,
				vertical = -k,
				radius = 0,
			};
		end;
		if n.target.attackCustomEnabled then
			return {
				height = n.target.customHeight or 2,
				front = n.target.customFront or 0,
				side = n.target.customSide or 0,
				vertical = n.target.customVertical or 0,
				radius = n.target.customRadius or 0,
			};
		end;
		local k = n.target.attackPosition or "default";
		local l = x();
		if k == "under" then
			return {
				height = 0,
				front = 0,
				side = 0,
				vertical = -((n.target.underOffset or 4)),
				radius = 0,
			};
		end;
		return {
			height = ((l and (l:lower()):find("sniper"))) and 8 or 2,
			front = 0,
			side = 0,
			vertical = 0,
			radius = 0,
		};
	end;
	local function MJ(k)
		local l = YJ();
		n.orbit.height = l.height;
	end;
	local function iJ(k)
		if not k then
			return Vector3.zero;
		end;
		local l = YJ();
		local p = k.Position;
		local a = k.CFrame.LookVector;
		local y = k.CFrame.RightVector;
		return ((p + (a * l.front)) + (y * l.side)) + Vector3.new(0, l.vertical, 0);
	end;
	local function RJ()
		local k = YJ();
		n.orbit.radius = k.radius or 0;
	end;
	local function BJ()
		MJ(x());
		RJ();
	end;
	local function WJ()
		if not n.state.csyncactive then
			return;
		end;
		if n.orbit.connection then
			n.orbit.connection:Disconnect();
			n.orbit.connection = nil;
		end;
		n.state.csyncactive = false;
		n.orbit.active = false;
		eJ();
		n.voidspam.phase = nil;
		if n.orbit.savedpos and (M and (M.Entity and (M.Entity.RootPart and not (getgenv()).FinhubUndergroundEnabled))) then
			local k, l = pcall(function()
					return n.orbit.savedpos;
				end);
			if k and l then
				local k = M.Entity.RootPart.CFrame;
				local p = ((k.Position - l.Position)).Magnitude;
				pcall(function()
					M.Entity.RootPart.CFrame = l;
				end);
			end;
		end;
		n.orbit.serverpos = nil;
		n.orbit.savedpos = nil;
		n.voidspam.phase = nil;
		i = nil;
	end;
	local function LJ()
		return n.voidspam.enabled and n.voidspam.phase == "hide";
	end;
	local function PJ()
		if not n.voidspam.enabled then
			return;
		end;
		local k = tick();
		local l = k - n.voidspam.lastswitch;
		if n.voidspam.phase == "shoot" then
			if l >= n.voidspam.currentduration then
				n.voidspam.phase = "hide";
				n.voidspam.currentduration = GJ(n.voidspam.hide_min, n.voidspam.hide_max);
				n.voidspam.lastswitch = k;
			end;
		elseif n.voidspam.phase == "hide" then
			if l >= n.voidspam.currentduration then
				n.voidspam.phase = "shoot";
				n.voidspam.currentduration = GJ(n.voidspam.shoot_min, n.voidspam.shoot_max);
				n.voidspam.lastswitch = k;
			end;
		else
			n.voidspam.phase = "shoot";
			n.voidspam.currentduration = GJ(n.voidspam.shoot_min, n.voidspam.shoot_max);
			n.voidspam.lastswitch = k;
		end;
	end;
	local function xJ(k)
		if not k then
			return false;
		end;
		return n.voidspam.enabled or n.state.outofammo;
	end;
	local function tJ(k, l)
		if k then
			eJ();
			return false;
		end;
		if not xJ(k) then
			eJ();
			return false;
		end;
		local p = n.state.outofammo or (n.voidspam.enabled and n.voidspam.phase == "hide");
		if p then
			if n.voidspam.enabled and (n.voidspam.phase == "hide" and not n.state.outofammo) then
				local k = tick() - n.voidspam.lastswitch;
				if k >= n.voidspam.currentduration then
					n.voidspam.phase = "shoot";
					n.voidspam.currentduration = GJ(n.voidspam.shoot_min, n.voidspam.shoot_max);
					n.voidspam.lastswitch = tick();
					eJ();
					return false;
				end;
			end;
			eJ();
			local k = NJ();
			if k then
				dJ(k);
			else
				if n.state.csyncactive then
					return true;
				end;
				local k = math.random(-10000, 10000);
				local l = math.random(-10000, 10000);
				n.orbit.serverpos = Vector3.new(k, -99999999999, l);
			end;
			return true;
		end;
		if n.voidspam.enabled and n.voidspam.phase == "shoot" then
			local k = tick() - n.voidspam.lastswitch;
			if k >= n.voidspam.currentduration then
				n.voidspam.phase = "hide";
				n.voidspam.currentduration = GJ(n.voidspam.hide_min, n.voidspam.hide_max);
				n.voidspam.lastswitch = tick();
			end;
		end;
		eJ();
		return false;
	end;
	local function SJ()
		if not n.voidspam.enabled then
			return;
		end;
		if u(x()) then
			return;
		end;
	end;
	local function VJ()
		if Y.active then
			return;
		end;
		Y.active = true;
		if not n.state.csyncactive then
			startsync();
		end;
	end;
	local XJ = function()
			Y.active = false;
			Y.currentVoidPos = nil;
			if Y.mainConnection then
				Y.mainConnection:Disconnect();
				Y.mainConnection = nil;
			end;
		end;
	local function ZJ()
		if n.state.csyncactive then
			return;
		end;
		if n.target.immune then
			return;
		end;
		if not n.target.character or not kJ(n.target.character) then
			return;
		end;
		r();
		n.state.csyncactive = true;
		n.orbit.active = true;
		local k = x();
		MJ(k);
		RJ();
		if not n.orbit.savedpos then
			if M and (M.Entity and M.Entity.RootPart) then
				n.orbit.savedpos = M.Entity.RootPart.CFrame;
			end;
		end;
		n.orbit.connection = p.Heartbeat:Connect(function(k)
				if not n.state.csyncactive then
					return;
				end;
				if n.target.immune then
					WJ();
					return;
				end;
				if not n.target.character or not kJ(n.target.character) then
					WJ();
					return;
				end;
				if not M or not M.Entity or not M.Entity.RootPart then
					WJ();
					return;
				end;
				if n.target.immune then
					WJ();
					return;
				end;
				RJ();
				i = M.Entity.RootPart.CFrame;
				local function l()
					if U() then
						return false;
					end;
					local k = G.fighter and G.fighter.LocalFighter;
					if not k or not k.EquippedItem then
						return false;
					end;
					local l = (tostring(k.EquippedItem:Get("Name") or "")):lower();
					return l:find("fist", 1, true) or l:find("utility", 1, true);
				end;
				local p = x();
				local a = u(p);
				if l() then
					return;
				end;
				r();
				if h(a) then
					return;
				end;
				if not a and (n.voidspam.enabled and not n.target.immune) then
					PJ();
					if n.voidspam.phase == "hide" then
						local k = math.random(-10000, 10000);
						local l = math.random(-10000, 10000);
						n.orbit.serverpos = Vector3.new(k, -99999999999, l);
						pcall(function()
							M.Entity.RootPart.CFrame = CFrame.new(n.orbit.serverpos);
						end);
						return;
					end;
				end;
				if not n.target.character then
					WJ();
					return;
				end;
				local y = n.target.character:FindFirstChild("HumanoidRootPart");
				if not y then
					WJ();
					return;
				end;
				if not a and LJ() then
					local k = math.random(-10000, 10000);
					local l = math.random(-10000, 10000);
					n.orbit.serverpos = Vector3.new(k, -99999999999, l);
					pcall(function()
						M.Entity.RootPart.CFrame = CFrame.new(n.orbit.serverpos);
					end);
					return;
				end;
				if a then
					if Y and Y.active then
						local k = n.target.character:FindFirstChild("HumanoidRootPart");
						if k then
							local l = (Vector3.new(k.CFrame.LookVector.X, 0, k.CFrame.LookVector.Z)).Unit;
							n.orbit.serverpos = (k.Position + Vector3.new(0, 15, 0)) + (l * 5);
						end;
					end;
				end;
				if n.state.outofammo and (not a and (p and not E())) then
					local k = NJ();
					if k then
						dJ(k);
					else
						local k = math.random(-10000, 10000);
						local l = math.random(-10000, 10000);
						n.orbit.serverpos = Vector3.new(k, -99999999999, l);
						pcall(function()
							M.Entity.RootPart.CFrame = CFrame.new(n.orbit.serverpos);
						end);
					end;
					return;
				end;
				local v = iJ(y);
				n.orbit.angle = n.orbit.angle + (n.orbit.speed * k);
				local j = Vector3.new(math.cos(n.orbit.angle) * n.orbit.radius, n.orbit.height, math.sin(n.orbit.angle) * n.orbit.radius);
				n.orbit.serverpos = v + j;
				if not (getgenv()).FinhubUndergroundEnabled then
					if M and (M.Entity and M.Entity.RootPart) then
						local k = M.Entity.RootPart.CFrame;
						local l = CFrame.new(n.orbit.serverpos);
						local p = ((l.Position - k.Position)).Magnitude;
						if p < 2000 then
							M.Entity.RootPart.CFrame = l;
							n.orbit.savedpos = k;
						end;
					end;
				end;
			end);
	end;
	local function EJ()
		if not u(x()) and (n.voidspam.enabled and n.voidspam.phase == "hide") then
			return;
		end;
		if not n.target.enabled or not n.target.character or not n.target.autoshoot then
			return;
		end;
		if n.target.immune then
			return;
		end;
		if not u(x()) and (n.voidspam.enabled and n.voidspam.phase == "hide") then
			return;
		end;
		if n.voidspam.enabled and n.voidspam.phase == "hide" then
			return;
		end;
		local l = x();
		local p = u(l);
		r();
		local a = G.fighter and G.fighter.LocalFighter;
		if not a or not a.EquippedItem then
			return;
		end;
		if not E() then
			if n.state.reloading or n.state.outofammo then
				return;
			end;
			if not jJ() then
				return;
			end;
		end;
		local y = n.target.character;
		if not kJ(y) then
			return;
		end;
		local v = aJ(y, n.target.hitpart);
		if not v then
			return;
		end;
		local s;
		local g;
		if Y and (Y.active and p) then
			local k = j.Character and j.Character:FindFirstChild("HumanoidRootPart");
			s = k and k.Position or Vector3.new();
			g = vJ(v, s);
		else
			if Y and Y.active then
				local k = j.Character and j.Character:FindFirstChild("HumanoidRootPart");
				s = k and k.Position or Vector3.new();
			elseif n.state.csyncactive and n.orbit.serverpos then
				s = n.orbit.serverpos;
			else
				local k = j.Character and j.Character:FindFirstChild("HumanoidRootPart");
				s = k and k.Position or Vector3.new();
			end;
			g = vJ(v, s);
		end;
		local T = { [utf8.char(1)] = {
					[utf8.char(0)] = G.utility:EncodeCFrame(CFrame.new(s, g)),
					[utf8.char(1)] = G.utility:EncodeCFrame(CFrame.new(s, g)),
					[utf8.char(2)] = v,
					[utf8.char(3)] = G.utility:EncodeCFrame(CFrame.new(.43, .25, .42)),
				} };
		local c = a.EquippedItem;
		if c and c:Get("ObjectID") then
			d.lastAttackTick = tick();
			local l = math.clamp(math.floor(tonumber(n.target.shootAttempts) or 1), 1, 3);
			for l = 1, l, 1 do
				pcall(function()
					k.Remotes.Replication.Fighter.UseItem:FireServer(c:Get("ObjectID"), G.enums:ToEnum("StartShooting"), T, nil);
				end);
			end;
			SJ();
		end;
	end;
	local JJ = G.camcontrol.Update;
	G.camcontrol.Update = function(...)
			if (getgenv()).FinhubUndergroundEnabled then
				return JJ(...);
			elseif n.state.csyncactive and (M and (M.Entity and (M.Entity.RootPart and i))) then
				M.Entity.RootPart.CFrame = i;
			end;
			return JJ(...);
		end;
	local UJ = {};
	local zJ = {};
	local wJ = {};
	local function qJ(k)
		table.insert(zJ, k);
	end;
	local function uJ(k)
		table.insert(wJ, k);
	end;
	p.Heartbeat:Connect(function()
		for k, l in pairs(l:GetPlayers()) do
			if l == j then
				continue;
			end;
			local p = l.Character;
			local a = p and p:FindFirstChild("HumanoidRootPart");
			local y = a and a:FindFirstChild("Attachment") ~= nil;
			if y and not UJ[l.Name] then
				UJ[l.Name] = true;
				for k, p in pairs(zJ) do
					p(l);
				end;
			elseif not y and UJ[l.Name] then
				UJ[l.Name] = nil;
				for k, p in pairs(wJ) do
					p(l);
				end;
			end;
		end;
	end);
	qJ(function(k)
		if n.target.lastplayer == k then
			n.target.immune = true;
			n.voidspam.phase = nil;
			eJ();
			R.Color = Color3.fromRGB(180, 0, 255);
			WJ();
		end;
	end);
	uJ(function(k)
		if n.target.lastplayer == k then
			n.target.immune = false;
			R.Color = Color3.fromRGB(255, 20, 147);
			if n.target.enabled and (n.target.character and kJ(n.target.character)) then
				ZJ();
			end;
		end;
	end);
	local function HJ()
		n.target.enabled = false;
		WJ();
		n.target.character = nil;
		n.target.lastchar = nil;
		n.target.lastplayer = nil;
		n.target.manualkey = false;
		n.target.immune = false;
		R.Color = Color3.fromRGB(255, 20, 147);
	end;
	local function FJ(k)
		if not k then
			return;
		end;
		local p = l:GetPlayerFromCharacter(k);
		n.target.enabled = true;
		n.target.character = k;
		n.target.lastchar = k;
		n.target.lastplayer = p;
		n.target.immune = false;
		R.Color = Color3.fromRGB(255, 20, 147);
		local a = k:FindFirstChild("HumanoidRootPart");
		if a and a:FindFirstChild("Attachment") then
			n.target.immune = true;
			R.Color = Color3.fromRGB(180, 0, 255);
		else
			r();
			ZJ();
		end;
	end;
	local QJ = { enabled = false, connections = {} };
	local function AJ()
		return u(x());
	end;
	local function IJ()
		local k = j.Character and j.Character:FindFirstChild("HumanoidRootPart");
		if not k then
			return nil;
		end;
		local p, a = nil, math.huge;
		for l, y in ipairs(l:GetPlayers()) do
			if y == j or not y.Character then
				continue;
			end;
			local v = y.Character:FindFirstChild("HitboxHead") or y.Character:FindFirstChild("Head");
			if not v then
				continue;
			end;
			local s = ((v.Position - k.Position)).Magnitude;
			if s < a then
				p = v;
				a = s;
			end;
		end;
		return p;
	end;
	local function mJ()
		if QJ.connections.touch then
			return;
		end;
		QJ.connections.touch = y.DescendantAdded:Connect(function(k)
				if k:IsA("BasePart") or k:IsA("Model") then
					if k.Name == "Slingshot" or k.Name == "CoreProjectile" or k.Name == "OuterProjectile" then
						task.spawn(function()
							task.wait(.06);
							local l = k:IsA("BasePart") and k or k:FindFirstChildWhichIsA("BasePart");
							if not l then
								return;
							end;
							l.CanTouch = true;
							for k = 1, 60, 1 do
								if not l.Parent or not l:IsDescendantOf(y) then
									break;
								end;
								local p = IJ();
								if p and (p:IsA("BasePart") and (p.Parent and p:IsDescendantOf(y))) then
									pcall(firetouchinterest, p, l, 0);
									pcall(firetouchinterest, p, l, 1);
								end;
								task.wait();
							end;
						end);
					end;
				end;
			end);
	end;
	local function oJ()
		if QJ.connections.touch then
			QJ.connections.touch:Disconnect();
			QJ.connections.touch = nil;
		end;
	end;
	local function rJ()
		if not n.target.character then
			return nil;
		end;
		local k = n.target.character:FindFirstChild("HumanoidRootPart");
		return k and k.Position;
	end;
	local function CJ()
		local k = AJ();
		local l = n.target.enabled and n.target.character ~= nil;
		if k then
			if not QJ.enabled then
				QJ.enabled = true;
				mJ();
			end;
			if (getgenv()).FinhubSetUnderground then
				(getgenv()).FinhubSetUnderground(true);
			end;
			Y.active = l;
			if l and not n.state.csyncactive then
				ZJ();
			elseif not l and (n.state.csyncactive and not n.target.enabled) then
				WJ();
			end;
		else
			if QJ.enabled then
				QJ.enabled = false;
				oJ();
			end;
			if not Toggles.AntiAimUnderground.Value then
				if (getgenv()).FinhubSetUnderground then
					(getgenv()).FinhubSetUnderground(false);
				end;
			end;
			Y.active = false;
		end;
	end;
	local function DJ()
		local k = n.target.enabled and (n.target.character and kJ(n.target.character));
		local l = AJ();
		if k and l then
			pcall(function()
				setfflag("TargetTimeDelayFacctorTenths", "999999");
			end);
		else
			pcall(function()
				setfflag("TargetTimeDelayFacctorTenths", "1");
			end);
		end;
	end;
	local hJ = tick();
	p.RenderStepped:Connect(function()
		local k = (getgenv()).FinhubIsInLobby and (getgenv()).FinhubIsInLobby();
		local l = n.visualizer.enabled or (n.ragestatus and n.ragestatus.enabled) or (n.hitNotifications and n.hitNotifications.enabled) or (n.target.enabled and (n.target.character and n.prediction.enabled));
		if k and not l then
			return;
		end;
		if X() then
			W.Visible = false;
			L.Visible = false;
			if KJ.hideRageStatus then
				KJ.hideRageStatus();
			end;
			if KJ.updateHitNotifications then
				KJ.updateHitNotifications(0);
			end;
			return;
		end;
		local p = tick();
		local a = math.clamp(p - hJ, 0, .05);
		hJ = p;
		if n.ragestatus and (n.ragestatus.enabled and KJ.drawStatus) then
			KJ.drawStatus();
		elseif KJ.hideRageStatus then
			KJ.hideRageStatus();
		end;
		if KJ.updateHitNotifications then
			KJ.updateHitNotifications(a);
		end;
		if n.target.enabled and (n.target.character and n.prediction.enabled) then
			yJ();
		end;
	end);
	p.Heartbeat:Connect(function()
		DJ();
		CJ();
		D();
		if n.target.auto and not n.target.manualkey then
			local k, l, p = z();
			local a = k == nil or k > 0 or p;
			if a and not n.state.reloading then
				if not n.target.enabled or not n.target.character or not kJ(n.target.character) then
					local k = pJ();
					if k then
						if n.target.enabled then
							WJ();
						end;
						FJ(k);
					end;
				end;
			end;
		end;
		if n.target.enabled and n.target.character then
			if not kJ(n.target.character) then
				HJ();
				return;
			end;
			r();
			if n.target.autoshoot and not n.target.immune then
				EJ();
			end;
		end;
	end);
	KJ.config = n;
	KJ.nearest = pJ;
	KJ.settarget = FJ;
	KJ.cleartarget = HJ;
	KJ.stopvoid = XJ;
	KJ.vhState = Y;
	KJ.ragePerf = d;
	KJ.getammo = z;
	KJ.muzzlepos = t;
	KJ.valid = kJ;
	KJ.player = j;
	KJ.eqSlot = r;
	KJ.refreshAtk = BJ;
	do
		local k = KJ.config;
		local l = KJ.ragePerf;
		local p = KJ.getammo;
		local a = KJ.muzzlepos;
		local v = KJ.valid;
		local j = KJ.player;
		local s = KJ.vhState;
		local G = y.CurrentCamera;
		local function g()
			local l = (getgenv()).FinhubTrackDrawingText(Drawing.new("Text"));
			l.Size = 13;
			l.Font = 2;
			l.Outline = true;
			l.Center = true;
			l.Visible = false;
			l.Color = k.ragestatus.color;
			pcall(function()
				l.OutlineColor = Color3.fromRGB(0, 0, 0);
			end);
			return l;
		end;
		local T = g();
		local c = g();
		local function e(l)
			l.Size = 13;
			l.Font = 2;
			l.Outline = true;
			l.Color = k.ragestatus.color;
			pcall(function()
				l.OutlineColor = Color3.fromRGB(0, 0, 0);
			end);
		end;
		local function b()
			if k.ragestatus.mode == "muzzle" then
				local k = a and a();
				if k then
					local l = (getgenv()).FinhubWorldToScreen or lJ;
					local p, a = l(k, G);
					if a and p then
						return Vector2.new(p.X, p.Y);
					end;
				end;
			end;
			local l = G or y.CurrentCamera;
			if not l then
				return Vector2.zero;
			end;
			return (l.ViewportSize / 2) + Vector2.new(k.ragestatus.staticOffsetX or 0, k.ragestatus.staticOffsetY or 0);
		end;
		local function f()
			return k.target.enabled or k.target.auto or k.voidspam.enabled;
		end;
		local function N()
			if k.target.lastplayer then
				return k.target.lastplayer.Name;
			end;
			if k.target.character then
				local l = (game:GetService("Players")):GetPlayerFromCharacter(k.target.character);
				if l then
					return l.Name;
				end;
			end;
			return "target";
		end;
		local function K()
			if s.active then
				return true;
			end;
			if k.voidspam.enabled and (k.voidspam.phase == "hide" and u(x())) then
				return true;
			end;
			if k.state.reloading and k.ragestatus.hideOnReload ~= false then
				return true;
			end;
			return false;
		end;
		local function n()
			if not f() then
				return false;
			end;
			if k.state.reloading or K() then
				return false;
			end;
			if k.target.immune then
				return false;
			end;
			if not k.target.enabled or not k.target.character or not v(k.target.character) then
				return false;
			end;
			if k.state.csyncactive then
				return true;
			end;
			if k.voidspam.enabled and (k.voidspam.phase == "shoot" and u(x())) then
				return true;
			end;
			if tick() - ((l.lastAttackTick or 0)) < .45 then
				return true;
			end;
			return false;
		end;
		local function d()
			if E() then
				return "\226\136\158";
			end;
			local k, l, a = p();
			if k == nil then
				return "\226\136\158";
			end;
			if a then
				return "melee";
			end;
			return string.format("%d/%d", math.floor(k or 0), math.max(math.floor(l or 0), 0));
		end;
		local function Y(k, l)
			local p = sJ(k);
			local a = sJ(l);
			if a < p then
				p, a = a, p;
			end;
			if math.abs(p - a) < .05 then
				return string.format("%.1fs", p);
			end;
			return string.format("%.1f-%.1fs", p, a);
		end;
		local function M()
			if not k.voidspam.enabled then
				return "";
			end;
			local l = k.voidspam;
			return string.format("void %s atk \194\183 %s hide", Y(l.shoot_min, l.shoot_max), Y(l.hide_min, l.hide_max));
		end;
		local function i()
			local p = tick();
			if n() then
				if not l.killStartAt then
					l.killStartAt = p;
				end;
			else
				l.killStartAt = nil;
			end;
			if k.state.reloading then
				return "ragebot: void hide", "reloading";
			end;
			if K() then
				local k = M();
				return "ragebot: void hide", k ~= "" and k or "";
			end;
			if n() then
				local a = N();
				local y = p - ((l.killStartAt or p));
				local v = k.target.character and k.target.character:FindFirstChildOfClass("Humanoid");
				local j = string.format("ragebot : attacking (%s) \194\183 %.1fs", a, y);
				local s = {};
				local G = v and string.format("%.0f hp", v.Health) or "hp ?";
				s[#s + 1] = G;
				local g = M();
				if g ~= "" then
					s[#s + 1] = g;
				end;
				return j, table.concat(s, " \194\183 ");
			end;
			if f() and (k.target.enabled and k.target.character) then
				local l = k.target.character:FindFirstChildOfClass("Humanoid");
				local p = l and string.format("%.0f hp", l.Health) or "locked";
				return "ragebot: ready", p;
			end;
			return "ragebot: idle", "";
		end;
		local function R()
			if not k.ragestatus.enabled or X() then
				T.Visible = false;
				c.Visible = false;
				return;
			end;
			local p = b();
			local a = k.ragestatus.lineGap or 14;
			local y = k.ragestatus.showAmmo ~= false;
			if l.lastRageColor ~= k.ragestatus.color then
				l.lastRageColor = k.ragestatus.color;
				e(T);
				e(c);
			end;
			local v, j = i();
			local s = y and d() or "";
			local G = j;
			if G ~= "" and s ~= "" then
				G = G .. (" \194\183 " .. s);
			elseif G == "" then
				G = s;
			end;
			local g = 1 + ((G ~= "" and 1 or 0));
			local f = -((g - 1)) * ((a / 2));
			if v ~= l.lastRageStatusText then
				l.lastRageStatusText = v;
				T.Text = v;
			end;
			T.Position = p + Vector2.new(0, f);
			T.Visible = k.ragestatus.enabled;
			local N = f + a;
			if G ~= "" then
				if G ~= l.lastRageDetailText then
					l.lastRageDetailText = G;
					c.Text = G;
				end;
				c.Position = p + Vector2.new(0, N);
				c.Visible = k.ragestatus.enabled;
			else
				l.lastRageDetailText = "";
				c.Visible = false;
			end;
		end;
		KJ.drawStatus = R;
		KJ.hideRageStatus = function()
				T.Visible = false;
				c.Visible = false;
			end;
		KJ.clrVoidSnap = eJ;
	end;
	do
		local k = KJ.config;
		local l = KJ.nearest;
		local p = KJ.settarget;
		local a = KJ.cleartarget;
		local y = KJ.stopvoid;
		local function v()
			if k.target.auto then
				return;
			end;
			if k.target.enabled and k.target.character then
				a();
			else
				local a = l();
				if a then
					k.target.manualkey = true;
					p(a);
				end;
			end;
		end;
		local j = { targetgroup = F.Combat:AddRightTabbox() };
		j.ragebottab = j.targetgroup:AddTab("ragebot");
		j.ragebotvisualtab = j.targetgroup:AddTab("visualizer");
		(j.ragebottab:AddToggle("TargetOn", { Text = "enable", Default = false, Callback = function(y)
				k.target.rageMasterOn = y;
				if k.target.auto then
					return;
				end;
				if y then
					local a = l();
					if a then
						k.target.manualkey = true;
						p(a);
					end;
				else
					a();
				end;
			end })):AddKeyPicker("TargetKey", {
			Text = "Ragebot",
			Default = "None",
			Mode = "Toggle",
			Callback = function()
				v();
			end,
		});
		j.ragebottab:AddToggle("AutoTarget", { Text = "auto target", Default = false, Callback = function(l)
				k.target.auto = l;
			end });
		local s = { Type = "Toggle", Value = false };
		local G = { Type = "Toggle", Value = false };
		local function g()
			local l = k.target.attackPosition or "default";
			local p = k.target.attackCustomEnabled == true;
			s.Value = (l == "under" and not p);
			G.Value = p;
			if attackUnderDep and attackUnderDep.Update then
				attackUnderDep:Update();
			end;
			if attackCustomDep and attackCustomDep.Update then
				attackCustomDep:Update();
			end;
		end;
		local T = j.ragebottab:AddDropdown("AutoTargetAttackPos", {
				Text = "attack position",
				Default = "default",
				Values = { "default", "under" },
				Callback = function(l)
					k.target.attackPosition = l;
					g();
					if k.state.csyncactive and KJ.refreshAtk then
						KJ.refreshAtk();
					end;
				end,
			});
		j.ragebottab:AddToggle("AttackCustomOverride", {
			Text = "attack position",
			Default = false,
			Tooltip = "overrides attack position dropdown when enabled",
			Callback = function(l)
				k.target.attackCustomEnabled = l;
				g();
				if k.state.csyncactive and KJ.refreshAtk then
					KJ.refreshAtk();
				end;
			end,
		});
		local c = j.ragebottab:AddDependencyBox();
		c:AddSlider("UnderAttackOffset", {
			Text = "under offset",
			Default = k.target.underOffset,
			Min = 1,
			Max = 25,
			Rounding = 1,
			Compact = true,
			Callback = function(l)
				k.target.underOffset = l;
				if k.target.attackPosition == "under" and (not k.target.attackCustomEnabled and (k.state.csyncactive and KJ.refreshAtk)) then
					KJ.refreshAtk();
				end;
			end,
		});
		c:SetupDependencies({ { s, true } });
		local e = j.ragebottab:AddDependencyBox();
		local function b()
			if k.target.attackCustomEnabled and (k.state.csyncactive and KJ.refreshAtk) then
				KJ.refreshAtk();
			end;
		end;
		e:AddSlider("CustomAttackHeight", {
			Text = "orbit height",
			Default = k.target.customHeight,
			Min = -25,
			Max = 30,
			Rounding = 1,
			Compact = true,
			Callback = function(l)
				k.target.customHeight = l;
				b();
			end,
		});
		e:AddSlider("CustomAttackFront", {
			Text = "front offset",
			Default = k.target.customFront,
			Min = -20,
			Max = 20,
			Rounding = 1,
			Compact = true,
			Callback = function(l)
				k.target.customFront = l;
				b();
			end,
		});
		e:AddSlider("CustomAttackSide", {
			Text = "side offset",
			Default = k.target.customSide,
			Min = -20,
			Max = 20,
			Rounding = 1,
			Compact = true,
			Callback = function(l)
				k.target.customSide = l;
				b();
			end,
		});
		e:AddSlider("CustomAttackVertical", {
			Text = "vertical offset",
			Default = k.target.customVertical,
			Min = -25,
			Max = 30,
			Rounding = 1,
			Compact = true,
			Callback = function(l)
				k.target.customVertical = l;
				b();
			end,
		});
		e:AddSlider("CustomAttackRadius", {
			Text = "orbit radius",
			Default = k.target.customRadius,
			Min = 0,
			Max = 25,
			Rounding = 1,
			Compact = true,
			Callback = function(l)
				k.target.customRadius = l;
				b();
			end,
		});
		e:SetupDependencies({ { G, true } });
		g();
		task.defer(g);
		j.ragebottab:AddDropdown("RageWeaponPick", {
			Text = "weapon",
			Default = k.target.weaponPick or "primary",
			Values = { "primary", "secondary", "melee" },
			Callback = function(l)
				k.target.weaponPick = l;
				if k.state.csyncactive and KJ.refreshAtk then
					KJ.refreshAtk();
				end;
				if KJ.eqSlot then
					KJ.eqSlot();
				end;
			end,
		});
		j.ragebottab:AddSlider("ShootAttempts", {
			Text = "shoot attempts",
			Default = 1,
			Min = 1,
			Max = 3,
			Rounding = 0,
			Suffix = "x",
			Callback = function(l)
				k.target.shootAttempts = math.clamp(math.floor(tonumber(l) or 1), 1, 3);
			end,
		});
		j.ragebottab:AddDropdown("TargetPart", {
			Text = "hit part",
			Default = "Head",
			Values = {
				"Head",
				"HumanoidRootPart",
				"Torso",
				"UpperTorso",
				"Closest",
				"Random",
			},
			Callback = function(l)
				k.target.hitpart = l;
			end,
		});
		j.ragebottab:AddToggle("PredictT", { Text = "prediction", Default = false, Callback = function(l)
				k.prediction.enabled = l;
			end });
		j.ragebottab:AddSlider("PredictMul", {
			Text = "prediction mult",
			Default = 1.2,
			Min = .1,
			Max = 3.0,
			Rounding = 1,
			Callback = function(l)
				k.prediction.multiplier = l;
			end,
		});
		j.ragebottab:AddToggle("VoidSpam", { Text = "voidspam", Default = false, Callback = function(l)
				k.voidspam.enabled = l;
				if l then
					if k.target.immune then
						k.voidspam.phase = nil;
						return;
					end;
					if u(x()) then
						k.voidspam.lastswitch = tick();
						k.voidspam.phase = "shoot";
						k.voidspam.currentduration = GJ(k.voidspam.shoot_min, k.voidspam.shoot_max);
					else
						k.voidspam.phase = nil;
					end;
				else
					k.voidspam.phase = nil;
					if KJ.clrVoidSnap then
						KJ.clrVoidSnap();
					end;
				end;
			end });
		j.ragebottab:AddSlider("VoidShootTime", {
			Default = 1,
			Text = "attack",
			Min = .1,
			Max = 2,
			Rounding = 1,
			Compact = true,
			Callback = function(l)
				l = sJ(l);
				k.voidspam.shoot_min = l;
				k.voidspam.shoot_max = l;
			end,
		});
		j.ragebottab:AddSlider("VoidHideTime", {
			Default = 1,
			Text = "hide",
			Min = .1,
			Max = 2,
			Rounding = 1,
			Compact = true,
			Callback = function(l)
				l = sJ(l);
				k.voidspam.hide_min = l;
				k.voidspam.hide_max = l;
			end,
		});
		local f = (j.ragebotvisualtab:AddToggle("RageStatus", { Text = "rage status", Default = k.ragestatus.enabled, Callback = function(l)
					k.ragestatus.enabled = l;
					if l then
						if KJ.drawStatus then
							KJ.drawStatus();
						end;
					else
						if KJ.hideRageStatus then
							KJ.hideRageStatus();
						end;
					end;
				end })):AddColorPicker("RageStatusColor", { Title = "text color", Default = k.ragestatus.color, Callback = function(l)
					k.ragestatus.color = l;
					if KJ.ragePerf then
						KJ.ragePerf.lastRageColor = nil;
					end;
				end });
		local N = j.ragebotvisualtab:AddDependencyBox();
		N:SetupDependencies({ { f, true } });
		N:AddDropdown("RageStatusMode", {
			Text = "position",
			Default = "static",
			Values = { "static", "muzzle" },
			Callback = function(l)
				k.ragestatus.mode = l;
			end,
		});
		N:AddSlider("RageStatusStaticX", {
			Text = "static x",
			Default = k.ragestatus.staticOffsetX,
			Min = -500,
			Max = 500,
			Rounding = 0,
			Compact = true,
			Callback = function(l)
				k.ragestatus.staticOffsetX = l;
			end,
		});
		N:AddSlider("RageStatusStaticY", {
			Text = "static y",
			Default = k.ragestatus.staticOffsetY,
			Min = -500,
			Max = 500,
			Rounding = 0,
			Compact = true,
			Callback = function(l)
				k.ragestatus.staticOffsetY = l;
			end,
		});
		N:AddToggle("RageStatusAmmo", { Text = "show ammo line", Default = true, Callback = function(l)
				k.ragestatus.showAmmo = l;
			end });
		N:AddToggle("RageStatusReloadHide", { Text = "hide while reloading", Default = true, Callback = function(l)
				k.ragestatus.hideOnReload = l;
			end });
		j.ragebotvisualtab:AddToggle("VisEnabled", { Text = "enable", Default = false, Callback = function(l)
				k.visualizer.enabled = l;
			end });
		(j.ragebotvisualtab:AddToggle("VisTracerEnabled", { Text = "tracer", Default = false, Callback = function(l)
				k.visualizer.tracer.enabled = l;
			end })):AddColorPicker("VisTracerColor", { Default = k.visualizer.tracer.color, Callback = function(l)
				k.visualizer.tracer.color = l;
			end });
		j.ragebotvisualtab:AddDropdown("VisTracerStart", {
			Text = "tracer start",
			Default = "cursor",
			Values = { "cursor", "muzzle" },
			Callback = function(l)
				k.visualizer.tracer.start_point = l;
			end,
		});
		j.ragebotvisualtab:AddToggle("VisTracerOutline", { Text = "tracer outline", Default = true, Callback = function(l)
				k.visualizer.tracer.outline = l;
			end });
		j.ragebotvisualtab:AddSlider("VisTracerThickness", {
			Text = "tracer thickness",
			Default = 1,
			Min = .1,
			Max = 3,
			Rounding = 1,
			Callback = function(l)
				k.visualizer.tracer.thickness = l;
			end,
		});
	end;
end;
do
	local k = KJ.config;
	local l = KJ.ragePerf;
	local p = KJ.isRageEnabled;
	local a = cloneref(game:GetService("Players"));
	local y = a.LocalPlayer;
	local v = game:GetService("TextService");
	local j = cloneref(game:GetService("UserInputService"));
	local s = {};
	local G = {};
	local g = {};
	local T = {};
	local c = {};
	local e = {};
	local f = {
			"fade",
			"slide left",
			"slide right",
			"slide down",
			"bounce",
			"fade bounce",
			"scale",
		};
	local N = 300;
	local K = b:Create("Frame", {
			Name = "HitNotifications",
			BackgroundTransparency = 1,
			Size = UDim2.new(0, N, 1, -24),
			Position = UDim2.fromOffset(12, 12),
			ZIndex = 250,
			Parent = b.ScreenGui,
		});
	local function n()
		local l = k.hitNotifications;
		local p = l.position or "Top Left";
		if p == "Custom" then
			return Vector2.new(l.offsetX or 12, l.offsetY or 12);
		end;
		local a = workspace.CurrentCamera and workspace.CurrentCamera.ViewportSize or Vector2.new(1920, 1080);
		local y = 12;
		local v = 280;
		local j = a.X * .5 - N * .5;
		local s = {
				["Top Left"] = Vector2.new(y, y),
				["Top Center"] = Vector2.new(j, y),
				["Top Right"] = Vector2.new((a.X - N) - y, y),
				["Center Left"] = Vector2.new(y, a.Y * .5 - v * .5),
				Center = Vector2.new(j, a.Y * .5 - v * .5),
				["Center Right"] = Vector2.new((a.X - N) - y, a.Y * .5 - v * .5),
				["Bottom Left"] = Vector2.new(y, (a.Y - v) - y),
				["Bottom Center"] = Vector2.new(j, (a.Y - v) - y),
				["Bottom Right"] = Vector2.new((a.X - N) - y, (a.Y - v) - y),
			};
		return s[p] or s["Top Left"];
	end;
	local function d()
		local k = n();
		K.Position = UDim2.fromOffset(math.floor(k.X), math.floor(k.Y));
	end;
	local function Y(k)
		return 1 - ((1 - k)) ^ 3;
	end;
	local function M(k)
		return (k * k) * k;
	end;
	local function i(k)
		local l = 1.70158;
		return (1 + ((l + 1)) * ((k - 1)) ^ 3) + l * ((k - 1)) ^ 2;
	end;
	local R = {};
	local B = {};
	R.fade = function(k)
			return Y(k), 0, 0, 1;
		end;
	R["slide left"] = function(k)
			local l = Y(k);
			return l, ((1 - l)) * -52, 0, 1;
		end;
	R["slide right"] = function(k)
			local l = Y(k);
			return l, ((1 - l)) * 52, 0, 1;
		end;
	R["slide down"] = function(k)
			local l = Y(k);
			return l, 0, ((1 - l)) * -32, 1;
		end;
	R.bounce = function(k)
			return math.clamp(k * 6, 0, 1), 0, 0, i(k);
		end;
	R["fade bounce"] = function(k)
			return Y(k), 0, 0, .78 + i(k) * .28;
		end;
	R.scale = function(k)
			local l = Y(k);
			return l, 0, 0, .62 + l * .38;
		end;
	B.fade = function(k)
			local l = M(k);
			return 1 - l, 0, 0, 1;
		end;
	B["slide left"] = function(k)
			local l = M(k);
			return 1 - l, l * -52, 0, 1;
		end;
	B["slide right"] = function(k)
			local l = M(k);
			return 1 - l, l * 52, 0, 1;
		end;
	B["slide down"] = function(k)
			local l = M(k);
			return 1 - l, 0, l * 32, 1;
		end;
	B.bounce = function(k)
			local l = M(k);
			return 1 - l, 0, 0, (1 - l * .1) + (math.sin(k * math.pi) * .09) * ((1 - l));
		end;
	B["fade bounce"] = function(k)
			local l = M(k);
			return 1 - l, 0, 0, (1 - l * .08) + (math.sin((k * math.pi) * 1.5) * .07) * ((1 - k));
		end;
	B.scale = function(k)
			local l = M(k);
			return 1 - l, 0, 0, 1 - l * .4;
		end;
	local function W(k, l, p)
		l = math.clamp(l, 0, 1);
		local a = p and B or R;
		local y = a[string.lower(k or "fade")] or a.fade;
		local v, j, s, G = y(l);
		return math.clamp(v, 0, 1), j, s, math.clamp(G, .01, 1.35);
	end;
	local function L(k, l, p, a, y)
		local v = 1 - math.clamp(l, 0, 1);
		k.outer.BackgroundTransparency = v;
		k.inner.BackgroundTransparency = v;
		k.label.TextTransparency = v;
		if k.uiScale then
			k.uiScale.Scale = y;
		end;
		local j = ((k.displayY or k.targetY or 0)) + a;
		k.outer.Position = UDim2.fromOffset(math.floor(p + .5), math.floor(j + .5));
	end;
	local function P(k, l)
		local p = v:GetTextSize(k, l, Enum.Font.Code, Vector2.new(1000, 40));
		return math.clamp(p.X + 20, 200, 540), math.max(p.Y + 12, 26);
	end;
	local function x(k)
		local l = s[k];
		if l and l.outer then
			pcall(function()
				l.outer:Destroy();
			end);
		end;
		table.remove(s, k);
	end;
	local function t(k, l, p)
		return string.format("hit %s for %d in the %s", k, math.floor(l + .5), p or "Body");
	end;
	local function S(l)
		local p = k.hitNotifications;
		local a = p.textSize or 14;
		local y, v = P(l, a);
		local j = b:Create("Frame", {
				Name = "HitNotifOuter",
				BackgroundColor3 = b.MainColor,
				BorderColor3 = b.OutlineColor,
				BorderMode = Enum.BorderMode.Inset,
				Size = UDim2.fromOffset(y, v),
				Position = UDim2.fromOffset(0, 0),
				BackgroundTransparency = 1,
				ZIndex = 251,
				Parent = K,
			});
		b:AddToRegistry(j, { BackgroundColor3 = "MainColor", BorderColor3 = "OutlineColor" }, true);
		local s = Instance.new("UIScale");
		s.Scale = .75;
		s.Parent = j;
		local G = b:Create("Frame", {
				Name = "HitNotifInner",
				BackgroundColor3 = b.BackgroundColor,
				BorderColor3 = b.OutlineColor,
				BorderMode = Enum.BorderMode.Inset,
				Position = UDim2.fromOffset(1, 1),
				Size = UDim2.new(1, -2, 1, -2),
				BackgroundTransparency = 1,
				ZIndex = 252,
				Parent = j,
			});
		b:AddToRegistry(G, { BackgroundColor3 = "BackgroundColor", BorderColor3 = "OutlineColor" }, true);
		local g = b:CreateLabel({
				Size = UDim2.new(1, -12, 1, 0),
				Position = UDim2.fromOffset(6, 0),
				Text = l,
				Font = Enum.Font.Code,
				TextSize = a,
				TextColor3 = p.color or b.FontColor,
				TextTransparency = 1,
				TextXAlignment = Enum.TextXAlignment.Left,
				TextYAlignment = Enum.TextYAlignment.Center,
				ZIndex = 254,
				Parent = G,
			}, false);
		return {
			outer = j,
			inner = G,
			label = g,
			uiScale = s,
			boxW = y,
			boxH = v,
		};
	end;
	local function V(l, p, a)
		if not k.hitNotifications.enabled or p <= 0 then
			return;
		end;
		local y = k.hitNotifications;
		local v = tick();
		d();
		K.Visible = true;
		local j = S(t(l, p, a));
		table.insert(s, 1, {
			outer = j.outer,
			inner = j.inner,
			label = j.label,
			uiScale = j.uiScale,
			boxW = j.boxW,
			boxH = j.boxH,
			start = v,
			phaseStart = v,
			phase = "in",
			duration = y.duration or 3,
			targetY = 0,
			displayY = 0,
		});
		while #s > math.clamp(y.maxVisible or 8, 1, 25) do
			x(#s);
		end;
	end;
	local function Z(k)
		if not k then
			return nil, nil, nil;
		end;
		local l = k:FindFirstAncestorOfClass("Model");
		if not l and (k.Parent and k.Parent:IsA("Model")) then
			l = k.Parent;
		end;
		if not l then
			return nil, nil, nil;
		end;
		local p = l:FindFirstChildOfClass("Humanoid");
		local y = a:GetPlayerFromCharacter(l);
		return y, p, k.Name;
	end;
	local function E(p)
		if not k.hitNotifications.enabled or p == y then
			return false;
		end;
		local v = (getgenv()).FinhubCombatLastShotAt or 0;
		if tick() - v < 4 then
			return true;
		end;
		if k.target.lastplayer == p then
			return true;
		end;
		if k.target.enabled and k.target.character then
			local l = a:GetPlayerFromCharacter(k.target.character);
			if l == p then
				return true;
			end;
		end;
		local j = l.lastHitAtByPlayer[p];
		return j ~= nil and tick() - j < 4;
	end;
	local function J(p, a, y, v)
		if not a or not a.Parent then
			return false;
		end;
		local j = G[p];
		if j == nil then
			j = a.Health;
			G[p] = j;
		end;
		local s = a.Health;
		local g = j - s;
		if g >= .01 then
			V(p.Name, g, y or l.hitPartByPlayer[p] or k.target.hitpart or "Body");
			G[p] = s;
			l.lastHitAtByPlayer[p] = tick();
			return true;
		end;
		if v then
			V(p.Name, 1, y or l.hitPartByPlayer[p] or k.target.hitpart or "Body");
			l.lastHitAtByPlayer[p] = tick();
			return true;
		end;
		return false;
	end;
	local function U(k, l)
		local p = l.hum;
		local a = l.bodyPart;
		if not p or not p.Parent then
			return;
		end;
		if J(k, p, a, false) then
			return;
		end;
		for l = 1, 15, 1 do
			task.wait(0);
			if J(k, p, a, false) then
				return;
			end;
		end;
		for l = 1, 8, 1 do
			task.wait(.03);
			if J(k, p, a, false) then
				return;
			end;
		end;
		J(k, p, a, true);
	end;
	local function z(k, l, p)
		c[k] = c[k] or {};
		table.insert(c[k], { hum = l, bodyPart = p, queuedAt = tick() });
		if e[k] then
			return;
		end;
		e[k] = true;
		task.spawn(function()
			while c[k] and #c[k] > 0 do
				local l = table.remove(c[k], 1);
				U(k, l);
			end;
			e[k] = false;
		end);
	end;
	local function w()
		if not k.hitNotifications.enabled then
			return;
		end;
		for p, a in ipairs(a:GetPlayers()) do
			if a == y then
				continue;
			end;
			if not E(a) then
 
			end;
			local v = a.Character and a.Character:FindFirstChildOfClass("Humanoid");
			if not v then
				continue;
			end;
			local j = G[a];
			if j == nil then
				G[a] = v.Health;
				continue;
			end;
			local s = v.Health;
			if s < j - .01 then
				local p = l.hitPartByPlayer[a] or k.target.hitpart or "Body";
				V(a.Name, j - s, p);
				G[a] = s;
				l.lastHitAtByPlayer[a] = tick();
				if s <= 0 then
					local k = (getgenv()).FinhubTryKillSound;
					if k then
						k(a, j, s);
					end;
				end;
			elseif s > j then
				G[a] = s;
			end;
		end;
	end;
	local function q(k, l, p)
		if not k or not l or k == y then
			return;
		end;
		localHitTargets[l] = {
				plr = k,
				bodyPart = p,
				hitAt = tick(),
				lastHp = l.Health,
			};
	end;
	local function u(p)
		if not p then
			return;
		end;
		local a, v, j = Z(p);
		if not a or not v or a == y then
			return;
		end;
		l.hitPartByPlayer[a] = j;
		l.lastHitAtByPlayer[a] = tick();
		q(a, v, j);
		if not k.hitNotifications.enabled then
			return;
		end;
		if not E(a) then
			return;
		end;
		z(a, v, j);
	end;
	KJ.notifyProjectileImpact = u;
	local function H(l, p)
		if not k.hitNotifications.enabled then
			return;
		end;
		if G[l] == nil then
			G[l] = p;
			return;
		end;
		if p > ((G[l] or p)) then
			G[l] = p;
		end;
	end;
	local function F(k)
		if g[k] then
			g[k]:Disconnect();
			g[k] = nil;
		end;
		if T[k] then
			T[k]:Disconnect();
			T[k] = nil;
		end;
		G[k] = nil;
		c[k] = nil;
		e[k] = nil;
	end;
	local function Q(p, a)
		if p == y or not a then
			return;
		end;
		if g[p] then
			g[p]:Disconnect();
			g[p] = nil;
		end;
		local v = a:FindFirstChildOfClass("Humanoid") or a:WaitForChild("Humanoid", 8);
		if not v then
			return;
		end;
		G[p] = v.Health;
		g[p] = v.HealthChanged:Connect(function(a)
				local j = localHitTargets and localHitTargets[v];
				if a <= 0 and (j and (j.plr and (j.plr ~= y and not k.hitNotifications.enabled))) then
					local k = (getgenv()).FinhubTryKillSound;
					if k then
						k(j.plr, G[p] or 0, a);
					end;
					if localHitTargets then
						localHitTargets[v] = nil;
					end;
				end;
				if not k.hitNotifications.enabled then
					local k = G[p];
					if k == nil or a > k then
						G[p] = a;
					end;
					return;
				end;
				local s = G[p];
				if s == nil then
					G[p] = a;
					return;
				end;
				if a < s - .01 then
					if E(p) then
						local y = l.hitPartByPlayer[p] or k.target.hitpart or "Body";
						V(p.Name, s - a, y);
						l.lastHitAtByPlayer[p] = tick();
					end;
					if a <= 0 then
						local k = (getgenv()).FinhubTryKillSound;
						if k then
							k(p, s, a);
						end;
					end;
					G[p] = a;
				elseif a > s then
					G[p] = a;
				end;
			end);
		v.Died:Connect(function()
			local l = G[p];
			local a = (getgenv()).FinhubTryKillSound;
			if a and (l and l > 0) then
				a(p, l, 0);
			end;
			local j = localHitTargets and localHitTargets[v];
			if j then
				if not k.hitNotifications.enabled then
					local k = (getgenv()).FinhubTryKillSound;
					if k and (j.plr and j.plr ~= y) then
						k(j.plr, l or 0, 0);
					end;
				end;
				if localHitTargets then
					localHitTargets[v] = nil;
				end;
			end;
		end);
	end;
	local function A(k)
		if k == y then
			return;
		end;
		if T[k] then
			T[k]:Disconnect();
		end;
		T[k] = k.CharacterAdded:Connect(function(l)
				Q(k, l);
			end);
		if k.Character then
			Q(k, k.Character);
		end;
	end;
	a.PlayerAdded:Connect(A);
	a.PlayerRemoving:Connect(F);
	for k, l in a:GetPlayers() do
		A(l);
	end;
	local function I(l)
		local p = k.hitNotifications;
		if not p.enabled or X() then
			K.Visible = false;
			if not p.enabled then
				for k = #s, 1, -1 do
					x(k);
				end;
			end;
			return;
		end;
		w();
		K.Visible = #s > 0;
		d();
		l = l or 0;
		local a = tick();
		local y = math.clamp(p.animInDuration or .52, .15, 2);
		local v = math.clamp(p.animOutDuration or .38, .15, 2);
		local j = p.duration or 3;
		local G = p.stackGap or 6;
		local g = p.inAnimation or "fade bounce";
		local T = p.outAnimation or "fade";
		local c = 0;
		for k, p in ipairs(s) do
			p.targetY = c;
			p.displayY = p.displayY or c;
			if l > 0 then
				p.displayY = p.displayY + ((p.targetY - p.displayY)) * ((1 - math.exp(-l * 14)));
			else
				p.displayY = p.targetY;
			end;
			c = (c + ((p.boxH or 26))) + G;
		end;
		local e = 1;
		while e <= #s do
			local k = s[e];
			local l = a - k.start;
			if k.phase == "in" then
				local l = ((a - k.phaseStart)) / y;
				if l >= 1 then
					k.phase = "hold";
					k.phaseStart = a;
					l = 1;
				end;
				local p, v, j, s = W(g, l, false);
				L(k, p, v, j, s);
			elseif k.phase == "hold" then
				L(k, 1, 0, 0, 1);
				if l >= j - v then
					k.phase = "out";
					k.phaseStart = a;
				end;
			elseif k.phase == "out" then
				local l = ((a - k.phaseStart)) / v;
				if l >= 1 then
					x(e);
					continue;
				end;
				local p, y, j, s = W(T, l, true);
				L(k, p, y, j, s);
			else
				k.phase = "in";
				k.phaseStart = a;
			end;
			e = e + 1;
		end;
	end;
	KJ.updateHitNotifications = I;
	KJ.clearHitNotifications = function()
			for k = #s, 1, -1 do
				x(k);
			end;
			if K then
				K.Visible = false;
			end;
		end;
	KJ.hitNotifAnimStyles = f;
	KJ.applyHitNotifRootPosition = d;
end;
local nJ = F.Character:AddRightGroupbox("profile");
if not config then
	config = {};
end;
if not config.profile then
	config.profile = { level = { enabled = false, value = 9999 }, winstreak = { enabled = false, value = 9999 } };
end;
nJ:AddToggle("LevelSpoof", { Text = "level", Default = false, Callback = function(k)
		config.profile.level.enabled = k;
	end });
nJ:AddInput("LevelValue", {
	Text = "level",
	Default = "9999",
	Numeric = true,
	Finished = false,
	Placeholder = "9999",
	Callback = function(k)
		if config and (config.profile and config.profile.level) then
			config.profile.level.value = tonumber(k) or 9999;
		end;
	end,
});
nJ:AddToggle("WinStreakSpoof", { Text = "win streak", Default = false, Callback = function(k)
		config.profile.winstreak.enabled = k;
	end });
nJ:AddInput("WinStreakValue", {
	Text = "win streak",
	Default = "9999",
	Numeric = true,
	Finished = false,
	Placeholder = "9999",
	Callback = function(k)
		if config and (config.profile and config.profile.winstreak) then
			config.profile.winstreak.value = tonumber(k) or 9999;
		end;
	end,
});
task.spawn(function()
	while true do
		local k = (game:GetService("Players")).LocalPlayer;
		if config and (config.profile and (config.profile.level and config.profile.level.enabled)) then
			k:SetAttribute("Level", config.profile.level.value);
		end;
		if config and (config.profile and (config.profile.winstreak and config.profile.winstreak.enabled)) then
			pcall(function()
				local l = k:FindFirstChild("CustomLeaderstats");
				if l then
					local k = l:FindFirstChild("Win Streak");
					if k then
						k.Value = config.profile.winstreak.value;
					end;
				end;
			end);
		end;
		task.wait(.4);
	end;
end);
local dJ = F.Character:AddLeftTabbox();
local YJ = dJ:AddTab("name");
local MJ = dJ:AddTab("skin");
local iJ = (game:GetService("Players")).LocalPlayer;
if not config.profile.namespoof then
	config.profile.namespoof = {
			enabled = false,
			value = "hi",
			verified = false,
			premium = false,
		};
end;
if not config.profile.skinchanger then
	config.profile.skinchanger = { enabled = false, userid = "1" };
end;
local RJ = "";
local BJ = false;
local function WJ(k)
	return k:gsub("([^%w])", "%%%1");
end;
local function LJ(k)
	if typeof(k) ~= "string" then
		return "";
	end;
	return (k:gsub(utf8.char(57344), "")):gsub(utf8.char(57345), "");
end;
local function PJ(k)
	if not config.profile.namespoof.enabled then
		return;
	end;
	local l = LJ(config.profile.namespoof.value or "hi");
	local p = ((config.profile.namespoof.premium and utf8.char(57345) or "")) .. ((config.profile.namespoof.verified and utf8.char(57344) or ""));
	local a = l .. p;
	if k then
		local l = k:WaitForChild("Humanoid", 5);
		if l then
			l.DisplayName = a;
			local k = l.DisplayDistanceType;
			l.DisplayDistanceType = Enum.HumanoidDisplayDistanceType.None;
			l.DisplayDistanceType = k;
		end;
	end;
end;
local function xJ()
	if not config.profile.namespoof.enabled or BJ then
		return;
	end;
	BJ = true;
	local k = LJ(config.profile.namespoof.value or "hi");
	local l = ((config.profile.namespoof.premium and utf8.char(57345) or "")) .. ((config.profile.namespoof.verified and utf8.char(57344) or ""));
	local p = k .. l;
	if iJ and iJ.Character then
		local k = iJ.Character:FindFirstChildOfClass("Humanoid");
		if k then
			pcall(function()
				k.DisplayName = p;
				local l = k.DisplayDistanceType;
				k.DisplayDistanceType = Enum.HumanoidDisplayDistanceType.None;
				k.DisplayDistanceType = l;
			end);
		end;
	end;
	RJ = p;
	BJ = false;
end;
local function tJ(k)
	if not ((config.profile.skinchanger.enabled and k)) then
		return;
	end;
	local l = k:WaitForChild("Humanoid", 5);
	local p = tonumber(config.profile.skinchanger.userid);
	if not l or not p then
		return;
	end;
	pcall(function()
		local l = (game:GetService("Players")):CreateHumanoidModelFromUserId(p);
		if not l then
			return;
		end;
		for k, l in ipairs(k:GetChildren()) do
			if l:IsA("Accessory") or l:IsA("Shirt") or l:IsA("Pants") or l:IsA("BodyColors") or l:IsA("CharacterMesh") or l:IsA("ShirtGraphic") then
				l:Destroy();
			end;
		end;
		local a = k:FindFirstChild("Head");
		if a then
			local k = a:FindFirstChildOfClass("Decal");
			if k then
				k:Destroy();
			end;
		end;
		for l, p in ipairs(l:GetChildren()) do
			if p:IsA("Accessory") or p:IsA("Shirt") or p:IsA("Pants") or p:IsA("BodyColors") or p:IsA("CharacterMesh") or p:IsA("ShirtGraphic") then
				(p:Clone()).Parent = k;
			elseif p.Name == "Head" then
				local k = p:FindFirstChildOfClass("Decal");
				if k and a then
					(k:Clone()).Parent = a;
				end;
			end;
		end;
		l:Destroy();
	end);
end;
YJ:AddToggle("NameSpoofEnabled", { Text = "enable", Default = false, Callback = function(k)
		config.profile.namespoof.enabled = k;
		if k then
			if iJ.Character then
				PJ(iJ.Character);
			end;
			xJ();
		end;
	end });
YJ:AddToggle("NameSpoofVerified", { Text = "verified", Default = false, Callback = function(k)
		config.profile.namespoof.verified = k;
		if config.profile.namespoof.enabled then
			if iJ.Character then
				PJ(iJ.Character);
			end;
			xJ();
		end;
	end });
YJ:AddToggle("NameSpoofPremium", { Text = "premium", Default = false, Callback = function(k)
		config.profile.namespoof.premium = k;
		if config.profile.namespoof.enabled then
			if iJ.Character then
				PJ(iJ.Character);
			end;
			xJ();
		end;
	end });
YJ:AddInput("NameSpoofValue", {
	Text = "custom name",
	Default = "hi",
	Numeric = false,
	Finished = false,
	Placeholder = "type name...",
	Callback = function(k)
		config.profile.namespoof.value = LJ(k or "hi");
		if config.profile.namespoof.enabled then
			if iJ.Character then
				PJ(iJ.Character);
			end;
			xJ();
		end;
	end,
});
MJ:AddToggle("SkinChangerEnabled", { Text = "enable", Default = false, Callback = function(k)
		config.profile.skinchanger.enabled = k;
		if k and iJ.Character then
			tJ(iJ.Character);
		end;
	end });
MJ:AddInput("SkinChangerValue", {
	Text = "user id",
	Default = "1",
	Numeric = true,
	Finished = true,
	Placeholder = "type user id...",
	Callback = function(k)
		config.profile.skinchanger.userid = k or "1";
		if config.profile.skinchanger.enabled and iJ.Character then
			tJ(iJ.Character);
		end;
	end,
});
iJ.CharacterAdded:Connect(function(k)
	PJ(k);
	tJ(k);
end);
local function SJ(k)
	if not config.profile.namespoof.enabled or BJ then
		return;
	end;
	if not ((k:IsA("TextLabel") or k:IsA("TextButton") or k:IsA("TextBox"))) then
		return;
	end;
	if not k.Text then
		return;
	end;
	local l = k:FindFirstAncestorOfClass("Model");
	if not l or l ~= iJ.Character then
		return;
	end;
	local p = LJ(config.profile.namespoof.value or "hi");
	local a = ((config.profile.namespoof.premium and utf8.char(57345) or "")) .. ((config.profile.namespoof.verified and utf8.char(57344) or ""));
	local y = p .. a;
	if k.Text:find(iJ.Name, 1, true) then
		BJ = true;
		k.Text = k.Text:gsub(WJ(iJ.Name), y);
		BJ = false;
	elseif RJ ~= "" and (RJ ~= y and k.Text:find(RJ, 1, true)) then
		BJ = true;
		k.Text = k.Text:gsub(WJ(RJ), y);
		BJ = false;
	end;
end;
game.DescendantAdded:Connect(SJ);
pcall(function()
	(game:GetService("CoreGui")).DescendantAdded:Connect(SJ);
end);
local VJ = F.Character:AddLeftTabbox();
local XJ = VJ:AddTab("fps");
local ZJ = VJ:AddTab("ms");
local EJ = VJ:AddTab("region");
if not config.profile.fpsspoof then
	config.profile.fpsspoof = { enabled = false, value = "1", fraud = false };
end;
if not config.profile.msspoof then
	config.profile.msspoof = { enabled = false, value = "1", fraud = false };
end;
if not config.profile.regionspoof then
	config.profile.regionspoof = { enabled = false, value = "W fin_wo" };
end;
_G.FPSSpoofConnections = _G.FPSSpoofConnections or {};
for k = 1, #_G.FPSSpoofConnections, 1 do
	_G.FPSSpoofConnections[k]:Disconnect();
end;
_G.FPSSpoofConnections = {};
_G.MSSpoofConnections = _G.MSSpoofConnections or {};
for k = 1, #_G.MSSpoofConnections, 1 do
	_G.MSSpoofConnections[k]:Disconnect();
end;
_G.MSSpoofConnections = {};
_G.RegionSpoofConnections = _G.RegionSpoofConnections or {};
for k = 1, #_G.RegionSpoofConnections, 1 do
	_G.RegionSpoofConnections[k]:Disconnect();
end;
_G.RegionSpoofConnections = {};
if _G.FPSFraudThread then
	_G.FPSFraudThread = false;
end;
if _G.MSFraudThread then
	_G.MSFraudThread = false;
end;
local JJ = (game:GetService("Players")).LocalPlayer:WaitForChild("PlayerGui");
local UJ = {};
local zJ = {};
local function wJ(k)
	local l = k:GetChildren();
	for k = 1, #l, 1 do
		local p = l[k];
		if p:IsA("TextLabel") and p.Name == "Title" then
			if p.Parent.Name == "ServerRegion" then
				zJ[#zJ + 1] = p;
			else
				UJ[#UJ + 1] = p;
			end;
		end;
		wJ(p);
	end;
end;
wJ(JJ);
local qJ = nil;
local uJ = nil;
local HJ = 0;
local FJ = 0;
local QJ = 0;
local AJ = 0;
local function IJ()
	local k = tonumber(config.profile.fpsspoof.value) or 60;
	if qJ == nil then
		qJ = k;
	end;
	local l = math.random(-8, 12);
	HJ = HJ + math.random(-3, 5);
	HJ = math.clamp(HJ, -15, 15);
	local p = l + HJ;
	if math.random(1, 100) <= 15 then
		p = p + math.random(-10, 20);
	end;
	if math.random(1, 100) <= 8 then
		p = p - math.random(5, 15);
	end;
	local a = k + p;
	if math.random(1, 100) <= 5 then
		a = a + math.random(20, 45);
	end;
	if a < 10 then
		a = math.random(10, 25);
	end;
	if a > 144 then
		a = math.random(120, 144);
	end;
	if math.random(1, 100) <= 3 then
		a = math.random(30, 50);
	end;
	qJ = a;
	return tostring(math.floor(a));
end;
local function mJ()
	local k = tonumber(config.profile.msspoof.value) or 50;
	if uJ == nil then
		uJ = k;
	end;
	FJ = FJ + math.random(-4, 6);
	FJ = math.clamp(FJ, -20, 25);
	local l = 0;
	if math.random(1, 100) <= 12 then
		l = math.random(15, 45);
	end;
	if math.random(1, 100) <= 7 then
		l = l - math.random(10, 30);
	end;
	local p = math.random(-12, 18);
	local a = ((k + p) + FJ) + l;
	if a < 8 then
		a = math.random(8, 20);
	end;
	if a > 250 then
		a = math.random(180, 250);
	end;
	if math.random(1, 100) <= 10 then
		a = a + math.random(5, 25);
	end;
	if math.random(1, 100) <= 6 then
		a = math.random(100, 200);
	end;
	uJ = a;
	return tostring(math.floor(a));
end;
local function oJ()
	for k = 1, #_G.FPSSpoofConnections, 1 do
		_G.FPSSpoofConnections[k]:Disconnect();
	end;
	_G.FPSSpoofConnections = {};
	_G.FPSFraudThread = false;
	qJ = nil;
	HJ = 0;
	if not config.profile.fpsspoof.enabled then
		return;
	end;
	if config.profile.fpsspoof.fraud then
		_G.FPSFraudThread = true;
		task.spawn(function()
			while _G.FPSFraudThread do
				local k = IJ();
				for l = 1, #UJ, 1 do
					local p = UJ[l];
					local a = p.Text:gsub("%d+fps", k .. "fps");
					if a ~= p.Text then
						p.Text = a;
					end;
				end;
				local l = .95 + math.random() * .4;
				task.wait(l);
			end;
		end);
	else
		local k = config.profile.fpsspoof.value;
		for l = 1, #UJ, 1 do
			local p = UJ[l];
			_G.FPSSpoofConnections[#_G.FPSSpoofConnections + 1] = (p:GetPropertyChangedSignal("Text")):Connect(function()
					local l = p.Text:gsub("%d+fps", k .. "fps");
					if l ~= p.Text then
						p.Text = l;
					end;
				end);
		end;
	end;
end;
local function rJ()
	for k = 1, #_G.MSSpoofConnections, 1 do
		_G.MSSpoofConnections[k]:Disconnect();
	end;
	_G.MSSpoofConnections = {};
	_G.MSFraudThread = false;
	uJ = nil;
	FJ = 0;
	if not config.profile.msspoof.enabled then
		return;
	end;
	if config.profile.msspoof.fraud then
		_G.MSFraudThread = true;
		task.spawn(function()
			while _G.MSFraudThread do
				local k = mJ();
				for l = 1, #UJ, 1 do
					local p = UJ[l];
					local a = p.Text:gsub("%d+ms", k .. "ms");
					if a ~= p.Text then
						p.Text = a;
					end;
				end;
				local l = .95 + math.random() * .4;
				task.wait(l);
			end;
		end);
	else
		local k = config.profile.msspoof.value;
		for l = 1, #UJ, 1 do
			local p = UJ[l];
			_G.MSSpoofConnections[#_G.MSSpoofConnections + 1] = (p:GetPropertyChangedSignal("Text")):Connect(function()
					local l = p.Text:gsub("%d+ms", k .. "ms");
					if l ~= p.Text then
						p.Text = l;
					end;
				end);
		end;
	end;
end;
local function CJ()
	for k = 1, #_G.RegionSpoofConnections, 1 do
		_G.RegionSpoofConnections[k]:Disconnect();
	end;
	_G.RegionSpoofConnections = {};
	if not config.profile.regionspoof.enabled then
		return;
	end;
	local k = config.profile.regionspoof.value;
	for l = 1, #zJ, 1 do
		local p = zJ[l];
		p.Text = k;
		_G.RegionSpoofConnections[#_G.RegionSpoofConnections + 1] = (p:GetPropertyChangedSignal("Text")):Connect(function()
				if p.Text ~= k then
					p.Text = k;
				end;
			end);
	end;
end;
XJ:AddToggle("FPSSpoofEnabled", { Text = "enable", Default = false, Callback = function(k)
		config.profile.fpsspoof.enabled = k;
		oJ();
	end });
XJ:AddToggle("FPSSpoofFraud", { Text = "fraud", Default = false, Callback = function(k)
		config.profile.fpsspoof.fraud = k;
		if config.profile.fpsspoof.enabled then
			oJ();
		end;
	end });
XJ:AddInput("FPSSpoofValue", {
	Text = "fps value",
	Default = "1",
	Numeric = true,
	Finished = false,
	Placeholder = "type fps...",
	Callback = function(k)
		config.profile.fpsspoof.value = k or "1";
		if config.profile.fpsspoof.enabled then
			oJ();
		end;
	end,
});
ZJ:AddToggle("MSSpoofEnabled", { Text = "enable", Default = false, Callback = function(k)
		config.profile.msspoof.enabled = k;
		rJ();
	end });
ZJ:AddToggle("MSSpoofFraud", { Text = "fraud", Default = false, Callback = function(k)
		config.profile.msspoof.fraud = k;
		if config.profile.msspoof.enabled then
			rJ();
		end;
	end });
ZJ:AddInput("MSSpoofValue", {
	Text = "ms value",
	Default = "1",
	Numeric = true,
	Finished = false,
	Placeholder = "type ms...",
	Callback = function(k)
		config.profile.msspoof.value = k or "1";
		if config.profile.msspoof.enabled then
			rJ();
		end;
	end,
});
EJ:AddToggle("RegionSpoofEnabled", { Text = "enable", Default = false, Callback = function(k)
		config.profile.regionspoof.enabled = k;
		CJ();
	end });
EJ:AddInput("RegionSpoofValue", {
	Text = "region value",
	Default = "W fin_wo",
	Numeric = false,
	Finished = false,
	Placeholder = "type region...",
	Callback = function(k)
		config.profile.regionspoof.value = k or "W fin_wo";
		if config.profile.regionspoof.enabled then
			CJ();
		end;
	end,
});
local DJ = F.Character:AddRightGroupbox("movement");
local hJ = game:GetService("UserInputService");
local OJ = game:GetService("RunService");
local kn = (game:GetService("Players")).LocalPlayer;
_G.cframeactive = false;
_G.keyheldcframe = false;
_G.cframevalue = 30;
_G.cframeflyactive = false;
_G.keyheldcframefly = false;
_G.cframeflyvalue = 50;
local ln = nil;
local pn = nil;
local function an(k)
	_G[k] = not _G[k];
end;
local function yn()
	if ln then
		ln:Destroy();
		ln = nil;
	end;
	if pn then
		pn:Destroy();
		pn = nil;
	end;
end;
(DJ:AddToggle("cframespf_enabled", { Text = "walkspeed", Default = false, Callback = function(k)
		_G.cframeactive = k;
		if (getgenv()).FinhubConfigLoading then
			return;
		end;
	end })):AddKeyPicker("cframespf_key", {
	Default = "None",
	NoUI = true,
	SyncToggleState = false,
	Mode = "Toggle",
	Text = "Velocity Walkspeed",
	Callback = function(k)
		_G.keyheldcframe = k == true;
	end,
});
DJ:AddSlider("spdd_value", {
	Text = "walkspeed speed",
	Default = 30,
	Min = 16,
	Max = 750,
	Rounding = 0,
	Compact = true,
	Callback = function(k)
		_G.cframevalue = k;
	end,
});
OJ.Heartbeat:Connect(function(k)
	if (getgenv()).FinhubConfigLoading then
		return;
	end;
	if not ((_G.cframeactive and _G.keyheldcframe)) then
		return;
	end;
	local l = kn.Character;
	local p = l and l:FindFirstChild("HumanoidRootPart");
	local a = l and l:FindFirstChildOfClass("Humanoid");
	if not p or not a then
		return;
	end;
	local y = a.MoveDirection;
	if y.Magnitude > 0 then
		local k = y * _G.cframevalue;
		p.AssemblyLinearVelocity = Vector3.new(k.X, p.AssemblyLinearVelocity.Y, k.Z);
	end;
end);
(DJ:AddToggle("cframefly_enabled", { Text = "fly", Default = false, Callback = function(k)
		_G.cframeflyactive = k;
		if (getgenv()).FinhubConfigLoading then
			return;
		end;
		if not k then
			yn();
			local k = kn.Character;
			local l = k and k:FindFirstChild("HumanoidRootPart");
			if l then
				l.AssemblyLinearVelocity = Vector3.new(0, 0, 0);
			end;
		end;
	end })):AddKeyPicker("cframefly_key", {
	Default = "None",
	NoUI = true,
	SyncToggleState = false,
	Mode = "Toggle",
	Text = "Velocity Fly",
	Callback = function(k)
		_G.keyheldcframefly = k;
		if not k then
			yn();
			local k = kn.Character;
			local l = k and k:FindFirstChild("HumanoidRootPart");
			if l then
				l.AssemblyLinearVelocity = Vector3.new(0, 0, 0);
			end;
		end;
	end,
});
DJ:AddSlider("cframefly_speed", {
	Text = "fly speed",
	Default = 50,
	Min = 16,
	Max = 750,
	Rounding = 0,
	Compact = true,
	Callback = function(k)
		_G.cframeflyvalue = k;
	end,
});
OJ.Heartbeat:Connect(function(k)
	if (getgenv()).FinhubConfigLoading then
		return;
	end;
	if not ((_G.cframeflyactive and _G.keyheldcframefly)) then
		return;
	end;
	local l = kn.Character;
	if not l then
		return;
	end;
	local p = l:FindFirstChild("HumanoidRootPart");
	local a = l:FindFirstChildOfClass("Humanoid");
	if not p or not a then
		return;
	end;
	if not ln then
		ln = Instance.new("BodyPosition");
		ln.MaxForce = Vector3.new(100000.0, 100000.0, 100000.0);
		ln.D = 1000;
		ln.P = 10000;
		ln.Position = p.Position;
		ln.Parent = p;
	end;
	if not pn then
		pn = Instance.new("BodyGyro");
		pn.MaxTorque = Vector3.new(100000.0, 100000.0, 100000.0);
		pn.D = 400;
		pn.P = 10000;
		pn.CFrame = p.CFrame;
		pn.Parent = p;
	end;
	local y = workspace.CurrentCamera;
	local v = y.CFrame.LookVector;
	local j = y.CFrame.RightVector;
	local s = Vector3.new();
	if hJ:IsKeyDown(Enum.KeyCode.W) then
		s += v;
	end;
	if hJ:IsKeyDown(Enum.KeyCode.S) then
		s -= v;
	end;
	if hJ:IsKeyDown(Enum.KeyCode.A) then
		s -= j;
	end;
	if hJ:IsKeyDown(Enum.KeyCode.D) then
		s += j;
	end;
	if hJ:IsKeyDown(Enum.KeyCode.Space) then
		s += Vector3.new(0, 1, 0);
	end;
	if hJ:IsKeyDown(Enum.KeyCode.LeftShift) then
		s -= Vector3.new(0, 1, 0);
	end;
	if s.Magnitude > 0 then
		s = s.Unit;
	end;
	ln.Position = p.Position + (((s * _G.cframeflyvalue) * k) * 10);
	ln.MaxForce = Vector3.new(100000.0, 100000.0, 100000.0);
	pn.CFrame = CFrame.new(p.Position, p.Position + v * Vector3.new(1, 0, 1));
end);
(getgenv()).FinhubCleanupMovement = function()
		yn();
		_G.keyheldcframe = false;
		_G.keyheldcframefly = false;
		local k = kn.Character;
		local l = k and k:FindFirstChild("HumanoidRootPart");
		if l then
			pcall(function()
				l.AssemblyLinearVelocity = Vector3.zero;
				l.AssemblyAngularVelocity = Vector3.zero;
			end);
		end;
	end;
local vn = F.Character:AddLeftTabbox();
local jn = vn:AddTab("self material");
local sn = vn:AddTab("animation player");
local Gn = {
		enabled = false,
		color1 = Color3.fromRGB(255, 255, 255),
		color2 = Color3.fromRGB(255, 255, 255),
		color3 = Color3.fromRGB(255, 255, 255),
		materialName = "Neon",
		transparency = .1,
		pulseSpeed = 3,
		phase = 0,
		parts = {},
		originals = {},
		conn = nil,
		cacheTick = 0,
	};
local gn = {
		"ForceField",
		"Neon",
		"SmoothPlastic",
		"Glass",
		"Ice",
		"Plastic",
		"Wood",
		"Marble",
		"Granite",
		"Brick",
		"Cobblestone",
		"Concrete",
		"Slate",
		"Foil",
	};
local function Tn()
	return iJ.Character;
end;
local function cn()
	local k = Tn();
	if not k then
		return {};
	end;
	local l = {};
	for k, p in ipairs(k:GetChildren()) do
		if p:IsA("BasePart") and p.Name ~= "HumanoidRootPart" then
			l[#l + 1] = p;
		end;
	end;
	return l;
end;
local function en(k)
	if Gn.originals[k] then
		return;
	end;
	Gn.originals[k] = { Material = k.Material, Color = k.Color, Transparency = k.Transparency };
end;
local function bn()
	for k, l in pairs(Gn.originals) do
		if k and k.Parent then
			k.Material = l.Material;
			k.Color = l.Color;
			k.Transparency = l.Transparency;
		end;
	end;
	Gn.originals = {};
end;
local function fn()
	Gn.parts = cn();
	for k, l in ipairs(Gn.parts) do
		en(l);
	end;
end;
local function Nn(k)
	if k < .5 then
		return Gn.color1:Lerp(Gn.color2, k * 2);
	end;
	return Gn.color2:Lerp(Gn.color3, ((k - .5)) * 2);
end;
local function Kn(k)
	if not Gn.enabled then
		return;
	end;
	if tick() - Gn.cacheTick > .5 then
		Gn.cacheTick = tick();
		fn();
	end;
	Gn.phase = Gn.phase + k * ((Gn.pulseSpeed or 3));
	local l = ((math.sin(Gn.phase) + 1)) * .5;
	local p = Nn(l);
	local a = Enum.Material[Gn.materialName] or Enum.Material.Neon;
	local y = math.clamp(Gn.transparency or .1, 0, 1);
	for k, l in ipairs(Gn.parts) do
		if l and l.Parent then
			l.Material = a;
			l.Color = p;
			l.Transparency = y;
		end;
	end;
end;
local function nn(k)
	Gn.enabled = k;
	if k then
		fn();
		if not Gn.conn then
			Gn.conn = (game:GetService("RunService")).RenderStepped:Connect(function(k)
					Kn(k);
				end);
		end;
	else
		if Gn.conn then
			Gn.conn:Disconnect();
			Gn.conn = nil;
		end;
		bn();
	end;
end;
if iJ then
	iJ.CharacterAdded:Connect(function()
		task.wait(.25);
		if Gn.enabled then
			fn();
		end;
	end);
end;
(((jn:AddToggle("SlfMtrlEnable", { Text = "enable", Default = false, Callback = function(k)
		nn(k);
	end })):AddColorPicker("SlfMtrlColor1", { Title = "color 1", Default = Gn.color1, Callback = function(k)
		Gn.color1 = k;
	end })):AddColorPicker("SlfMtrlColor2", { Title = "color 2", Default = Gn.color2, Callback = function(k)
		Gn.color2 = k;
	end })):AddColorPicker("SlfMtrlColor3", { Title = "color 3", Default = Gn.color3, Callback = function(k)
		Gn.color3 = k;
	end });
jn:AddDropdown("SlfMtrlMaterial", {
	Text = "material",
	Default = Gn.materialName,
	Values = gn,
	Callback = function(k)
		Gn.materialName = k;
	end,
});
jn:AddSlider("SlfMtrlTransparency", {
	Text = "transparency",
	Default = Gn.transparency,
	Min = 0,
	Max = 1,
	Rounding = 2,
	Callback = function(k)
		Gn.transparency = k;
	end,
});
jn:AddSlider("SlfMtrlPulseSpeed", {
	Text = "pulse speed",
	Default = Gn.pulseSpeed,
	Min = .1,
	Max = 12,
	Rounding = 1,
	Callback = function(k)
		Gn.pulseSpeed = k;
	end,
});
local dn = {
		animationId = "",
		loop = true,
		track = nil,
		stoppedConn = nil,
	};
local function Yn()
	local k = iJ.Character;
	local l = k and k:FindFirstChildOfClass("Humanoid");
	if not l then
		return nil;
	end;
	local p = l:FindFirstChildOfClass("Animator");
	if not p then
		p = Instance.new("Animator");
		p.Parent = l;
	end;
	return p;
end;
local function Mn(k)
	local l = tostring(k or "");
	local p = l:match("%d+");
	if not p then
		return nil;
	end;
	return "rbxassetid://" .. p;
end;
local function Rn()
	if dn.stoppedConn then
		dn.stoppedConn:Disconnect();
		dn.stoppedConn = nil;
	end;
	if dn.track then
		pcall(function()
			dn.track:Stop();
			dn.track:Destroy();
		end);
		dn.track = nil;
	end;
end;
local function Bn()
	local k = Mn(dn.animationId);
	if not k then
		b:Notify("invalid animation id", 2);
		return;
	end;
	local l = Yn();
	if not l then
		b:Notify("humanoid not found", 2);
		return;
	end;
	Rn();
	local p = Instance.new("Animation");
	p.AnimationId = k;
	local a, y = pcall(function()
			return l:LoadAnimation(p);
		end);
	p:Destroy();
	if not a or not y then
		b:Notify("failed to load animation", 2);
		return;
	end;
	dn.track = y;
	y.Looped = dn.loop;
	y:Play(.05, 1, 1);
	dn.stoppedConn = y.Stopped:Connect(function()
			if dn.loop then
				task.delay(.03, function()
					if dn.track == y then
						pcall(function()
							y:Play(.05, 1, 1);
						end);
					end;
				end);
			end;
		end);
end;
do
	local k = game:GetService("RunService");
	local l = game:GetService("Players");
	local p = l.LocalPlayer;
	local a = game:GetService("Workspace");
	local y = {
			enabled = false,
			yawtype = "none",
			pitchtype = "none",
			angletype = "none",
			bodyyaw = "none",
			pitchpreset = "none",
			customangle = 0,
			minspeed = 10,
			maxspeed = 20,
			minangle = 30,
			maxangle = 60,
			randomangle = false,
			spindirection = 1,
			fakelag = false,
			fakelagstuds = 4,
			desync = false,
			desyncstuds = 3,
			microjitter = false,
			microstrength = 25,
			velocitybreaker = false,
		};
	local j = {
			lastupdate = tick(),
			invertstate = false,
			smoothyaw = 0,
			smoothpitch = 0,
			smoothroll = 0,
			framecounter = 0,
		};
	local s = { getrandominrange = function(k, l)
				return k + math.random() * ((l - k));
			end };
	local G = {};
	local g = {};
	local T = A:FindFirstChild("Remotes") and (A.Remotes:FindFirstChild("Replication") and (A.Remotes.Replication:FindFirstChild("Fighter") and A.Remotes.Replication.Fighter:FindFirstChild("UpdateCameraRotation")));
	local c = function(k)
			if not C or not C.EncodeCameraRotation then
				return nil;
			end;
			return C:EncodeCameraRotation(k);
		end;
	local function e()
		G = {};
		g = {};
		local k = h.Character;
		if not k then
			return;
		end;
		for k, l in pairs(k:GetDescendants()) do
			if l:IsA("Motor6D") then
				G[l.Name] = l;
				g[l] = l.C0;
			end;
		end;
	end;
	local function b()
		if not h.Character or not G.Neck then
			return;
		end;
		local k = G.Neck;
		if k and k.Parent then
			k.C0 = k.C0 * CFrame.Angles(math.pi * .85, math.pi, 0);
		end;
		if T then
			pcall(function()
				local k = c(Vector2.new(512, 0));
				if k then
					T:FireServer(k, nil);
				end;
			end);
		end;
	end;
	local function N()
		for k, l in pairs(g) do
			if k and k.Parent then
				k.C0 = l;
			end;
		end;
	end;
	local function K()
		local k = a:FindFirstChild("ViewModels");
		if not k then
			return nil;
		end;
		local l = k:FindFirstChild("FirstPerson");
		if not l then
			return nil;
		end;
		for k, l in ipairs(l:GetChildren()) do
			local p = l.Name;
			local a = {};
			for k in p:gmatch("[^-]+") do
				table.insert(a, k:match("^%s*(.-)%s*$"));
			end;
			if #a >= 2 then
				return a[2];
			end;
		end;
		return nil;
	end;
	local n = v((game:GetService("Players")).LocalPlayer.PlayerScripts.Controllers.FighterController);
	local d = n.LocalFighter;
	local Y = false;
	local M = v((game:GetService("Players")).LocalPlayer.PlayerScripts.Controllers.CameraController);
	local R;
	local function B(k)
		local l = k;
		local p = Vector3.new(0, -500, 0);
		local y = RaycastParams.new();
		y.FilterType = Enum.RaycastFilterType.Exclude;
		if d and (d.Entity and d.Entity.RootPart) then
			y.FilterDescendantsInstances = { d.Entity.RootPart.Parent };
		end;
		local v = a:Raycast(l, p, y);
		if v then
			return CFrame.new(Vector3.new(k.X, v.Position.Y - 2, k.Z));
		end;
		return nil;
	end;
	local W = M.Update;
	M.Update = function(...)
			local k = { ... };
			if Y and (d and (d.Entity and (d.Entity.RootPart and R))) then
				d.Entity.RootPart.CFrame = R;
			end;
			return W(table.unpack(k));
		end;
	(game:GetService("RunService")).Heartbeat:Connect(function()
		if (getgenv()).FinhubConfigLoading then
			return;
		end;
		if not Y then
			R = nil;
			return;
		end;
		local k = K();
		if not k then
			R = nil;
			return;
		end;
		if d and (d.Entity and d.Entity.RootPart) then
			R = d.Entity.RootPart.CFrame;
			local k = d.Entity.RootPart.Position;
			local l = B(k);
			if l then
				d.Entity.RootPart.CFrame = l;
			end;
		end;
	end);
	local L = { calculateyaw = function(k)
				local l = 0;
				local p = tick();
				if y.yawtype == "jitter" then
					local k = math.rad(y.minangle);
					local p = math.rad(y.maxangle);
					if y.randomangle then
						l = s.getrandominrange(-p, p);
					else
						l = math.random() > .5 and k or -k;
					end;
				elseif y.yawtype == "spinbot" then
					local k = s.getrandominrange(y.minspeed / 10, y.maxspeed / 10);
					l = ((p * k)) % ((2 * math.pi));
				elseif y.yawtype == "random" then
					if j.framecounter % 30 == 0 then
						l = s.getrandominrange(-math.rad(y.maxangle), math.rad(y.maxangle));
					else
						l = j.smoothyaw;
					end;
				end;
				return l;
			end, calculatepitch = function()
				local k = 0;
				if y.pitchtype == "jitter" then
					local l = math.rad(y.minangle);
					local p = math.rad(y.maxangle);
					if y.randomangle then
						k = s.getrandominrange(-p, p);
					else
						k = math.random() > .5 and l or -l;
					end;
				elseif y.pitchtype == "spinbot" then
					k = math.sin(tick() * ((y.maxspeed / 10))) * math.rad(y.maxangle);
				elseif y.pitchtype == "random" then
					if j.framecounter % 20 == 0 then
						k = s.getrandominrange(math.rad(-89), math.rad(89));
					else
						k = j.smoothpitch;
					end;
				end;
				return k;
			end, calculateroll = function()
				local k = 0;
				if y.angletype == "tilt 45" then
					k = math.rad(45);
				elseif y.angletype == "tilt 90" then
					k = math.rad(90);
				elseif y.angletype == "upside down" then
					k = math.rad(180);
				elseif y.angletype == "custom" then
					k = math.rad(y.customangle);
				end;
				return k;
			end };
	local function P(k)
		if not y.enabled then
			return;
		end;
		if (getgenv()).FinhubConfigLoading then
			return;
		end;
		if (getgenv()).FinhubConfigLoading == nil then
			return;
		end;
		if y.yawtype == "none" and (y.pitchtype == "none" and y.angletype == "none") then
			return;
		end;
		local l = K();
		if not l then
			return;
		end;
		local a = p.Character;
		if not a then
			return;
		end;
		local v = a:FindFirstChild("HumanoidRootPart");
		if not v then
			return;
		end;
		j.framecounter = j.framecounter + 1;
		local s = L.calculateyaw(k);
		local G = L.calculatepitch();
		local g = L.calculateroll();
		local T = CFrame.Angles(G, s, g);
		v.CFrame = v.CFrame * T;
	end;
	local function x()
		R = nil;
		j.framecounter = 0;
		j.smoothyaw = 0;
		j.smoothpitch = 0;
		j.smoothroll = 0;
		N();
		local k = p.Character;
		local l = k and k:FindFirstChild("HumanoidRootPart");
		if l then
			pcall(function()
				l.AssemblyAngularVelocity = Vector3.zero;
			end);
		end;
	end;
	(getgenv()).FinhubFlushMovementState = function()
			x();
			if (getgenv()).FinhubCleanupMovement then
				pcall((getgenv()).FinhubCleanupMovement);
			else
				_G.keyheldcframe = false;
				_G.keyheldcframefly = false;
			end;
		end;
	(getgenv()).FinhubSyncAfterConfigLoad = function()
			if (getgenv()).FinhubFlushMovementState then
				pcall((getgenv()).FinhubFlushMovementState);
			end;
			_G.keyheldcframe = false;
			_G.keyheldcframefly = false;
			if Toggles and Toggles.AntiAimEnable then
				y.enabled = Toggles.AntiAimEnable.Value == true;
			else
				y.enabled = false;
			end;
			if Toggles and Toggles.AntiAimUnderground then
				Y = Toggles.AntiAimUnderground.Value == true;
				(getgenv()).FinhubUndergroundEnabled = Y;
				if not Y then
					R = nil;
				end;
			else
				Y = false;
				(getgenv()).FinhubUndergroundEnabled = false;
				R = nil;
			end;
			if Options and Options.AntiAimYaw then
				y.yawtype = Options.AntiAimYaw.Value or "none";
			end;
			if Options and Options.AntiAimPitch then
				y.pitchtype = Options.AntiAimPitch.Value or "none";
			end;
			if Options and Options.AntiAimAngle then
				y.angletype = Options.AntiAimAngle.Value or "none";
			end;
			if not y.enabled or (y.yawtype == "none" and (y.pitchtype == "none" and y.angletype == "none")) then
				x();
				R = nil;
			end;
		end;
	p.CharacterAdded:Connect(function()
		task.defer(x);
	end);
	local t = F.Combat:AddLeftGroupbox("anti aim");
	t:AddToggle("AntiAimEnable", { Text = "enable", Default = false, Callback = function(k)
			y.enabled = k;
			if (getgenv()).FinhubConfigLoading then
				return;
			end;
			if not k then
				x();
			end;
		end });
	t:AddDropdown("AntiAimYaw", {
		Values = {
			"none",
			"jitter",
			"spinbot",
			"random",
		},
		Default = "none",
		Text = "yaw",
		Callback = function(k)
			y.yawtype = k;
		end,
	});
	t:AddDropdown("AntiAimPitch", {
		Values = {
			"none",
			"jitter",
			"spinbot",
			"random",
		},
		Default = "none",
		Text = "pitch",
		Callback = function(k)
			y.pitchtype = k;
		end,
	});
	t:AddDropdown("AntiAimAngle", {
		Values = {
			"none",
			"tilt 45",
			"tilt 90",
			"upside down",
			"custom",
		},
		Default = "none",
		Text = "angle",
		Callback = function(k)
			y.angletype = k;
		end,
	});
	t:AddSlider("AntiAimCustomAngle", {
		Text = "custom angle",
		Default = 0,
		Min = 0,
		Max = 360,
		Rounding = 1,
		Suffix = "\194\176",
		Callback = function(k)
			y.customangle = k;
		end,
	});
	t:AddSlider("speedslider", {
		Text = "min speed",
		Default = 10,
		Min = 1,
		Max = 50,
		Rounding = 1,
		Suffix = "",
		Callback = function(k)
			y.minspeed = k;
		end,
	});
	t:AddSlider("angleslider", {
		Text = "max speed",
		Default = 20,
		Min = 1,
		Max = 100,
		Rounding = 1,
		Suffix = "",
		Callback = function(k)
			y.maxspeed = k;
		end,
	});
	t:AddSlider("minangleslider", {
		Text = "min angle",
		Default = 30,
		Min = 1,
		Max = 180,
		Rounding = 1,
		Suffix = "\194\176",
		Callback = function(k)
			y.minangle = k;
		end,
	});
	t:AddSlider("maxangleslider", {
		Text = "max angle",
		Default = 60,
		Min = 1,
		Max = 180,
		Rounding = 1,
		Suffix = "\194\176",
		Callback = function(k)
			y.maxangle = k;
		end,
	});
	t:AddToggle("randomangle", { Text = "random angle", Default = false, Callback = function(k)
			y.randomangle = k;
		end });
	t:AddToggle("AntiAimUnderground", { Text = "underground", Default = false, Callback = function(k)
			Y = k;
			(getgenv()).FinhubUndergroundEnabled = k;
			if (getgenv()).FinhubConfigLoading then
				return;
			end;
			if not k then
				R = nil;
			end;
		end });
	(getgenv()).FinhubSetUnderground = function(k)
			Y = k;
			(getgenv()).FinhubUndergroundEnabled = k;
			if not k then
				R = nil;
			end;
		end;
	k.Heartbeat:Connect(P);
	v3 = F.Misc:AddLeftGroupbox("auto ban - ranked");
	weapons = {
			"Assault Rifle",
			"Sniper",
			"Bow",
			"Burst Rifle",
			"Crossbow",
			"Gunblade",
			"RPG",
			"Shotgun",
			"Energy Rifle",
			"Flamethrower",
			"Grenade Launcher",
			"Minigun",
			"Paintball Gun",
			"Distortion",
			"Permafrost",
			"Handgun",
			"Daggers",
			"Flare Gun",
			"Revolver",
			"Shorty",
			"Spray",
			"Uzi",
			"Energy Pistols",
			"Exogun",
			"Slingshot",
			"Warper",
			"Fists",
			"Battle Axe",
			"Chainsaw",
			"Katana",
			"Knife",
			"Riot Shield",
			"Scythe",
			"Maul",
			"Trowel",
			"Grenade",
			"Flashbang",
			"Freeze Ray",
			"Jump Pad",
			"Molotov",
			"Satchel",
			"Smoke Grenade",
			"War Horn",
			"Medkit",
			"Substapce Tripmine",
			"Warpstone",
			"Hook",
			"Spear",
		};
	enabled = false;
	weapon1 = "Riot Shield";
	weapon2 = "Katana";
	thread = nil;
	storage = game:GetService("ReplicatedStorage");
	remote = ((storage:WaitForChild("Remotes")):WaitForChild("Duels")):WaitForChild("Vote");
	stop = function()
			if thread then
				thread = nil;
			end;
		end;
	start = function()
			stop();
			thread = task.spawn(function()
					while enabled and task.wait(1) do
						list = {};
						if weapon1 and weapon1 ~= "" then
							table.insert(list, weapon1);
						end;
						if weapon2 and weapon2 ~= "" then
							table.insert(list, weapon2);
						end;
						if #list > 0 then
							i = 1;
							while enabled do
								remote:FireServer(list[i]);
								i = i % #list + 1;
								task.wait(1);
								if not thread then
									break;
								end;
							end;
						end;
					end;
				end);
		end;
	v3:AddToggle("AutoBanQueueEnable", { Text = "enable", Default = false, Callback = function(k)
			enabled = k;
			if (getgenv()).FinhubConfigLoading then
				return;
			end;
			if k then
				start();
			else
				stop();
			end;
		end });
	first = v3:AddDropdown("first", {
			Text = "weapon 1",
			Values = weapons,
			Default = 1,
			Multi = false,
			Callback = function(k)
				weapon1 = k;
				if enabled then
					stop();
					start();
				end;
			end,
		});
	search1 = v3:AddInput("search1", {
			Text = "search weapon 1",
			Placeholder = "",
			ClearTextOnFocus = false,
			Callback = function(k)
				if k == "" then
					first:SetValues(weapons);
					return;
				end;
				f = {};
				k = k:lower();
				for l, p in ipairs(weapons) do
					if (p:lower()):find(k, 1, true) then
						table.insert(f, p);
					end;
				end;
				if #f > 0 then
					first:SetValues(f);
					weapon1 = f[1];
					first:SetValue(weapon1);
					if enabled then
						stop();
						start();
					end;
				end;
			end,
		});
	second = v3:AddDropdown("second", {
			Text = "weapon 2",
			Values = weapons,
			Default = 2,
			Multi = false,
			Callback = function(k)
				weapon2 = k;
				if enabled then
					stop();
					start();
				end;
			end,
		});
	search2 = v3:AddInput("search2", {
			Text = "search weapon 2",
			Placeholder = "",
			ClearTextOnFocus = false,
			Callback = function(k)
				if k == "" then
					second:SetValues(weapons);
					return;
				end;
				f = {};
				k = k:lower();
				for l, p in ipairs(weapons) do
					if (p:lower()):find(k, 1, true) then
						table.insert(f, p);
					end;
				end;
				if #f > 0 then
					second:SetValues(f);
					weapon2 = f[1];
					second:SetValue(weapon2);
					if enabled then
						stop();
						start();
					end;
				end;
			end,
		});
	(game:GetService("Players")).LocalPlayer.CharacterRemoving:Connect(function()
		stop();
		enabled = false;
	end);
	v4 = F.Misc:AddRightGroupbox("auto queue");
	queuemode = "1v1";
	ranked = false;
	queueenabled = false;
	queuethread = nil;
	queuestop = function()
			if queuethread then
				queuethread = nil;
			end;
		end;
	queuestart = function()
			queuestop();
			queuethread = task.spawn(function()
					while queueenabled and task.wait(1) do
						local k, l = pcall(function()
								local k = game:GetService("ReplicatedStorage");
								local l = k:WaitForChild("Remotes");
								local p = l:WaitForChild("Matchmaking");
								local a = p:WaitForChild("JoinQueue");
								if ranked then
									return a:InvokeServer(queuemode, true);
								else
									return a:InvokeServer(queuemode);
								end;
							end);
						if not k and not string.find(l:lower(), "already in queue") then
							queuethread = nil;
							break;
						end;
					end;
				end);
		end;
	v4:AddToggle("queueenabled", { Text = "enable", Default = false, Callback = function(k)
			queueenabled = k;
			if k then
				queuestart();
			else
				queuestop();
			end;
		end });
	v4:AddDropdown("queuemode", {
		Text = "queue mode",
		Values = {
			"1v1",
			"2v2",
			"3v3",
			"4v4",
			"5v5",
		},
		Default = 1,
		Multi = false,
		Callback = function(k)
			queuemode = k;
			if queueenabled then
				queuestop();
				queuestart();
			end;
		end,
	});
	v4:AddToggle("ranked", { Text = "ranked", Default = false, Callback = function(k)
			ranked = k;
			if queueenabled then
				queuestop();
				queuestart();
			end;
		end });
	(game:GetService("Players")).LocalPlayer.CharacterRemoving:Connect(function()
		queuestop();
		queueenabled = false;
	end);
end;
local Wn = F.Misc:AddRightGroupbox("miscellaneous");
do
	local k = game:GetService("Players");
	local l = game:GetService("RunService");
	local p = k.LocalPlayer;
	_G.Features = _G.Features or {};
	_G.Features.Noclip = _G.Features.Noclip or { Enabled = false, Conn = nil };
	_G.Features.ModDetector = _G.Features.ModDetector or { Enabled = true, Connection = nil, Checking = false };
	local function a()
		if _G.Features.Noclip.Conn then
			_G.Features.Noclip.Conn:Disconnect();
			_G.Features.Noclip.Conn = nil;
		end;
		local k = p.Character;
		if not k then
			return;
		end;
		if _G.Features.Noclip.Enabled then
			_G.Features.Noclip.Conn = l.Stepped:Connect(function()
					local k = p.Character;
					if not k then
						return;
					end;
					for k, l in pairs(k:GetDescendants()) do
						if l:IsA("BasePart") then
							l.CanCollide = false;
						end;
					end;
				end);
		else
			for k, l in pairs(k:GetDescendants()) do
				if l:IsA("BasePart") then
					l.CanCollide = true;
				end;
			end;
		end;
	end;
	p.CharacterAdded:Connect(function()
		task.wait(.2);
		if _G.Features.Noclip.Enabled then
			a();
		end;
	end);
	Wn:AddToggle("noclip", { Text = "noclip", Default = _G.Features.Noclip.Enabled, Callback = function(k)
			_G.Features.Noclip.Enabled = k;
			a();
		end });
	_G.Features.Handicaps = _G.Features.Handicaps or { Enabled = false };
	local function y()
		if not _G.Features.Handicaps.Enabled then
			pcall(function()
				local k = p:FindFirstChild("PlayerScripts");
				local l = k and k:FindFirstChild("Controllers");
				local a = l and l:FindFirstChild("DebugController");
				if not a then
					return;
				end;
				local y = require(a);
				y:SetHandicapsEnabled(false);
			end);
			return;
		end;
		pcall(function()
			local k = p:FindFirstChild("PlayerScripts");
			local l = k and k:FindFirstChild("Controllers");
			local a = l and l:FindFirstChild("DebugController");
			if not a then
				return;
			end;
			local y = require(a);
			y:SetHandicapsEnabled(true);
		end);
	end;
	Wn:AddToggle("handcaps", { Text = "enable handicaps", Default = _G.Features.Handicaps.Enabled, Callback = function(k)
			_G.Features.Handicaps.Enabled = k;
			y();
		end });
	task.defer(function()
		if _G.Features.Handicaps.Enabled then
			y();
		end;
	end);
	local v;
	local j = game:GetService("RunService");
	local s, G;
	local function g()
		pcall(function()
			local k = (game:GetService("Players")).LocalPlayer;
			v = k and (k.Character and k.Character:FindFirstChild("HumanoidRootPart"));
		end);
	end;
	local function T()
		pcall(function()
			if not v then
				return;
			end;
			for k, l in ipairs(workspace:GetChildren()) do
				if l.Name == "SubspaceTripmineHitbox" then
					local k = l:FindFirstChild("Hitbox");
					if k and (v and v:IsA("BasePart")) then
						firetouchinterest(v, k, 1);
						firetouchinterest(v, k, 0);
					end;
				end;
			end;
		end);
	end;
	Wn:AddToggle("AntiTrip", { Text = "anti subspace tripmine", Default = false, Callback = function(k)
			if k then
				if s then
					s:Disconnect();
					s = nil;
				end;
				if G then
					G:Disconnect();
					G = nil;
				end;
				g();
				s = j.Heartbeat:Connect(g);
				G = j.Heartbeat:Connect(T);
			else
				if s then
					s:Disconnect();
					s = nil;
				end;
				if G then
					G:Disconnect();
					G = nil;
				end;
				v = nil;
			end;
		end });
	local function c()
		if _G.Features.ModDetector.Connection then
			_G.Features.ModDetector.Connection:Disconnect();
			_G.Features.ModDetector.Connection = nil;
		end;
		_G.Features.ModDetector.Checking = false;
	end;
	local function e()
		if not _G.Features.ModDetector.Enabled then
			return;
		end;
		if _G.Features.ModDetector.Checking then
			return;
		end;
		if game.GameId ~= 6035872082 then
			return;
		end;
		_G.Features.ModDetector.Checking = true;
		task.spawn(function()
			repeat
				task.wait();
			until game:IsLoaded();
			if game.CreatorType ~= Enum.CreatorType.Group then
				_G.Features.ModDetector.Checking = false;
				return;
			end;
			local l = game:GetService("HttpService");
			local a = game.CreatorId;
			local function y()
				p:Kick("Mod Detected!");
				task.wait(.5);
				if p.Parent then
					game:Shutdown();
				end;
			end;
			local function v(k)
				if not k or typeof(k) ~= "string" then
					return false;
				end;
				local l = string.lower(k);
				return string.find(l, "mod") or string.find(l, "staff") or string.find(l, "contributor") or string.find(l, "script") or string.find(l, "build") or string.find(l, "admin") or string.find(l, "owner");
			end;
			local function j(k)
				if k == p then
					return false;
				end;
				local l, y = pcall(function()
						return k:GetRoleInGroup(a);
					end);
				if l and (y and v(y)) then
					return true;
				end;
				return false;
			end;
			local function s()
				for k, l in ipairs(k:GetPlayers()) do
					if j(l) then
						return true;
					end;
				end;
				return false;
			end;
			if s() then
				y();
				_G.Features.ModDetector.Checking = false;
				return;
			end;
			_G.Features.ModDetector.Connection = k.PlayerAdded:Connect(function(k)
					if not _G.Features.ModDetector.Enabled then
						return;
					end;
					task.wait(1);
					if j(k) then
						y();
					end;
				end);
			while _G.Features.ModDetector.Enabled and task.wait(15) do
				if not _G.Features.ModDetector.Enabled then
					break;
				end;
				if s() then
					y();
					break;
				end;
			end;
			_G.Features.ModDetector.Checking = false;
		end);
	end;
	local function b()
		if _G.Features.ModDetector.Enabled then
			e();
		else
			c();
		end;
	end;
	Wn:AddToggle("moddetector", { Text = "mod detector", Default = _G.Features.ModDetector.Enabled, Callback = function(k)
			_G.Features.ModDetector.Enabled = k;
			b();
		end });
	if _G.Features.ModDetector.Enabled then
		e();
	end;
	local f = {
			Active = false,
			Track = nil,
			Heartbeat = nil,
			AnimConnection = nil,
			EmoteId = "rbxassetid://70883871260184",
			FreezeTime = .1265,
			SpinSpeed = 120,
			Gap = .025,
			BounceIntensity = 4,
			BouncePhase = 0,
			Mode = "vertical",
			SolarAngle = 0,
			SolarRadius = 5,
			HeadHidden = {},
		};
	local function N()
		local k = p.Character or p.CharacterAdded:Wait();
		return k:WaitForChild("Humanoid");
	end;
	local function K(k)
		for k, l in pairs(f.HeadHidden) do
			if k and k.Parent then
				k.Transparency = l.Transparency;
				k.CanCollide = l.CanCollide;
				if l.Size then
					k.Size = l.Size;
				end;
				if l.CFrame then
					k.CFrame = l.CFrame;
				end;
			end;
		end;
		f.HeadHidden = {};
	end;
	local function n(k)
		if not k then
			return;
		end;
		local l = k:FindFirstChild("Head");
		if l and not f.HeadHidden[l] then
			f.HeadHidden[l] = {
					Transparency = l.Transparency,
					CanCollide = l.CanCollide,
					Size = l.Size,
					CFrame = l.CFrame,
				};
			l.Transparency = 1;
			l.CanCollide = false;
			l.Size = Vector3.new(.05, .05, .05);
		end;
		for k, l in ipairs(k:GetChildren()) do
			if l:IsA("Accessory") then
				local k = l:FindFirstChild("Handle");
				if k and not f.HeadHidden[k] then
					f.HeadHidden[k] = {
							Transparency = k.Transparency,
							CanCollide = k.CanCollide,
							Size = k.Size,
							CFrame = k.CFrame,
						};
					k.Transparency = 1;
					k.CanCollide = false;
					k.Size = Vector3.new(.05, .05, .05);
				end;
			end;
		end;
	end;
	Wn:AddToggle("ACBypassToggle", {
		Text = "AC Bypass",
		Default = false,
		Tooltip = "Enable/disable anti-cheat bypass",
		Callback = function(k)
			if k then
				if (getgenv()).FinhubACBypassReload then
					(getgenv()).FinhubACBypassReload();
				end;
				if (getgenv()).FinhubNotify then
					(getgenv()).FinhubNotify("AC Bypass enabled", 3);
				end;
			else
				(getgenv()).FinhubACBypass = (getgenv()).FinhubACBypass or {};
				(getgenv()).FinhubACBypass._bypassed = false;
				if (getgenv()).FinhubNotify then
					(getgenv()).FinhubNotify("AC Bypass disabled", 3);
				end;
			end;
		end,
	});
	Wn:AddToggle("AntiFlashbang", { Text = "anti flashbang", Default = false, Callback = function(k)
			if not k then
				return;
			end;
			local function l()
				local k = game:GetService("ReplicatedStorage");
				if not k then
					return;
				end;
				local l = k:FindFirstChild("Modules");
				if not l then
					return;
				end;
				local p = l:FindFirstChild("ItemLibrary");
				if not p then
					return;
				end;
				local a, y = pcall(function()
						return require(p);
					end);
				if a and (y and (y.Items and y.Items.Flashbang)) then
					local k = y.Items.Flashbang;
					if k then
						k.BlindDuration = 0;
						if k.Info then
							k.Info.BlindDuration = 0;
						end;
					end;
				end;
			end;
			l();
			local p = game:GetService("ReplicatedStorage");
			if p then
				p.ChildAdded:Connect(function(k)
					if k.Name == "Modules" then
						k.ChildAdded:Connect(function(k)
							if k.Name == "ItemLibrary" then
								l();
							end;
						end);
					end;
				end);
			end;
		end });
end;
local Ln = game:GetService("Players");
local Pn = game:GetService("ReplicatedStorage");
local xn = game:GetService("HttpService");
local tn = Ln.LocalPlayer;
local Sn = tn.PlayerScripts;
local Vn = Sn.Controllers;
local Xn = require(Pn.Modules:WaitForChild("EnumLibrary", 10));
if Xn then
	Xn:WaitForEnumBuilder();
end;
local Zn = require(Pn.Modules:WaitForChild("CosmeticLibrary", 10));
local En = require(Pn.Modules:WaitForChild("ItemLibrary", 10));
local Jn = require(Vn:WaitForChild("PlayerDataController", 10));
local Un, zn = {}, {};
local wn, qn = nil, nil;
local un = nil;
local function Hn(k, l, p)
	local a = Zn.Cosmetics[k];
	if not a then
		return nil;
	end;
	local y = {};
	for k, l in pairs(a) do
		y[k] = l;
	end;
	y.Name = k;
	y.Type = y.Type or l;
	y.Seed = y.Seed or math.random(1, 1000000);
	if Xn then
		local l, p = pcall(Xn.ToEnum, Xn, k);
		if l and p then
			y.Enum, y.ObjectID = p, y.ObjectID or p;
		end;
	end;
	if p then
		if p.inverted ~= nil then
			y.Inverted = p.inverted;
		end;
		if p.favoritesOnly ~= nil then
			y.OnlyUseFavorites = p.favoritesOnly;
		end;
	end;
	return y;
end;
local Fn = "unlockall/config.json";
local function Qn()
	if not writefile then
		return;
	end;
	pcall(function()
		local k = { equipped = {}, favorites = zn };
		for l, p in pairs(Un) do
			k.equipped[l] = {};
			for p, a in pairs(p) do
				if a and a.Name then
					k.equipped[l][p] = { name = a.Name, seed = a.Seed, inverted = a.Inverted };
				end;
			end;
		end;
		makefolder("unlockall");
		writefile(Fn, xn:JSONEncode(k));
	end);
end;
local function An()
	if not readfile or not isfile or not isfile(Fn) then
		return;
	end;
	pcall(function()
		local k = xn:JSONDecode(readfile(Fn));
		if k.equipped then
			for k, l in pairs(k.equipped) do
				Un[k] = {};
				for l, p in pairs(l) do
					local a = Hn(p.name, l, { inverted = p.inverted });
					if a then
						a.Seed = p.seed;
						Un[k][l] = a;
					end;
				end;
			end;
		end;
		zn = k.favorites or {};
	end);
end;
Zn.OwnsCosmeticNormally = function(k, l, p, a)
		local y = Zn.Cosmetics[p];
		if y and y.Type == "Skin" then
			return true;
		end;
		return false;
	end;
Zn.OwnsCosmeticUniversally = function(k, l, p, a)
		local y = Zn.Cosmetics[p];
		if y and y.Type == "Skin" then
			return true;
		end;
		return false;
	end;
Zn.OwnsCosmeticForWeapon = function(k, l, p, a)
		local y = Zn.Cosmetics[p];
		if y and y.Type == "Skin" then
			return true;
		end;
		return false;
	end;
local In = Zn.OwnsCosmetic;
Zn.OwnsCosmetic = function(k, l, p, a)
		if p:find("MISSING_") then
			return In(k, l, p, a);
		end;
		local y = Zn.Cosmetics[p];
		if y and y.Type == "Skin" then
			return true;
		end;
		return In(k, l, p, a);
	end;
local mn = Jn.Get;
Jn.Get = function(k, l)
		local p = mn(k, l);
		if l == "CosmeticInventory" then
			local k = {};
			if p then
				for l, p in pairs(p) do
					local a = Zn.Cosmetics[l];
					if a and a.Type == "Skin" then
						k[l] = p;
					end;
				end;
			end;
			return setmetatable(k, { __index = function(k, l)
					local p = Zn.Cosmetics[l];
					if p and p.Type == "Skin" then
						return true;
					end;
					return nil;
				end });
		end;
		if l == "FavoritedCosmetics" then
			local k = p and table.clone(p) or {};
			for l, p in pairs(zn) do
				k[l] = k[l] or {};
				for p, a in pairs(p) do
					local y = Zn.Cosmetics[p];
					if y and y.Type == "Skin" then
						k[l][p] = a;
					end;
				end;
			end;
			return k;
		end;
		return p;
	end;
local on = Jn.GetWeaponData;
Jn.GetWeaponData = function(k, l)
		local p = on(k, l);
		if not p then
			return nil;
		end;
		local a = {};
		for k, l in pairs(p) do
			a[k] = l;
		end;
		a.Name = l;
		if Un[l] then
			for k, l in pairs(Un[l]) do
				if k == "Skin" then
					a[k] = l;
				end;
			end;
		end;
		return a;
	end;
local rn;
pcall(function()
	rn = require(Vn:WaitForChild("FighterController", 10));
end);
if hookmetamethod then
	local k = Pn:FindFirstChild("Remotes");
	local l = k and k:FindFirstChild("Data");
	local p = l and l:FindFirstChild("EquipCosmetic");
	local a = l and l:FindFirstChild("FavoriteCosmetic");
	local y = k and k:FindFirstChild("Replication");
	local v = y and y:FindFirstChild("Fighter");
	local j = v and v:FindFirstChild("UseItem");
	if p then
		local k;
		k = hookmetamethod(game, "__namecall", function(l, ...)
				if getnamecallmethod() ~= "FireServer" then
					return k(l, ...);
				end;
				local y = { ... };
				if j and l == j then
					local k = y[1];
					if rn then
						pcall(function()
							local l = rn:GetFighter(tn);
							if l and l.Items then
								for l, p in pairs(l.Items) do
									if p:Get("ObjectID") == k then
										un = p.Name;
										break;
									end;
								end;
							end;
						end);
					end;
				end;
				if l == p then
					local p, a, v, j = y[1], y[2], y[3], y[4] or {};
					if a ~= "Skin" then
						return k(l, ...);
					end;
					if v and (v ~= "None" and v ~= "") then
						local p = Jn:Get("CosmeticInventory");
						if p and rawget(p, v) then
							return k(l, ...);
						end;
					end;
					Un[p] = Un[p] or {};
					if not v or v == "None" or v == "" then
						Un[p][a] = nil;
						if not next(Un[p]) then
							Un[p] = nil;
						end;
					else
						local k = Hn(v, a, { inverted = j.IsInverted, favoritesOnly = j.OnlyUseFavorites });
						if k then
							Un[p][a] = k;
						end;
					end;
					task.defer(function()
						pcall(function()
							Jn.CurrentData:Replicate("WeaponInventory");
						end);
						task.wait(.2);
						Qn();
					end);
					return;
				end;
				if l == a then
					local k = Zn.Cosmetics[y[2]];
					if k and k.Type == "Skin" then
						zn[y[1]] = zn[y[1]] or {};
						zn[y[1]][y[2]] = y[3] or nil;
						Qn();
						task.spawn(function()
							pcall(function()
								Jn.CurrentData:Replicate("FavoritedCosmetics");
							end);
						end);
					end;
					return;
				end;
				return k(l, ...);
			end);
	end;
end;
local Cn;
pcall(function()
	Cn = require(tn.PlayerScripts.Modules.ClientReplicatedClasses.ClientFighter.ClientItem);
end);
if Cn and Cn._CreateViewModel then
	local k = Cn._CreateViewModel;
	Cn._CreateViewModel = function(l, p)
			local a = l.Name;
			local y = l.ClientFighter and l.ClientFighter.Player;
			wn = (y == tn) and a or nil;
			if y == tn and (Un[a] and (Un[a].Skin and p)) then
				local k, y, v = l:ToEnum("Data"), l:ToEnum("Skin"), l:ToEnum("Name");
				if p[k] then
					p[k][y] = Un[a].Skin;
					p[k][v] = Un[a].Skin.Name;
				elseif p.Data then
					p.Data.Skin = Un[a].Skin;
					p.Data.Name = Un[a].Skin.Name;
				end;
			end;
			local v = k(l, p);
			wn = nil;
			return v;
		end;
end;
local Dn = tn.PlayerScripts.Modules.ClientReplicatedClasses.ClientFighter.ClientItem:FindFirstChild("ClientViewModel");
if Dn then
	local k = require(Dn);
	local l = k.new;
	k.new = function(k, p)
			local a = p.ClientFighter and p.ClientFighter.Player;
			local y = wn or p.Name;
			if a == tn and Un[y] then
				local l = require(Pn.Modules.ReplicatedClass);
				local p = l:ToEnum("Data");
				k[p] = k[p] or {};
				local a = Un[y];
				if a.Skin then
					k[p][l:ToEnum("Skin")] = a.Skin;
				end;
			end;
			local v = l(k, p);
			return v;
		end;
end;
local hn = En.GetViewModelImageFromWeaponData;
En.GetViewModelImageFromWeaponData = function(k, l, p)
		if not l then
			return hn(k, l, p);
		end;
		local a = l.Name;
		local y = (l.Skin and (Un[a] and l.Skin == Un[a].Skin)) or (qn == tn and (Un[a] and Un[a].Skin));
		if y and (Un[a] and Un[a].Skin) then
			local l = k.ViewModels[Un[a].Skin.Name];
			if l then
				return l[p and "ImageHighResolution" or "Image"] or l.Image;
			end;
		end;
		return hn(k, l, p);
	end;
local On = Zn.OwnsCosmetic;
Zn.OwnsCosmetic = function(k, l, p, a)
		if p:find("MISSING_") then
			return On(k, l, p, a);
		end;
		local y = Zn.Cosmetics[p];
		if y and ((y.Type == "Charm" or (p:lower()):find("charm"))) then
			return true;
		end;
		return On(k, l, p, a);
	end;
local kt = Jn.Get;
Jn.Get = function(k, l)
		local p = kt(k, l);
		if l == "CosmeticInventory" then
			local k = {};
			if p then
				for l, p in pairs(p) do
					local a = Zn.Cosmetics[l];
					if a and ((a.Type == "Charm" or (l:lower()):find("charm"))) then
						k[l] = p;
					end;
				end;
			end;
			return setmetatable(k, { __index = function(k, l)
					local p = Zn.Cosmetics[l];
					if p and ((p.Type == "Charm" or (l:lower()):find("charm"))) then
						return true;
					end;
					return nil;
				end });
		end;
		if l == "FavoritedCosmetics" then
			local k = p and table.clone(p) or {};
			for l, p in pairs(zn) do
				k[l] = k[l] or {};
				for p, a in pairs(p) do
					local y = Zn.Cosmetics[p];
					if y and ((y.Type == "Charm" or (p:lower()):find("charm"))) then
						k[l][p] = a;
					end;
				end;
			end;
			return k;
		end;
		return p;
	end;
local lt = Jn.GetWeaponData;
Jn.GetWeaponData = function(k, l)
		local p = lt(k, l);
		if not p then
			return nil;
		end;
		local a = {};
		for k, l in pairs(p) do
			a[k] = l;
		end;
		a.Name = l;
		if Un[l] then
			for k, l in pairs(Un[l]) do
				if k == "Charm" then
					a[k] = l;
				end;
			end;
		end;
		return a;
	end;
if hookmetamethod then
	local k = Pn:FindFirstChild("Remotes");
	local l = k and k:FindFirstChild("Data");
	local p = l and l:FindFirstChild("EquipCosmetic");
	local a = l and l:FindFirstChild("FavoriteCosmetic");
	if p then
		local k;
		k = hookmetamethod(game, "__namecall", function(l, ...)
				if getnamecallmethod() ~= "FireServer" then
					return k(l, ...);
				end;
				local y = { ... };
				if l == p then
					local p, a, v, j = y[1], y[2], y[3], y[4] or {};
					if a ~= "Charm" then
						return k(l, ...);
					end;
					if v and (v ~= "None" and v ~= "") then
						local p = Jn:Get("CosmeticInventory");
						if p and rawget(p, v) then
							return k(l, ...);
						end;
					end;
					Un[p] = Un[p] or {};
					if not v or v == "None" or v == "" then
						Un[p][a] = nil;
						if not next(Un[p]) then
							Un[p] = nil;
						end;
					else
						local k = Hn(v, a, { inverted = j.IsInverted, favoritesOnly = j.OnlyUseFavorites });
						if k then
							Un[p][a] = k;
						end;
					end;
					task.defer(function()
						pcall(function()
							Jn.CurrentData:Replicate("WeaponInventory");
						end);
						task.wait(.2);
						Qn();
					end);
					return;
				end;
				if l == a then
					local k = Zn.Cosmetics[y[2]];
					if k and ((k.Type == "Charm" or (y[2]:lower()):find("charm"))) then
						zn[y[1]] = zn[y[1]] or {};
						zn[y[1]][y[2]] = y[3] or nil;
						Qn();
						task.spawn(function()
							pcall(function()
								Jn.CurrentData:Replicate("FavoritedCosmetics");
							end);
						end);
					end;
					return;
				end;
				return k(l, ...);
			end);
	end;
end;
if Cn and Cn._CreateViewModel then
	local k = Cn._CreateViewModel;
	Cn._CreateViewModel = function(l, p)
			local a = l.Name;
			local y = l.ClientFighter and l.ClientFighter.Player;
			wn = (y == tn) and a or nil;
			if y == tn and (Un[a] and (Un[a].Charm and p)) then
				local k, y, v = l:ToEnum("Data"), l:ToEnum("Charm"), l:ToEnum("Name");
				if p[k] then
					p[k][y] = Un[a].Charm;
					p[k][v] = Un[a].Charm.Name;
				elseif p.Data then
					p.Data.Charm = Un[a].Charm;
					p.Data.Name = Un[a].Charm.Name;
				end;
			end;
			local v = k(l, p);
			wn = nil;
			return v;
		end;
end;
if Dn then
	local k = require(Dn);
	if k.GetCharm then
		local l = k.GetCharm;
		k.GetCharm = function(k)
				local p = k.ClientItem and k.ClientItem.Name;
				local a = k.ClientItem and (k.ClientItem.ClientFighter and k.ClientItem.ClientFighter.Player);
				if p and (a == tn and (Un[p] and Un[p].Charm)) then
					return Un[p].Charm;
				end;
				return l(k);
			end;
	end;
	local l = k.new;
	k.new = function(k, p)
			local a = p.ClientFighter and p.ClientFighter.Player;
			local y = wn or p.Name;
			if a == tn and Un[y] then
				local l = require(Pn.Modules.ReplicatedClass);
				local p = l:ToEnum("Data");
				k[p] = k[p] or {};
				local a = Un[y];
				if a.Charm then
					k[p][l:ToEnum("Charm")] = a.Charm;
				end;
			end;
			local v = l(k, p);
			return v;
		end;
end;
local pt = Zn.OwnsCosmetic;
Zn.OwnsCosmetic = function(k, l, p, a)
		if p:find("MISSING_") then
			return pt(k, l, p, a);
		end;
		local y = Zn.Cosmetics[p];
		if y and ((y.Type == "Dance" or y.Type == "Emote" or (p:lower()):find("dance") or (p:lower()):find("emote"))) then
			return true;
		end;
		return pt(k, l, p, a);
	end;
local at = Jn.Get;
Jn.Get = function(k, l)
		local p = at(k, l);
		if l == "CosmeticInventory" then
			local k = {};
			if p then
				for l, p in pairs(p) do
					local a = Zn.Cosmetics[l];
					if a and ((a.Type == "Dance" or a.Type == "Emote" or (l:lower()):find("dance") or (l:lower()):find("emote"))) then
						k[l] = p;
					end;
				end;
			end;
			return setmetatable(k, { __index = function(k, l)
					local p = Zn.Cosmetics[l];
					if p and ((p.Type == "Dance" or p.Type == "Emote" or (l:lower()):find("dance") or (l:lower()):find("emote"))) then
						return true;
					end;
					return nil;
				end });
		end;
		if l == "FavoritedCosmetics" then
			local k = p and table.clone(p) or {};
			for l, p in pairs(zn) do
				k[l] = k[l] or {};
				for p, a in pairs(p) do
					local y = Zn.Cosmetics[p];
					if y and ((y.Type == "Dance" or y.Type == "Emote" or (p:lower()):find("dance") or (p:lower()):find("emote"))) then
						k[l][p] = a;
					end;
				end;
			end;
			return k;
		end;
		return p;
	end;
local yt = Jn.GetWeaponData;
Jn.GetWeaponData = function(k, l)
		local p = yt(k, l);
		if not p then
			return nil;
		end;
		local a = {};
		for k, l in pairs(p) do
			a[k] = l;
		end;
		a.Name = l;
		return a;
	end;
if hookmetamethod then
	local k = Pn:FindFirstChild("Remotes");
	local l = k and k:FindFirstChild("Data");
	local p = l and l:FindFirstChild("EquipCosmetic");
	local a = l and l:FindFirstChild("FavoriteCosmetic");
	if p then
		local k;
		k = hookmetamethod(game, "__namecall", function(l, ...)
				if getnamecallmethod() ~= "FireServer" then
					return k(l, ...);
				end;
				local y = { ... };
				if l == p then
					local p, a, v, j = y[1], y[2], y[3], y[4] or {};
					if a == "Dance" or a == "Emote" or (v and (((v:lower()):find("dance") or (v:lower()):find("emote")))) then
						Un.Dances = Un.Dances or {};
						if not v or v == "None" or v == "" then
							Un.Dances[a] = nil;
						else
							local k = Hn(v, a, { inverted = j.IsInverted, favoritesOnly = j.OnlyUseFavorites });
							if k then
								Un.Dances[a] = k;
							end;
						end;
						task.defer(function()
							pcall(function()
								Jn.CurrentData:Replicate("CosmeticInventory");
							end);
							task.wait(.2);
							Qn();
						end);
						return;
					end;
					return k(l, ...);
				end;
				if l == a then
					local k = Zn.Cosmetics[y[2]];
					if k and ((k.Type == "Dance" or k.Type == "Emote" or (y[2]:lower()):find("dance") or (y[2]:lower()):find("emote"))) then
						zn[y[1]] = zn[y[1]] or {};
						zn[y[1]][y[2]] = y[3] or nil;
						Qn();
						task.spawn(function()
							pcall(function()
								Jn.CurrentData:Replicate("FavoritedCosmetics");
							end);
						end);
					end;
					return;
				end;
				return k(l, ...);
			end);
	end;
end;
local vt;
pcall(function()
	vt = require(Vn:WaitForChild("EmoteController", 10));
	if vt and vt.GetEmotes then
		local k = vt.GetEmotes;
		vt.GetEmotes = function(l)
				local p = k(l);
				for k, l in pairs(Zn.Cosmetics) do
					if l and ((l.Type == "Dance" or l.Type == "Emote" or (k:lower()):find("dance") or (k:lower()):find("emote"))) then
						if not p[k] then
							p[k] = {
									Name = k,
									Type = l.Type,
									ObjectID = l.ObjectID,
									Enum = l.Enum,
								};
						end;
					end;
				end;
				return p;
			end;
	end;
end);
local jt = Zn.OwnsCosmetic;
Zn.OwnsCosmetic = function(k, l, p, a)
		if p:find("MISSING_") then
			return jt(k, l, p, a);
		end;
		local y = Zn.Cosmetics[p];
		if y and ((y.Type == "Wrap" or y.Type == "Wrapping" or (p:lower()):find("wrap"))) then
			return true;
		end;
		return jt(k, l, p, a);
	end;
local st = Jn.Get;
Jn.Get = function(k, l)
		local p = st(k, l);
		if l == "CosmeticInventory" then
			local k = {};
			if p then
				for l, p in pairs(p) do
					local a = Zn.Cosmetics[l];
					if a and ((a.Type == "Wrap" or a.Type == "Wrapping" or (l:lower()):find("wrap"))) then
						k[l] = p;
					end;
				end;
			end;
			return setmetatable(k, { __index = function(k, l)
					local p = Zn.Cosmetics[l];
					if p and ((p.Type == "Wrap" or p.Type == "Wrapping" or (l:lower()):find("wrap"))) then
						return true;
					end;
					return nil;
				end });
		end;
		if l == "FavoritedCosmetics" then
			local k = p and table.clone(p) or {};
			for l, p in pairs(zn) do
				k[l] = k[l] or {};
				for p, a in pairs(p) do
					local y = Zn.Cosmetics[p];
					if y and ((y.Type == "Wrap" or y.Type == "Wrapping" or (p:lower()):find("wrap"))) then
						k[l][p] = a;
					end;
				end;
			end;
			return k;
		end;
		return p;
	end;
local Gt = Jn.GetWeaponData;
Jn.GetWeaponData = function(k, l)
		local p = Gt(k, l);
		if not p then
			return nil;
		end;
		local a = {};
		for k, l in pairs(p) do
			a[k] = l;
		end;
		a.Name = l;
		if Un[l] then
			for k, l in pairs(Un[l]) do
				if k == "Wrap" or k == "Wrapping" then
					a[k] = l;
				end;
			end;
		end;
		return a;
	end;
if hookmetamethod then
	local k = Pn:FindFirstChild("Remotes");
	local l = k and k:FindFirstChild("Data");
	local p = l and l:FindFirstChild("EquipCosmetic");
	local a = l and l:FindFirstChild("FavoriteCosmetic");
	if p then
		local k;
		k = hookmetamethod(game, "__namecall", function(l, ...)
				if getnamecallmethod() ~= "FireServer" then
					return k(l, ...);
				end;
				local y = { ... };
				if l == p then
					local p, a, v, j = y[1], y[2], y[3], y[4] or {};
					if a ~= "Wrap" and a ~= "Wrapping" then
						return k(l, ...);
					end;
					if v and (v ~= "None" and v ~= "") then
						local p = Jn:Get("CosmeticInventory");
						if p and rawget(p, v) then
							return k(l, ...);
						end;
					end;
					Un[p] = Un[p] or {};
					if not v or v == "None" or v == "" then
						Un[p][a] = nil;
						if not next(Un[p]) then
							Un[p] = nil;
						end;
					else
						local k = Hn(v, a, { inverted = j.IsInverted, favoritesOnly = j.OnlyUseFavorites });
						if k then
							Un[p][a] = k;
						end;
					end;
					task.defer(function()
						pcall(function()
							Jn.CurrentData:Replicate("WeaponInventory");
						end);
						task.wait(.2);
						Qn();
					end);
					return;
				end;
				if l == a then
					local k = Zn.Cosmetics[y[2]];
					if k and ((k.Type == "Wrap" or k.Type == "Wrapping" or (y[2]:lower()):find("wrap"))) then
						zn[y[1]] = zn[y[1]] or {};
						zn[y[1]][y[2]] = y[3] or nil;
						Qn();
						task.spawn(function()
							pcall(function()
								Jn.CurrentData:Replicate("FavoritedCosmetics");
							end);
						end);
					end;
					return;
				end;
				return k(l, ...);
			end);
	end;
end;
if Cn and Cn._CreateViewModel then
	local k = Cn._CreateViewModel;
	Cn._CreateViewModel = function(l, p)
			local a = l.Name;
			local y = l.ClientFighter and l.ClientFighter.Player;
			wn = (y == tn) and a or nil;
			if y == tn and (Un[a] and (Un[a].Wrap and p)) then
				local k, y, v = l:ToEnum("Data"), l:ToEnum("Wrap"), l:ToEnum("Name");
				if p[k] then
					p[k][y] = Un[a].Wrap;
					p[k][v] = Un[a].Wrap.Name;
				elseif p.Data then
					p.Data.Wrap = Un[a].Wrap;
					p.Data.Name = Un[a].Wrap.Name;
				end;
			end;
			local v = k(l, p);
			wn = nil;
			return v;
		end;
end;
if Dn then
	local k = require(Dn);
	if k.GetWrap then
		local l = k.GetWrap;
		k.GetWrap = function(k)
				local p = k.ClientItem and k.ClientItem.Name;
				local a = k.ClientItem and (k.ClientItem.ClientFighter and k.ClientItem.ClientFighter.Player);
				if p and (a == tn and (Un[p] and Un[p].Wrap)) then
					return Un[p].Wrap;
				end;
				return l(k);
			end;
	end;
	local l = k.new;
	k.new = function(k, p)
			local a = p.ClientFighter and p.ClientFighter.Player;
			local y = wn or p.Name;
			if a == tn and Un[y] then
				local l = require(Pn.Modules.ReplicatedClass);
				local p = l:ToEnum("Data");
				k[p] = k[p] or {};
				local a = Un[y];
				if a.Wrap then
					k[p][l:ToEnum("Wrap")] = a.Wrap;
				end;
			end;
			local v = l(k, p);
			if a == tn and (Un[y] and (Un[y].Wrap and v._UpdateWrap)) then
				v:_UpdateWrap();
				task.delay(.1, function()
					if not v._destroyed then
						v:_UpdateWrap();
					end;
				end);
			end;
			return v;
		end;
end;
pcall(function()
	local k = require(tn.PlayerScripts.Modules.Pages.ViewProfile);
	if k and k.Fetch then
		local l = k.Fetch;
		k.Fetch = function(k, p)
				qn = p;
				return l(k, p);
			end;
	end;
end);
An();
local gt = jJ:AddTab("hit notif");
local Tt = KJ.config.hitNotifications;
local ct = (gt:AddToggle("HitNotifications", { Text = "hit notifications", Default = false, Callback = function(k)
			Tt.enabled = k;
			if not k and KJ.clearHitNotifications then
				KJ.clearHitNotifications();
			end;
		end })):AddColorPicker("HitNotificationsColor", { Default = Tt.color, Title = "text color", Callback = function(k)
			Tt.color = k;
		end });
local et = gt:AddDependencyBox();
et:AddSlider("HitNotifDuration", {
	Text = "duration",
	Default = Tt.duration,
	Min = 1,
	Max = 8,
	Rounding = 1,
	Compact = true,
	Suffix = "s",
	Callback = function(k)
		Tt.duration = k;
	end,
});
et:AddSlider("HitNotifTextSize", {
	Text = "text size",
	Default = Tt.textSize,
	Min = 10,
	Max = 24,
	Rounding = 0,
	Compact = true,
	Callback = function(k)
		Tt.textSize = k;
	end,
});
et:AddSlider("HitNotifMaxVisible", {
	Text = "max on screen",
	Default = Tt.maxVisible,
	Min = 1,
	Max = 25,
	Rounding = 0,
	Compact = true,
	Callback = function(k)
		Tt.maxVisible = math.clamp(k, 1, 25);
	end,
});
local bt = et:AddDropdown("HitNotifPosition", {
		Text = "position",
		Default = Tt.position or "Top Left",
		Values = {
			"Top Left",
			"Top Center",
			"Top Right",
			"Center Left",
			"Center",
			"Center Right",
			"Bottom Left",
			"Bottom Center",
			"Bottom Right",
			"Custom",
		},
		Callback = function(k)
			Tt.position = k;
			if KJ.applyHitNotifRootPosition then
				KJ.applyHitNotifRootPosition();
			end;
			if b.UpdateDependencyBoxes then
				b:UpdateDependencyBoxes();
			end;
		end,
	});
local ft = et:AddDependencyBox();
ft:AddSlider("HitNotifOffsetX", {
	Text = "custom x",
	Default = Tt.offsetX,
	Min = 0,
	Max = 1200,
	Rounding = 0,
	Compact = true,
	Callback = function(k)
		Tt.offsetX = k;
		if KJ.applyHitNotifRootPosition then
			KJ.applyHitNotifRootPosition();
		end;
	end,
});
ft:AddSlider("HitNotifOffsetY", {
	Text = "custom y",
	Default = Tt.offsetY,
	Min = 0,
	Max = 800,
	Rounding = 0,
	Compact = true,
	Callback = function(k)
		Tt.offsetY = k;
		if KJ.applyHitNotifRootPosition then
			KJ.applyHitNotifRootPosition();
		end;
	end,
});
ft:SetupDependencies({ { bt, "Custom" } });
et:AddSlider("HitNotifStackGap", {
	Text = "stack gap",
	Default = Tt.stackGap,
	Min = 2,
	Max = 24,
	Rounding = 0,
	Compact = true,
	Callback = function(k)
		Tt.stackGap = k;
	end,
});
local Nt = KJ.hitNotifAnimStyles or {
		"fade",
		"slide left",
		"slide right",
		"slide down",
		"bounce",
		"fade bounce",
		"scale",
	};
et:AddDropdown("HitNotifInAnim", {
	Text = "in animation",
	Default = Tt.inAnimation,
	Values = Nt,
	Callback = function(k)
		Tt.inAnimation = k;
	end,
});
et:AddDropdown("HitNotifOutAnim", {
	Text = "out animation",
	Default = Tt.outAnimation,
	Values = Nt,
	Callback = function(k)
		Tt.outAnimation = k;
	end,
});
et:AddSlider("HitNotifInSpeed", {
	Text = "in speed",
	Default = Tt.animInDuration,
	Min = .15,
	Max = 1.5,
	Rounding = 2,
	Compact = true,
	Suffix = "s",
	Callback = function(k)
		Tt.animInDuration = k;
	end,
});
et:AddSlider("HitNotifOutSpeed", {
	Text = "out speed",
	Default = Tt.animOutDuration,
	Min = .15,
	Max = 1.5,
	Rounding = 2,
	Compact = true,
	Suffix = "s",
	Callback = function(k)
		Tt.animOutDuration = k;
	end,
});
et:SetupDependencies({ { ct, true } });
local Kt = F.Misc:AddRightGroupbox("device spoof");
_G.Features.DeviceSpoof = _G.Features.DeviceSpoof or { Enabled = false, CurrentDevice = "Console", LastApplied = 0 };
_G.Features.SlideBoost = _G.Features.SlideBoost or { Enabled = false, Speed = 300 };
local nt = {
		Mobile = { Display = "Mobile", Code = "Touch" },
		Console = { Display = "Console", Code = "Gamepad" },
		VR = { Display = "VR", Code = "VR" },
		PC = { Display = "PC", Code = "MouseKeyboard" },
	};
local function dt()
	local k = r:GetLastInputType();
	if k == Enum.UserInputType.Touch then
		return "Mobile";
	elseif k == Enum.UserInputType.Gamepad1 then
		return "Console";
	elseif k == Enum.UserInputType.VR then
		return "VR";
	else
		return "PC";
	end;
end;
local function Yt()
	if not _G.Features.DeviceSpoof.Enabled then
		return;
	end;
	local k = _G.Features.DeviceSpoof.CurrentDevice;
	local l = nt[k];
	if not l then
		return;
	end;
	local p = tick();
	if p - _G.Features.DeviceSpoof.LastApplied < .5 then
		return;
	end;
	_G.Features.DeviceSpoof.LastApplied = p;
	for k = 1, 3, 1 do
		local p = pcall(function()
				local k = game:GetService("ReplicatedStorage");
				if k:FindFirstChild("Remotes") then
					k = k.Remotes;
					if k:FindFirstChild("Replication") then
						k = k.Replication;
						if k:FindFirstChild("Fighter") then
							k = k.Fighter;
							if k:FindFirstChild("SetControls") then
								k.SetControls:FireServer(l.Code);
								return true;
							end;
						end;
					end;
				end;
				return false;
			end);
		if p then
			break;
		else
			task.wait(.2);
		end;
	end;
end;
local function Mt()
	if not _G.Features.DeviceSpoof.Enabled then
		return;
	end;
	local k = tick();
	if k - _G.Features.DeviceSpoof.LastApplied > 30 then
		Yt();
	end;
end;
Kt:AddToggle("device_spoof", { Text = "enable", Default = _G.Features.DeviceSpoof.Enabled, Callback = function(k)
		_G.Features.DeviceSpoof.Enabled = k;
		if k then
			Yt();
		end;
	end });
Kt:AddDropdown("device_type", {
	Text = "device",
	Default = _G.Features.DeviceSpoof.CurrentDevice,
	Values = {
		"Mobile",
		"Console",
		"VR",
		"PC",
	},
	Callback = function(k)
		_G.Features.DeviceSpoof.CurrentDevice = k;
		if _G.Features.DeviceSpoof.Enabled then
			Yt();
		end;
	end,
});
local it = F.Misc:AddRightGroupbox("slide boost");
it:AddToggle("slide_boost", { Text = "enable", Default = _G.Features.SlideBoost.Enabled, Callback = function(k)
		_G.Features.SlideBoost.Enabled = k;
	end });
it:AddSlider("slide_speed", {
	Text = "slide boost",
	Default = _G.Features.SlideBoost.Speed,
	Min = 50,
	Max = 1000,
	Compact = true,
	Rounding = 0,
	Callback = function(k)
		_G.Features.SlideBoost.Speed = k;
	end,
});
task.spawn(function()
	local k = game:GetService("RunService");
	local l = game:GetService("Players");
	local p = l.LocalPlayer;
	while not p do
		task.wait();
		p = l.LocalPlayer;
	end;
	local a;
	while true do
		local k, l = pcall(function()
				return require(p.PlayerScripts.Controllers.MechanicsController);
			end);
		if k then
			a = l;
			break;
		end;
		task.wait(1);
	end;
	local y;
	y = k.RenderStepped:Connect(function()
			if _G.Features.SlideBoost.Enabled and (a and a.IsSliding) then
				local k = pcall(function()
						a._sliding_velocity.Velocity = a._sliding_velocity.Velocity.Unit * _G.Features.SlideBoost.Speed;
					end);
				if not k then
					local k, l = pcall(function()
							return require(p.PlayerScripts.Controllers.MechanicsController);
						end);
					if k then
						a = l;
					end;
				end;
			end;
		end);
	(game:GetService("Players")).PlayerRemoving:Connect(function(k)
		if k == p then
			y:Disconnect();
		end;
	end);
end);
h.CharacterAdded:Connect(function()
	task.wait(1);
	if _G.Features.DeviceSpoof.Enabled then
		task.wait(.5);
		Yt();
	end;
end);
task.spawn(function()
	(game:GetService("ReplicatedStorage")):WaitForChild("Remotes", 10);
	task.wait(2);
	if _G.Features.DeviceSpoof.Enabled then
		Yt();
	end;
end);
task.spawn(function()
	while true do
		task.wait(10);
		Mt();
	end;
end);
local Rt = game:GetService("Lighting");
_G.LightingSettings = _G.LightingSettings or {
		Enabled = false,
		Ambient = { Enabled = false, Color = Color3.fromRGB(255, 255, 255) },
		OutdoorAmbient = { Enabled = false, Color = Color3.fromRGB(255, 255, 255) },
		GlobalShadows = { Enabled = true },
		ShadowSoftness = { Enabled = false, Value = .2 },
		SunColor = { Enabled = false, Color = Color3.fromRGB(255, 255, 255) },
		ColorShiftTop = { Enabled = false, Color = Color3.fromRGB(255, 255, 255) },
		ColorShiftBottom = { Enabled = false, Color = Color3.fromRGB(255, 255, 255) },
		ClockTime = { Enabled = false, Value = 12 },
		GeographicLatitude = { Enabled = false, Value = 41.8 },
		Fog = {
			Enabled = false,
			Color = Color3.fromRGB(255, 255, 255),
			Start = 0,
			End = 100,
		},
		EnvironmentDiffuseScale = { Enabled = false, Value = 1 },
		EnvironmentSpecularScale = { Enabled = false, Value = 1 },
		ColorCorrection = {
			Enabled = false,
			Contrast = 0,
			Saturation = 0,
			Brightness = 0,
		},
		Bloom = {
			Enabled = false,
			Multiplier = .3,
			Size = 4,
			Threshold = .7,
		},
	};
local function Bt(k, l)
	if not k or not k.Enabled then
		return false;
	end;
	local p = k[l];
	return p ~= nil and p.Enabled == true;
end;
_G.Features.QOL = _G.Features.QOL or {
		MotionBlur = false,
		MotionBlurAmount = 8,
		MotionBlurAmplifier = 2,
		SmoothGameplay = false,
		Shaders = false,
	};
local Wt = F.Misc:AddLeftGroupbox("QOL");
Wt:AddToggle("motion_blur_enabled", { Text = "motion blur", Default = false, Callback = function(k)
		_G.Features.QOL.MotionBlur = k;
		if not k and motionBlur then
			motionBlur.Size = 0;
		end;
	end });
Wt:AddSlider("motion_blur_amount", {
	Text = "blur amount",
	Default = 8,
	Min = 1,
	Max = 20,
	Compact = true,
	Rounding = 0,
	Callback = function(k)
		_G.Features.QOL.MotionBlurAmount = k;
	end,
});
Wt:AddSlider("motion_blur_amplifier", {
	Text = "blur amplifier",
	Default = 2,
	Min = 1,
	Max = 10,
	Compact = true,
	Rounding = 0,
	Callback = function(k)
		_G.Features.QOL.MotionBlurAmplifier = k;
	end,
});
Wt:AddToggle("smooth_gameplay", { Text = "smooth gameplay", Default = false, Callback = function(k)
		_G.Features.QOL.SmoothGameplay = k;
	end });
(Wt:AddToggle("shaders_enabled", { Text = "shaders", Default = false, Callback = function(k)
		_G.Features.QOL.Shaders = k;
		if k then
			applyultramodaurashadersminecraft();
		else
			removeshaders();
		end;
	end })):AddKeyPicker("shaders_key", {
	Text = "Shaders",
	Default = "None",
	NoUI = true,
	SyncToggleState = false,
	Mode = "Toggle",
	Callback = function(k)
		_G.Features.QOL.Shaders = k;
		if k then
			applyultramodaurashadersminecraft();
		else
			removeshaders();
		end;
	end,
});
local Lt = workspace.CurrentCamera;
local Pt = Lt.CFrame.LookVector;
local xt = Instance.new("BlurEffect", Lt);
xt.Name = "QOL_MotionBlur";
xt.Size = 0;
local tt = game:GetService("RunService");
workspace.Changed:Connect(function(k)
	if k == "CurrentCamera" then
		Lt = workspace.CurrentCamera;
		if xt and xt.Parent then
			xt.Parent = Lt;
		else
			xt = Instance.new("BlurEffect", Lt);
			xt.Name = "QOL_MotionBlur";
			xt.Size = 0;
		end;
		Pt = Lt.CFrame.LookVector;
	end;
end);
tt.Heartbeat:Connect(function()
	if not xt or xt.Parent == nil then
		xt = Instance.new("BlurEffect", Lt);
		xt.Name = "QOL_MotionBlur";
		xt.Size = 0;
	end;
	if _G.Features.QOL.MotionBlur then
		local k = ((Lt.CFrame.LookVector - Pt)).Magnitude;
		xt.Size = ((math.abs(k) * _G.Features.QOL.MotionBlurAmount) * _G.Features.QOL.MotionBlurAmplifier) / 2;
	else
		xt.Size = 0;
	end;
	Pt = Lt.CFrame.LookVector;
end);
local St = game:GetService("TweenService");
local Vt = {};
local Xt = nil;
local Zt = {};
local Et = TweenInfo.new(1.4, Enum.EasingStyle.Sine, Enum.EasingDirection.Out);
local Jt = TweenInfo.new(.9, Enum.EasingStyle.Sine, Enum.EasingDirection.In);
local function Ut()
	Vt = {
			Ambient = Rt.Ambient,
			OutdoorAmbient = Rt.OutdoorAmbient,
			Brightness = Rt.Brightness,
			ClockTime = Rt.ClockTime,
			FogEnd = Rt.FogEnd,
			FogStart = Rt.FogStart,
			FogColor = Rt.FogColor,
			ShadowSoftness = Rt.ShadowSoftness,
			EnvironmentDiffuseScale = Rt.EnvironmentDiffuseScale,
			EnvironmentSpecularScale = Rt.EnvironmentSpecularScale,
			ExposureCompensation = Rt.ExposureCompensation,
			ColorShift_Bottom = Rt.ColorShift_Bottom,
			ColorShift_Top = Rt.ColorShift_Top,
		};
	Xt = Rt.Technology;
end;
function applyultramodaurashadersminecraft()
	Ut();
	pcall(function()
		Rt.Technology = Enum.Technology.Future;
	end);
	local k = Rt:FindFirstChildOfClass("Atmosphere");
	if not k then
		k = Instance.new("Atmosphere");
		k.Parent = Rt;
		table.insert(Zt, k);
	end;
	k.Density = .3;
	k.Offset = .07;
	k.Color = Color3.fromRGB(175, 195, 225);
	k.Decay = Color3.fromRGB(85, 125, 195);
	k.Glare = .15;
	k.Haze = .78;
	local l = Rt:FindFirstChildOfClass("Sky");
	if not l then
		l = Instance.new("Sky");
		l.Parent = Rt;
		table.insert(Zt, l);
	end;
	l.SunAngularSize = 7.8;
	l.MoonAngularSize = 9;
	local p = Instance.new("SunRaysEffect");
	p.Name = "QOL_SunRays";
	p.Intensity = .075;
	p.Spread = .9;
	p.Parent = Rt;
	table.insert(Zt, p);
	local a = Instance.new("BloomEffect");
	a.Name = "QOL_Bloom";
	a.Intensity = .14;
	a.Size = 16;
	a.Threshold = 1.85;
	a.Parent = Rt;
	table.insert(Zt, a);
	local y = Instance.new("ColorCorrectionEffect");
	y.Name = "QOL_ColorCorrection";
	y.Brightness = .015;
	y.Contrast = .2;
	y.Saturation = .22;
	y.TintColor = Color3.fromRGB(255, 249, 238);
	y.Parent = Rt;
	table.insert(Zt, y);
	local v = Instance.new("DepthOfFieldEffect");
	v.Name = "QOL_DOF";
	v.FarIntensity = .045;
	v.NearIntensity = .0;
	v.FocusDistance = 80;
	v.InFocusRadius = 58;
	v.Parent = Rt;
	table.insert(Zt, v);
	local j = {};
	local s = _G.LightingSettings;
	if not s or not s.Enabled then
		j.Brightness = 2.1;
	end;
	if not Bt(s, "Ambient") then
		j.Ambient = Color3.fromRGB(20, 23, 35);
	end;
	if not Bt(s, "OutdoorAmbient") then
		j.OutdoorAmbient = Color3.fromRGB(88, 108, 140);
	end;
	if not Bt(s, "ShadowSoftness") then
		j.ShadowSoftness = .2;
	end;
	if not Bt(s, "EnvironmentDiffuseScale") then
		j.EnvironmentDiffuseScale = .95;
	end;
	if not Bt(s, "EnvironmentSpecularScale") then
		j.EnvironmentSpecularScale = .98;
	end;
	if not Bt(s, "ColorShiftTop") then
		j.ColorShift_Top = Color3.fromRGB(12, 16, 32);
	end;
	if not Bt(s, "ColorShiftBottom") then
		j.ColorShift_Bottom = Color3.fromRGB(6, 10, 25);
	end;
	j.ExposureCompensation = .18;
	(St:Create(Rt, Et, j)):Play();
	local function G()
		for k, l in ipairs(workspace:GetDescendants()) do
			if l:IsA("BasePart") then
				pcall(function()
					l.CastShadow = true;
				end);
			end;
		end;
	end;
	task.spawn(G);
	workspace.DescendantAdded:Connect(function(k)
		if _G.Features.QOL.Shaders and k:IsA("BasePart") then
			pcall(function()
				k.CastShadow = true;
			end);
		end;
	end);
end;
function removeshaders()
	for k, l in ipairs(Zt) do
		if l and l.Parent then
			l:Destroy();
		end;
	end;
	Zt = {};
	pcall(function()
		if Xt then
			Rt.Technology = Xt;
		end;
	end);
	if next(Vt) then
		local k = {};
		local l = _G.LightingSettings;
		if not l or not l.Enabled then
			k.Brightness = Vt.Brightness;
		end;
		if not Bt(l, "Ambient") then
			k.Ambient = Vt.Ambient;
		end;
		if not Bt(l, "OutdoorAmbient") then
			k.OutdoorAmbient = Vt.OutdoorAmbient;
		end;
		if not Bt(l, "ShadowSoftness") then
			k.ShadowSoftness = Vt.ShadowSoftness;
		end;
		if not Bt(l, "EnvironmentDiffuseScale") then
			k.EnvironmentDiffuseScale = Vt.EnvironmentDiffuseScale;
		end;
		if not Bt(l, "EnvironmentSpecularScale") then
			k.EnvironmentSpecularScale = Vt.EnvironmentSpecularScale;
		end;
		if not Bt(l, "ColorShiftTop") then
			k.ColorShift_Top = Vt.ColorShift_Top;
		end;
		if not Bt(l, "ColorShiftBottom") then
			k.ColorShift_Bottom = Vt.ColorShift_Bottom;
		end;
		k.ExposureCompensation = Vt.ExposureCompensation;
		(St:Create(Rt, Jt, k)):Play();
		Vt = {};
	end;
end;
task.spawn(function()
	local k = game:GetService("RunService");
	local l = game:GetService("UserInputService");
	local p = game:GetService("Players");
	local a = p.LocalPlayer;
	local y = 30;
	local v = {};
	local j = 0;
	local s = 1;
	for k = 1, y, 1 do
		v[k] = 60;
	end;
	j = 60 * y;
	local G = os.clock();
	k:BindToRenderStep("QOL_SmoothGameplay", Enum.RenderPriority.Input.Value, function()
		local k = os.clock();
		local p = k - G;
		G = k;
		local a = (p > 0) and (1 / p) or 60;
		j = (j - v[s]) + a;
		v[s] = a;
		s = (s % y) + 1;
		local g = j / y;
		if _G.Features.QOL.SmoothGameplay then
			if g < 45 then
				pcall(task.desynchronize);
				pcall(task.synchronize);
			end;
			pcall(function()
				l:GetKeysPressed();
				l:GetMouseDelta();
			end);
		end;
	end);
	p.PlayerRemoving:Connect(function(l)
		if l == a then
			pcall(function()
				k:UnbindFromRenderStep("QOL_SmoothGameplay");
			end);
			if _G.Features.QOL.Shaders then
				removeshaders();
			end;
		end;
	end);
end);
((function()
	local k = game:GetService("Players");
	local l = game:GetService("RunService");
	local p = game:GetService("Workspace");
	local a = game:GetService("GuiService");
	local y = game:GetService("HttpService");
	local v = k.LocalPlayer;
	local j = workspace.CurrentCamera;
	local s = a:GetGuiInset();
	local G = (getgenv()).FinhubGetAspectStretch;
	local g = (getgenv()).FinhubWorldToScreenEsp or (getgenv()).FinhubWorldToScreen;
	local T = (getgenv()).FinhubScreenAnchor;
	_G.Config = _G.Config or {
			Box = {
				MasterEnabled = false,
				Enable = false,
				OutlineColor = Color3.fromRGB(255, 255, 255),
				OutlineColor2 = Color3.fromRGB(255, 255, 255),
				FillColor = Color3.fromRGB(255, 255, 255),
				Filled = { Enable = false, Transparency = .5 },
				Healthbar = { Enable = false, Thickness = 4 },
				HealthLerpColors = { Color1 = Color3.fromRGB(255, 255, 255), Color2 = Color3.fromRGB(255, 255, 255), Color3 = Color3.fromRGB(255, 255, 255) },
			},
			Filled = {
				Enable = false,
				Transparency = .7,
				ColorStart = Color3.fromRGB(255, 255, 255),
				ColorEnd = Color3.fromRGB(255, 255, 255),
				Rotation = 0,
				Animated = false,
				Speed = 1,
			},
			Glow = {
				Enable = false,
				Transparency = .5,
				ColorStart = Color3.fromRGB(255, 255, 255),
				ColorEnd = Color3.fromRGB(255, 255, 255),
				Rotation = 0,
			},
			TextESP = {
				Names = false,
				Distance = false,
				Tools = false,
				NameSize = 8,
				DistanceSize = 8,
				ToolsSize = 8,
				NameColor = Color3.fromRGB(255, 255, 255),
				NameColor2 = Color3.fromRGB(255, 255, 255),
				DistanceColor = Color3.fromRGB(255, 255, 255),
				DistanceColor2 = Color3.fromRGB(255, 255, 255),
				ToolColor = Color3.fromRGB(255, 255, 255),
				ToolColor2 = Color3.fromRGB(255, 255, 255),
			},
			Skeleton = {
				Enable = false,
				Color = Color3.fromRGB(255, 255, 255),
				Color2 = Color3.fromRGB(255, 255, 255),
				Thickness = 1,
			},
			Tracers = {
				Enable = false,
				Color = Color3.fromRGB(255, 255, 255),
				Color2 = Color3.fromRGB(255, 255, 255),
				Thickness = 1,
				FromBottom = true,
			},
			Chams = {
				Enable = false,
				Color = Color3.fromRGB(255, 255, 255),
				Color2 = Color3.fromRGB(255, 255, 255),
				Transparency = 0,
			},
			ESP = { HighRefresh = true, HealthLerpSpeed = 32, TargetHz = 240 },
		};
	_G.ESPObjects = _G.ESPObjects or {};
	local c = _G.Config.Box;
	local e = _G.ESPObjects;
	local b = Instance.new("ScreenGui");
	b.Name = "ESP";
	b.DisplayOrder = 9000000000.0;
	b.ResetOnSpawn = false;
	b.IgnoreGuiInset = true;
	b.ZIndexBehavior = Enum.ZIndexBehavior.Sibling;
	b.Parent = game.CoreGui;
	local f = {
			{ "UpperTorso", "Head" },
			{ "UpperTorso", "LowerTorso" },
			{ "UpperTorso", "LeftUpperArm" },
			{ "LeftUpperArm", "LeftLowerArm" },
			{ "LeftLowerArm", "LeftHand" },
			{ "UpperTorso", "RightUpperArm" },
			{ "RightUpperArm", "RightLowerArm" },
			{ "RightLowerArm", "RightHand" },
			{ "LowerTorso", "LeftUpperLeg" },
			{ "LeftUpperLeg", "LeftLowerLeg" },
			{ "LeftLowerLeg", "LeftFoot" },
			{ "LowerTorso", "RightUpperLeg" },
			{ "RightUpperLeg", "RightLowerLeg" },
			{ "RightLowerLeg", "RightFoot" },
		};
	local N = {
			{ "Torso", "Head" },
			{ "Torso", "Left Arm" },
			{ "Torso", "Right Arm" },
			{ "Torso", "Left Leg" },
			{ "Torso", "Right Leg" },
		};
	local K = {};
	local n = pcall(function()
			local k = "esp_font.ttf";
			if not isfile(k) then
				writefile(k, game:HttpGet("https://github.com/i77lhm/storage/raw/refs/heads/main/fonts/smallest_pixel-7.ttf"));
			end;
			if isfile("esp_font.font") then
				delfile("esp_font.font");
			end;
			local l = { name = "ESPFont", faces = { {
							name = "Regular",
							weight = 400,
							style = "normal",
							assetId = getcustomasset(k),
						} } };
			writefile("esp_font.font", y:JSONEncode(l));
			K.main = Font.new(getcustomasset("esp_font.font"));
		end);
	if not n then
		K.main = Font.fromEnum(Enum.Font.Code);
	end;
	local d = 0;
	local function Y(k, l, p)
		return Color3.new(k.R + ((l.R - k.R)) * p, k.G + ((l.G - k.G)) * p, k.B + ((l.B - k.B)) * p);
	end;
	local function M(k, l, p)
		if not k then
			return;
		end;
		local a = k:FindFirstChild("EspTextGradient");
		if not a then
			a = Instance.new("UIGradient");
			a.Name = "EspTextGradient";
			a.Rotation = 0;
			a.Parent = k;
		end;
		a.Color = ColorSequence.new({ ColorSequenceKeypoint.new(0, l), ColorSequenceKeypoint.new(1, p) });
		k.TextColor3 = Color3.fromRGB(255, 255, 255);
	end;
	local function i(k)
		local l = c.HealthLerpColors or { Color1 = Color3.fromRGB(0, 255, 0), Color2 = Color3.fromRGB(255, 255, 0), Color3 = Color3.fromRGB(255, 0, 0) };
		if k > .5 then
			return Y(l.Color2, l.Color1, ((k - .5)) * 2);
		else
			return Y(l.Color3, l.Color2, k * 2);
		end;
	end;
	local function R(k)
		if not k or k == v then
			return true;
		end;
		local l = v:GetAttribute("TeamID");
		local p = k:GetAttribute("TeamID");
		if l ~= nil and p ~= nil then
			return l == p;
		end;
		return v.Team and (k.Team and v.Team == k.Team);
	end;
	local function B(k, l)
		local p = k:FindFirstChild(l);
		if p and p:IsA("BasePart") then
			return p;
		end;
		local a = {
				["Left Arm"] = { "LeftArm", "LeftUpperArm" },
				["Right Arm"] = { "RightArm", "RightUpperArm" },
				["Left Leg"] = { "LeftLeg", "LeftUpperLeg" },
				["Right Leg"] = { "RightLeg", "RightUpperLeg" },
				Torso = { "UpperTorso", "LowerTorso" },
			};
		for l, a in ipairs(a[l] or {}) do
			p = k:FindFirstChild(a);
			if p and p:IsA("BasePart") then
				return p;
			end;
		end;
		return nil;
	end;
	local function W(k)
		local l = k.Character;
		if not l then
			return false;
		end;
		local p = l:FindFirstChild("BodyEffects");
		if p then
			local k = p:FindFirstChild("K.O") or p:FindFirstChild("KO");
			return k and k.Value;
		end;
		return false;
	end;
	local function L(k)
		local l = k.Character;
		local p = v.Character;
		if l and p then
			local k = l:FindFirstChild("HumanoidRootPart");
			local a = p:FindFirstChild("HumanoidRootPart");
			if k and a then
				return ((k.Position - a.Position)).Magnitude;
			end;
		end;
		return math.huge;
	end;
	local function P(k)
		return NJ(k);
	end;
	local function x(k)
		local l = Instance.new("TextLabel");
		l.Parent = k;
		l.Size = UDim2.new(0, 200, 0, 20);
		l.BackgroundTransparency = 1;
		l.TextColor3 = Color3.fromRGB(255, 255, 255);
		l.TextStrokeTransparency = 0;
		l.TextScaled = false;
		l.TextSize = 6;
		l.TextStrokeColor3 = Color3.fromRGB(0, 0, 0);
		l.FontFace = K.main;
		l.TextXAlignment = Enum.TextXAlignment.Left;
		l.TextYAlignment = Enum.TextYAlignment.Top;
		return l;
	end;
	local function t(k)
		local l = Instance.new("Highlight");
		l.Name = "ESPHighlight";
		l.Adornee = k;
		l.FillColor = _G.Config.Chams.Color;
		l.OutlineColor = _G.Config.Chams.Color2 or _G.Config.Chams.Color;
		l.FillTransparency = _G.Config.Chams.Transparency;
		l.OutlineTransparency = math.clamp(_G.Config.Chams.Transparency, 0, 1);
		l.DepthMode = Enum.HighlightDepthMode.AlwaysOnTop;
		l.Parent = k;
		return l;
	end;
	local function S(k)
		if e[k] then
			return;
		end;
		local l = {};
		l.box = {};
		l.box.square = Drawing.new("Square");
		l.box.inline = Drawing.new("Square");
		l.box.outline = Drawing.new("Square");
		l.filled = Instance.new("Frame");
		l.filled.BackgroundColor3 = Color3.new(1, 1, 1);
		l.filled.BackgroundTransparency = _G.Config.Filled.Transparency;
		l.filled.BorderSizePixel = 0;
		l.filled.Visible = false;
		l.filled.ZIndex = 2;
		l.filled.Parent = b;
		l.filled_gradient = Instance.new("UIGradient");
		l.filled_gradient.Color = ColorSequence.new({ ColorSequenceKeypoint.new(0, _G.Config.Filled.ColorStart), ColorSequenceKeypoint.new(1, _G.Config.Filled.ColorEnd) });
		l.filled_gradient.Rotation = 0;
		l.filled_gradient.Parent = l.filled;
		l.glow = Instance.new("ImageLabel");
		l.glow.Image = "rbxassetid://110204605000367";
		l.glow.ScaleType = Enum.ScaleType.Slice;
		l.glow.SliceCenter = Rect.new(Vector2.new(21, 21), Vector2.new(79, 79));
		l.glow.AutomaticSize = Enum.AutomaticSize.XY;
		l.glow.ImageTransparency = _G.Config.Glow.Transparency;
		l.glow.ResampleMode = Enum.ResamplerMode.Pixelated;
		l.glow.BackgroundTransparency = 1;
		l.glow.BorderSizePixel = 0;
		l.glow.BackgroundColor3 = Color3.fromRGB(255, 255, 255);
		l.glow.ZIndex = 1;
		l.glow.Visible = false;
		l.glow.Parent = b;
		l.glow_gradient = Instance.new("UIGradient");
		l.glow_gradient.Rotation = _G.Config.Glow.Rotation;
		l.glow_gradient.Color = ColorSequence.new({ ColorSequenceKeypoint.new(0, _G.Config.Glow.ColorStart), ColorSequenceKeypoint.new(1, _G.Config.Glow.ColorEnd) });
		l.glow_gradient.Transparency = NumberSequence.new({ NumberSequenceKeypoint.new(0, 0), NumberSequenceKeypoint.new(1, 0) });
		l.glow_gradient.Parent = l.glow;
		local p = Instance.new("UIPadding");
		p.PaddingTop = UDim.new(0, 21);
		p.PaddingBottom = UDim.new(0, 20);
		p.PaddingLeft = UDim.new(0, 21);
		p.PaddingRight = UDim.new(0, 20);
		p.Parent = l.glow;
		l.bars = {};
		l.bars.hp_bars = {};
		l.bars.hp_outline = Drawing.new("Square");
		l.bars.last_hp = 1;
		for k = 1, 50, 1 do
			l.bars.hp_bars[k] = Drawing.new("Line");
		end;
		l.skeleton = { lines = {}, outlines = {} };
		for k = 1, 15, 1 do
			local p = Drawing.new("Line");
			p.Color = Color3.new(0, 0, 0);
			p.Thickness = 3;
			p.Visible = false;
			l.skeleton.outlines[k] = p;
			local a = Drawing.new("Line");
			a.Color = Color3.new(1, 1, 1);
			a.Thickness = 1;
			a.Visible = false;
			l.skeleton.lines[k] = a;
		end;
		local a = Instance.new("ScreenGui", game.CoreGui);
		local y = Instance.new("ScreenGui", game.CoreGui);
		local v = Instance.new("ScreenGui", game.CoreGui);
		l.text = {};
		l.text.studs = x(a);
		l.text.tool = x(v);
		l.text.name = x(y);
		l.Tracer = Drawing.new("Line");
		l.Tracer.Visible = false;
		l.Tracer.Color = _G.Config.Tracers.Color or Color3.fromRGB(255, 255, 255);
		l.Tracer.Thickness = _G.Config.Tracers.Thickness or 1;
		e[k] = l;
	end;
	local function V(k)
		if e[k] then
			local l = e[k];
			if l.box then
				l.box.square:Remove();
				l.box.outline:Remove();
				l.box.inline:Remove();
			end;
			if l.filled then
				l.filled:Destroy();
			end;
			if l.glow then
				l.glow:Destroy();
			end;
			if l.text then
				if l.text.studs then
					l.text.studs.Parent:Destroy();
				end;
				if l.text.tool then
					l.text.tool.Parent:Destroy();
				end;
				if l.text.name then
					l.text.name.Parent:Destroy();
				end;
			end;
			if l.bars then
				l.bars.hp_outline:Remove();
				for k, l in pairs(l.bars.hp_bars) do
					l:Remove();
				end;
			end;
			if l.skeleton then
				for k, l in pairs(l.skeleton.lines) do
					l:Remove();
				end;
				for k, l in pairs(l.skeleton.outlines) do
					l:Remove();
				end;
			end;
			if l.Tracer then
				l.Tracer:Remove();
			end;
			if l.Cham then
				l.Cham:Destroy();
				l.Cham = nil;
			end;
			e[k] = nil;
		end;
	end;
	local function X(k)
		if not e[k] then
			return;
		end;
		local l = e[k];
		if l.box then
			l.box.square.Visible = false;
			l.box.outline.Visible = false;
			l.box.inline.Visible = false;
		end;
		if l.filled then
			l.filled.Visible = false;
		end;
		if l.glow then
			l.glow.Visible = false;
		end;
		if l.text then
			l.text.studs.Visible = false;
			l.text.tool.Visible = false;
			l.text.name.Visible = false;
		end;
		if l.bars then
			l.bars.hp_outline.Visible = false;
			for k, l in pairs(l.bars.hp_bars) do
				l.Visible = false;
			end;
		end;
		if l.skeleton then
			for k, l in pairs(l.skeleton.lines) do
				l.Visible = false;
			end;
			for k, l in pairs(l.skeleton.outlines) do
				l.Visible = false;
			end;
		end;
		if l.Tracer then
			l.Tracer.Visible = false;
		end;
		if l.Cham then
			l.Cham.Enabled = false;
		end;
	end;
	local function Z(k, l, p)
		if not _G.Config.Chams.Enable then
			if l.Cham then
				l.Cham.Enabled = false;
			end;
			return;
		end;
		if not l.Cham or not l.Cham.Parent then
			l.Cham = t(p);
		end;
		if l.Cham then
			l.Cham.Enabled = true;
			l.Cham.FillColor = _G.Config.Chams.Color;
			l.Cham.OutlineColor = _G.Config.Chams.Color2 or _G.Config.Chams.Color;
			l.Cham.FillTransparency = _G.Config.Chams.Transparency;
			l.Cham.OutlineTransparency = math.clamp(_G.Config.Chams.Transparency, 0, 1);
		end;
	end;
	local function E(k, l, p, a, y, v)
		if not _G.Config.Skeleton.Enable then
			for k, l in pairs(l.skeleton.lines) do
				l.Visible = false;
			end;
			for k, l in pairs(l.skeleton.outlines) do
				l.Visible = false;
			end;
			return;
		end;
		local j = (a.RigType == Enum.HumanoidRigType.R15) and f or N;
		local s = _G.Config.Skeleton.Color or Color3.fromRGB(255, 255, 255);
		local G = _G.Config.Skeleton.Color2 or s;
		local T = _G.Config.Skeleton.Thickness or 1;
		for k = 1, 15, 1 do
			local a = j[k];
			local y = l.skeleton.lines[k];
			local v = l.skeleton.outlines[k];
			if a then
				local k = B(p, a[1]);
				local l = B(p, a[2]);
				if k and l then
					local p, a = g(k.Position);
					local j, c = g(l.Position);
					if a and c then
						local k = Vector2.new(p.X, p.Y);
						local l = Vector2.new(j.X, j.Y);
						local a = ((k.X + l.X)) * .5;
						local g = a >= k.X and Y(s, G, math.clamp(((a - k.X)) / math.max(math.abs(l.X - k.X), 1), 0, 1)) or s;
						v.Visible = true;
						v.From = k;
						v.To = l;
						v.Thickness = T + 2;
						v.Color = Color3.new(0, 0, 0);
						y.Visible = true;
						y.From = k;
						y.To = l;
						y.Color = g;
						y.Thickness = T;
					else
						y.Visible = false;
						v.Visible = false;
					end;
				else
					y.Visible = false;
					v.Visible = false;
				end;
			else
				y.Visible = false;
				v.Visible = false;
			end;
		end;
	end;
	local function J(k, l, p, a, y, v)
		if not ((c.Healthbar and c.Healthbar.Enable)) then
			l.bars.hp_outline.Visible = false;
			for k, l in pairs(l.bars.hp_bars) do
				l.Visible = false;
			end;
			return;
		end;
		local j = math.clamp(p.Health / p.MaxHealth, 0, 1);
		local s = l.bars.last_hp or j;
		local G = _G.Config.ESP or {};
		local g = G.HealthLerpSpeed or 32;
		if G.HighRefresh ~= false then
			v = v or (1 / ((G.TargetHz or 240)));
			local k = s + ((j - s)) * math.min(1, v * g);
			l.bars.last_hp = k;
		else
			l.bars.last_hp = s + ((j - s)) * .05;
		end;
		local T = l.bars.last_hp;
		local e = math.ceil(a.Y * T);
		local b = y.X - 5;
		local f = y.Y + ((a.Y - e));
		l.bars.hp_outline.Visible = true;
		l.bars.hp_outline.Position = Vector2.new(b - 1, y.Y - 1);
		l.bars.hp_outline.Size = Vector2.new(3, a.Y + 2);
		l.bars.hp_outline.Color = Color3.new(0, 0, 0);
		l.bars.hp_outline.Filled = false;
		local N = math.max(math.min(math.floor(e / 2), 50), 10);
		for k = 1, 50, 1 do
			local p = l.bars.hp_bars[k];
			if k <= N then
				local l = e / N;
				local y = ((k - 1)) * l;
				local v = 1 - (((y + ((a.Y - e)))) / a.Y);
				p.Visible = true;
				p.From = Vector2.new(b, f + y);
				p.To = Vector2.new(b + 1, f + y);
				p.Color = i(v);
				p.Thickness = math.max(l, 1);
			else
				p.Visible = false;
			end;
		end;
	end;
	local function U(k, l, p, a)
		if not ((c.MasterEnabled and c.Enable)) then
			l.box.square.Visible = false;
			l.box.outline.Visible = false;
			l.box.inline.Visible = false;
			return;
		end;
		local y = math.max(math.floor(p.X), 4);
		local v = math.max(math.floor(p.Y), 8);
		p = Vector2.new(y, v);
		a = Vector2.new(math.floor(a.X), math.floor(a.Y));
		l.box.square.Visible = true;
		l.box.square.Position = a;
		l.box.square.Size = p;
		local j = c.OutlineColor2 or c.OutlineColor;
		l.box.square.Color = Y(c.OutlineColor, j, .5);
		l.box.square.Thickness = 1;
		l.box.square.Filled = false;
		l.box.outline.Visible = true;
		l.box.outline.Position = a - Vector2.new(1, 1);
		l.box.outline.Size = p + Vector2.new(2, 2);
		l.box.outline.Color = Color3.new(0, 0, 0);
		l.box.outline.Thickness = 1;
		l.box.outline.Filled = false;
		l.box.inline.Visible = true;
		l.box.inline.Position = a + Vector2.new(1, 1);
		l.box.inline.Size = Vector2.new(math.max(y - 2, 2), math.max(v - 2, 4));
		l.box.inline.Color = Color3.new(0, 0, 0);
		l.box.inline.Thickness = 1;
		l.box.inline.Filled = false;
	end;
	local function z(k, l, p, a)
		local y = _G.Config.Filled;
		if not y.Enable then
			l.filled.Visible = false;
			return;
		end;
		l.filled.Visible = true;
		l.filled.BackgroundTransparency = y.Transparency;
		l.filled.Position = UDim2.fromOffset(a.X, a.Y);
		l.filled.Size = UDim2.fromOffset(p.X, p.Y);
		l.filled_gradient.Color = ColorSequence.new({ ColorSequenceKeypoint.new(0, y.ColorStart), ColorSequenceKeypoint.new(1, y.ColorEnd) });
		if y.Animated then
			l.filled_gradient.Rotation = (math.sin(d * y.Speed) * 90) + y.Rotation;
		else
			l.filled_gradient.Rotation = y.Rotation;
		end;
	end;
	local function w(k, l, p, a)
		local y = _G.Config.Glow;
		if not y.Enable or not ((c.MasterEnabled and c.Enable)) then
			l.glow.Visible = false;
			return;
		end;
		l.glow.Visible = true;
		l.glow.ImageTransparency = y.Transparency;
		l.glow.ImageColor3 = y.ColorStart;
		l.glow.Position = UDim2.fromOffset(a.X - 21, a.Y - 21);
		l.glow.Size = UDim2.fromOffset(p.X + 42, p.Y + 42);
		l.glow_gradient.Color = ColorSequence.new({ ColorSequenceKeypoint.new(0, y.ColorStart), ColorSequenceKeypoint.new(1, y.ColorEnd) });
		l.glow_gradient.Rotation = 0;
	end;
	local function q(k, l, p, a, y)
		local v = a.Y - s.Y;
		if _G.Config.TextESP and _G.Config.TextESP.Names then
			l.text.name.Visible = true;
			l.text.name.Position = UDim2.new(0, (a.X + (p.X / 2)) - 100, 0, v - 15);
			l.text.name.Text = k.Name;
			l.text.name.TextSize = _G.Config.TextESP.NameSize or 13;
			M(l.text.name, _G.Config.TextESP.NameColor or Color3.fromRGB(255, 255, 255), _G.Config.TextESP.NameColor2 or _G.Config.TextESP.NameColor or Color3.fromRGB(255, 255, 255));
			l.text.name.TextXAlignment = Enum.TextXAlignment.Center;
		else
			l.text.name.Visible = false;
		end;
		if _G.Config.TextESP and _G.Config.TextESP.Tools then
			l.text.tool.Visible = true;
			l.text.tool.Position = UDim2.new(0, (a.X + p.X) + 5, 0, v);
			l.text.tool.Text = P(k);
			l.text.tool.TextSize = _G.Config.TextESP.ToolsSize or 11;
			M(l.text.tool, _G.Config.TextESP.ToolColor or Color3.fromRGB(255, 255, 255), _G.Config.TextESP.ToolColor2 or _G.Config.TextESP.ToolColor or Color3.fromRGB(255, 255, 255));
			l.text.tool.TextXAlignment = Enum.TextXAlignment.Left;
		else
			l.text.tool.Visible = false;
		end;
		if _G.Config.TextESP and _G.Config.TextESP.Distance then
			l.text.studs.Visible = true;
			l.text.studs.Position = UDim2.new(0, (a.X + (p.X / 2)) - 100, 0, (v + p.Y) + 2);
			l.text.studs.Text = string.format("%.0f studs", y);
			l.text.studs.TextSize = _G.Config.TextESP.DistanceSize or 11;
			M(l.text.studs, _G.Config.TextESP.DistanceColor or Color3.fromRGB(255, 255, 255), _G.Config.TextESP.DistanceColor2 or _G.Config.TextESP.DistanceColor or Color3.fromRGB(255, 255, 255));
			l.text.studs.TextXAlignment = Enum.TextXAlignment.Center;
		else
			l.text.studs.Visible = false;
		end;
	end;
	local function u(k, l, p)
		if not ((_G.Config.Tracers and _G.Config.Tracers.Enable)) then
			l.Tracer.Visible = false;
			return;
		end;
		local a, y = g(p.Position);
		if y then
			l.Tracer.Visible = true;
			l.Tracer.To = Vector2.new(a.X, a.Y);
			l.Tracer.From = ((_G.Config.Tracers.FromBottom == nil or _G.Config.Tracers.FromBottom)) and T(.5, 1) or T(.5, .5);
			l.Tracer.Color = Y(_G.Config.Tracers.Color or Color3.fromRGB(255, 255, 255), _G.Config.Tracers.Color2 or _G.Config.Tracers.Color or Color3.fromRGB(255, 255, 255), .5);
			l.Tracer.Thickness = _G.Config.Tracers.Thickness or 1;
		else
			l.Tracer.Visible = false;
		end;
	end;
	local function H(k)
		local l = workspace.CurrentCamera;
		if not l or not k then
			return nil, nil;
		end;
		local p, a, y, v = math.huge, math.huge, -math.huge, -math.huge;
		local j = false;
		for k, s in ipairs(k:GetDescendants()) do
			if s:IsA("BasePart") and (s.Transparency < 1 and s.Size.Magnitude > 0) then
				local k = s.CFrame;
				local G = s.Size;
				local T = {
						k * Vector3.new(-G.X / 2, -G.Y / 2, -G.Z / 2),
						k * Vector3.new(G.X / 2, -G.Y / 2, -G.Z / 2),
						k * Vector3.new(-G.X / 2, G.Y / 2, -G.Z / 2),
						k * Vector3.new(G.X / 2, G.Y / 2, -G.Z / 2),
						k * Vector3.new(-G.X / 2, -G.Y / 2, G.Z / 2),
						k * Vector3.new(G.X / 2, -G.Y / 2, G.Z / 2),
						k * Vector3.new(-G.X / 2, G.Y / 2, G.Z / 2),
						k * Vector3.new(G.X / 2, G.Y / 2, G.Z / 2),
					};
				for k, s in ipairs(T) do
					local G, T = g(s, l);
					if T then
						j = true;
						p = math.min(p, G.X);
						a = math.min(a, G.Y);
						y = math.max(y, G.X);
						v = math.max(v, G.Y);
					end;
				end;
			end;
		end;
		if not j then
			return nil, nil;
		end;
		return Vector2.new(math.max(y - p, 4), math.max(v - a, 8)), Vector2.new(p, a);
	end;
	local function Q(k, l)
		if not k or not e[k] then
			return;
		end;
		if not c.MasterEnabled then
			X(k);
			return;
		end;
		if k == v or R(k) then
			X(k);
			return;
		end;
		local p = k.Character;
		if not p or W(k) then
			X(k);
			return;
		end;
		local a = p:FindFirstChild("HumanoidRootPart");
		local y = p:FindFirstChildWhichIsA("Humanoid");
		if not a or not y then
			X(k);
			return;
		end;
		local j, s = g(a.Position);
		if not s then
			X(k);
			return;
		end;
		local G = L(k);
		local T, b = H(p);
		local f, N;
		if T and b then
			f = Vector2.new(math.max(math.floor(T.X), 4), math.max(math.floor(T.Y), 8));
			N = Vector2.new(math.floor(b.X), math.floor(b.Y));
		else
			local l = p:FindFirstChild("Head");
			local y = (l and l.Position + Vector3.new(0, .5, 0)) or (a.Position + Vector3.new(0, 3, 0));
			local v = a.Position - Vector3.new(0, 3, 0);
			local s, G = g(y);
			local T, c = g(v);
			if not G or not c then
				X(k);
				return;
			end;
			local e = math.abs(T.Y - s.Y);
			local b = math.max(e * .55, 8);
			f = Vector2.new(math.max(math.floor(b), 4), math.max(math.floor(math.max(e, 10)), 8));
			N = Vector2.new(math.floor(j.X - f.X / 2), math.floor(math.min(s.Y, T.Y)));
		end;
		local K = e[k];
		local n = _G.Config;
		z(k, K, f, N);
		U(k, K, f, N);
		w(k, K, f, N);
		if n.Skeleton and n.Skeleton.Enable then
			E(k, K, p, y, f, N);
		else
			for k, l in pairs(K.skeleton.lines) do
				l.Visible = false;
			end;
			for k, l in pairs(K.skeleton.outlines) do
				l.Visible = false;
			end;
		end;
		J(k, K, y, f, N, l);
		q(k, K, f, N, G);
		if n.Tracers and n.Tracers.Enable then
			u(k, K, a);
		elseif K.Tracer then
			K.Tracer.Visible = false;
		end;
		if n.Chams and n.Chams.Enable then
			Z(k, K, p);
		elseif K.Cham then
			K.Cham.Enabled = false;
		end;
	end;
	local A = "FinhubESPUpdate";
	local I = false;
	local function m(l)
		l = l or (1 / ((_G.Config.ESP and _G.Config.ESP.TargetHz or 240)));
		local p = workspace.CurrentCamera;
		if p then
			j = p;
		end;
		d = os.clock();
		if not c.MasterEnabled then
			if I then
				I = false;
				for k in pairs(e) do
					X(k);
				end;
			end;
			return;
		end;
		I = true;
		for k, p in ipairs(k:GetPlayers()) do
			if p ~= v then
				Q(p, l);
			end;
		end;
	end;
	local o = pcall(function()
			l:UnbindFromRenderStep(A);
			l:BindToRenderStep(A, Enum.RenderPriority.Camera.Value + 1, m);
		end);
	if not o then
		l.RenderStepped:Connect(m);
	end;
	for k, l in ipairs(k:GetPlayers()) do
		if l ~= v then
			S(l);
		end;
	end;
	k.PlayerAdded:Connect(function(k)
		if k ~= v then
			S(k);
		end;
	end);
	k.PlayerRemoving:Connect(V);
	local r = F.Visuals:AddLeftGroupbox("esp");
	r:AddToggle("box_enabled", { Text = "enable", Default = c.MasterEnabled or false, Callback = function(k)
			c.MasterEnabled = k;
		end });
	local C = r:AddDependencyBox();
	((C:AddToggle("box_enabled2", { Text = "boxes", Default = c.Enable or false, Callback = function(k)
			c.Enable = k;
		end })):AddColorPicker("box_outline_color", {
		Title = "box color 1",
		Default = c.OutlineColor or Color3.fromRGB(255, 255, 255),
		Transparency = false,
		Callback = function(k)
			c.OutlineColor = k;
		end,
	})):AddColorPicker("box_outline_color2", {
		Title = "box color 2",
		Default = c.OutlineColor2 or Color3.fromRGB(255, 255, 255),
		Transparency = false,
		Callback = function(k)
			c.OutlineColor2 = k;
		end,
	});
	((C:AddToggle("box_filled", { Text = "box fill", Default = _G.Config.Filled.Enable or false, Callback = function(k)
			_G.Config.Filled.Enable = k;
		end })):AddColorPicker("box_filled_color_start", {
		Title = "fill color start",
		Default = _G.Config.Filled.ColorStart or Color3.fromRGB(255, 255, 255),
		Transparency = false,
		Callback = function(k)
			_G.Config.Filled.ColorStart = k;
		end,
	})):AddColorPicker("box_filled_color_end", {
		Title = "fill color end",
		Default = _G.Config.Filled.ColorEnd or Color3.fromRGB(255, 255, 255),
		Transparency = false,
		Callback = function(k)
			_G.Config.Filled.ColorEnd = k;
		end,
	});
	C:AddToggle("box_filled_animated", { Text = "fill rotation", Default = _G.Config.Filled.Animated or false, Callback = function(k)
			_G.Config.Filled.Animated = k;
		end });
	((C:AddToggle("box_glow", { Text = "box glow", Default = _G.Config.Glow.Enable or false, Callback = function(k)
			_G.Config.Glow.Enable = k;
		end })):AddColorPicker("box_glow_color_start", {
		Title = "glow color start",
		Default = _G.Config.Glow.ColorStart or Color3.fromRGB(255, 255, 255),
		Transparency = false,
		Callback = function(k)
			_G.Config.Glow.ColorStart = k;
		end,
	})):AddColorPicker("box_glow_color_end", {
		Title = "glow color end",
		Default = _G.Config.Glow.ColorEnd or Color3.fromRGB(255, 255, 255),
		Transparency = false,
		Callback = function(k)
			_G.Config.Glow.ColorEnd = k;
		end,
	});
	(((C:AddToggle("healthbar", { Text = "healthbar", Default = (c.Healthbar and c.Healthbar.Enable) or false, Callback = function(k)
			if not c.Healthbar then
				c.Healthbar = { Enable = false, Thickness = 4 };
			end;
			c.Healthbar.Enable = k;
		end })):AddColorPicker("ColorPicker121212", {
		Default = (c.HealthLerpColors and c.HealthLerpColors.Color1) or Color3.fromRGB(255, 255, 255),
		Title = "high health",
		Transparency = 0,
		Callback = function(k)
			if not c.HealthLerpColors then
				c.HealthLerpColors = { Color1 = Color3.fromRGB(255, 255, 255), Color2 = Color3.fromRGB(255, 255, 255), Color3 = Color3.fromRGB(255, 255, 255) };
			end;
			c.HealthLerpColors.Color1 = k;
		end,
	})):AddColorPicker("ColorPicker21", {
		Default = (c.HealthLerpColors and c.HealthLerpColors.Color2) or Color3.fromRGB(255, 255, 255),
		Title = "mid health",
		Transparency = 0,
		Callback = function(k)
			if not c.HealthLerpColors then
				c.HealthLerpColors = { Color1 = Color3.fromRGB(255, 255, 255), Color2 = Color3.fromRGB(255, 255, 255), Color3 = Color3.fromRGB(255, 255, 255) };
			end;
			c.HealthLerpColors.Color2 = k;
		end,
	})):AddColorPicker("ColorPicker45", {
		Default = (c.HealthLerpColors and c.HealthLerpColors.Color3) or Color3.fromRGB(255, 255, 255),
		Title = "low health",
		Transparency = 0,
		Callback = function(k)
			if not c.HealthLerpColors then
				c.HealthLerpColors = { Color1 = Color3.fromRGB(255, 255, 255), Color2 = Color3.fromRGB(255, 255, 255), Color3 = Color3.fromRGB(255, 255, 255) };
			end;
			c.HealthLerpColors.Color3 = k;
		end,
	});
	((r:AddToggle("textesp", { Text = "names", Default = _G.Config.TextESP.Names or false, Callback = function(k)
			_G.Config.TextESP.Names = k;
		end })):AddColorPicker("name_color", {
		Title = "name color 1",
		Default = _G.Config.TextESP.NameColor or Color3.fromRGB(255, 255, 255),
		Transparency = false,
		Callback = function(k)
			_G.Config.TextESP.NameColor = k;
		end,
	})):AddColorPicker("name_color2", {
		Title = "name color 2",
		Default = _G.Config.TextESP.NameColor2 or Color3.fromRGB(255, 255, 255),
		Transparency = false,
		Callback = function(k)
			_G.Config.TextESP.NameColor2 = k;
		end,
	});
	((r:AddToggle("textesp_distance", { Text = "distance", Default = _G.Config.TextESP.Distance or false, Callback = function(k)
			_G.Config.TextESP.Distance = k;
		end })):AddColorPicker("distance_color", {
		Title = "distance color 1",
		Default = _G.Config.TextESP.DistanceColor or Color3.fromRGB(255, 255, 255),
		Transparency = false,
		Callback = function(k)
			_G.Config.TextESP.DistanceColor = k;
		end,
	})):AddColorPicker("distance_color2", {
		Title = "distance color 2",
		Default = _G.Config.TextESP.DistanceColor2 or Color3.fromRGB(255, 255, 255),
		Transparency = false,
		Callback = function(k)
			_G.Config.TextESP.DistanceColor2 = k;
		end,
	});
	((r:AddToggle("textesp_tools", { Text = "tools", Default = _G.Config.TextESP.Tools or false, Callback = function(k)
			_G.Config.TextESP.Tools = k;
		end })):AddColorPicker("tool_color", {
		Title = "tool color 1",
		Default = _G.Config.TextESP.ToolColor or Color3.fromRGB(255, 255, 255),
		Transparency = false,
		Callback = function(k)
			_G.Config.TextESP.ToolColor = k;
		end,
	})):AddColorPicker("tool_color2", {
		Title = "tool color 2",
		Default = _G.Config.TextESP.ToolColor2 or Color3.fromRGB(255, 255, 255),
		Transparency = false,
		Callback = function(k)
			_G.Config.TextESP.ToolColor2 = k;
		end,
	});
	((r:AddToggle("skeleton", { Text = "skeleton", Default = _G.Config.Skeleton.Enable or false, Callback = function(k)
			_G.Config.Skeleton.Enable = k;
		end })):AddColorPicker("skeleton_color", {
		Title = "skeleton color 1",
		Default = _G.Config.Skeleton.Color or Color3.fromRGB(255, 255, 255),
		Transparency = false,
		Callback = function(k)
			_G.Config.Skeleton.Color = k;
		end,
	})):AddColorPicker("skeleton_color2", {
		Title = "skeleton color 2",
		Default = _G.Config.Skeleton.Color2 or Color3.fromRGB(255, 255, 255),
		Transparency = false,
		Callback = function(k)
			_G.Config.Skeleton.Color2 = k;
		end,
	});
	((r:AddToggle("tracers", { Text = "tracers", Default = _G.Config.Tracers.Enable or false, Callback = function(k)
			_G.Config.Tracers.Enable = k;
		end })):AddColorPicker("tracers_color", {
		Title = "tracer color 1",
		Default = _G.Config.Tracers.Color or Color3.fromRGB(255, 255, 255),
		Transparency = false,
		Callback = function(k)
			_G.Config.Tracers.Color = k;
		end,
	})):AddColorPicker("tracers_color2", {
		Title = "tracer color 2",
		Default = _G.Config.Tracers.Color2 or Color3.fromRGB(255, 255, 255),
		Transparency = false,
		Callback = function(k)
			_G.Config.Tracers.Color2 = k;
		end,
	});
	((r:AddToggle("chams", { Text = "chams", Default = _G.Config.Chams.Enable or false, Callback = function(k)
			_G.Config.Chams.Enable = k;
		end })):AddColorPicker("chams_color", {
		Title = "chams color 1",
		Default = _G.Config.Chams.Color or Color3.fromRGB(255, 255, 255),
		Transparency = false,
		Callback = function(k)
			_G.Config.Chams.Color = k;
		end,
	})):AddColorPicker("chams_color2", {
		Title = "chams color 2",
		Default = _G.Config.Chams.Color2 or Color3.fromRGB(255, 255, 255),
		Transparency = false,
		Callback = function(k)
			_G.Config.Chams.Color2 = k;
		end,
	});
	r:AddSlider("box_filled_transparency", {
		Text = "fill transparency",
		Default = _G.Config.Filled.Transparency or .7,
		Min = 0,
		Max = 1,
		Rounding = 2,
		Compact = true,
		Callback = function(k)
			_G.Config.Filled.Transparency = k;
		end,
	});
	r:AddSlider("box_filled_rotation", {
		Text = "fill rotation",
		Default = _G.Config.Filled.Rotation or 0,
		Min = 0,
		Max = 360,
		Rounding = 0,
		Compact = true,
		Callback = function(k)
			_G.Config.Filled.Rotation = k;
		end,
	});
	r:AddSlider("box_filled_speed", {
		Text = "fill speed",
		Default = _G.Config.Filled.Speed or 1,
		Min = .1,
		Max = 10,
		Rounding = 1,
		Compact = true,
		Callback = function(k)
			_G.Config.Filled.Speed = k;
		end,
	});
	r:AddSlider("box_glow_transparency", {
		Text = "glow transparency",
		Default = _G.Config.Glow.Transparency or .8,
		Min = 0,
		Max = 1,
		Rounding = 2,
		Compact = true,
		Callback = function(k)
			_G.Config.Glow.Transparency = k;
		end,
	});
	r:AddSlider("box_glow_rotation", {
		Text = "glow rotation",
		Default = _G.Config.Glow.Rotation or 0,
		Min = 0,
		Max = 360,
		Rounding = 0,
		Compact = true,
		Callback = function(k)
			_G.Config.Glow.Rotation = k;
		end,
	});
	r:AddSlider("textesp_namesize", {
		Text = "name size",
		Default = _G.Config.TextESP.NameSize or 8,
		Min = 8,
		Max = 16,
		Rounding = 1,
		Compact = true,
		Callback = function(k)
			_G.Config.TextESP.NameSize = k;
		end,
	});
	r:AddSlider("textesp_distancesize", {
		Text = "distance size",
		Default = _G.Config.TextESP.DistanceSize or 8,
		Min = 8,
		Max = 14,
		Rounding = 1,
		Compact = true,
		Callback = function(k)
			_G.Config.TextESP.DistanceSize = k;
		end,
	});
	r:AddSlider("textesp_toolssize", {
		Text = "tools size",
		Default = _G.Config.TextESP.ToolsSize or 8,
		Min = 8,
		Max = 14,
		Rounding = 1,
		Compact = true,
		Callback = function(k)
			_G.Config.TextESP.ToolsSize = k;
		end,
	});
	r:AddSlider("skeleton_thickness", {
		Text = "skeleton thickness",
		Default = _G.Config.Skeleton.Thickness or 1,
		Min = .5,
		Max = 5,
		Rounding = 1,
		Compact = true,
		Callback = function(k)
			_G.Config.Skeleton.Thickness = k;
		end,
	});
	r:AddSlider("tracers_thickness", {
		Text = "tracer thickness",
		Default = _G.Config.Tracers.Thickness or 1,
		Min = .5,
		Max = 5,
		Rounding = 1,
		Compact = true,
		Callback = function(k)
			_G.Config.Tracers.Thickness = k;
		end,
	});
	r:AddSlider("chams_transparency", {
		Text = "chams transparency",
		Default = _G.Config.Chams.Transparency or 0,
		Min = 0,
		Max = 1,
		Rounding = 2,
		Compact = true,
		Callback = function(k)
			_G.Config.Chams.Transparency = k;
		end,
	});
end))();
((function()
	local k = (getgenv()).FinhubWorldToScreenEsp or (getgenv()).FinhubWorldToScreen;
	local l = {};
	local p = pcall(function()
			local k = "util_font.ttf";
			if not isfile(k) then
				writefile(k, game:HttpGet("https://github.com/i77lhm/storage/raw/refs/heads/main/fonts/smallest_pixel-7.ttf"));
			end;
			l.path = k;
		end);
	local a = {
			enable = false,
			color = Color3.fromRGB(255, 255, 255),
			color2 = Color3.fromRGB(255, 255, 255),
			selected = {},
			distance = false,
			images = false,
		};
	local y = 36;
	local v = 14;
	local j = 6;
	local s = game:GetService("ReplicatedStorage");
	local G;
	pcall(function()
		G = require(s.Modules.ItemLibrary);
	end);
	local g = {
			Grenade = "Grenade",
			SubspaceTripmineHitbox = "Substapce Tripmine",
			Flashbang = "Flashbang",
			Satchel = "Satchel",
			Warpstone = "Warpstone",
			Molotov = "Molotov",
			["Smoke Grenade"] = "Smoke Grenade",
		};
	local T = {};
	local c = {
			Grenade = "rbxassetid://6603835352",
			Flashbang = "rbxassetid://243660364",
			Satchel = "rbxassetid://446111271",
			Warpstone = "rbxassetid://12781852245",
			Molotov = "rbxassetid://1084982556",
			["Smoke Grenade"] = "rbxassetid://241650108",
			["Subspace Tripmine"] = "rbxassetid://120777973",
		};
	local function e(k)
		if not k or k == "" then
			return "";
		end;
		k = tostring(k);
		if tonumber(k) then
			return "rbxassetid://" .. k;
		end;
		return k;
	end;
	local function b(k)
		if T[k] ~= nil then
			return T[k];
		end;
		local l = "";
		local p = {};
		local a = g[k];
		if a then
			p[#p + 1] = a;
		end;
		if k == "SubspaceTripmineHitbox" then
			p[#p + 1] = "Subspace Tripmine";
		end;
		p[#p + 1] = k;
		pcall(function()
			for k, p in ipairs(p) do
				if l ~= "" then
					break;
				end;
				if G and G.GetWeaponImage then
					l = G:GetWeaponImage(p, true) or "";
				end;
				if l == "" and (G and G.GetImageFromWeaponData) then
					l = G:GetImageFromWeaponData({ Name = p, Weapon = p, WeaponName = p }, true) or "";
				end;
			end;
		end);
		l = e(l);
		if l == "" then
			l = c[k] or c[g[k]] or "";
		end;
		T[k] = l;
		return l;
	end;
	local f = {
			grenade = "Grenade",
			["subspace tripmine"] = "SubspaceTripmineHitbox",
			flashbang = "Flashbang",
			satchel = "Satchel",
			warpstone = "Warpstone",
			molotov = "Molotov",
			["smoke grenade"] = "Smoke Grenade",
		};
	local N = {};
	for k, l in pairs(f) do
		N[l] = k;
	end;
	local function K()
		for k in pairs(f) do
			if a.selected[k] then
				return true;
			end;
		end;
		return false;
	end;
	local function n(k)
		if not K() then
			for l, p in pairs(f) do
				if p == k then
					return true;
				end;
			end;
			return false;
		end;
		for l, p in pairs(f) do
			if p == k and a.selected[l] then
				return true;
			end;
		end;
		return false;
	end;
	local function d(k)
		if (getgenv()).FinhubTrackDrawingText then
			return (getgenv()).FinhubTrackDrawingText(k);
		end;
		return k;
	end;
	local Y = {};
	local function M(k, l)
		if not Drawing or not Drawing.new then
			return nil;
		end;
		local p, a = pcall(function()
				local k = Drawing.new("Text");
				return d(k);
			end);
		if not p or not a then
			return nil;
		end;
		a.Text = k;
		a.Color = l;
		a.Size = 15;
		a.Visible = false;
		a.Outline = true;
		a.OutlineColor = Color3.new(0, 0, 0);
		a.Center = true;
		a.Font = (getgenv()).FinhubHudDrawFont or 2;
		return a;
	end;
	local function i(k)
		local l = Instance.new("ImageLabel");
		l.BackgroundTransparency = 1;
		l.BorderSizePixel = 0;
		l.Image = k or "";
		l.ImageTransparency = 0;
		l.Size = UDim2.fromOffset(y, y);
		l.Visible = false;
		l.ZIndex = 10;
		l.Parent = espgui;
		return l;
	end;
	local function R(k)
		if not k then
			return nil, nil;
		end;
		if k:IsA("Model") then
			local l = k.PrimaryPart or k:FindFirstChild("Hitbox") or k:FindFirstChildWhichIsA("BasePart");
			return k, l;
		end;
		if k:IsA("BasePart") then
			local l = k.Parent and (k.Parent:IsA("Model") and k.Parent) or nil;
			return l or k, k;
		end;
		return nil, nil;
	end;
	local function B(k)
		local l, p = R(k);
		if not l or not p then
			return false;
		end;
		local a = l:IsA("Model") and l.Name or k.Name;
		return n(a);
	end;
	local function W(k)
		if Y[k] then
			for k, l in ipairs(Y[k].drawings) do
				pcall(function()
					l:Remove();
				end);
			end;
			if Y[k].image then
				pcall(function()
					if typeof(Y[k].image) == "Finhub" and Y[k].image:IsA("GuiObject") then
						Y[k].image:Destroy();
					else
						Y[k].image:Remove();
					end;
				end);
			end;
			if Y[k].conn then
				Y[k].conn:Disconnect();
			end;
			Y[k] = nil;
		end;
	end;
	local function L()
		for k, l in pairs(Y) do
			W(k);
		end;
	end;
	local function P(l)
		local p, s = R(l);
		if not p or not s then
			return;
		end;
		if Y[p] then
			return;
		end;
		local G = p:IsA("Model") and p.Name or tostring(p);
		local g = N[G] or string.lower(G);
		local T = M(g, a.color);
		local c = M("", a.color);
		local e = i(b(G));
		if not T and not c then
			return;
		end;
		Y[p] = {
				drawings = {},
				image = e,
				conn = nil,
				root = s,
			};
		if T then
			Y[p].drawings[#Y[p].drawings + 1] = T;
		end;
		if c then
			Y[p].drawings[#Y[p].drawings + 1] = c;
		end;
		local f = (game:GetService("RunService")).RenderStepped:Connect(function()
				pcall(function()
					local l = Y[p] and Y[p].root;
					if not l or not l.Parent then
						W(p);
						return;
					end;
					local s = workspace.CurrentCamera;
					if not s or not k then
						return;
					end;
					local g, f = k(l.Position);
					if f and (a.enable and n(G)) then
						if a.images and (e and ((not e.Image or e.Image == ""))) then
							local k = b(G);
							if k ~= "" then
								e.Image = k;
							end;
						end;
						local k = a.images and (e and (e.Image and e.Image ~= ""));
						local p = y * .5;
						if T then
							if k then
								T.Position = Vector2.new(g.X, (g.Y - p) - v);
							else
								T.Position = Vector2.new(g.X, g.Y - 8);
							end;
							T.Visible = true;
						end;
						if e then
							if k then
								e.Position = UDim2.new(0, g.X - p, 0, g.Y - p);
								e.Visible = true;
							else
								e.Visible = false;
							end;
						end;
						if c then
							if a.distance then
								local a = math.floor(((s.CFrame.Position - l.Position)).Magnitude);
								c.Text = (getgenv()).FinhubL("studs_short", a);
								if k then
									c.Position = Vector2.new(g.X, (g.Y + p) + j);
								else
									c.Position = Vector2.new(g.X, g.Y + 6);
								end;
								c.Visible = true;
							else
								c.Visible = false;
							end;
						end;
					else
						if T then
							T.Visible = false;
						end;
						if c then
							c.Visible = false;
						end;
						if e then
							e.Visible = false;
						end;
					end;
				end);
			end);
		Y[p].conn = f;
		p.AncestryChanged:Connect(function(k, l)
			if not l then
				W(p);
			end;
		end);
	end;
	local function x()
		L();
		if not a.enable then
			return;
		end;
		for k, l in ipairs(workspace:GetDescendants()) do
			if ((l:IsA("Model") or l:IsA("BasePart"))) and B(l) then
				P(l);
			end;
		end;
	end;
	local t = F.Visuals:AddLeftGroupbox("utility");
	((t:AddToggle("utilenable", { Text = "enable", Default = a.enable, Callback = function(k)
			a.enable = k;
			x();
		end })):AddColorPicker("utilcolor", {
		Title = "text color 1",
		Default = a.color,
		Transparency = false,
		Callback = function(k)
			a.color = k;
			for l, p in pairs(Y) do
				for l, p in ipairs(p.drawings) do
					p.Color = k;
				end;
			end;
		end,
	})):AddColorPicker("utilcolor2", {
		Title = "text color 2",
		Default = a.color2 or a.color,
		Transparency = false,
		Callback = function(k)
			a.color2 = k;
		end,
	});
	t:AddDropdown("utilitems", {
		Text = "utilities",
		Values = {
			"grenade",
			"subspace tripmine",
			"flashbang",
			"satchel",
			"warpstone",
			"molotov",
			"smoke grenade",
		},
		Multi = true,
		Default = {},
		Callback = function(k)
			a.selected = k;
			x();
		end,
	});
	t:AddToggle("utildistance", { Text = "distance", Default = a.distance, Callback = function(k)
			a.distance = k;
		end });
	t:AddToggle("utilimages", { Text = "images", Default = a.images, Callback = function(k)
			a.images = k;
		end });
	workspace.DescendantAdded:Connect(function(k)
		if not a.enable then
			return;
		end;
		if not k:IsA("Model") and not k:IsA("BasePart") then
			return;
		end;
		task.delay(.15, function()
			if not a.enable then
				return;
			end;
			if B(k) then
				P(k);
			end;
		end);
	end);
end))();
((function()
	_G.LightingSettings = _G.LightingSettings or {
			Enabled = false,
			Ambient = { Enabled = false, Color = Color3.fromRGB(255, 255, 255) },
			OutdoorAmbient = { Enabled = false, Color = Color3.fromRGB(255, 255, 255) },
			GlobalShadows = { Enabled = true },
			ShadowSoftness = { Enabled = false, Value = .2 },
			SunColor = { Enabled = false, Color = Color3.fromRGB(255, 255, 255) },
			ColorShiftTop = { Enabled = false, Color = Color3.fromRGB(255, 255, 255) },
			ColorShiftBottom = { Enabled = false, Color = Color3.fromRGB(255, 255, 255) },
			ClockTime = { Enabled = false, Value = 12 },
			GeographicLatitude = { Enabled = false, Value = 41.8 },
			Fog = {
				Enabled = false,
				Color = Color3.fromRGB(255, 255, 255),
				Start = 0,
				End = 100,
			},
			EnvironmentDiffuseScale = { Enabled = false, Value = 1 },
			EnvironmentSpecularScale = { Enabled = false, Value = 1 },
			ColorCorrection = {
				Enabled = false,
				Contrast = 0,
				Saturation = 0,
				Brightness = 0,
			},
			Bloom = { Enabled = false, Multiplier = .3, Threshold = .6 },
		};
	local k = game:GetService("Lighting");
	local l = {};
	local p = nil;
	local a = nil;
	local y = {};
	local v = {};
	local function j(k)
		if not k or not k:IsA("BloomEffect") or k.Name == "FinhubExtraBloom" then
			return;
		end;
		if not y[k] then
			y[k] = {
					Intensity = k.Intensity,
					Size = k.Size,
					Threshold = k.Threshold,
					Enabled = k.Enabled,
				};
		end;
		if v[k] then
			return;
		end;
		v[k] = true;
		k.AncestryChanged:Connect(function(l, p)
			if not p then
				y[k] = nil;
				v[k] = nil;
			end;
		end);
	end;
	local function s()
		local l = _G.LightingSettings and _G.LightingSettings.Bloom;
		if not l then
			return;
		end;
		local p = math.clamp(tonumber(l.Multiplier) or 1, 0, 1.5);
		for k, l in ipairs(k:GetChildren()) do
			if l:IsA("BloomEffect") and l.Name ~= "FinhubExtraBloom" then
				l.Enabled = false;
			end;
		end;
		if l.Enabled then
			local l = k:FindFirstChild("FinhubExtraBloom");
			if not l then
				l = Instance.new("BloomEffect");
				l.Name = "FinhubExtraBloom";
				l.Parent = k;
			end;
			l.Intensity = p * .8;
			l.Size = 4;
			l.Threshold = .7;
			l.Enabled = true;
		else
			local l = k:FindFirstChild("FinhubExtraBloom");
			if l then
				l:Destroy();
			end;
			for k, l in ipairs(k:GetChildren()) do
				if l:IsA("BloomEffect") and l.Name ~= "FinhubExtraBloom" then
					local k = y[l];
					if k then
						l.Intensity = k.Intensity;
						l.Size = k.Size;
						l.Threshold = k.Threshold;
						l.Enabled = k.Enabled;
					end;
				end;
			end;
		end;
	end;
	local function G()
		for k, l in pairs(y) do
			if k and k.Parent then
				k.Intensity = l.Intensity;
				k.Size = l.Size;
				k.Threshold = l.Threshold;
				k.Enabled = l.Enabled;
			end;
		end;
		local l = k:FindFirstChild("FinhubExtraBloom");
		if l then
			l:Destroy();
		end;
	end;
	local function T()
		l.Ambient = k.Ambient;
		l.OutdoorAmbient = k.OutdoorAmbient;
		l.GlobalShadows = k.GlobalShadows;
		l.ShadowSoftness = k.ShadowSoftness;
		l.ColorShift_Top = k.ColorShift_Top;
		l.ColorShift_Bottom = k.ColorShift_Bottom;
		l.ClockTime = k.ClockTime;
		l.GeographicLatitude = k.GeographicLatitude;
		l.FogColor = k.FogColor;
		l.FogStart = k.FogStart;
		l.FogEnd = k.FogEnd;
		l.EnvironmentDiffuseScale = k.EnvironmentDiffuseScale;
		l.EnvironmentSpecularScale = k.EnvironmentSpecularScale;
	end;
	T();
	local function c()
		return _G.Features and (_G.Features.QOL and _G.Features.QOL.Shaders);
	end;
	local function e(k, l, p)
		return Color3.new(k.R + ((l.R - k.R)) * p, k.G + ((l.G - k.G)) * p, k.B + ((l.B - k.B)) * p);
	end;
	local function b()
		local l = _G.LightingSettings.ColorCorrection;
		if l and l.Enabled then
			if not a or not a.Parent then
				a = Instance.new("ColorCorrectionEffect");
				a.Name = "FinhubExtraColorCorrection";
				a.Parent = k;
			end;
			a.Contrast = l.Contrast / 10;
			a.Saturation = l.Saturation / 10;
			a.Brightness = l.Brightness / 10;
			a.Enabled = true;
		elseif a then
			a.Enabled = false;
		end;
		s();
	end;
	local function f()
		for k, l in pairs(y) do
			if k.Parent then
				k.Intensity = l.Intensity;
				k.Size = l.Size;
				k.Threshold = l.Threshold;
				k.Enabled = l.Enabled;
			end;
		end;
		local l = k:FindFirstChild("FinhubExtraBloom");
		if l then
			l:Destroy();
		end;
	end;
	local function N()
		if a then
			a.Enabled = false;
		end;
		f();
	end;
	local function K()
		if p then
			p:Disconnect();
			p = nil;
		end;
		p = g.RenderStepped:Connect(function()
				if not _G.LightingSettings.Enabled then
					return;
				end;
				if _G.LightingSettings.Ambient.Enabled then
					k.Ambient = _G.LightingSettings.Ambient.Color;
				elseif not c() then
					k.Ambient = l.Ambient;
				end;
				if _G.LightingSettings.SunColor.Enabled then
					local p = _G.LightingSettings.OutdoorAmbient.Enabled and _G.LightingSettings.OutdoorAmbient.Color or (c() and Color3.fromRGB(88, 108, 140) or l.OutdoorAmbient);
					k.OutdoorAmbient = e(p, _G.LightingSettings.SunColor.Color, .55);
				elseif _G.LightingSettings.OutdoorAmbient.Enabled then
					k.OutdoorAmbient = _G.LightingSettings.OutdoorAmbient.Color;
				elseif not c() then
					k.OutdoorAmbient = l.OutdoorAmbient;
				end;
				if _G.LightingSettings.GlobalShadows.Enabled ~= nil then
					k.GlobalShadows = _G.LightingSettings.GlobalShadows.Enabled;
				else
					k.GlobalShadows = l.GlobalShadows;
				end;
				if _G.LightingSettings.ShadowSoftness.Enabled then
					k.ShadowSoftness = _G.LightingSettings.ShadowSoftness.Value;
				elseif not c() then
					k.ShadowSoftness = l.ShadowSoftness;
				end;
				if _G.LightingSettings.SunColor.Enabled then
					k.ColorShift_Top = _G.LightingSettings.SunColor.Color;
				elseif _G.LightingSettings.ColorShiftTop.Enabled then
					k.ColorShift_Top = _G.LightingSettings.ColorShiftTop.Color;
				elseif not c() then
					k.ColorShift_Top = l.ColorShift_Top;
				end;
				if _G.LightingSettings.ColorShiftBottom.Enabled then
					k.ColorShift_Bottom = _G.LightingSettings.ColorShiftBottom.Color;
				elseif not c() then
					k.ColorShift_Bottom = l.ColorShift_Bottom;
				end;
				if _G.LightingSettings.ClockTime.Enabled then
					k.ClockTime = _G.LightingSettings.ClockTime.Value;
				else
					k.ClockTime = l.ClockTime;
				end;
				if _G.LightingSettings.GeographicLatitude.Enabled then
					k.GeographicLatitude = _G.LightingSettings.GeographicLatitude.Value;
				else
					k.GeographicLatitude = l.GeographicLatitude;
				end;
				if _G.LightingSettings.Fog.Enabled then
					k.FogColor = _G.LightingSettings.Fog.Color;
					k.FogStart = _G.LightingSettings.Fog.Start;
					k.FogEnd = _G.LightingSettings.Fog.End;
				else
					k.FogColor = l.FogColor;
					k.FogStart = l.FogStart;
					k.FogEnd = l.FogEnd;
				end;
				if _G.LightingSettings.EnvironmentDiffuseScale.Enabled then
					k.EnvironmentDiffuseScale = _G.LightingSettings.EnvironmentDiffuseScale.Value;
				elseif not c() then
					k.EnvironmentDiffuseScale = l.EnvironmentDiffuseScale;
				end;
				if _G.LightingSettings.EnvironmentSpecularScale.Enabled then
					k.EnvironmentSpecularScale = _G.LightingSettings.EnvironmentSpecularScale.Value;
				elseif not c() then
					k.EnvironmentSpecularScale = l.EnvironmentSpecularScale;
				end;
			end);
	end;
	local function n()
		if p then
			p:Disconnect();
			p = nil;
		end;
		k.GlobalShadows = l.GlobalShadows;
		k.ClockTime = l.ClockTime;
		k.GeographicLatitude = l.GeographicLatitude;
		k.FogColor = l.FogColor;
		k.FogStart = l.FogStart;
		k.FogEnd = l.FogEnd;
		if not c() then
			k.Ambient = l.Ambient;
			k.OutdoorAmbient = l.OutdoorAmbient;
			k.ShadowSoftness = l.ShadowSoftness;
			k.ColorShift_Top = l.ColorShift_Top;
			k.ColorShift_Bottom = l.ColorShift_Bottom;
			k.EnvironmentDiffuseScale = l.EnvironmentDiffuseScale;
			k.EnvironmentSpecularScale = l.EnvironmentSpecularScale;
			for k, l in ipairs(k:GetChildren()) do
				if l:IsA("Atmosphere") then
					l:Destroy();
				end;
			end;
			if origatmo then
				local l = Instance.new("Atmosphere");
				l.Density = origatmo.Density;
				l.Offset = origatmo.Offset;
				l.Haze = origatmo.Haze;
				l.Glare = origatmo.Glare;
				l.Color = origatmo.Color;
				l.Decay = origatmo.Decay;
				l.Parent = k;
			end;
		end;
		b();
	end;
	local d = F.Visuals:AddRightGroupbox("lighting");
	d:AddToggle("lighting_master", { Text = "enable", Default = _G.LightingSettings.Enabled, Callback = function(k)
			_G.LightingSettings.Enabled = k;
			if k then
				K();
			else
				n();
			end;
		end });
	local Y = d:AddToggle("lighting_ambient", { Text = "custom ambient", Default = _G.LightingSettings.Ambient.Enabled, Callback = function(k)
				_G.LightingSettings.Ambient.Enabled = k;
			end });
	Y:AddColorPicker("lighting_ambient_color", { Default = _G.LightingSettings.Ambient.Color, Title = "ambient color", Callback = function(k)
			_G.LightingSettings.Ambient.Color = k;
		end });
	local M = d:AddToggle("lighting_outdoorambient", { Text = "custom outdoor ambient", Default = _G.LightingSettings.OutdoorAmbient.Enabled, Callback = function(k)
				_G.LightingSettings.OutdoorAmbient.Enabled = k;
			end });
	M:AddColorPicker("lighting_outdoorambient_color", { Default = _G.LightingSettings.OutdoorAmbient.Color, Title = "outdoor ambient color", Callback = function(k)
			_G.LightingSettings.OutdoorAmbient.Color = k;
		end });
	local i = d:AddToggle("lighting_suncolor", { Text = "custom sun color", Default = _G.LightingSettings.SunColor.Enabled, Callback = function(k)
				_G.LightingSettings.SunColor.Enabled = k;
			end });
	i:AddColorPicker("lighting_suncolor_color", { Default = _G.LightingSettings.SunColor.Color, Title = "sun color", Callback = function(k)
			_G.LightingSettings.SunColor.Color = k;
		end });
	d:AddToggle("lighting_globalshadows", { Text = "global shadows", Default = _G.LightingSettings.GlobalShadows.Enabled, Callback = function(k)
			_G.LightingSettings.GlobalShadows.Enabled = k;
		end });
	d:AddToggle("lighting_shadowsoftness", { Text = "custom shadow softness", Default = _G.LightingSettings.ShadowSoftness.Enabled, Callback = function(k)
			_G.LightingSettings.ShadowSoftness.Enabled = k;
		end });
	local R = d:AddToggle("lighting_colorshifttop", { Text = "color shift top", Default = _G.LightingSettings.ColorShiftTop.Enabled, Callback = function(k)
				_G.LightingSettings.ColorShiftTop.Enabled = k;
			end });
	R:AddColorPicker("lighting_colorshifttop_color", { Default = _G.LightingSettings.ColorShiftTop.Color, Title = "top color", Callback = function(k)
			_G.LightingSettings.ColorShiftTop.Color = k;
		end });
	local B = d:AddToggle("lighting_colorshiftbottom", { Text = "color shift bottom", Default = _G.LightingSettings.ColorShiftBottom.Enabled, Callback = function(k)
				_G.LightingSettings.ColorShiftBottom.Enabled = k;
			end });
	B:AddColorPicker("lighting_colorshiftbottom_color", { Default = _G.LightingSettings.ColorShiftBottom.Color, Title = "bottom color", Callback = function(k)
			_G.LightingSettings.ColorShiftBottom.Color = k;
		end });
	d:AddToggle("lighting_clocktime", { Text = "custom time", Default = _G.LightingSettings.ClockTime.Enabled, Callback = function(k)
			_G.LightingSettings.ClockTime.Enabled = k;
		end });
	d:AddToggle("lighting_latitude", { Text = "custom latitude", Default = _G.LightingSettings.GeographicLatitude.Enabled, Callback = function(k)
			_G.LightingSettings.GeographicLatitude.Enabled = k;
		end });
	local W = d:AddToggle("lighting_fog", { Text = "custom fog", Default = _G.LightingSettings.Fog.Enabled, Callback = function(k)
				_G.LightingSettings.Fog.Enabled = k;
			end });
	W:AddColorPicker("lighting_fog_color", { Default = _G.LightingSettings.Fog.Color, Title = "fog color", Callback = function(k)
			_G.LightingSettings.Fog.Color = k;
		end });
	d:AddToggle("lighting_diffuse", { Text = "custom diffuse scale", Default = _G.LightingSettings.EnvironmentDiffuseScale.Enabled, Callback = function(k)
			_G.LightingSettings.EnvironmentDiffuseScale.Enabled = k;
		end });
	d:AddToggle("lighting_specular", { Text = "custom specular scale", Default = _G.LightingSettings.EnvironmentSpecularScale.Enabled, Callback = function(k)
			_G.LightingSettings.EnvironmentSpecularScale.Enabled = k;
		end });
	d:AddSlider("lighting_shadowsoftness_value", {
		Text = "shadow softness",
		Min = 0,
		Max = 1,
		Default = .2,
		Rounding = 2,
		Compact = true,
		Callback = function(k)
			_G.LightingSettings.ShadowSoftness.Value = k;
		end,
	});
	d:AddSlider("lighting_clocktime_value", {
		Text = "hour",
		Min = 0,
		Max = 24,
		Default = 12,
		Rounding = 1,
		Compact = true,
		Callback = function(k)
			_G.LightingSettings.ClockTime.Value = k;
		end,
	});
	d:AddSlider("lighting_latitude_value", {
		Text = "geographic latitude",
		Min = -90,
		Max = 90,
		Default = 41.8,
		Rounding = 1,
		Compact = true,
		Callback = function(k)
			_G.LightingSettings.GeographicLatitude.Value = k;
		end,
	});
	d:AddSlider("lighting_fogstart", {
		Text = "fog start",
		Min = 0,
		Max = 1000,
		Default = 0,
		Rounding = 0,
		Compact = true,
		Callback = function(k)
			_G.LightingSettings.Fog.Start = k;
		end,
	});
	d:AddSlider("lighting_fogend", {
		Text = "fog end",
		Min = 0,
		Max = 100000,
		Default = 100,
		Rounding = 0,
		Compact = true,
		Callback = function(k)
			_G.LightingSettings.Fog.End = k;
		end,
	});
	d:AddSlider("lighting_diffuse_value", {
		Text = "diffuse scale",
		Min = 0,
		Max = 2,
		Default = 1,
		Rounding = 2,
		Compact = true,
		Callback = function(k)
			_G.LightingSettings.EnvironmentDiffuseScale.Value = k;
		end,
	});
	d:AddSlider("lighting_specular_value", {
		Text = "specular scale",
		Min = 0,
		Max = 2,
		Default = 1,
		Rounding = 2,
		Compact = true,
		Callback = function(k)
			_G.LightingSettings.EnvironmentSpecularScale.Value = k;
		end,
	});
	local L = F.Visuals:AddRightTabbox();
	local P = L:AddTab("color");
	local x = L:AddTab("bloom");
	P:AddToggle("lighting_color_correction", { Text = "enable", Default = _G.LightingSettings.ColorCorrection.Enabled, Callback = function(k)
			_G.LightingSettings.ColorCorrection.Enabled = k;
			b();
		end });
	P:AddSlider("lighting_cc_contrast", {
		Text = "contrast",
		Min = -10,
		Max = 10,
		Default = _G.LightingSettings.ColorCorrection.Contrast,
		Rounding = 1,
		Compact = true,
		Callback = function(k)
			_G.LightingSettings.ColorCorrection.Contrast = k;
			if _G.LightingSettings.ColorCorrection.Enabled then
				b();
			end;
		end,
	});
	P:AddSlider("lighting_cc_saturation", {
		Text = "saturation",
		Min = -10,
		Max = 10,
		Default = _G.LightingSettings.ColorCorrection.Saturation,
		Rounding = 1,
		Compact = true,
		Callback = function(k)
			_G.LightingSettings.ColorCorrection.Saturation = k;
			if _G.LightingSettings.ColorCorrection.Enabled then
				b();
			end;
		end,
	});
	P:AddSlider("lighting_cc_brightness", {
		Text = "brightness",
		Min = -10,
		Max = 10,
		Default = _G.LightingSettings.ColorCorrection.Brightness,
		Rounding = 1,
		Compact = true,
		Callback = function(k)
			_G.LightingSettings.ColorCorrection.Brightness = k;
			if _G.LightingSettings.ColorCorrection.Enabled then
				b();
			end;
		end,
	});
	x:AddToggle("lighting_bloom_modifier", { Text = "enable", Default = _G.LightingSettings.Bloom.Enabled, Callback = function(k)
			_G.LightingSettings.Bloom.Enabled = k;
			if not k then
				f();
			end;
			b();
		end });
	x:AddSlider("lighting_bloom_multiplier", {
		Text = "emissive intensity",
		Min = 0,
		Max = 1.5,
		Default = _G.LightingSettings.Bloom.Multiplier,
		Rounding = 2,
		Compact = true,
		Callback = function(l)
			_G.LightingSettings.Bloom.Multiplier = l;
			if _G.LightingSettings.Bloom.Enabled then
				local p = k:FindFirstChild("FinhubExtraBloom");
				if p then
					p.Intensity = l * .8;
				end;
			end;
		end,
	});
	x:AddSlider("lighting_bloom_size", {
		Text = "glow size",
		Min = 2,
		Max = 8,
		Default = 4,
		Rounding = 1,
		Compact = true,
		Callback = function(l)
			_G.LightingSettings.Bloom.Size = l;
			if _G.LightingSettings.Bloom.Enabled then
				local p = k:FindFirstChild("FinhubExtraBloom");
				if p then
					p.Size = l;
				end;
			end;
		end,
	});
	x:AddSlider("lighting_bloom_threshold", {
		Text = "brightness threshold",
		Min = .5,
		Max = 1,
		Default = .7,
		Rounding = 2,
		Compact = true,
		Callback = function(l)
			_G.LightingSettings.Bloom.Threshold = l;
			if _G.LightingSettings.Bloom.Enabled then
				local p = k:FindFirstChild("FinhubExtraBloom");
				if p then
					p.Threshold = l;
				end;
			end;
		end,
	});
	k.ChildAdded:Connect(function(k)
		if k:IsA("BloomEffect") then
			task.defer(b);
		end;
	end);
	(game:GetService("ScriptContext")).DescendantRemoving:Connect(function(k)
		if k == script then
			n();
			N();
		end;
	end);
end))();
((function()
	(getgenv()).crosshair = {
			enabled = false,
			refreshrate = 0,
			mode = "mouse",
			position = Vector2.new(0, 0),
			crosshair_mode = "static",
			width = 1.5,
			length = 10,
			radius = 11,
			crosshair_color = Color3.fromRGB(255, 105, 180),
			color1 = Color3.fromRGB(255, 105, 180),
			color2 = Color3.fromRGB(255, 180, 0),
			color3 = Color3.fromRGB(255, 160, 0),
			gradient_rotation = 0,
			spin = true,
			spin_speed = 150,
			spin_max = 340,
			spin_style = Enum.EasingStyle.Sine,
			resize = true,
			resize_speed = 150,
			resize_min = 5,
			resize_max = 22,
			scale_min = 1,
			scale_max = 1,
			show_ammo = false,
			show_watermark = false,
			show_lines = true,
		};
	local k = game:GetService("RunService");
	local l = game:GetService("UserInputService");
	local p = game:GetService("TweenService");
	local a = workspace.CurrentCamera;
	local y = (game:GetService("Players")).LocalPlayer;
	local function v(k, l, p, a, y)
		local v = math.rad(l);
		local j = ((k * math.cos(v) + ((1 - k)) * math.sin(v))) % 1;
		if j < .5 then
			local k = j * 2;
			return Color3.new(p.R + ((a.R - p.R)) * k, p.G + ((a.G - p.G)) * k, p.B + ((a.B - p.B)) * k);
		else
			local k = ((j - .5)) * 2;
			return Color3.new(a.R + ((y.R - a.R)) * k, a.G + ((y.G - a.G)) * k, a.B + ((y.B - a.B)) * k);
		end;
	end;
	local j = (getgenv()).FinhubTrackDrawingText(Drawing.new("Text"));
	j.Size = 13;
	j.Font = 2;
	j.Outline = true;
	j.Color = Color3.new(1, 1, 1);
	j.Visible = false;
	local s = "";
	local G = 0;
	local g = {};
	for k = 1, 8, 1 do
		g[k] = Drawing.new("Line");
	end;
	local T = 0;
	local c = {
			0,
			90,
			180,
			270,
		};
	local function e(k, l)
		local p = math.rad(k);
		return Vector2.new(math.sin(p) * l, math.cos(p) * l);
	end;
	local function b()
		local k = workspace:FindFirstChild("ViewModels");
		if not k then
			return nil;
		end;
		local l = k:FindFirstChild("FirstPerson");
		if not l then
			return nil;
		end;
		local p = y.Name;
		for k, l in pairs(l:GetChildren()) do
			if l:IsA("Model") and l.Name:find("^" .. p) then
				local k = l:FindFirstChild("ItemVisual");
				if k then
					local l = k:FindFirstChild("Body");
					if l then
						local k = l:FindFirstChild("BodyPrimary");
						if k then
							local l = k:FindFirstChild("_muzzle");
							if l and l:IsA("Attachment") then
								return l.WorldPosition;
							end;
						end;
					end;
				end;
			end;
		end;
		return nil;
	end;
	local f = {
			frame = nil,
			reserve = nil,
			ammo = nil,
			rescanAt = 0,
		};
	local function N(k)
		if f.frame and (f.frame.Parent and f.frame.Visible) then
			local k = tonumber(f.reserve.Text:match("%d+")) or 0;
			local l = tonumber(f.ammo.Text:match("%d+")) or 0;
			return k, l;
		end;
		if k < f.rescanAt then
			return 0, 0;
		end;
		f.rescanAt = k + .5;
		f.frame, f.reserve, f.ammo = nil, nil, nil;
		for k, l in ipairs(y.PlayerGui:GetDescendants()) do
			if l:IsA("Frame") and l.Visible then
				local k = l:FindFirstChild("Reserve");
				if k and (l:FindFirstChild("Icon") and l:FindFirstChild("ItemName")) then
					local p = k:FindFirstChild("Ammo");
					if p then
						f.frame = l;
						f.reserve = k;
						f.ammo = p;
						local a = tonumber(k.Text:match("%d+")) or 0;
						local y = tonumber(p.Text:match("%d+")) or 0;
						return a, y;
					end;
				end;
			end;
		end;
		return 0, 0;
	end;
	k.PostSimulation:Connect(function()
		local k = os.clock();
		if k - T < (getgenv()).crosshair.refreshrate then
			return;
		end;
		T = k;
		if V and V() then
			j.Visible = false;
			for k = 1, 8, 1 do
				g[k].Visible = false;
			end;
			return;
		end;
		local y = (getgenv()).crosshair;
		local v;
		if y.crosshair_mode == "follow muzzle" then
			local k = b();
			if k then
				local p, j = lJ(k, a);
				if j then
					v = Vector2.new(p.X, p.Y);
				else
					v = y.mode == "center" and a.ViewportSize / 2 or y.mode == "mouse" and l:GetMouseLocation() or y.position;
				end;
			else
				v = y.mode == "center" and a.ViewportSize / 2 or y.mode == "mouse" and l:GetMouseLocation() or y.position;
			end;
		else
			v = y.mode == "center" and a.ViewportSize / 2 or y.mode == "mouse" and l:GetMouseLocation() or y.position;
		end;
		if not y.enabled then
			j.Visible = false;
			for k = 1, 8, 1 do
				g[k].Visible = false;
			end;
			return;
		end;
		local f = y.scale_min + (math.sin(k * y.resize_speed) * ((y.scale_max - y.scale_min)));
		local K = y.length;
		if y.resize then
			local l = ((k * y.resize_speed)) % 180;
			K = y.resize_min + math.sin(math.rad(l)) * y.resize_max;
		end;
		local n = v.Y + ((y.show_lines and ((y.radius + y.length) + 14) or 15));
		local d;
		if y.show_lines then
			d = ((v.Y + y.radius) + y.length) + 18;
		else
			d = v.Y + 18;
		end;
		if y.show_ammo then
			local l, p = N(k);
			local a = tostring(l) .. (" / " .. tostring(p));
			if a ~= s then
				s = a;
				j.Text = a;
				G = j.TextBounds.X;
			end;
			j.Position = Vector2.new(v.X - G / 2, d);
			j.Visible = true;
		else
			j.Visible = false;
		end;
		if y.show_lines then
			local l = 0;
			if y.spin then
				local a = -((k * y.spin_speed)) % y.spin_max;
				l = p:GetValue(a / 360, y.spin_style, Enum.EasingDirection.InOut) * 360;
			end;
			for k = 1, 4, 1 do
				local p = c[k] + l;
				local a = v + e(p, y.radius * f);
				local j = v + e(p, ((y.radius + K)) * f);
				local s = g[k + 4];
				s.Visible = true;
				s.Color = y.crosshair_color;
				s.From = a;
				s.To = j;
				s.Thickness = y.width;
				local G = g[k];
				G.Visible = true;
				G.Color = Color3.new(0, 0, 0);
				G.From = v + e(p, y.radius * f - 1);
				G.To = v + e(p, ((y.radius + K)) * f + 1);
				G.Thickness = y.width + 1.5;
			end;
		else
			for k = 1, 8, 1 do
				g[k].Visible = false;
			end;
		end;
	end);
	local K = F.Visuals:AddRightGroupbox("crosshair");
	((((K:AddToggle("crosshaireeee", { Text = "enable", Default = false, Callback = function(k)
			(getgenv()).crosshair.enabled = k;
		end })):AddColorPicker("CrosshairColor", { Default = (getgenv()).crosshair.crosshair_color, Title = "Crosshair Color", Callback = function(k)
			(getgenv()).crosshair.crosshair_color = k;
		end })):AddColorPicker("GradientColor1", { Default = (getgenv()).crosshair.color1, Title = "Text Color 1", Callback = function(k)
			(getgenv()).crosshair.color1 = k;
		end })):AddColorPicker("GradientColor2", { Default = (getgenv()).crosshair.color2, Title = "Text Color 2", Callback = function(k)
			(getgenv()).crosshair.color2 = k;
		end })):AddColorPicker("GradientColor3", { Default = (getgenv()).crosshair.color3, Title = "Text Color 3", Callback = function(k)
			(getgenv()).crosshair.color3 = k;
		end });
	K:AddToggle("showlines", { Text = "crosshair lines", Default = (getgenv()).crosshair.show_lines, Callback = function(k)
			(getgenv()).crosshair.show_lines = k;
		end });
	K:AddToggle("showammo", { Text = "ammo", Default = false, Callback = function(k)
			(getgenv()).crosshair.show_ammo = k;
		end });
	K:AddSlider("ooetg", {
		Text = "speed",
		Default = (getgenv()).crosshair.spin_speed,
		Min = 0,
		Max = 340,
		Rounding = 2,
		Suffix = "",
		Callback = function(k)
			(getgenv()).crosshair.spin_speed = k;
		end,
	});
	K:AddSlider("GradientRotation", {
		Text = "rotation",
		Default = (getgenv()).crosshair.gradient_rotation,
		Min = 0,
		Max = 360,
		Rounding = 0,
		Suffix = "",
		Callback = function(k)
			(getgenv()).crosshair.gradient_rotation = k;
		end,
	});
	K:AddDropdown("crosshairmode", {
		Text = "crosshair mode",
		Default = 1,
		Values = { "static", "follow muzzle" },
		Callback = function(k)
			(getgenv()).crosshair.crosshair_mode = k;
		end,
	});
end))();
((function()
	local k = game:GetService("Lighting");
	local l = game:GetService("SoundService");
	local p = game:GetService("RunService");
	local a = game:GetService("Players");
	local y = F.World:AddRightTabbox();
	local v = y:AddTab("atmosphere");
	local j = y:AddTab("ambience");
	_G.LightingSettings.Atmosphere = _G.LightingSettings.Atmosphere or {
			Enabled = false,
			Density = 12,
			Offset = .25,
			Haze = 8,
			Glare = .6,
			Color = Color3.fromRGB(140, 160, 190),
			Decay = Color3.fromRGB(90, 110, 140),
			SoundEnabled = false,
			SelectedSound = "None",
			SoundVolume = .6,
		};
	local s = nil;
	local G = nil;
	local g = {
			None = nil,
			Rain = 132717551555191,
			Night = 125710682931536,
			["Birds Chirping"] = 9112831327,
			["Jungle Birds"] = 9114892930,
			["Campfire Crackling"] = 109494611784143,
			["Night Crickets"] = 9112764040,
			["Snow Storm"] = 551546110,
		};
	local function T()
		local l = k:FindFirstChildOfClass("Atmosphere");
		if l then
			s = {
					Density = l.Density,
					Offset = l.Offset,
					Haze = l.Haze,
					Glare = l.Glare,
					Color = l.Color,
					Decay = l.Decay,
				};
		end;
	end;
	T();
	local function c()
		for k, l in pairs(k:GetChildren()) do
			if l:IsA("Atmosphere") and l.Name == "CustomAtmosphere" then
				l:Destroy();
			end;
		end;
	end;
	local function e()
		c();
		if not _G.LightingSettings.Atmosphere.Enabled then
			return;
		end;
		local l = Instance.new("Atmosphere");
		l.Name = "CustomAtmosphere";
		l.Density = _G.LightingSettings.Atmosphere.Density;
		l.Offset = _G.LightingSettings.Atmosphere.Offset;
		l.Haze = _G.LightingSettings.Atmosphere.Haze;
		l.Glare = _G.LightingSettings.Atmosphere.Glare;
		l.Color = _G.LightingSettings.Atmosphere.Color;
		l.Decay = _G.LightingSettings.Atmosphere.Decay;
		l.Parent = k;
	end;
	local function b()
		if G then
			G:Stop();
			G:Destroy();
			G = nil;
		end;
		local k = g[_G.LightingSettings.Atmosphere.SelectedSound];
		if not k or not _G.LightingSettings.Atmosphere.SoundEnabled then
			return;
		end;
		G = Instance.new("Sound");
		G.Name = "CustomAmbientSound";
		G.SoundId = "rbxassetid://" .. k;
		G.Looped = true;
		G.Volume = _G.LightingSettings.Atmosphere.SoundVolume;
		G.Parent = l;
		G:Play();
	end;
	local function f()
		if _G.LightingSettings.Atmosphere.Enabled then
			if not k:FindFirstChild("CustomAtmosphere") then
				e();
			end;
		end;
	end;
	p.Heartbeat:Connect(f);
	a.LocalPlayer.CharacterAdded:Connect(function()
		task.wait(.5);
		e();
		b();
	end);
	((v:AddToggle("atmosphere_master", { Text = "enable", Default = _G.LightingSettings.Atmosphere.Enabled, Callback = function(k)
			_G.LightingSettings.Atmosphere.Enabled = k;
			e();
		end })):AddColorPicker("atmosphere_color", { Default = _G.LightingSettings.Atmosphere.Color, Title = "atmosphere color", Callback = function(k)
			_G.LightingSettings.Atmosphere.Color = k;
			e();
		end })):AddColorPicker("atmosphere_decay", { Default = _G.LightingSettings.Atmosphere.Decay, Title = "decay color", Callback = function(k)
			_G.LightingSettings.Atmosphere.Decay = k;
			e();
		end });
	v:AddSlider("atmosphere_density", {
		Text = "density: ",
		Min = 0,
		Max = 1,
		Default = _G.LightingSettings.Atmosphere.Density,
		Rounding = 1,
		Compact = true,
		Callback = function(k)
			_G.LightingSettings.Atmosphere.Density = k;
			e();
		end,
	});
	v:AddSlider("atmosphere_haze", {
		Text = "haze: ",
		Min = 0,
		Max = 5,
		Default = _G.LightingSettings.Atmosphere.Haze,
		Rounding = 1,
		Compact = true,
		Callback = function(k)
			_G.LightingSettings.Atmosphere.Haze = k;
			e();
		end,
	});
	v:AddSlider("atmosphere_glare", {
		Text = "glare: ",
		Min = 0,
		Max = 5,
		Default = _G.LightingSettings.Atmosphere.Glare,
		Rounding = 2,
		Compact = true,
		Callback = function(k)
			_G.LightingSettings.Atmosphere.Glare = k;
			e();
		end,
	});
	v:AddSlider("atmosphere_offset", {
		Text = "offset: ",
		Min = 0,
		Max = 1,
		Default = _G.LightingSettings.Atmosphere.Offset,
		Rounding = 2,
		Compact = true,
		Callback = function(k)
			_G.LightingSettings.Atmosphere.Offset = k;
			e();
		end,
	});
	j:AddToggle("atmosphere_sound_toggle", { Text = "enable", Default = _G.LightingSettings.Atmosphere.SoundEnabled, Callback = function(k)
			_G.LightingSettings.Atmosphere.SoundEnabled = k;
			b();
		end });
	j:AddDropdown("atmosphere_ambient_sound", {
		Text = "ambient sound",
		Values = {
			"None",
			"Rain",
			"Night",
			"Birds Chirping",
			"Jungle Birds",
			"Campfire Crackling",
			"Night Crickets",
			"Snow Storm",
		},
		Default = _G.LightingSettings.Atmosphere.SelectedSound,
		Multi = false,
		Callback = function(k)
			_G.LightingSettings.Atmosphere.SelectedSound = k;
			b();
		end,
	});
	j:AddSlider("atmosphere_sound_volume", {
		Text = "volume",
		Min = 0,
		Max = 10,
		Default = _G.LightingSettings.Atmosphere.SoundVolume,
		Rounding = 2,
		Compact = true,
		Callback = function(k)
			_G.LightingSettings.Atmosphere.SoundVolume = k;
			if G then
				G.Volume = k;
			end;
		end,
	});
	local N = F.World:AddRightGroupbox("kill sounds");
	_G.Features.KillSounds = _G.Features.KillSounds or {
			Enabled = false,
			Sound = "sound 1",
			Volume = 50,
			Pitch = 100,
		};
	local K = _G.Features.KillSounds;
	local n = (game:GetService("Players")).LocalPlayer;
	local d = "KillSounds";
	local Y = d .. "/sounds";
	local M = { "sound 1", "sound 2", "sound 3" };
	local i = { ["sound 1"] = "kill_1.mp3", ["sound 2"] = "kill_2.mp3", ["sound 3"] = "kill_3.mp3" };
	local R = { ["kill_1.mp3"] = "https://files.catbox.moe/ub7f65.mp3", ["kill_2.mp3"] = "https://files.catbox.moe/gdixkv.mp3", ["kill_3.mp3"] = "https://files.catbox.moe/68n74h.mp3" };
	local B = {};
	local function W()
		if not ((isfolder and (makefolder and (writefile and (isfile and type(game.HttpGet) == "function"))))) then
			return;
		end;
		if not isfolder(d) then
			makefolder(d);
		end;
		if not isfolder(Y) then
			makefolder(Y);
		end;
		for k, l in pairs(R) do
			local p = Y .. ("/" .. k);
			if not isfile(p) then
				local k, a = pcall(function()
						return game:HttpGet(l, true);
					end);
				if k and (type(a) == "string" and #a > 128) then
					writefile(p, a);
				end;
				task.wait(.6);
			end;
		end;
	end;
	task.spawn(W);
	local function L(k)
		if not k or k == n then
			return false;
		end;
		local l = (getgenv()).FinhubCombatLastShotAt or 0;
		if tick() - l < 4 then
			return true;
		end;
		local p = KJ and KJ.config;
		local a = KJ and KJ.ragePerf;
		if p and (p.target and p.target.lastplayer == k) then
			return true;
		end;
		if p and (p.target and (p.target.enabled and p.target.character)) then
			local l = (game:GetService("Players")):GetPlayerFromCharacter(p.target.character);
			if l == k then
				return true;
			end;
		end;
		if a and a.lastHitAtByPlayer then
			local l = a.lastHitAtByPlayer[k];
			if l ~= nil and tick() - l < 4 then
				return true;
			end;
		end;
		return false;
	end;
	local function P()
		if not K.Enabled then
			return;
		end;
		local k = getsynasset or getcustomasset;
		if not ((isfile and k)) then
			return;
		end;
		local l = K.Sound or "sound 1";
		local p = i[l] or i["sound 1"];
		local a = Y .. ("/" .. p);
		if not isfile(a) then
			task.spawn(W);
			return;
		end;
		local y, v = pcall(k, a);
		if not y or type(v) ~= "string" or v == "" then
			return;
		end;
		local j = Instance.new("Sound");
		j.SoundId = v;
		j.Volume = math.clamp(((K.Volume or 50)) / 100, 0, 1);
		j.PlaybackSpeed = math.clamp(((K.Pitch or 100)) / 100, .1, 3);
		j.Parent = workspace.CurrentCamera or workspace;
		j:Play();
		task.delay(6, function()
			if j and j.Parent then
				j:Destroy();
			end;
		end);
	end;
	(getgenv()).FinhubPlayKillSound = P;
	(getgenv()).FinhubTryKillSound = function(k, l, p)
			if not K.Enabled then
				return;
			end;
			if not k or k == n then
				return;
			end;
			if ((p or 0)) > 0 then
				return;
			end;
			if ((l or 0)) <= 0 then
				return;
			end;
			if not L(k) then
				return;
			end;
			local a = tick();
			if a - ((B[k] or 0)) < .35 then
				return;
			end;
			B[k] = a;
			P();
		end;
	local x = N:AddToggle("KillSoundsEnabled", { Text = "enable", Default = K.Enabled, Callback = function(k)
				K.Enabled = k;
			end });
	local t = N:AddDependencyBox();
	t:SetupDependencies({ { x, true } });
	t:AddDropdown("KillSoundStyle", {
		Text = "sound",
		Default = K.Sound or "sound 1",
		Values = M,
		Callback = function(k)
			K.Sound = k;
		end,
	});
	t:AddSlider("KillSoundVolume", {
		Text = "volume",
		Default = K.Volume or 50,
		Min = 0,
		Max = 100,
		Rounding = 0,
		Compact = true,
		Callback = function(k)
			K.Volume = k;
		end,
	});
	t:AddSlider("KillSoundPitch", {
		Text = "pitch",
		Default = K.Pitch or 100,
		Min = 50,
		Max = 200,
		Rounding = 0,
		Compact = true,
		Callback = function(k)
			K.Pitch = k;
		end,
	});
end))();
((function()
	local k = game:GetService("Lighting");
	local l = game:GetService("RunService");
	local p = F.World:AddLeftTabbox();
	local a = p:AddTab("skybox");
	local y = p:AddTab("aspect ratio");
	_G.SkyboxSettings = _G.SkyboxSettings or {
			Enabled = false,
			CurrentSkybox = "Default",
			CustomTextures = {
				SkyboxBk = "rbxassetid://91458024",
				SkyboxDn = "rbxassetid://91457980",
				SkyboxFt = "rbxassetid://91458024",
				SkyboxLf = "rbxassetid://91458024",
				SkyboxRt = "rbxassetid://91458024",
				SkyboxUp = "rbxassetid://91458002",
			},
			DeleteDefaultSkybox = false,
			DisabledElements = {},
		};
	local v = {
			Default = {
				SkyboxBk = "rbxassetid://91458024",
				SkyboxDn = "rbxassetid://91457980",
				SkyboxFt = "rbxassetid://91458024",
				SkyboxLf = "rbxassetid://91458024",
				SkyboxRt = "rbxassetid://91458024",
				SkyboxUp = "rbxassetid://91458002",
			},
			Neptune = {
				SkyboxBk = "rbxassetid://218955819",
				SkyboxDn = "rbxassetid://218953419",
				SkyboxFt = "rbxassetid://218954524",
				SkyboxLf = "rbxassetid://218958493",
				SkyboxRt = "rbxassetid://218957134",
				SkyboxUp = "rbxassetid://218950090",
			},
			["Among Us"] = {
				SkyboxBk = "rbxassetid://5752463190",
				SkyboxDn = "rbxassetid://5752463190",
				SkyboxFt = "rbxassetid://5752463190",
				SkyboxLf = "rbxassetid://5752463190",
				SkyboxRt = "rbxassetid://5752463190",
				SkyboxUp = "rbxassetid://5752463190",
			},
			Nebula = {
				SkyboxBk = "rbxassetid://159454299",
				SkyboxDn = "rbxassetid://159454296",
				SkyboxFt = "rbxassetid://159454293",
				SkyboxLf = "rbxassetid://159454286",
				SkyboxRt = "rbxassetid://159454300",
				SkyboxUp = "rbxassetid://159454288",
			},
			Vaporwave = {
				SkyboxBk = "rbxassetid://1417494030",
				SkyboxDn = "rbxassetid://1417494146",
				SkyboxFt = "rbxassetid://1417494253",
				SkyboxLf = "rbxassetid://1417494402",
				SkyboxRt = "rbxassetid://1417494499",
				SkyboxUp = "rbxassetid://1417494643",
			},
			Clouds = {
				SkyboxBk = "rbxassetid://570557514",
				SkyboxDn = "rbxassetid://570557775",
				SkyboxFt = "rbxassetid://570557559",
				SkyboxLf = "rbxassetid://570557620",
				SkyboxRt = "rbxassetid://570557672",
				SkyboxUp = "rbxassetid://570557727",
			},
			Twilight = {
				SkyboxBk = "rbxassetid://264908339",
				SkyboxDn = "rbxassetid://264907909",
				SkyboxFt = "rbxassetid://264909420",
				SkyboxLf = "rbxassetid://264909758",
				SkyboxRt = "rbxassetid://264908886",
				SkyboxUp = "rbxassetid://264907379",
			},
			DaBaby = {
				SkyboxBk = "rbxassetid://7245418472",
				SkyboxDn = "rbxassetid://7245418472",
				SkyboxFt = "rbxassetid://7245418472",
				SkyboxLf = "rbxassetid://7245418472",
				SkyboxRt = "rbxassetid://7245418472",
				SkyboxUp = "rbxassetid://7245418472",
			},
			Minecraft = {
				SkyboxBk = "rbxassetid://1876545003",
				SkyboxDn = "rbxassetid://1876544331",
				SkyboxFt = "rbxassetid://1876542941",
				SkyboxLf = "rbxassetid://1876543392",
				SkyboxRt = "rbxassetid://1876543764",
				SkyboxUp = "rbxassetid://1876544642",
			},
			Chill = {
				SkyboxBk = "rbxassetid://5084575798",
				SkyboxDn = "rbxassetid://5084575916",
				SkyboxFt = "rbxassetid://5103949679",
				SkyboxLf = "rbxassetid://5103948542",
				SkyboxRt = "rbxassetid://5103948784",
				SkyboxUp = "rbxassetid://5084576400",
			},
			Redshift = {
				SkyboxBk = "rbxassetid://401664839",
				SkyboxDn = "rbxassetid://401664862",
				SkyboxFt = "rbxassetid://401664960",
				SkyboxLf = "rbxassetid://401664881",
				SkyboxRt = "rbxassetid://401664901",
				SkyboxUp = "rbxassetid://401664936",
			},
			["Blue Stars"] = {
				SkyboxBk = "rbxassetid://149397684",
				SkyboxDn = "rbxassetid://149397686",
				SkyboxFt = "rbxassetid://149397688",
				SkyboxLf = "rbxassetid://149397692",
				SkyboxRt = "rbxassetid://149397697",
				SkyboxUp = "rbxassetid://149397702",
			},
			["Blue Aurora"] = {
				SkyboxBk = "rbxassetid://12063984",
				SkyboxDn = "rbxassetid://12064107",
				SkyboxFt = "rbxassetid://12064152",
				SkyboxLf = "rbxassetid://12064121",
				SkyboxRt = "rbxassetid://12064115",
				SkyboxUp = "rbxassetid://12064131",
			},
			Realistic = {
				SkyboxBk = "rbxassetid://144933338",
				SkyboxDn = "rbxassetid://144931530",
				SkyboxFt = "rbxassetid://144933262",
				SkyboxLf = "rbxassetid://144933244",
				SkyboxRt = "rbxassetid://144933299",
				SkyboxUp = "rbxassetid://144931564",
			},
			StarsShader = {
				SkyboxBk = "rbxassetid://169210090",
				SkyboxDn = "rbxassetid://169210108",
				SkyboxFt = "rbxassetid://169210121",
				SkyboxLf = "rbxassetid://169210133",
				SkyboxRt = "rbxassetid://169210143",
				SkyboxUp = "rbxassetid://169210149",
			},
			Gloomy = {
				SkyboxBk = "rbxassetid://5346760450",
				SkyboxDn = "rbxassetid://5346760689",
				SkyboxFt = "rbxassetid://5346760919",
				SkyboxLf = "rbxassetid://5346761102",
				SkyboxRt = "rbxassetid://5346761335",
				SkyboxUp = "rbxassetid://5346761509",
			},
			["Nebula Purple"] = {
				SkyboxBk = "rbxassetid://129876530632297",
				SkyboxDn = "rbxassetid://108406529909981",
				SkyboxFt = "rbxassetid://104400530594543",
				SkyboxLf = "rbxassetid://73372229972523",
				SkyboxRt = "rbxassetid://87408857415924",
				SkyboxUp = "rbxassetid://13781740568136",
			},
			Jungle = {
				SkyboxBk = "rbxassetid://214399891",
				SkyboxDn = "rbxassetid://214399887",
				SkyboxFt = "rbxassetid://214399894",
				SkyboxLf = "rbxassetid://214405668",
				SkyboxRt = "rbxassetid://214399899",
				SkyboxUp = "rbxassetid://214399889",
			},
			Spongebob = {
				SkyboxBk = "rbxassetid://15962101128",
				SkyboxDn = "rbxassetid://15970246218",
				SkyboxFt = "rbxassetid://15962101128",
				SkyboxLf = "rbxassetid://15962101128",
				SkyboxRt = "rbxassetid://15962101128",
				SkyboxUp = "rbxassetid://15962901054",
			},
			["mountain scape"] = {
				SkyboxBk = "rbxassetid://12474836637",
				SkyboxDn = "rbxassetid://12474837052",
				SkyboxFt = "rbxassetid://12474836748",
				SkyboxLf = "rbxassetid://12474836935",
				SkyboxRt = "rbxassetid://12474836446",
				SkyboxUp = "rbxassetid://12474835757",
			},
			["yellowy cloud"] = {
				SkyboxBk = "rbxassetid://252760981",
				SkyboxDn = "rbxassetid://252763035",
				SkyboxFt = "rbxassetid://252761439",
				SkyboxLf = "rbxassetid://252760980",
				SkyboxRt = "rbxassetid://252760986",
				SkyboxUp = "rbxassetid://252762652",
			},
			Aurora = {
				SkyboxBk = "rbxassetid://340908398",
				SkyboxDn = "rbxassetid://340908450",
				SkyboxFt = "rbxassetid://340908468",
				SkyboxLf = "rbxassetid://340908504",
				SkyboxRt = "rbxassetid://340908530",
				SkyboxUp = "rbxassetid://340908586",
			},
			["winter mountain"] = {
				SkyboxBk = "rbxassetid://402229526",
				SkyboxDn = "rbxassetid://402229596",
				SkyboxFt = "rbxassetid://402229293",
				SkyboxLf = "rbxassetid://402229368",
				SkyboxRt = "rbxassetid://402229417",
				SkyboxUp = "rbxassetid://402229564",
			},
			stormy = {
				SkyboxBk = "rbxassetid://255027929",
				SkyboxDn = "rbxassetid://255027967",
				SkyboxFt = "rbxassetid://255027923",
				SkyboxLf = "rbxassetid://255027938",
				SkyboxRt = "rbxassetid://255027946",
				SkyboxUp = "rbxassetid://255027960",
			},
		};
	local j = nil;
	local s = nil;
	local G = nil;
	local g = nil;
	local T = nil;
	local function c()
		g = k:FindFirstChildOfClass("Sky");
		if g then
			T = g:Clone();
		end;
	end;
	c();
	local function e()
		if not G then
			G = Instance.new("Sky");
			G.Name = "CustomSkybox";
			G.Parent = k;
		end;
		return G;
	end;
	local function b()
		if G then
			G:Destroy();
			G = nil;
		end;
	end;
	local function f()
		if j then
			j:Disconnect();
		end;
		j = l.RenderStepped:Connect(function()
				if not _G.SkyboxSettings.Enabled then
					b();
					return;
				end;
				local l = e();
				local p = v[_G.SkyboxSettings.CurrentSkybox] or _G.SkyboxSettings.CustomTextures;
				l.SkyboxBk = p.SkyboxBk;
				l.SkyboxDn = p.SkyboxDn;
				l.SkyboxFt = p.SkyboxFt;
				l.SkyboxLf = p.SkyboxLf;
				l.SkyboxRt = p.SkyboxRt;
				l.SkyboxUp = p.SkyboxUp;
				l.SunAngularSize = table.find(_G.SkyboxSettings.DisabledElements, "Sun") and 0 or 20;
				l.MoonAngularSize = table.find(_G.SkyboxSettings.DisabledElements, "Moon") and 0 or 11;
				l.StarCount = table.find(_G.SkyboxSettings.DisabledElements, "Stars") and 0 or 3000;
				for k, p in pairs(k:GetChildren()) do
					if p:IsA("Sky") and p ~= l then
						p:Destroy();
					end;
				end;
			end);
	end;
	local function N()
		if j then
			j:Disconnect();
			j = nil;
		end;
		b();
	end;
	a:AddToggle("skybox_master", { Text = "enable", Default = _G.SkyboxSettings.Enabled, Callback = function(k)
			_G.SkyboxSettings.Enabled = k;
			if k then
				f();
			else
				N();
			end;
		end });
	local K = {};
	for k, l in pairs(v) do
		table.insert(K, k);
	end;
	table.sort(K);
	a:AddDropdown("skybox_selection", {
		Text = "skybox",
		Default = _G.SkyboxSettings.CurrentSkybox,
		Values = K,
		Callback = function(k)
			_G.SkyboxSettings.CurrentSkybox = k;
		end,
	});
	a:AddDropdown("disable_elements", {
		Text = "disable elements",
		Values = { "Sun", "Moon", "Stars" },
		Default = _G.SkyboxSettings.DisabledElements,
		Multi = true,
		Callback = function(k)
			_G.SkyboxSettings.DisabledElements = k;
		end,
	});
	_G.AspectRatioSettings = _G.AspectRatioSettings or { Enabled = false, X = 13, Y = 10 };
	local n = workspace.CurrentCamera;
	local d = nil;
	local function Y()
		return _G.AspectRatioSettings.X / _G.AspectRatioSettings.Y;
	end;
	local M = "FinhubAspectRatio";
	local function i()
		if not _G.AspectRatioSettings.Enabled or not n then
			return;
		end;
		local k = Y();
		local l = n.CFrame;
		n.CFrame = CFrame.fromMatrix(l.Position, l.RightVector, l.UpVector * k, -l.LookVector);
	end;
	local function R()
		if d then
			if typeof(d) == "RBXScriptConnection" then
				d:Disconnect();
			end;
			d = nil;
		end;
		pcall(function()
			l:UnbindFromRenderStep(M);
		end);
		local k = pcall(function()
				l:BindToRenderStep(M, Enum.RenderPriority.Camera.Value + 2, i);
			end);
		if not k then
			d = l.RenderStepped:Connect(i);
		end;
	end;
	local function B()
		if d then
			if typeof(d) == "RBXScriptConnection" then
				d:Disconnect();
			end;
			d = nil;
		end;
		pcall(function()
			l:UnbindFromRenderStep(M);
		end);
	end;
	y:AddToggle("aspect_ratio_master", { Text = "enable", Default = _G.AspectRatioSettings.Enabled, Callback = function(k)
			_G.AspectRatioSettings.Enabled = k;
			if k then
				R();
			else
				B();
			end;
		end });
	y:AddSlider("aspect_ratio_x", {
		Text = "X ratio",
		Default = _G.AspectRatioSettings.X,
		Min = 1,
		Max = 13,
		Compact = true,
		Rounding = 0,
		Callback = function(k)
			_G.AspectRatioSettings.X = k;
		end,
	});
	y:AddSlider("aspect_ratio_y", {
		Text = "Y ratio",
		Default = _G.AspectRatioSettings.Y,
		Min = 10,
		Max = 32,
		Compact = true,
		Rounding = 0,
		Callback = function(k)
			_G.AspectRatioSettings.Y = k;
		end,
	});
	if _G.SkyboxSettings.Enabled then
		f();
	end;
	if _G.AspectRatioSettings.Enabled then
		R();
	end;
	(getgenv()).FinhubStopSkyboxUpdater = N;
end))();
((function()
	local k = game:GetService("RunService");
	local l = game:GetService("Workspace");
	local p = {
			rain = {
				enabled = false,
				color = Color3.fromRGB(255, 255, 255),
				sizeScale = 1,
				rate = 100,
				speed_min = 60,
				speed_max = 60,
				size_min = 10,
				size_max = 10,
				transparency_min = .22,
				transparency_max = 1,
				spread = 0,
				brightness = 1,
				light_emission = .05,
				glowScale = 0,
				base_rate = 600,
				texture = "rbxassetid://1822883048",
				locked = true,
				orientation = Enum.ParticleOrientation.FacingCameraWorldUp,
				light_influence = .9,
				lifetime_min = .8,
				lifetime_max = .8,
			},
			snow = {
				enabled = false,
				color = Color3.fromRGB(255, 255, 255),
				sizeScale = 1,
				rate = 100,
				amount = 100,
				speed_min = 30,
				speed_max = 30,
				size_min = .33,
				size_max = .4,
				transparency_min = .74,
				transparency_max = 1,
				spread = 0,
				brightness = 1,
				light_emission = .5,
				glowScale = 0,
				base_rate = 1000,
				texture = "http://www.roblox.com/asset/?id=99851851",
				locked = false,
				orientation = Enum.ParticleOrientation.FacingCamera,
				light_influence = 1,
				lifetime_min = 4,
				lifetime_max = 4,
			},
			lightrain = {
				enabled = false,
				color = Color3.fromRGB(255, 255, 255),
				sizeScale = 1,
				rate = 100,
				speed_min = 30,
				speed_max = 50,
				size_min = .2,
				size_max = .2,
				transparency_min = 0,
				transparency_max = 0,
				spread = 0,
				brightness = 2,
				light_emission = .5,
				glowScale = 0,
				base_rate = 500,
				texture = "rbxasset://textures/particles/sparkles_main.dds",
				locked = true,
				orientation = Enum.ParticleOrientation.FacingCameraWorldUp,
				light_influence = .3,
				lifetime_min = 9,
				lifetime_max = 9,
			},
			stars = {
				enabled = false,
				color = Color3.fromRGB(255, 255, 255),
				sizeScale = 1,
				rate = 100,
				amount = 100,
				speed_min = 30,
				speed_max = 30,
				size_min = .33,
				size_max = .4,
				transparency_min = .74,
				transparency_max = 1,
				spread = 0,
				brightness = 1,
				light_emission = .5,
				glowScale = 0,
				base_rate = 1000,
				texture = "http://www.roblox.com/asset/?id=6822501679",
				locked = false,
				orientation = Enum.ParticleOrientation.FacingCamera,
				light_influence = 1,
				lifetime_min = 4,
				lifetime_max = 4,
			},
			hearts = {
				enabled = false,
				color = Color3.fromRGB(255, 255, 255),
				sizeScale = 1,
				rate = 100,
				amount = 100,
				speed_min = 30,
				speed_max = 30,
				size_min = .33,
				size_max = .4,
				transparency_min = .74,
				transparency_max = 1,
				spread = 0,
				brightness = 1,
				light_emission = .5,
				glowScale = 0,
				base_rate = 1000,
				texture = "http://www.roblox.com/asset/?id=36721240",
				locked = false,
				orientation = Enum.ParticleOrientation.FacingCamera,
				light_influence = 1,
				lifetime_min = 4,
				lifetime_max = 4,
			},
		};
	local a = {};
	local y = {};
	local v = 0;
	local j = l.CurrentCamera;
	local s = Vector3.new(0, 20, 0);
	local function G(k, l)
		k.EmissionDirection = Enum.NormalId.Bottom;
		k.LockedToPart = l.locked;
		k.Orientation = l.orientation;
		k.Texture = l.texture;
		k.Speed = NumberRange.new(l.speed_min, math.max(l.speed_min, l.speed_max));
		k.Lifetime = NumberRange.new(l.lifetime_min, math.max(l.lifetime_min, l.lifetime_max));
		k.SpreadAngle = Vector2.new(l.spread, l.spread);
		k.LightInfluence = l.light_influence;
		local p = math.clamp(tonumber(l.glowScale) or 0, 0, 5);
		if p > 0 then
			local a = p / 5;
			k.Brightness = l.brightness * ((1 + p * 1.75));
			k.LightEmission = math.clamp(((l.light_emission or 0)) + p * .2, 0, 1);
			k.LightInfluence = math.clamp(((l.light_influence or 1)) * ((1 - a * .98)), 0, 1);
		else
			k.Brightness = l.brightness;
			k.LightEmission = l.light_emission;
		end;
		if l.amount then
			k.Rate = math.clamp(l.amount, 1, 1000);
		else
			k.Rate = l.base_rate * ((l.rate / 100));
		end;
		k.Size = NumberSequence.new({ NumberSequenceKeypoint.new(0, l.size_min * ((l.sizeScale or 1))), NumberSequenceKeypoint.new(1, l.size_max * ((l.sizeScale or 1))) });
		k.Transparency = NumberSequence.new({
				NumberSequenceKeypoint.new(0, l.transparency_max),
				NumberSequenceKeypoint.new(.25, l.transparency_min),
				NumberSequenceKeypoint.new(.75, l.transparency_min),
				NumberSequenceKeypoint.new(1, l.transparency_max),
			});
		k.Color = ColorSequence.new(l.color);
	end;
	local function g(k)
		local j = p[k];
		if a[k] then
			a[k]:Destroy();
			a[k] = nil;
			y[k] = nil;
			v = v - 1;
		end;
		local s = Instance.new("Part");
		s.Size = Vector3.new(40, 1, 85);
		s.CanCollide = false;
		s.Massless = true;
		s.CastShadow = false;
		s.Transparency = 1;
		s.Anchored = true;
		s.Name = "\000";
		s.Parent = l;
		a[k] = s;
		local g = Instance.new("ParticleEmitter");
		G(g, j);
		g.Parent = s;
		y[k] = g;
		v = v + 1;
	end;
	local function T(k)
		if a[k] then
			a[k]:Destroy();
			a[k] = nil;
			y[k] = nil;
			v = v - 1;
		end;
	end;
	local function c(k)
		if y[k] then
			G(y[k], p[k]);
		end;
	end;
	local function e()
		for k in next, y do
			G(y[k], p[k]);
		end;
	end;
	k.PostSimulation:Connect(function()
		if v == 0 then
			return;
		end;
		local k = j.CFrame.Position + s;
		local l = CFrame.new(k);
		for k, p in next, a do
			p.CFrame = l;
		end;
	end);
	local b = {
			color = Color3.fromRGB(255, 255, 255),
			rate = 100,
			speed_min = 40,
			speed_max = 60,
			size_min = .33,
			size_max = .4,
			transparency_min = .5,
			transparency_max = 1,
			spread = 0,
			brightness = 1,
			light_emission = .5,
			glowScale = 0,
			sizeScale = 1,
		};
	local function f(k)
		local l = p[k];
		l.color = b.color;
		l.rate = b.rate;
		l.speed_min = b.speed_min;
		l.speed_max = b.speed_max;
		l.size_min = b.size_min;
		l.size_max = b.size_max;
		l.transparency_min = b.transparency_min;
		l.transparency_max = b.transparency_max;
		l.spread = b.spread;
		l.brightness = b.brightness;
		l.light_emission = b.light_emission;
		l.glowScale = b.glowScale;
		l.sizeScale = b.sizeScale;
	end;
	local N = F.World:AddLeftGroupbox("weather");
	local K = false;
	(N:AddToggle("weather_master_enable", { Text = "enable", Default = false, Callback = function(k)
			K = k;
			if not k then
				for k in next, p do
					T(k);
				end;
			end;
		end })):AddColorPicker("weather_color", { Default = b.color, Title = "color", Callback = function(k)
			b.color = k;
			for l in next, y do
				p[l].color = k;
				y[l].Color = ColorSequence.new(k);
			end;
		end });
	local n = {
			rain = "rain",
			snow = "snow",
			["light rain"] = "lightrain",
			stars = "stars",
			hearts = "hearts",
		};
	N:AddDropdown("weather_effects", {
		Text = "effects",
		Values = {
			"rain",
			"snow",
			"light rain",
			"stars",
			"hearts",
		},
		Default = {},
		Multi = true,
		Tooltip = "select which weather effects to enable",
		Callback = function(k)
			for l, a in next, n do
				local y = k[l] == true;
				p[a].enabled = y;
				if y and K then
					g(a);
					f(a);
					c(a);
				elseif not y then
					T(a);
				end;
			end;
		end,
	});
	N:AddSlider("weather_rate", {
		Text = "rate",
		Default = b.rate,
		Min = 0,
		Max = 100,
		Rounding = 0,
		Suffix = "%",
		Compact = true,
		Callback = function(k)
			b.rate = k;
			for l in next, y do
				p[l].rate = k;
				y[l].Rate = p[l].base_rate * ((k / 100));
			end;
		end,
	});
	N:AddSlider("weather_speed_min", {
		Text = "speed min",
		Default = b.speed_min,
		Min = 0,
		Max = 200,
		Rounding = 1,
		Compact = true,
		Callback = function(k)
			b.speed_min = k;
			for l in next, y do
				p[l].speed_min = k;
			end;
			e();
		end,
	});
	N:AddSlider("weather_speed_max", {
		Text = "speed max",
		Default = b.speed_max,
		Min = 0,
		Max = 200,
		Rounding = 1,
		Compact = true,
		Callback = function(k)
			b.speed_max = k;
			for l in next, y do
				p[l].speed_max = k;
			end;
			e();
		end,
	});
	N:AddSlider("weather_size_min", {
		Text = "size min",
		Default = b.size_min,
		Min = .1,
		Max = 50,
		Rounding = 2,
		Compact = true,
		Callback = function(k)
			b.size_min = k;
			for l in next, y do
				p[l].size_min = k;
			end;
			e();
		end,
	});
	N:AddSlider("weather_size_max", {
		Text = "size max",
		Default = b.size_max,
		Min = .1,
		Max = 50,
		Rounding = 2,
		Compact = true,
		Callback = function(k)
			b.size_max = k;
			for l in next, y do
				p[l].size_max = k;
			end;
			e();
		end,
	});
	N:AddSlider("weather_opacity_min", {
		Text = "opacity min",
		Default = math.floor(((1 - b.transparency_min)) * 100),
		Min = 0,
		Max = 100,
		Rounding = 0,
		Suffix = "%",
		Compact = true,
		Callback = function(k)
			b.transparency_min = 1 - (k / 100);
			for k in next, y do
				p[k].transparency_min = b.transparency_min;
			end;
			e();
		end,
	});
	N:AddSlider("weather_opacity_max", {
		Text = "opacity max",
		Default = math.floor(((1 - b.transparency_max)) * 100),
		Min = 0,
		Max = 100,
		Rounding = 0,
		Suffix = "%",
		Compact = true,
		Callback = function(k)
			b.transparency_max = 1 - (k / 100);
			for k in next, y do
				p[k].transparency_max = b.transparency_max;
			end;
			e();
		end,
	});
	N:AddSlider("weather_spread", {
		Text = "spread",
		Default = 0,
		Min = 0,
		Max = 180,
		Rounding = 0,
		Suffix = "\194\176",
		Compact = true,
		Callback = function(k)
			b.spread = k;
			for l in next, y do
				p[l].spread = k;
			end;
			e();
		end,
	});
	N:AddSlider("weather_brightness", {
		Text = "brightness",
		Default = b.brightness * 100,
		Min = 0,
		Max = 500,
		Rounding = 0,
		Suffix = "%",
		Compact = true,
		Callback = function(k)
			b.brightness = k / 100;
			for k in next, y do
				p[k].brightness = b.brightness;
			end;
			e();
		end,
	});
	N:AddSlider("weather_emission", {
		Text = "light emission",
		Default = b.light_emission * 100,
		Min = 0,
		Max = 100,
		Rounding = 0,
		Suffix = "%",
		Compact = true,
		Callback = function(k)
			b.light_emission = k / 100;
			for k in next, y do
				p[k].light_emission = b.light_emission;
			end;
			e();
		end,
	});
	N:AddSlider("weather_glow", {
		Text = "glow",
		Default = b.glowScale,
		Min = 0,
		Max = 5,
		Rounding = 2,
		Suffix = "x",
		Compact = true,
		Callback = function(k)
			b.glowScale = k;
			for l in next, y do
				p[l].glowScale = k;
			end;
			e();
		end,
	});
	N:AddSlider("weather_sizescale", {
		Text = "particle size",
		Default = b.sizeScale,
		Min = .1,
		Max = 5,
		Rounding = 2,
		Suffix = "x",
		Compact = true,
		Callback = function(k)
			b.sizeScale = k;
			for l in next, y do
				p[l].sizeScale = k;
			end;
			e();
		end,
	});
	(game:GetService("ScriptContext")).DescendantRemoving:Connect(function(k)
		if k == script then
			local k = (getgenv()).FinhubStopSkyboxUpdater;
			if k then
				k();
			end;
		end;
	end);
end))();
((function()
	b:OnUnload(function()
		local k = (getgenv()).FinhubMenuCursor;
		if k then
			if k.cursor then
				pcall(function()
					k.cursor:Remove();
				end);
			end;
			if k.outline then
				pcall(function()
					k.outline:Remove();
				end);
			end;
			k.initialized = false;
		end;
		if kbHeartbeatConn then
			kbHeartbeatConn:Disconnect();
			kbHeartbeatConn = nil;
		end;
		local l = (getgenv()).FinhubKeybindList;
		if l and l.gui then
			pcall(function()
				l.gui:Destroy();
			end);
		end;
		(getgenv()).FinhubKeybindList = nil;
		b.Unloaded = true;
	end);
	b.KeybindFrame.Visible = false;
	(getgenv()).FinhubKeybindList = (getgenv()).FinhubKeybindList or {};
	local k = (getgenv()).FinhubKeybindList;
	local l = 18;
	local p = 8;
	local a = 28;
	local y = 36;
	local v = 3;
	local j = 148;
	local s = 260;
	local G = 4;
	local g = 24;
	local T = .5;
	local c = 0;
	local e = "";
	local f = true;
	local N = nil;
	local K = {
			SilentAimKey = "silent aim",
			SelectTargetKey = "backshoot",
			AimbotKey = "aimbot",
			TargetKey = "ragebot",
			ThirdPersonKey = "third person",
			cframespf_key = "walkspeed",
			cframefly_key = "fly",
			shaders_key = "shaders",
			MenuKeybind = "menu",
			AnimKey = "anim",
		};
	local function n(k)
		return string.lower(tostring(k or ""));
	end;
	local function d(k, l)
		local p = K[k];
		if p then
			return p;
		end;
		local a = l and ((l.Text or l.Title));
		if type(a) == "string" and a ~= "" then
			return n(a);
		end;
		return n((((tostring(k))):gsub("_key$", "")):gsub("_", " "));
	end;
	local function Y(k, l)
		if b and b[k] then
			return b[k];
		end;
		return l;
	end;
	local function M(k, l)
		k = tostring(k or "");
		local p = game:GetService("TextService");
		local a, y = pcall(function()
				local a = (getgenv()).FinhubGetUiFont and (getgenv()).FinhubGetUiFont();
				if typeof(a) == "Font" then
					return p:GetTextSize(k, l, a, Vector2.new(999, 24));
				end;
				return p:GetTextSize(k, l, Enum.Font.GothamMedium, Vector2.new(999, 24));
			end);
		if a and y then
			return y.X;
		end;
		return #k * 6.5;
	end;
	local function i(k)
		local l = (M("keybinds", 14) + p * 2) + 8;
		for k, a in ipairs(k) do
			l = math.max(l, (M(a.text, 12) + p * 2) + 12);
		end;
		return math.clamp(math.ceil(l), j, s);
	end;
	local R;
	local function B()
		if k.gui and k.uiVer == G then
			return;
		end;
		if k.gui then
			pcall(function()
				k.gui:Destroy();
			end);
			k.gui = nil;
			k.outer = nil;
			k.inner = nil;
			k.scroll = nil;
			k.rowPool = {};
		end;
		local s = (gethui and gethui()) or game:GetService("CoreGui");
		local T = Instance.new("ScreenGui");
		T.Name = "instance_keybind_list";
		T.ResetOnSpawn = false;
		T.IgnoreGuiInset = true;
		T.ZIndexBehavior = Enum.ZIndexBehavior.Global;
		T.DisplayOrder = L;
		T.Enabled = false;
		T.Parent = s;
		local c = Instance.new("Frame");
		c.Name = "KbOuter";
		c.Size = UDim2.fromOffset(j, 72);
		c.Position = UDim2.new(0, 10, .5, -36);
		c.BorderSizePixel = 0;
		c.BackgroundColor3 = Color3.new(0, 0, 0);
		c.Active = true;
		c.Draggable = true;
		c.Parent = T;
		local e = Instance.new("Frame");
		e.Name = "KbInner";
		e.Size = UDim2.new(1, -2, 1, -2);
		e.Position = UDim2.fromOffset(1, 1);
		e.BorderSizePixel = 1;
		e.BorderMode = Enum.BorderMode.Inset;
		e.Parent = c;
		local b = Instance.new("Frame");
		b.BackgroundTransparency = 1;
		b.Size = UDim2.new(1, 0, 0, a);
		b.Parent = e;
		local N = Instance.new("TextLabel");
		N.Name = "Title";
		N.BackgroundTransparency = 1;
		N.Size = UDim2.new(1, 0, 1, 0);
		N.Position = UDim2.fromOffset(0, 0);
		N.Text = "keybinds";
		N.TextXAlignment = Enum.TextXAlignment.Center;
		N.TextYAlignment = Enum.TextYAlignment.Center;
		N.Parent = b;
		(getgenv()).FinhubApplyUiFont(N, 14);
		local K = Instance.new("Frame");
		K.Name = "Divider";
		K.BorderSizePixel = 0;
		K.Size = UDim2.new(1, -((p * 2)), 0, 1);
		K.Position = UDim2.new(0, p, 0, a - 1);
		K.Parent = e;
		local n = Instance.new("ScrollingFrame");
		n.Name = "List";
		n.BorderSizePixel = 1;
		n.BorderMode = Enum.BorderMode.Inset;
		n.BackgroundTransparency = 0;
		n.Size = UDim2.new(1, -((p * 2)), 1, -((y + p)));
		n.Position = UDim2.new(0, p, 0, y);
		n.CanvasSize = UDim2.fromOffset(0, 0);
		n.ScrollBarThickness = 3;
		n.ScrollBarImageTransparency = .25;
		n.AutomaticCanvasSize = Enum.AutomaticSize.None;
		n.ScrollingDirection = Enum.ScrollingDirection.Y;
		n.Parent = e;
		local d = Instance.new("UIListLayout");
		d.Name = "KbListLayout";
		d.SortOrder = Enum.SortOrder.LayoutOrder;
		d.Padding = UDim.new(0, v);
		d.HorizontalAlignment = Enum.HorizontalAlignment.Center;
		d.VerticalAlignment = Enum.VerticalAlignment.Center;
		d.Parent = n;
		local Y = Instance.new("UIPadding");
		Y.PaddingLeft = UDim.new(0, 4);
		Y.PaddingRight = UDim.new(0, 4);
		Y.PaddingTop = UDim.new(0, 4);
		Y.PaddingBottom = UDim.new(0, 4);
		Y.Parent = n;
		k.gui = T;
		k.outer = c;
		k.inner = e;
		k.scroll = n;
		k.layout = d;
		k.title = N;
		k.divider = K;
		k.rowPool = {};
		for p = 1, g, 1 do
			local a = Instance.new("Frame");
			a.BorderSizePixel = 0;
			a.BackgroundTransparency = 1;
			a.Size = UDim2.new(1, 0, 0, l);
			a.Visible = false;
			a.Parent = n;
			local y = Instance.new("TextLabel");
			y.BackgroundTransparency = 1;
			y.Size = UDim2.new(1, 0, 1, 0);
			y.Position = UDim2.fromOffset(0, 0);
			y.TextXAlignment = Enum.TextXAlignment.Center;
			y.TextYAlignment = Enum.TextYAlignment.Center;
			y.TextTruncate = Enum.TextTruncate.AtEnd;
			y.Parent = a;
			(getgenv()).FinhubApplyUiFont(y, 12);
			k.rowPool[p] = { frame = a, lbl = y };
		end;
		k.uiVer = G;
		f = true;
		if R then
			R();
		end;
	end;
	R = function()
			if not k.inner then
				return;
			end;
			local l = Y("MainColor", Color3.fromRGB(8, 8, 8));
			local p = Y("BackgroundColor", Color3.fromRGB(5, 5, 5));
			local a = Y("AccentColor", Color3.fromRGB(255, 105, 180));
			local y = Y("FontColor", Color3.fromRGB(255, 255, 255));
			local v = Y("OutlineColor", Color3.fromRGB(25, 25, 25));
			if k.outer then
				k.outer.BackgroundColor3 = Color3.new(0, 0, 0);
				k.outer.BorderSizePixel = 0;
			end;
			k.inner.BackgroundColor3 = l;
			k.inner.BorderColor3 = v;
			if k.title then
				k.title.TextColor3 = y;
				(getgenv()).FinhubApplyUiFont(k.title, 14);
			end;
			if k.divider then
				k.divider.BackgroundColor3 = a;
				k.divider.BorderSizePixel = 0;
			end;
			if k.scroll then
				k.scroll.BackgroundColor3 = p;
				k.scroll.BackgroundTransparency = 0;
				k.scroll.BorderSizePixel = 1;
				k.scroll.BorderColor3 = v;
				k.scroll.ScrollBarImageColor3 = a;
				k.scroll.ScrollBarImageTransparency = .25;
			end;
			for k, l in ipairs(k.rowPool) do
				if l.lbl then
					l.lbl.TextColor3 = y;
					(getgenv()).FinhubApplyUiFont(l.lbl, 12);
				end;
				if l.frame then
					l.frame.BackgroundTransparency = 1;
				end;
			end;
			f = false;
		end;
	local function W()
		if not Options then
			return "";
		end;
		local k = {};
		for l, p in pairs(Options) do
			if p and p.Type == "KeyPicker" then
				local a = p.Value;
				if a and (a ~= "None" and a ~= "") then
					k[#k + 1] = tostring(l) .. (":" .. tostring(a));
				end;
			end;
		end;
		table.sort(k);
		return table.concat(k, "|");
	end;
	local function P()
		local k = {};
		if not Options then
			return k;
		end;
		for l, p in pairs(Options) do
			if p and p.Type == "KeyPicker" then
				local a = p.Value;
				if a and (a ~= "None" and a ~= "") then
					local y = d(l, p);
					local v = n(a);
					k[#k + 1] = { text = y .. (" - " .. v) };
				end;
			end;
		end;
		table.sort(k, function(k, l)
			return k.text < l.text;
		end);
		return k;
	end;
	local function S(a)
		if not k.scroll then
			return;
		end;
		local j = W();
		if not a and j == e then
			return;
		end;
		e = j;
		local s = P();
		local G = math.min(#s, g);
		for l = 1, g, 1 do
			local p = k.rowPool[l];
			if not p then
				continue;
			end;
			if l <= G then
				local k = s[l];
				p.frame.Visible = true;
				if p.lbl and k then
					local l = k.text;
					if p.lbl.Text ~= l then
						p.lbl.Text = l;
					end;
				end;
			elseif p.frame then
				p.frame.Visible = false;
			end;
		end;
		local T = l + v;
		local c = math.max(G * l + math.max(G - 1, 0) * v, 0);
		local b = i(s);
		local N = math.max(G, 1) * T + 8;
		local K = math.clamp(((y + p) + N) + p, 72, 360);
		local n = (K - y) - p;
		local d = math.max(c, n);
		if k.layout then
			k.layout.VerticalAlignment = G <= 2 and Enum.VerticalAlignment.Center or Enum.VerticalAlignment.Top;
		end;
		if k.scroll.CanvasSize.Y.Offset ~= d then
			k.scroll.CanvasSize = UDim2.fromOffset(0, d);
		end;
		if k.outer then
			local l = k.outer.Size;
			if l.X.Offset ~= b or l.Y.Offset ~= K then
				k.outer.Size = UDim2.fromOffset(b, K);
			end;
		end;
		if f then
			R();
		end;
	end;
	(getgenv()).FinhubApplyKbListTheme = function()
			f = true;
			R();
		end;
	(getgenv()).FinhubRefreshKbList = function(k)
			S(k == true);
		end;
	(getgenv()).syncKbListVisible = function()
			B();
			local l = Toggles and (Toggles.KeybindVisible and Toggles.KeybindVisible.Value);
			local p = V();
			k.visible = l == true and not p;
			if k.gui then
				k.gui.Enabled = k.visible;
			end;
			if k.visible then
				e = "";
				f = true;
				S(true);
			end;
		end;
	local function X()
		if N then
			return;
		end;
		local l = game:GetService("RunService");
		N = l.Heartbeat:Connect(function(l)
				if b.Unloaded then
					if N then
						N:Disconnect();
						N = nil;
					end;
					return;
				end;
				if not k.visible or not k.gui or not k.gui.Enabled then
					c = 0;
					return;
				end;
				c = c + ((l or 0));
				if c < T then
					return;
				end;
				c = 0;
				pcall(S);
			end);
	end;
	X();
	do
		local k = (getgenv()).FinhubSyncAfterConfigLoad;
		(getgenv()).FinhubSyncAfterConfigLoad = function()
				if k then
					pcall(k);
				end;
				if (getgenv()).syncKbListVisible then
					pcall((getgenv()).syncKbListVisible);
				end;
			end;
	end;
	MenuGroup = F["UI Settings"]:AddLeftGroupbox("menu settings");
	local function Z()
		if not b or not b.ScreenGui then
			return;
		end;
		for k, l in ipairs(b.ScreenGui:GetChildren()) do
			if l:IsA("Frame") and (l.ZIndex >= 14 and l.ZIndex <= 16) then
				l.Visible = false;
			end;
		end;
	end;
	local function E()
		x();
		local k = V();
		Z();
		if b and b.KeybindFrame then
			b.KeybindFrame.Visible = false;
		end;
		if (getgenv()).syncKbListVisible then
			(getgenv()).syncKbListVisible();
		end;
		if (getgenv()).syncCosmeticChangerVisibility then
			(getgenv()).syncCosmeticChangerVisibility();
		end;
		if k and ((getgenv()).FinhubCosmeticUIState and (getgenv()).FinhubCosmeticUIState.applyLiveViewModelUpdate) then
			task.defer(function()
				pcall((getgenv()).FinhubCosmeticUIState.applyLiveViewModelUpdate);
			end);
		end;
		if k and (getgenv()).FinhubApplyCosmeticUiTheme then
			pcall((getgenv()).FinhubApplyCosmeticUiTheme);
		end;
	end;
	if t and t.Holder then
		(t.Holder:GetPropertyChangedSignal("Visible")):Connect(E);
	end;
	task.defer(E);
	(MenuGroup:AddLabel("menu bind")):AddKeyPicker("MenuKeybind", { Default = "RightShift", NoUI = true, Text = "menu keybind" });
	b.ToggleKeybind = Options.MenuKeybind;
	MenuGroup:AddToggle("KeybindVisible", { Text = "keybind list", Default = false, Callback = function()
			if (getgenv()).syncKbListVisible then
				(getgenv()).syncKbListVisible();
			end;
		end });
	MenuGroup:AddSlider("tgbtgbbktbkmb", {
		Text = "cap fps at (0 = off)",
		Default = 0,
		Min = 0,
		Max = 9999,
		Rounding = 1,
		Compact = true,
		Callback = function(k)
			if not setfpscap then
				return;
			end;
			if k == nil or k <= 0 or k >= 9999 then
				setfpscap(0);
			else
				setfpscap(math.floor(k));
			end;
		end,
	});
	MenuGroup:AddDropdown("FinhubLanguage", {
		Text = "language",
		Values = { "english", "korean", "spanish" },
		Default = (getgenv()).FinhubLanguage or "english",
		Callback = function(k)
			(getgenv()).FinhubLanguage = k;
			if (getgenv()).FinhubReloadLanguageFont then
				(getgenv()).FinhubReloadLanguageFont(k);
			end;
			if (getgenv()).FinhubApplyUIFont then
				(getgenv()).FinhubApplyUIFont();
			end;
			if (getgenv()).FinhubRefreshLocaleUI then
				pcall((getgenv()).FinhubRefreshLocaleUI);
			end;
		end,
	});
	MenuGroup:AddButton("Unload Menu", function()
		b:Unload();
	end);
end))();
((function()
	local k = F["UI Settings"]:AddRightGroupbox("server");
	local l = game:GetService("HttpService");
	local p = game:GetService("TeleportService");
	local a = game:GetService("Players");
	local y = a.LocalPlayer;
	local function v(k)
		local p = "https://games.roblox.com/v1/games/" .. (game.PlaceId .. "/servers/Public?sortOrder=Asc&limit=100");
		if k then
			p = p .. ("&cursor=" .. k);
		end;
		local a, y = pcall(function()
				return game:HttpGet(p);
			end);
		if not a then
			return nil;
		end;
		local v, j = pcall(function()
				return l:JSONDecode(y);
			end);
		if not v or not j or not j.data then
			return nil;
		end;
		return j;
	end;
	local function j()
		local k = {};
		local l = nil;
		repeat
			local p = v(l);
			if not p then
				break;
			end;
			for l, p in ipairs(p.data) do
				if p.playing < p.maxPlayers and p.id ~= game.JobId then
					table.insert(k, p);
				end;
			end;
			l = p.nextPageCursor;
		until not l;
		return k;
	end;
	k:AddButton("Rejoin Server", function()
		if game.JobId ~= "" then
			b:Notify("Rejoining server...", 3);
			p:TeleportToPlaceInstance(game.PlaceId, game.JobId, y);
		else
			b:Notify("No valid JobId.", 3);
		end;
	end);
	k:AddButton("Server Hop (Random)", function()
		local k = j();
		if #k == 0 then
			b:Notify("No servers found.", 3);
			return;
		end;
		local l = k[math.random(#k)];
		b:Notify("Hopping to server (" .. (l.playing .. ("/" .. (l.maxPlayers .. " players)"))), 5);
		p:TeleportToPlaceInstance(game.PlaceId, l.id, y);
	end);
	k:AddButton("Join Lowest Players Server", function()
		local k = j();
		if #k == 0 then
			b:Notify("No servers found.", 3);
			return;
		end;
		table.sort(k, function(k, l)
			return k.playing < l.playing;
		end);
		local l = k[1];
		b:Notify("Joining lowest server (" .. (l.playing .. ("/" .. (l.maxPlayers .. " players)"))), 5);
		p:TeleportToPlaceInstance(game.PlaceId, l.id, y);
	end);
	k:AddButton("Join Biggest Server", function()
		local k = j();
		if #k == 0 then
			b:Notify("No servers found.", 3);
			return;
		end;
		table.sort(k, function(k, l)
			return k.playing > l.playing;
		end);
		local l = k[1];
		b:Notify("Joining biggest server (" .. (l.playing .. ("/" .. (l.maxPlayers .. " players)"))), 5);
		p:TeleportToPlaceInstance(game.PlaceId, l.id, y);
	end);
end))();
local zt = F.Character:AddRightTabbox();
local wt = (game:GetService("Players")).LocalPlayer;
local qt = game:GetService("Workspace");
local ut = game:GetService("RunService");
(getgenv())._animCmd = (getgenv())._animCmd or nil;
local Ht = {
		Meditate = "rbxassetid://507771019",
		Tornado = "rbxassetid://313762630",
		["Orbit V2"] = "rbxassetid://429730430",
		["Orbit V3"] = "rbxassetid://186934910",
		Floss = "rbxassetid://181526230",
		Invincible = "rbxassetid://188632011",
		Cute = "rbxassetid://179224234",
		["30 Meters"] = "rbxassetid://180612465",
		["Ashi Dance"] = "rbxassetid://182724289",
		Crazy = "rbxassetid://429703734",
		["Take the L"] = "rbxassetid://204292303",
		["Prince of Egypt"] = "rbxassetid://204295235",
		Invisible = "rbxassetid://33796059",
		["Sea Lion"] = "rbxassetid://184574340",
		Gangnam = "rbxassetid://35978879",
		["Trip Out"] = "rbxassetid://35154961",
		["The Man"] = "rbxassetid://282574440",
		["Spider Shit"] = "rbxassetid://204328711",
		["Low Cortisol"] = "rbxassetid://45834924",
		["Zombie Run"] = "rbxassetid://204062532",
	};
local Ft = {
		"Meditate",
		"Tornado",
		"Orbit V2",
		"Orbit V3",
		"Floss",
		"Invincible",
		"Cute",
		"30 Meters",
		"Ashi Dance",
		"Crazy",
		"Take the L",
		"Prince of Egypt",
		"Invisible",
		"Sea Lion",
		"Gangnam",
		"Trip Out",
		"The Man",
		"Spider Shit",
		"Low Cortisol",
		"Zombie Run",
	};
(sn:AddToggle("AnimEnabled", { Text = "enabled", Default = false })):AddKeyPicker("AnimKey", {
	Default = "None",
	SyncToggleState = true,
	Mode = "Toggle",
	Text = "anim",
});
sn:AddToggle("AnimServerVisible", { Text = "server side", Default = true });
sn:AddToggle("AnimJitter", { Text = "jitter mode", Default = false });
sn:AddSlider("JitterSpeed", {
	Text = "jitter interval",
	Default = .1,
	Min = .01,
	Max = 2,
	Rounding = 2,
	Suffix = "s",
});
sn:AddToggle("AnimLoop", { Text = "loop", Default = true });
sn:AddToggle("AnimAutoRespawn", { Text = "spawn proof", Default = true });
sn:AddInput("AnimForceID", {
	Default = "96579993895076",
	Text = "primary anim id",
	Placeholder = "numbers",
	Numeric = false,
	Finished = true,
});
sn:AddInput("AnimJitterID", {
	Default = "120819498172771",
	Text = "jitter anim id",
	Placeholder = "numbers",
	Numeric = false,
	Finished = true,
});
sn:AddSlider("AnimSpeed", {
	Text = "play speed",
	Default = 2,
	Min = .1,
	Max = 200,
	Rounding = 1,
});
sn:AddDropdown("AnimPresetSelector", { Text = "primary presets", Values = Ft, Default = "Meditate" });
sn:AddDropdown("AnimJitterSelector", { Text = "jitter presets", Values = Ft, Default = "Crazy" });
Options.AnimPresetSelector:OnChanged(function()
	local k = Ht[Options.AnimPresetSelector.Value];
	if k then
		Options.AnimForceID:SetValue(k);
		if Toggles.AnimEnabled.Value then
			(getgenv())._animCmd = "Play";
		end;
	end;
end);
Options.AnimJitterSelector:OnChanged(function()
	local k = Ht[Options.AnimJitterSelector.Value];
	if k then
		Options.AnimJitterID:SetValue(k);
		if Toggles.AnimEnabled.Value and Toggles.AnimJitter.Value then
			(getgenv())._animCmd = "Play";
		end;
	end;
end);
sn:AddButton("play", function()
	(getgenv())._animCmd = "Play";
end);
sn:AddButton("stop", function()
	(getgenv())._animCmd = "Stop";
end);
((function()
	local k = {};
	local l = {
			primary = "",
			jitter = "",
			jitterOn = false,
			serverVisible = false,
		};
	local p = 0;
	local a = 1;
	local y = {};
	local v = 0;
	local function j(k)
		local l = (tostring(k or "")):gsub("rbxassetid://", "");
		local p = l:match("%d+");
		return p or "";
	end;
	local function s(k)
		if not k then
			return;
		end;
		pcall(function()
			local l = k:FindFirstChild("Animate");
			if l then
				l.Disabled = true;
				l:Destroy();
			end;
		end);
	end;
	local function G()
		local k, l = pcall(function()
				return require(wt.PlayerScripts.Controllers.FighterController);
			end);
		if k and (l and (l.LocalFighter and l.LocalFighter.Entity)) then
			return l.LocalFighter.Entity;
		end;
		return nil;
	end;
	local function g(k)
		return typeof(k) == "Finhub" and k:IsA("Model");
	end;
	local function T()
		local k = tick();
		if k - v < .35 and #y > 0 then
			return y;
		end;
		v = k;
		y = {};
		local l = qt:FindFirstChild("Live");
		local p = l and l:FindFirstChild(wt.Name);
		if p and typeof(p) == "Finhub" then
			local k = {};
			local function l(l)
				if g(l) and (l.Parent and not k[l]) then
					k[l] = true;
					table.insert(y, l);
				end;
			end;
			if g(p) then
				l(p);
			end;
			pcall(function()
				for k, p in p:GetChildren() do
					if g(p) and ((p:FindFirstChild("HumanoidRootPart") or p:FindFirstChild("RootPart"))) then
						l(p);
					end;
				end;
			end);
			if #y == 0 and g(p) then
				l(p);
			end;
		end;
		local a = G();
		if g(a) then
			local k = false;
			for l, p in y do
				if p == a then
					k = true;
					break;
				end;
			end;
			if not k then
				table.insert(y, a);
			end;
		end;
		return y;
	end;
	local function c(k)
		k = j(k);
		if k == "" then
			return nil;
		end;
		local l, p = pcall(function()
				return game:GetObjects("rbxassetid://" .. k);
			end);
		if not l or not p or #p == 0 then
			return nil;
		end;
		local a = nil;
		for k, l in pairs(p) do
			if typeof(l) == "Finhub" and l:IsA("Animation") then
				a = l;
			elseif typeof(l) == "Finhub" then
				for k, l in pairs(l:GetDescendants()) do
					if l:IsA("Animation") and l.AnimationId ~= "" then
						a = l;
						break;
					end;
				end;
			end;
			if a then
				break;
			end;
		end;
		if a then
			return a;
		end;
		local y = Instance.new("Animation");
		y.AnimationId = "rbxassetid://" .. k;
		return y;
	end;
	local function e(k, l)
		if not k or not k.Parent then
			return nil, nil;
		end;
		local p = k:FindFirstChildOfClass("Humanoid");
		if not p then
			p = k:FindFirstChildWhichIsA("Humanoid", true);
		end;
		if not p then
			local a, y = pcall(function()
					return k:WaitForChild("Humanoid", l or 8);
				end);
			p = a and y or nil;
		end;
		if not p then
			return nil, nil;
		end;
		local a = p:FindFirstChildOfClass("Animator");
		if not a then
			local k, y = pcall(function()
					return p:WaitForChild("Animator", l or 8);
				end);
			a = k and y or nil;
		end;
		if not a then
			a = Instance.new("Animator");
			a.Parent = p;
		end;
		return a, p;
	end;
	local function f(k, l, p, a, y)
		if not k or not l then
			return nil;
		end;
		local v, j = pcall(function()
				return k:LoadAnimation(l);
			end);
		if not v or not j then
			return nil;
		end;
		j.Priority = p or Enum.AnimationPriority.Action4;
		j.Looped = y;
		j:Play(.1, 1, a);
		return j;
	end;
	local function N()
		for k, l in ipairs(k) do
			for k, l in pairs(l.tracks) do
				pcall(function()
					l:Stop(0);
					l:Destroy();
				end);
			end;
			if l.hum then
				pcall(function()
					for k, l in ipairs(l.hum:GetPlayingAnimationTracks()) do
						l:Stop(0);
					end;
				end);
			end;
		end;
		k = {};
	end;
	local function K(k, l, p, a, y, v)
		if not k or not k.Parent or not l then
			return nil;
		end;
		s(k);
		local j, G = e(k, 8);
		if not j or not G then
			return nil;
		end;
		local g = {};
		g[1] = f(j, l, Enum.AnimationPriority.Action4, y, v);
		if not g[1] then
			return nil;
		end;
		if a and p then
			g[2] = f(j, p, Enum.AnimationPriority.Action3, y, v);
			if g[2] then
				pcall(function()
					g[2]:AdjustWeight(0);
				end);
			end;
		end;
		return {
			rig = k,
			hum = G,
			animator = j,
			tracks = g,
		};
	end;
	local function n(l, p, a, y, v, j)
		if not Toggles.AnimEnabled or not Toggles.AnimEnabled.Value then
			return false;
		end;
		N();
		local G = c(l);
		if not G then
			b:Notify("animation bypass failed (bad bundle id?)", 3);
			return false;
		end;
		local g = nil;
		if a and (p ~= "" and p ~= l) then
			g = c(p);
		end;
		local e = wt.Character or wt.CharacterAdded:Wait();
		s(e);
		local f = K(e, G, g, a, v, j);
		if f then
			table.insert(k, f);
		end;
		if y then
			for l, p in T() do
				local y = K(p, G, g, a, v, j);
				if y then
					table.insert(k, y);
				end;
			end;
		end;
		return #k > 0;
	end;
	local function d(k, l, p, a)
		if not k or not k.animator then
			return;
		end;
		local y = k.tracks;
		local v = {};
		if y[1] then
			v[y[1]] = true;
		end;
		if y[2] then
			v[y[2]] = true;
		end;
		pcall(function()
			for k, l in pairs(k.animator:GetPlayingAnimationTracks()) do
				if not v[l] then
					l:Stop(0);
				end;
			end;
		end);
		if y[1] then
			pcall(function()
				if p then
					if not y[1].IsPlaying then
						y[1]:Play(0, 1, l);
					end;
					y[1]:AdjustSpeed(l);
					y[1]:AdjustWeight(1);
				else
					y[1]:AdjustWeight(0);
				end;
			end);
		end;
		if y[2] then
			pcall(function()
				if a then
					if not y[2].IsPlaying then
						y[2]:Play(0, 1, l);
					end;
					y[2]:AdjustSpeed(l);
					y[2]:AdjustWeight(1);
				else
					y[2]:AdjustWeight(0);
				end;
			end);
		end;
	end;
	ut.Heartbeat:Connect(function()
		if b.Unloaded then
			return;
		end;
		local y = Toggles.AnimEnabled and Toggles.AnimEnabled.Value;
		if not y then
			if #k > 0 then
				N();
			end;
			return;
		end;
		local v = j(Options.AnimForceID and Options.AnimForceID.Value);
		local s = j(Options.AnimJitterID and Options.AnimJitterID.Value);
		local G = Toggles.AnimJitter and Toggles.AnimJitter.Value;
		local g = Toggles.AnimServerVisible and Toggles.AnimServerVisible.Value;
		local T = Options.AnimSpeed and Options.AnimSpeed.Value or 2;
		local c = Toggles.AnimLoop and Toggles.AnimLoop.Value;
		if (getgenv())._animCmd == "Stop" then
			(getgenv())._animCmd = nil;
			N();
			l.primary = "";
			l.jitter = "";
			l.jitterOn = false;
			l.serverVisible = false;
			return;
		end;
		if v == "" then
			return;
		end;
		local e = (getgenv())._animCmd == "Play" or #k == 0 or (k[1] and (k[1].tracks[1] and not k[1].tracks[1].IsPlaying));
		local f = v ~= l.primary or s ~= l.jitter or G ~= l.jitterOn or g ~= l.serverVisible;
		if e or f then
			(getgenv())._animCmd = nil;
			l.primary = v;
			l.jitter = s;
			l.jitterOn = G;
			l.serverVisible = g;
			a = 1;
			p = tick();
			if not n(v, s, G, g, T, c) then
				return;
			end;
		end;
		local K = true;
		local Y = false;
		if G and (k[1] and k[1].tracks[1]) then
			local l = Options.JitterSpeed and Options.JitterSpeed.Value or .1;
			if tick() - p >= l then
				p = tick();
				if k[1].tracks[2] then
					a = a == 1 and 2 or 1;
				else
					for k, l in ipairs(k) do
						local p = l.tracks[1];
						if p then
							pcall(function()
								p:Stop(0);
								p:Play(0, 1, T);
							end);
						end;
					end;
				end;
			end;
			if k[1].tracks[2] then
				K = a == 1;
				Y = a == 2;
			end;
		end;
		for k, l in ipairs(k) do
			if l.rig and l.rig.Parent then
				d(l, T, K, Y);
			end;
		end;
	end);
	wt.CharacterAdded:Connect(function()
		y = {};
		v = 0;
		task.wait(1.5);
		local k = Toggles.AnimAutoRespawn and Toggles.AnimAutoRespawn.Value;
		local l = Toggles.AnimEnabled and Toggles.AnimEnabled.Value;
		if k and l then
			(getgenv())._animCmd = "Play";
		end;
	end);
end))();
((function()
	local k = game:GetService("Players");
	local l = game:GetService("RunService");
	local p = game:GetService("Workspace");
	local a = game:GetService("Lighting");
	_G.Features.WorldTextures = _G.Features.WorldTextures or {
			Enabled = false,
			Selected = { all = true },
			Color = Color3.fromRGB(160, 160, 165),
			Dark = false,
		};
	_G.Features.SmoothTextures = _G.Features.SmoothTextures or { Enabled = false };
	_G.Features.TransparentTextures = _G.Features.TransparentTextures or { Enabled = false, Transparency = .6 };
	do
		local k = a:FindFirstChild("FinhubTextureBlur");
		if k then
			k:Destroy();
		end;
	end;
	local y = {
			"all",
			"grass",
			"sand",
			"rock",
			"metal",
			"wood",
			"concrete",
			"brick",
			"glass",
			"plastic",
			"fabric",
			"ice",
			"ground",
			"other",
		};
	local v = {
			grass = { Enum.Material.Grass },
			sand = { Enum.Material.Sand },
			ground = { Enum.Material.Ground, Enum.Material.Mud, Enum.Material.LeafyGrass },
			rock = {
				Enum.Material.Rock,
				Enum.Material.Slate,
				Enum.Material.Basalt,
				Enum.Material.Pebble,
				Enum.Material.Limestone,
				Enum.Material.Salt,
				Enum.Material.CrackedLava,
				Enum.Material.Glacier,
				Enum.Material.Snow,
				Enum.Material.Marble,
				Enum.Material.Granite,
			},
			metal = {
				Enum.Material.Metal,
				Enum.Material.DiamondPlate,
				Enum.Material.CorrodedMetal,
				Enum.Material.Foil,
			},
			wood = { Enum.Material.Wood, Enum.Material.WoodPlanks },
			concrete = { Enum.Material.Concrete, Enum.Material.Cobblestone },
			brick = { Enum.Material.Brick },
			glass = { Enum.Material.Glass },
			plastic = { Enum.Material.Plastic, Enum.Material.SmoothPlastic },
			fabric = { Enum.Material.Fabric },
			ice = { Enum.Material.Ice },
		};
	local j = {};
	for k, l in pairs(v) do
		for l, p in ipairs(l) do
			j[p] = k;
		end;
	end;
	local s = {};
	local G = {};
	local g = {};
	local T = nil;
	local c = nil;
	local e = false;
	local b = 128;
	local f = Instance.new("Folder");
	f.Name = "\000SmoothTexSA";
	f.Parent = game.CoreGui;
	local N = "7658055825";
	local function K(k)
		if not k then
			return false;
		end;
		local l, p = pcall(function()
				return tostring(k);
			end);
		if not l or not p then
			return false;
		end;
		return p:find(N, 1, true) ~= nil;
	end;
	local function n(k)
		if not k then
			return false;
		end;
		local l, p = pcall(function()
				if k:IsA("Texture") then
					return K(k.Texture) or K(k.ColorMap) or K(k.ColorMapContent);
				elseif k:IsA("Decal") then
					return K(k.Texture) or K(k.ColorMap) or K(k.ColorMapContent);
				elseif k:IsA("SurfaceAppearance") then
					return K(k.ColorMap) or K(k.Albedo) or K(k.ColorMapContent);
				end;
				return false;
			end);
		return l and p;
	end;
	local d = {};
	local Y = nil;
	local function M(k, l)
		local p, a = pcall(function()
				return k[l];
			end);
		return p and a or nil;
	end;
	local function i(k, l, p)
		pcall(function()
			k[l] = p;
		end);
	end;
	local function R(k)
		if not k then
			return false;
		end;
		return (tostring(k)):find(N, 1, true) ~= nil;
	end;
	local function B(k)
		if not k then
			return false;
		end;
		if k:IsA("Texture") then
			for l, p in ipairs({ "Texture", "ColorMap", "ColorMapContent" }) do
				if R(M(k, p)) then
					return true;
				end;
			end;
		elseif k:IsA("Decal") then
			for l, p in ipairs({
				"Texture",
				"TextureId",
				"TextureContent",
				"ColorMap",
				"ColorMapContent",
			}) do
				if R(M(k, p)) then
					return true;
				end;
			end;
		elseif k:IsA("SurfaceAppearance") then
			for l, p in ipairs({
				"ColorMap",
				"Albedo",
				"ColorMapContent",
				"NormalMap",
				"MetalnessMap",
				"RoughnessMap",
			}) do
				if R(M(k, p)) then
					return true;
				end;
			end;
		end;
		return false;
	end;
	local function W(k)
		if not k or d[k] or not B(k) then
			return;
		end;
		d[k] = {
				Texture = M(k, "Texture"),
				TextureId = M(k, "TextureId"),
				TextureContent = M(k, "TextureContent"),
				ColorMap = M(k, "ColorMap"),
				ColorMapContent = M(k, "ColorMapContent"),
				Albedo = M(k, "Albedo"),
				NormalMap = M(k, "NormalMap"),
				MetalnessMap = M(k, "MetalnessMap"),
				RoughnessMap = M(k, "RoughnessMap"),
				Transparency = M(k, "Transparency"),
			};
	end;
	local function L(k)
		local l = d[k];
		if not l then
			return;
		end;
		if k:IsA("Texture") or k:IsA("Decal") or k:IsA("SurfaceAppearance") then
			if l.Texture ~= nil then
				i(k, "Texture", l.Texture);
			end;
			if l.TextureId ~= nil then
				i(k, "TextureId", l.TextureId);
			end;
			if l.TextureContent ~= nil then
				i(k, "TextureContent", l.TextureContent);
			end;
			if l.ColorMap ~= nil then
				i(k, "ColorMap", l.ColorMap);
			end;
			if l.ColorMapContent ~= nil then
				i(k, "ColorMapContent", l.ColorMapContent);
			end;
			if l.Albedo ~= nil then
				i(k, "Albedo", l.Albedo);
			end;
			if l.NormalMap ~= nil then
				i(k, "NormalMap", l.NormalMap);
			end;
			if l.MetalnessMap ~= nil then
				i(k, "MetalnessMap", l.MetalnessMap);
			end;
			if l.RoughnessMap ~= nil then
				i(k, "RoughnessMap", l.RoughnessMap);
			end;
			if l.Transparency ~= nil then
				i(k, "Transparency", l.Transparency);
			end;
		end;
		d[k] = nil;
	end;
	local function P()
		for k in pairs(d) do
			L(k);
		end;
		table.clear(d);
	end;
	local function x(k)
		if not k or not B(k) then
			return;
		end;
		W(k);
		if k:IsA("Texture") then
			i(k, "Transparency", 1);
			if R(M(k, "Texture")) then
				i(k, "Texture", "");
			end;
			if R(M(k, "ColorMap")) then
				i(k, "ColorMap", "");
			end;
			if R(M(k, "ColorMapContent")) then
				i(k, "ColorMapContent", "");
			end;
		elseif k:IsA("Decal") then
			i(k, "Transparency", 1);
			if R(M(k, "Texture")) then
				i(k, "Texture", "");
			end;
			if R(M(k, "TextureId")) then
				i(k, "TextureId", "");
			end;
			if R(M(k, "TextureContent")) then
				i(k, "TextureContent", "");
			end;
		elseif k:IsA("SurfaceAppearance") then
			if R(M(k, "ColorMap")) then
				i(k, "ColorMap", "");
			end;
			if R(M(k, "Albedo")) then
				i(k, "Albedo", "");
			end;
			if R(M(k, "ColorMapContent")) then
				i(k, "ColorMapContent", "");
			end;
			if R(M(k, "NormalMap")) then
				i(k, "NormalMap", "");
			end;
			if R(M(k, "MetalnessMap")) then
				i(k, "MetalnessMap", "");
			end;
			if R(M(k, "RoughnessMap")) then
				i(k, "RoughnessMap", "");
			end;
		end;
	end;
	local function t(k)
		if not k then
			return;
		end;
		if B(k) then
			x(k);
		end;
		for k, l in ipairs(k:GetDescendants()) do
			if B(l) then
				x(l);
			end;
		end;
	end;
	local function S()
		for k, l in ipairs(game:GetDescendants()) do
			if B(l) then
				x(l);
			end;
		end;
	end;
	local function V()
		S();
		if Y then
			Y:Disconnect();
		end;
		Y = game.DescendantAdded:Connect(function(k)
				task.wait(.1);
				t(k);
			end);
	end;
	local function X()
		if Y then
			Y:Disconnect();
			Y = nil;
		end;
		P();
	end;
	local function Z()
		if _G.Features.SmoothTextures.Enabled then
			V();
		else
			X();
		end;
	end;
	local function E(k)
		if not k then
			return Color3.new(.2, .2, .2);
		end;
		return Color3.new(k.R * .45, k.G * .45, k.B * .45);
	end;
	local J = {};
	local U = nil;
	local function z(k)
		if not k or not k:IsA("BasePart") then
			return;
		end;
		if J[k] ~= nil then
			return;
		end;
		J[k] = k.Color;
		pcall(function()
			k.Color = E(k.Color);
		end);
	end;
	local function w()
		for k, l in ipairs(p:GetDescendants()) do
			z(l);
		end;
		if U then
			U:Disconnect();
		end;
		U = p.DescendantAdded:Connect(function(k)
				task.defer(function()
					if _G.Features.WorldTextures.Dark then
						z(k);
					end;
				end);
			end);
	end;
	local function q()
		if U then
			U:Disconnect();
			U = nil;
		end;
		for k, l in pairs(J) do
			if k and k.Parent then
				pcall(function()
					k.Color = l;
				end);
			end;
		end;
		table.clear(J);
	end;
	local function u()
		if _G.Features.WorldTextures.Dark then
			w();
		else
			q();
		end;
	end;
	local H = {};
	local Q = nil;
	local function A(l)
		if not l or not l:IsA("BasePart") then
			return false;
		end;
		if l.Transparency >= .95 then
			return false;
		end;
		local p = l:FindFirstAncestorWhichIsA("Model");
		if p then
			if k:GetPlayerFromCharacter(p) then
				return false;
			end;
			if p:FindFirstChildOfClass("Humanoid") then
				return false;
			end;
			if p.Name:find("Weapon") or p.Name:find("Projectile") or p.Name:find("Effect") then
				return false;
			end;
		end;
		if l.Parent and ((l.Parent.Name == "ViewModels" or l.Parent.Name == "Effects")) then
			return false;
		end;
		if l:FindFirstAncestor("Camera") then
			return false;
		end;
		return true;
	end;
	local function I(k)
		if not A(k) then
			return;
		end;
		if H[k] then
			return;
		end;
		H[k] = k.Transparency;
		local l = math.clamp(_G.Features.TransparentTextures.Transparency, .05, .95);
		pcall(function()
			k.Transparency = l;
		end);
	end;
	local function m()
		for k, l in pairs(H) do
			if k and k.Parent then
				local l = math.clamp(_G.Features.TransparentTextures.Transparency, .05, .95);
				pcall(function()
					k.Transparency = l;
				end);
			end;
		end;
	end;
	local function o()
		for k, l in ipairs(p:GetDescendants()) do
			I(l);
		end;
		if Q then
			Q:Disconnect();
		end;
		Q = p.DescendantAdded:Connect(function(k)
				task.defer(function()
					if _G.Features.TransparentTextures.Enabled then
						I(k);
					end;
				end);
			end);
	end;
	local function r()
		if Q then
			Q:Disconnect();
			Q = nil;
		end;
		for k, l in pairs(H) do
			if k and k.Parent then
				pcall(function()
					k.Transparency = l;
				end);
			end;
		end;
		table.clear(H);
	end;
	local function C()
		if _G.Features.TransparentTextures.Enabled then
			o();
		else
			r();
		end;
	end;
	(getgenv()).FinhubUpdSmoothTextures = Z;
	(getgenv()).FinhubUpdDarkTextures = u;
	(getgenv()).FinhubUpdTransparentTextures = C;
	local D = F.Misc:AddLeftGroupbox("textures");
	D:AddToggle("smooth_textures", {
		Text = "smooth textures",
		Default = _G.Features.SmoothTextures.Enabled,
		Tooltip = "makes map textures smooth (smooth plastic, hides surface detail)",
		Callback = function(k)
			_G.Features.SmoothTextures.Enabled = k;
			local l = (getgenv()).FinhubUpdSmoothTextures;
			if l then
				l();
			end;
		end,
	});
	D:AddToggle("dark_textures", {
		Text = "dark textures",
		Default = _G.Features.WorldTextures.Dark,
		Tooltip = "darkens world parts for a deeper, darker texture look",
		Callback = function(k)
			_G.Features.WorldTextures.Dark = k;
			local l = (getgenv()).FinhubUpdDarkTextures;
			if l then
				l();
			end;
		end,
	});
	local h = D:AddToggle("transparent_textures", {
			Text = "transparent textures",
			Default = _G.Features.TransparentTextures.Enabled,
			Tooltip = "makes world textures semi-transparent",
			Callback = function(k)
				_G.Features.TransparentTextures.Enabled = k;
				local l = (getgenv()).FinhubUpdTransparentTextures;
				if l then
					l();
				end;
			end,
		});
	D:AddSlider("TransparentStrength", {
		Text = "transparency",
		Default = _G.Features.TransparentTextures.Transparency,
		Min = .5,
		Max = 1,
		Rounding = 2,
		Compact = true,
		Callback = function(k)
			_G.Features.TransparentTextures.Transparency = k;
			if _G.Features.TransparentTextures.Enabled then
				m();
			end;
		end,
	});
	if _G.Features.SmoothTextures.Enabled then
		Z();
	end;
	if _G.Features.WorldTextures.Dark then
		u();
	end;
	if _G.Features.TransparentTextures.Enabled then
		C();
	end;
end))();
((function()
	N:SetLibrary(b);
	local k = N.ThemeUpdate;
	function N.ThemeUpdate(l)
		k(l);
		if (getgenv()).FinhubApplyAccentTheme then
			pcall((getgenv()).FinhubApplyAccentTheme);
		end;
		if applyTargetHudTheme then
			applyTargetHudTheme();
		end;
		if (getgenv()).FinhubApplyCosmeticUiTheme then
			pcall((getgenv()).FinhubApplyCosmeticUiTheme);
		end;
		if (getgenv()).FinhubKeybindList and (getgenv()).FinhubKeybindList.inner then
			if (getgenv()).FinhubApplyKbListTheme then
				pcall((getgenv()).FinhubApplyKbListTheme);
			end;
		end;
	end;
	N:ApplyToTab(F["UI Settings"]);
	K:SetLibrary(b);
	K:SetIgnoreIndexes({});
	N:SetFolder("Finhub");
	K:SetFolder("instance/rivals");
	K:BuildConfigSection(F["UI Settings"]);
	(getgenv()).FinhubConfigLoading = true;
	R();
	task.defer(function()
		task.wait(.35);
		pcall(R);
		pcall(function()
			local k = Options and Options.tgbtgbbktbkmb;
			if k and setfpscap then
				local l = tonumber(k.Value) or 0;
				if l <= 0 or l >= 9999 then
					setfpscap(0);
				else
					setfpscap(math.floor(l));
				end;
			end;
		end);
		task.wait(1.25);
		pcall(R);
		(getgenv()).FinhubConfigLoading = false;
	end);
end))();
(getgenv())._Finhub_unlockOnce = false;
(getgenv())._Finhub_unlockRan = false;
local function Qt()
	if (getgenv())._Finhub_unlockOnce then
		return;
	end;
	(getgenv())._Finhub_unlockOnce = true;
	local k = (game:GetService("Players")).LocalPlayer;
	local l = game:GetService("ReplicatedStorage");
	local p = game:GetService("HttpService");
	local a = k.PlayerScripts;
	local y = a.Controllers;
	local v, j, s, G;
	local g, T = pcall(function()
			return require(l.Modules:WaitForChild("EnumLibrary", 1));
		end);
	if g then
		v = T;
	end;
	local c, e = pcall(function()
			local k = require(l.Modules:WaitForChild("CosmeticLibrary", 1));
			if k and k.WaitForEnumBuilder then
				local k = false;
				task.delay(2, function()
					k = true;
				end);
			end;
			return k;
		end);
	if c then
		j = e;
	end;
	local b, f = pcall(function()
			return require(l.Modules:WaitForChild("ItemLibrary", 1));
		end);
	if b then
		s = f;
	end;
	local N, K = pcall(function()
			return require(y:WaitForChild("PlayerDataController", 1));
		end);
	if N then
		G = K;
	end;
	if not j or not G then
		warn("[Finhub] Failed to load required modules for UnlockAll");
		return;
	end;
	local n, d = {}, {};
	local Y, M = nil, nil;
	local i = nil;
	local function R(k, l, p)
		if not j.Cosmetics then
			return nil;
		end;
		local a = j.Cosmetics[k];
		if not a then
			return nil;
		end;
		local y = {};
		for k, l in pairs(a) do
			y[k] = l;
		end;
		y.Name = k;
		y.Type = y.Type or l;
		y.Seed = y.Seed or math.random(1, 1000000);
		if v and v.ToEnum then
			local l, p = pcall(v.ToEnum, v, k);
			if l and p then
				y.Enum, y.ObjectID = p, y.ObjectID or p;
			end;
		end;
		if p then
			if p.inverted ~= nil then
				y.Inverted = p.inverted;
			end;
			if p.favoritesOnly ~= nil then
				y.OnlyUseFavorites = p.favoritesOnly;
			end;
		end;
		return y;
	end;
	local B = "unlockall/config.json";
	local function W()
		if not writefile then
			return;
		end;
		pcall(function()
			local k = { equipped = {}, favorites = d };
			for l, p in pairs(n) do
				k.equipped[l] = {};
				for p, a in pairs(p) do
					if a and a.Name then
						k.equipped[l][p] = { name = a.Name, seed = a.Seed, inverted = a.Inverted };
					end;
				end;
			end;
			makefolder("unlockall");
			writefile(B, p:JSONEncode(k));
		end);
	end;
	local function L()
		if not readfile or not isfile or not isfile(B) then
			return;
		end;
		pcall(function()
			local k = p:JSONDecode(readfile(B));
			if k.equipped then
				for k, l in pairs(k.equipped) do
					n[k] = {};
					for l, p in pairs(l) do
						local a = R(p.name, l, { inverted = p.inverted });
						if a then
							a.Seed = p.seed;
							n[k][l] = a;
						end;
					end;
				end;
			end;
			d = k.favorites or {};
		end);
	end;
	if not (getgenv())._Finhub_Originals.OwnsCosmeticNormally then
		(getgenv())._Finhub_Originals.OwnsCosmeticNormally = j.OwnsCosmeticNormally;
	end;
	if not (getgenv())._Finhub_Originals.OwnsCosmeticUniversally then
		(getgenv())._Finhub_Originals.OwnsCosmeticUniversally = j.OwnsCosmeticUniversally;
	end;
	if not (getgenv())._Finhub_Originals.OwnsCosmeticForWeapon then
		(getgenv())._Finhub_Originals.OwnsCosmeticForWeapon = j.OwnsCosmeticForWeapon;
	end;
	if not (getgenv())._Finhub_Originals.OwnsCosmetic then
		(getgenv())._Finhub_Originals.OwnsCosmetic = j.OwnsCosmetic;
	end;
	if not (getgenv())._Finhub_Originals.DataControllerGet then
		(getgenv())._Finhub_Originals.DataControllerGet = G.Get;
	end;
	if not (getgenv())._Finhub_Originals.DataControllerGetWeaponData then
		(getgenv())._Finhub_Originals.DataControllerGetWeaponData = G.GetWeaponData;
	end;
	if not (getgenv())._Finhub_Originals._ownsCosmeticNormallyHooked then
		(getgenv())._Finhub_Originals._ownsCosmeticNormallyHooked = true;
		j.OwnsCosmeticNormally = function()
				return true;
			end;
	end;
	if not (getgenv())._Finhub_Originals._ownsCosmeticUniversallyHooked then
		(getgenv())._Finhub_Originals._ownsCosmeticUniversallyHooked = true;
		j.OwnsCosmeticUniversally = function()
				return true;
			end;
	end;
	if not (getgenv())._Finhub_Originals._ownsCosmeticForWeaponHooked then
		(getgenv())._Finhub_Originals._ownsCosmeticForWeaponHooked = true;
		j.OwnsCosmeticForWeapon = function()
				return true;
			end;
	end;
	local P = (getgenv())._Finhub_Originals.OwnsCosmetic;
	local x = false;
	j.OwnsCosmetic = function(k, l, p, a)
			if x then
				return P(k, l, p, a);
			end;
			if p:find("MISSING_") then
				x = true;
				local y = P(k, l, p, a);
				x = false;
				return y;
			end;
			return true;
		end;
	local t = (getgenv())._Finhub_Originals.DataControllerGet;
	G.Get = function(k, l)
			local p = t(k, l);
			if l == "CosmeticInventory" then
				local k = {};
				if p then
					for l, p in pairs(p) do
						k[l] = p;
					end;
				end;
				return setmetatable(k, { __index = function()
						return true;
					end });
			end;
			if l == "FavoritedCosmetics" then
				local k = {};
				if p then
					for l, p in pairs(p) do
						k[l] = p;
					end;
				end;
				for l, p in pairs(d) do
					k[l] = k[l] or {};
					for p, a in pairs(p) do
						k[l][p] = a;
					end;
				end;
				return k;
			end;
			return p;
		end;
	local S = (getgenv())._Finhub_Originals.DataControllerGetWeaponData;
	G.GetWeaponData = function(k, l)
			local p = S(k, l);
			if not p then
				return nil;
			end;
			local a = {};
			for k, l in pairs(p) do
				a[k] = l;
			end;
			a.Name = l;
			if n[l] then
				for k, l in pairs(n[l]) do
					a[k] = l;
				end;
			end;
			return a;
		end;
	local V;
	pcall(function()
		V = require(y:WaitForChild("FighterController", 1));
	end);
	if hookmetamethod then
		local p = l:FindFirstChild("Remotes");
		local a = p and p:FindFirstChild("Data");
		local y = a and a:FindFirstChild("EquipCosmetic");
		local v = a and a:FindFirstChild("FavoriteCosmetic");
		local j = p and p:FindFirstChild("Replication");
		local s = j and j:FindFirstChild("Fighter");
		local g = s and s:FindFirstChild("UseItem");
		if y then
			local l;
			l = hookmetamethod(game, "__namecall", function(p, ...)
					if getnamecallmethod() ~= "FireServer" then
						return l(p, ...);
					end;
					local a = { ... };
					if g and p == g then
						local l = a[1];
						if V then
							pcall(function()
								local p = V:GetFighter(k);
								if p and p.Items then
									for k, p in pairs(p.Items) do
										if p:Get("ObjectID") == l then
											i = p.Name;
											break;
										end;
									end;
								end;
							end);
						end;
					end;
					if p == y then
						local k, y, v, j = a[1], a[2], a[3], a[4] or {};
						if v and (v ~= "None" and v ~= "") then
							local k = G:Get("CosmeticInventory");
							if k and rawget(k, v) then
								return l(p, ...);
							end;
						end;
						n[k] = n[k] or {};
						if not v or v == "None" or v == "" then
							n[k][y] = nil;
							if not next(n[k]) then
								n[k] = nil;
							end;
						else
							local l = R(v, y, { inverted = j.IsInverted, favoritesOnly = j.OnlyUseFavorites });
							if l then
								n[k][y] = l;
							end;
						end;
						task.defer(function()
							pcall(function()
								if G.CurrentData then
									G.CurrentData:Replicate("WeaponInventory");
								end;
							end);
							task.wait(.2);
							W();
						end);
						return;
					end;
					if p == v then
						d[a[1]] = d[a[1]] or {};
						d[a[1]][a[2]] = a[3] or nil;
						W();
						task.spawn(function()
							pcall(function()
								if G.CurrentData then
									G.CurrentData:Replicate("FavoritedCosmetics");
								end;
							end);
						end);
						return;
					end;
					return l(p, ...);
				end);
			(getgenv())._Finhub_Hooks.NamecallHook = l;
		end;
	end;
	local X;
	pcall(function()
		X = require(k.PlayerScripts.Modules.ClientReplicatedClasses.ClientFighter.ClientItem);
	end);
	if X and (X._CreateViewModel and not (getgenv())._Finhub_Originals.CreateViewModel) then
		(getgenv())._Finhub_Originals.CreateViewModel = X._CreateViewModel;
		local l = X._CreateViewModel;
		X._CreateViewModel = function(p, a)
				local y = p.Name;
				local v = p.ClientFighter and p.ClientFighter.Player;
				Y = (v == k) and y or nil;
				if v == k and (n[y] and (n[y].Skin and a)) then
					local k, l, v = p:ToEnum("Data"), p:ToEnum("Skin"), p:ToEnum("Name");
					if a[k] then
						a[k][l] = n[y].Skin;
						a[k][v] = n[y].Skin.Name;
					elseif a.Data then
						a.Data.Skin = n[y].Skin;
						a.Data.Name = n[y].Skin.Name;
					end;
				end;
				local j = l(p, a);
				Y = nil;
				return j;
			end;
	end;
	local Z = k.PlayerScripts.Modules.ClientReplicatedClasses.ClientFighter.ClientItem:FindFirstChild("ClientViewModel");
	if Z then
		local p = require(Z);
		if p.GetWrap and not (getgenv())._Finhub_Originals.GetWrap then
			(getgenv())._Finhub_Originals.GetWrap = p.GetWrap;
			local l = p.GetWrap;
			p.GetWrap = function(p)
					local a = p.ClientItem and p.ClientItem.Name;
					local y = p.ClientItem and (p.ClientItem.ClientFighter and p.ClientItem.ClientFighter.Player);
					if a and (y == k and (n[a] and n[a].Wrap)) then
						return n[a].Wrap;
					end;
					return l(p);
				end;
		end;
		if p.new and not (getgenv())._Finhub_Originals.ViewModelNew then
			(getgenv())._Finhub_Originals.ViewModelNew = p.new;
			local a = p.new;
			p.new = function(p, y)
					local v = y.ClientFighter and y.ClientFighter.Player;
					local j = Y or y.Name;
					if v == k and n[j] then
						local k = require(l.Modules.ReplicatedClass);
						local a = k:ToEnum("Data");
						p[a] = p[a] or {};
						local y = n[j];
						if y.Skin then
							p[a][k:ToEnum("Skin")] = y.Skin;
						end;
						if y.Wrap then
							p[a][k:ToEnum("Wrap")] = y.Wrap;
						end;
						if y.Charm then
							p[a][k:ToEnum("Charm")] = y.Charm;
						end;
					end;
					local s = a(p, y);
					if v == k and (n[j] and (n[j].Wrap and s._UpdateWrap)) then
						s:_UpdateWrap();
						task.delay(.1, function()
							if not s._destroyed then
								s:_UpdateWrap();
							end;
						end);
					end;
					return s;
				end;
		end;
	end;
	if s and not (getgenv())._Finhub_Originals.GetViewModelImage then
		(getgenv())._Finhub_Originals.GetViewModelImage = s.GetViewModelImageFromWeaponData;
		local l = s.GetViewModelImageFromWeaponData;
		s.GetViewModelImageFromWeaponData = function(p, a, y)
				if not a then
					return l(p, a, y);
				end;
				local v = a.Name;
				local j = (a.Skin and (n[v] and a.Skin == n[v].Skin)) or (M == k and (n[v] and n[v].Skin));
				if j and (n[v] and n[v].Skin) then
					local k = p.ViewModels[n[v].Skin.Name];
					if k then
						return k[y and "ImageHighResolution" or "Image"] or k.Image;
					end;
				end;
				return l(p, a, y);
			end;
	end;
	pcall(function()
		local l = require(k.PlayerScripts.Modules.Pages.ViewProfile);
		if l and (l.Fetch and not (getgenv())._Finhub_Originals.ViewProfileFetch) then
			(getgenv())._Finhub_Originals.ViewProfileFetch = l.Fetch;
			local k = l.Fetch;
			l.Fetch = function(l, p)
					M = p;
					return k(l, p);
				end;
		end;
	end);
	local E;
	pcall(function()
		E = require(k.PlayerScripts.Modules.ClientReplicatedClasses.ClientEntity);
	end);
	if E and (E.ReplicateFromServer and not (getgenv())._Finhub_Originals.ClientEntityReplicate) then
		(getgenv())._Finhub_Originals.ClientEntityReplicate = E.ReplicateFromServer;
		local l = E.ReplicateFromServer;
		E.ReplicateFromServer = function(p, a, ...)
				if a == "FinisherEffect" then
					local y = { ... };
					local j = y[3];
					local s = j;
					if type(j) == "userdata" and (v and v.FromEnum) then
						local k, l = pcall(v.FromEnum, v, j);
						if k and l then
							s = l;
						end;
					end;
					local G = tostring(s) == k.Name or (tostring(s)):lower() == k.Name:lower();
					if G and (i and (n[i] and n[i].Finisher)) then
						local k = n[i].Finisher;
						local j = k.Enum;
						if not j and v then
							local l, p = pcall(v.ToEnum, v, k.Name);
							if l and p then
								j = p;
							end;
						end;
						if j then
							y[4] = j;
							return l(p, a, unpack(y));
						end;
					end;
				end;
				return l(p, a, ...);
			end;
	end;
	L();
	(getgenv())._Finhub_unlockRan = true;
	print("All cosmetics unlocked!");
end;
(getgenv())._Finhub_SkinsUnlocked = (getgenv())._Finhub_SkinsUnlocked or false;
(getgenv())._Finhub_AllSkinsData = (getgenv())._Finhub_AllSkinsData or {};
local function At()
	if (getgenv())._Finhub_SkinsUnlocked then
		warn("[Finhub] Skins already unlocked!");
		return;
	end;
	local k = (game:GetService("Players")).LocalPlayer;
	local l = game:GetService("ReplicatedStorage");
	local p = game:GetService("HttpService");
	local a, y, v;
	local j, s = pcall(function()
			return require(l.Modules:WaitForChild("EnumLibrary", 2));
		end);
	if j then
		a = s;
	end;
	local G, g = pcall(function()
			return require(l.Modules:WaitForChild("CosmeticLibrary", 2));
		end);
	if G then
		y = g;
	end;
	local T, c = pcall(function()
			return require(l.Modules:WaitForChild("ItemLibrary", 2));
		end);
	if T then
		v = c;
	end;
	if not y then
		warn("[Finhub] CosmeticLibrary not found - cannot unlock skins");
		return;
	end;
	local e = {};
	local b = 0;
	if y.Cosmetics then
		for k, l in pairs(y.Cosmetics) do
			if l and ((l.Type == "Skin" or l.Type == nil or (type(l) == "table" and not l.Type))) then
				e[k] = l;
				b = b + 1;
			end;
		end;
	end;
	if y.Skins then
		for k, l in pairs(y.Skins) do
			e[k] = l;
			b = b + 1;
		end;
	end;
	print(string.format("[Finhub] Found %d skins to unlock", b));
	local function f(k, l)
		local p = {};
		if type(l) == "table" then
			for k, l in pairs(l) do
				p[k] = l;
			end;
		end;
		p.Name = k;
		p.Type = "Skin";
		p.Seed = p.Seed or math.random(1, 1000000);
		if a and a.ToEnum then
			local l, y = pcall(a.ToEnum, a, k);
			if l and y then
				p.Enum = y;
				p.ObjectID = p.ObjectID or y;
			end;
		end;
		return p;
	end;
	(getgenv())._Finhub_AllSkinsData = {};
	for k, l in pairs(e) do
		(getgenv())._Finhub_AllSkinsData[k] = f(k, l);
	end;
	local N = y.OwnsCosmetic;
	if N then
		y.OwnsCosmetic = function(k, l, p, a)
				local v = false;
				if e[p] then
					v = true;
				elseif y.Cosmetics and y.Cosmetics[p] then
					local k = y.Cosmetics[p];
					if type(k) == "table" and ((k.Type == "Skin" or k.Type == nil)) then
						v = true;
					end;
				end;
				if v and not p:find("MISSING_") then
					return true;
				end;
				local j = false;
				local s = N(k, l, p, a);
				return s;
			end;
	end;
	if y.OwnsCosmeticUniversally then
		local k = y.OwnsCosmeticUniversally;
		y.OwnsCosmeticUniversally = function(l, p)
				if e[p] and not p:find("MISSING_") then
					return true;
				end;
				return k(l, p);
			end;
	end;
	if y.OwnsCosmeticNormally then
		local k = y.OwnsCosmeticNormally;
		y.OwnsCosmeticNormally = function(l, p)
				if e[p] and not p:find("MISSING_") then
					return true;
				end;
				return k(l, p);
			end;
	end;
	if y.OwnsCosmeticForWeapon then
		local k = y.OwnsCosmeticForWeapon;
		y.OwnsCosmeticForWeapon = function(l, p, a)
				if e[p] and not p:find("MISSING_") then
					return true;
				end;
				return k(l, p, a);
			end;
	end;
	local K = k:WaitForChild("PlayerScripts", 5);
	if K then
		local k = K:FindFirstChild("Controllers");
		if k then
			local l;
			pcall(function()
				l = require(k:WaitForChild("PlayerDataController", 2));
			end);
			if l and l.Get then
				local k = l.Get;
				l.Get = function(l, p)
						local a = k(l, p);
						if p == "CosmeticInventory" then
							local k = {};
							if a then
								for l, p in pairs(a) do
									k[l] = p;
								end;
							end;
							for l, p in pairs(e) do
								k[l] = true;
							end;
							return setmetatable(k, { __index = function()
									return true;
								end });
						end;
						return a;
					end;
			end;
		end;
	end;
	(getgenv())._Finhub_SkinsUnlocked = true;
	print(string.format("[Finhub] Successfully unlocked ALL %d skins!", b));
	return b;
end;
if (getgenv())._Finhub_AutoUnlockSkins then
	task.defer(At);
end;
(getgenv()).UnlockAllSkins = At;
(getgenv()).FinhubUnlockSkins = At;
pcall(function()
	if F and F["UI Settings"] then
		local k = F["UI Settings"]:AddLeftGroupbox("Skins");
		k:AddButton("Unlock All Skins", function()
			local k = At();
		end);
		k:AddToggle("AutoUnlockSkins", { Text = "Auto-unlock skins on load", Default = false, Callback = function(k)
				(getgenv())._Finhub_AutoUnlockSkins = k;
				if k then
					At();
				end;
			end });
	end;
end);
print("[Finhub] Unlock All Skins module loaded successfully!");
print("[Finhub] Use UnlockAllSkins() to unlock all skins manually");
